---

### 18:00 — TRA-137: Show commits and PR merges in issue activity feed

**Review type**: initial
**Result**: 0 🔴, 1 🟡, 1 🟢
**Files**: `crates/trakkt-github/src/events.rs`, `crates/trakkt-auth/src/activity_service.rs`, `crates/trakkt-types/src/enums.rs`, `crates/trakkt-types/src/models.rs`, `crates/trakkt-ui/src/pages/issues/issue_detail.rs`, `crates/trakkt-ui/src/pages/activity.rs`, `crates/trakkt-ui/src/components/attribution.rs`, `apps/server/migrations/20260610000000_activity_actor_nullable.sql`, `apps/server/migrations-sqlite/20260610000000_activity_actor_nullable.sql`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Copy-Paste / Missing Abstractions | `activity.rs:118` and `issue_detail.rs:2708` | `github_author_login()` in `activity.rs` and the inline JSON parsing in `activity_actor_display()` in `issue_detail.rs` both extract `author_login` from activity metadata and format it as `@login`. Identical logic duplicated across two UI modules. Should be a shared helper. |
| 2 | 🟢 | Fallback Code | `issue_detail.rs:2647,2652,2654,2684,2709` | `.unwrap_or("")` and `.unwrap_or(1)` on individual JSON field reads inside `format_github_activity_description`. After the parse is already validated, silent empty-string fallbacks for `commit_sha` / `commit_message` / `branch` produce broken/blank UI elements rather than logging a warning. Low risk (fields always present when set by recorder) but inconsistent with the explicit `tracing::warn!` style used for the URL/pr_number missing cases in the same function. |

**Anti-pattern tally**: Copy-Paste ×1, Fallback Code ×1

**Notes**: Solid implementation overall. All acceptance criteria are met: `commit_pushed` records one-per-issue batched activity; `pr_activity_type` mapping is correct and exhaustively tested; `branch_created` guards on `created == true`; both migrations are correct and in lockstep; best-effort error handling is consistently applied with `tracing::warn!`; all `target="_blank"` anchors carry `rel="noopener noreferrer"`; `ActionSource::Github` is added to `as_str`, `FromStr`, and the `attribution.rs` exhaustive match; actor resolution via email is implemented correctly in `resolve_author_actor`; the `activity_actor_display` fallback chain is logically sound. The main issue is the DRY violation between the two UI modules for the `@login` extraction helper.

---

### 18:30 — TRA-137: Show commits and PR merges in issue activity feed

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/utils/github.rs`, `crates/trakkt-ui/src/utils/mod.rs`, `crates/trakkt-ui/src/pages/activity.rs`, `crates/trakkt-ui/src/pages/issues/issue_detail.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|

No issues found.

**Anti-pattern tally**: none

**Notes**: Both cycle-1 issues fully resolved. (1) `github_author_login_from_metadata` is correctly implemented as a `pub fn` in the new `crates/trakkt-ui/src/utils/github.rs` module, exported via `utils/mod.rs`, and imported at both call sites via `use crate::utils::github::github_author_login_from_metadata`. No inline duplicate logic remains in either `activity.rs` or `issue_detail.rs`. The helper correctly returns `None` on absent metadata, missing field, or malformed JSON, and only warns on malformed JSON — matching the documented contract. (2) The `commit_pushed` arm now guards both `url` and `commit_sha` together in a `match (url, short_sha)` with `.filter(|sha| !sha.is_empty())`, so no empty `<a>` anchor is ever rendered; the missing/empty case falls through to `tracing::warn!` + `github_activity_fallback`. The remaining `.unwrap_or("")` uses for `commit_message` and `branch` are display-only fields with no anchoring role, so degrading to empty strings in those positions is acceptable. No new banned patterns introduced. Signed.

---

### 19:15 — TRA-189: Full-text search: expose via REST API (offset/pagination + SearchResponse)

**Review type**: initial
**Result**: 0 🔴, 2 🟡, 2 🟢
**Files**: `crates/trakkt-types/src/api.rs`, `crates/trakkt-auth/src/search_service.rs`, `crates/trakkt-api/src/issues.rs`, `crates/trakkt-ui/src/server_fns/issues.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Logical Correctness / Fallback Code | `search_service.rs:136`, `issues.rs:583` | Negative `offset` values are not validated. `params.offset as usize` on a negative `i64` wraps to `usize::MAX` on 64-bit, causing `.skip()` to silently return an empty iterator while `total` reports a non-zero count. The API handler already clamps `limit` with `.clamp(1, 100)` but applies no equivalent guard to `offset`. Fix: add `.max(0)` to the offset in the API handler alongside the existing limit clamp. |
| 2 | 🟡 | Missing Test Coverage | `search_service.rs:91–141` | The entire new logic path in `search()` — `uncapped_params` construction, deduplication ordering, `total = all_results.len() as i64`, and the `skip(offset).take(limit)` slice — has zero unit test coverage. Existing tests only cover pure helper functions that did not change. The slice/total logic should be extracted into a testable pure helper, or integration tests added for the key edge cases (offset=0, offset=N, offset-beyond-total, empty query). |
| 3 | 🟢 | Code Quality | `search_service.rs:107–116` | `uncapped_params` is constructed by listing every `SearchParams` field manually. If `SearchParams` grows a new field, this copy will silently use a wrong value. Derive `Clone` on `SearchParams` and use struct update syntax `SearchParams { limit: 1000, offset: 0, ..params.clone() }` to make the intent clear and future-proof. |
| 4 | 🟢 | Spec Adherence | `issues.rs` `operations()` description string | The search operation's description string in `operations()` does not document the new `offset` parameter or the `{ results, total }` response shape. MCP agents and API consumers read this string to discover capabilities. |

**Anti-pattern tally**: Logical Correctness ×1, Missing Test Coverage ×1, Code Quality ×1, Spec Adherence ×1

**Notes**: Sound overall design — the choice to fetch up to 1000 rows and paginate in Rust rather than issuing a separate COUNT against the complex UNION SQL is well-reasoned and documented. Both call sites (REST API and UI server function) were correctly updated. The JSON response shape matches the ticket spec exactly. The main gap is the missing guard on negative offset and the lack of unit tests for the new slice/total logic, both of which are straightforward to fix.

---
