
---

### 03:52 — Connect page: browser terminal UI integration

**Review type**: initial
**Result**: 0 🔴, 2 🟡, 4 🟢
**Files**: `crates/trakkt-ui/src/pages/connect.rs`, `crates/trakkt-ui/src/pages/mod.rs`, `crates/trakkt-ui/src/app.rs`, `crates/trakkt-ui/src/components/layout.rs`, `crates/trakkt-ui/Cargo.toml`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Logical Correctness | connect.rs:231–242, 316–318 | `ListSessions` never sent after connect. Neither `onopen` nor the `Ready` handler sends `ServerMessage::ListSessions`. The server requires an explicit request (confirmed in server routes). Page shows empty state even when the agent has active sessions. |
| 2 | 🟡 | State Management Issues | connect.rs:526–540 | `keydown` and `paste` listeners added via `add_event_listener_with_callback` but never removed. No `remove_event_listener_with_callback` in any `on_cleanup`. Stale handlers leak on every navigate-away from the page. |
| 3 | 🟢 | Design System / Component Violations | connect.rs:719,721,757,786,788,817 | Raw Tailwind color classes `bg-green-500`, `bg-red-500`, `text-[#666]` used for agent status indicator and empty-state text. CODING_STANDARDS.md mandates design tokens (`bg-success`, `bg-error`, `text-muted-foreground`). |
| 4 | 🟢 | Code Quality | connect.rs:630 | Session ID generated from millisecond timestamp only. Two rapid `+` clicks in the same millisecond produce a duplicate ID. Use timestamp + random suffix. |
| 5 | 🟢 | Code Quality | connect.rs:272–278 | `TerminalHandler` created per byte — correct but unusual. A comment explaining the zero-cost rationale prevents future readers from incorrectly "optimizing" it. |
| 6 | 🟢 | Copy-Paste / Missing Abstractions | connect.rs:710–768, 778–824 | Page header and terminal viewport markup duplicated between WASM and SSR branches. Design fixes must be applied twice. |

**Anti-pattern tally**: Logical Correctness ×1, State Management Issues ×1, Design System ×1, Code Quality ×2, Copy-Paste ×1

**Notes**: WebSocket lifecycle is correct (intentional_close flag, proper closure GC, exponential backoff, full cleanup on unmount). SSR gating is clean throughout. Grid copy faithfully transfers all mode flags and cursor state. Route and sidebar wiring match existing patterns exactly. Cargo.toml feature additions are correct. The two 🟡 issues are functional gaps: issue #1 means the page is broken for reconnecting users; issue #2 is a resource leak on every navigation.

---

### 19:15 — Terminal Emulator: VTE handler, DOM renderer, keyboard input, tab manager

**Review type**: initial
**Result**: 0 🔴, 3 🟡, 4 🟢
**Files**: `crates/trakkt-ui/src/components/terminal/vte_handler.rs`, `crates/trakkt-ui/src/components/terminal/renderer.rs`, `crates/trakkt-ui/src/components/terminal/input.rs`, `crates/trakkt-ui/src/components/terminal/tab_manager.rs`, `crates/trakkt-ui/src/components/terminal/mod.rs`, `crates/trakkt-ui/Cargo.toml`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Logical Correctness | vte_handler.rs:327–332 | DECSET/DECRST ?1049 (xterm alt-screen) must save/restore cursor on enter/exit. Current code only sets the flag and clears the screen. Programs relying on 1049 (vim, less, htop) assume their cursor position is restored when they exit, and that the cursor was saved at the moment they entered. Without save/restore the cursor lands at the wrong position after every TUI program exits. |
| 2 | 🟡 | Logical Correctness | vte_handler.rs:83–87 | `cursor_to` is called without applying the DECOM (origin mode) offset. When `modes.origin_mode` is true, CUP/HVP addresses are relative to `scroll_top`; the target row should be `row + scroll_top`, not `row`. Origin mode is already set via ?6, so programs that use it (e.g. some pagers) will place their cursor at the wrong absolute row. |
| 3 | 🟡 | Design System / Component Violations | tab_manager.rs:75–83 | The close-session "×" button is a `<span role="button">` inside a `<button>`. A `<span>` with `role="button"` is not keyboard-reachable by default (no `tabindex`, no `onkeydown`/`onkeyup` handler). Keyboard-only users cannot close a tab. The correct fix is a nested `<button type="button">` — semantically correct, inherently focusable, receives click from Enter/Space. Nested `<button>` inside `<button>` is technically invalid HTML but universally handled by browsers; if strict HTML validity is desired, restructure as two sibling elements via CSS positioning. |
| 4 | 🟢 | Code Quality | tab_manager.rs:14 | `SessionTab::active` is a declared public field but is never read anywhere in the component — the component derives active state from the `active_session: Signal<Option<String>>` prop. Dead field. Either remove it or document why it exists (e.g. for callers to construct `SessionTab` values without needing a separate signal). |
| 5 | 🟢 | Code Quality | renderer.rs:183 | `_on_input: Callback<Vec<u8>>` is accepted as a prop but prefixed with `_` and never used. The doc comment says it is "reserved for the input module." This is a leaky abstraction: the parent is forced to construct a `Callback` that does nothing. If the input module handles keyboard events independently, the prop should not be on `TerminalRenderer`. If it is intended for future wiring, a comment should explain the intended data-flow so the next developer knows what to do rather than treating it as cargo. |
| 6 | 🟢 | Design System / Component Violations | tab_manager.rs:41,62–70 | Tab buttons are missing `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring`. Per `CODING_STANDARDS.md`: "All interactive elements need `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring`." The new-session `<button>` at line 92 has the same omission. |
| 7 | 🟢 | Code Quality | vte_handler.rs:53–56 | The colon-separated subparam flattening (`sub[0]`) is correct for the `38;5;n` / `38;2;r;g;b` semicolon style but silently drops the modern colon-style encoding (`38:2::r:g:b`). This is an acceptable limitation for a v1 but should be documented with a `// TODO: colon-style 38:2::r:g:b subparams not yet handled` comment so the behavior is explicit rather than an invisible gap. |

**Anti-pattern tally**: Logical Correctness ×2, Design System / Component Violations ×2, Code Quality ×3

**Notes**: Overall the implementation is solid. The VTE handler covers all required CSI sequences with accurate parameter defaulting, the SGR path handles 256-color and true-color correctly, and the test coverage across all four modules is comprehensive. The input translation table is complete and correct (F1–F4 using SS3, F5–F12 using CSI tilde, application vs. normal cursor mode, ctrl-key byte generation). The DOM renderer's color palette, span-style builder, and cursor overlay are all technically correct. The three 🟡 issues are functional gaps: the 1049 cursor-save omission will cause cursor misplacement after every TUI program; the DECOM offset miss will cause incorrect cursor placement in origin-mode programs; and the inaccessible close button is an a11y regression. None of these are regressions against a prior passing state (this is new code), but all three need fixing before any real terminal traffic.

---

### 17:38 — Terminal Grid: WASM terminal emulator grid state machine (re-review, cycle 2)

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/components/terminal/grid.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|

*(No issues found)*

**Anti-pattern tally**: none

**Notes**: All five prior findings resolved correctly. (1) `CursorState::visible` is gone; `modes.cursor_visible` is the sole source of truth for cursor visibility — `save_cursor`/`restore_cursor` are unaffected. (2) `#[derive(Debug, Clone)]` is present on both `TerminalModes` and `StyledSpan`. (3) The always-true guards in `insert_chars`/`delete_chars` were replaced with early-return on `col >= self.cols`; `insert_lines`/`delete_lines` use `debug_assert!` before the loop — the assert is safe because each loop iteration does one `remove` + one `insert`, keeping `cells.len()` constant, so `bottom`/`row` remain valid for all iterations. Issues #4 and #5 (erase_cells_range background color and O(n²) scroll) remain tracked as known limitations out of scope for this PR. All 15 unit tests pass. No regressions introduced.

---

### 17:05 — Terminal Grid: WASM terminal emulator grid state machine (initial)

**Review type**: initial
**Result**: 1 🟡, 4 🟢 (approved — no 🔴 issues)
**Files**: `crates/trakkt-ui/src/components/terminal/grid.rs`, `crates/trakkt-ui/src/components/terminal/mod.rs`, `crates/trakkt-ui/src/components/mod.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | State Management Issues | grid.rs:116,158 | `CursorState::visible` is dead state — declared, initialized to `true`, never read or written by any Grid method. `TerminalModes::cursor_visible` is the active field. `save_cursor`/`restore_cursor` do not save/restore `cursor.visible`. Two sources of truth with no synchronization. |
| 2 | 🟢 | Code Quality | grid.rs:144,181 | `TerminalModes` and `StyledSpan` lack `#[derive(Debug, Clone)]`. Limits usability in tests and logging without any design reason to withhold them. |
| 3 | 🟢 | Fallback Code | grid.rs:504 | `if col < self.cols` guard inside `insert_chars` loop is always-true when the loop body should execute; never guards a real error — confuses intent. Same issue at grid.rs:601 (`if bottom < self.cells.len()`) and grid.rs:624 (`if row < self.cells.len()`). |
| 4 | 🟢 | Code Quality | grid.rs:733 | `erase_cells_range` always erases to `Cell::default()` (default colors), ignoring `current_bg`. ANSI spec requires erase operations to use the current background color attribute. Acceptable stub at this stage but should be tracked. |
| 5 | 🟢 | Code Quality | grid.rs:554,578 | `scroll_up`/`scroll_down` use `Vec::remove` + `Vec::insert` (both O(rows)) inside a loop — O(rows²) per scroll operation. Acceptable for a state machine stub; a ring-buffer would fix this when performance matters. |

**Anti-pattern tally**: State Management Issues ×1, Code Quality ×3, Fallback Code ×1

**Notes**: Implementation is complete and spec-compliant. All required types (Color, CellAttrs, Cell, CursorState, TerminalModes, StyledSpan, Grid), methods, and the module structure are present. No banned patterns (`#[allow(...)]`, `unwrap_or_default()`, OS-specific imports). WASM safety is clean — only `std::collections::HashSet` is imported, no platform-specific code. License headers present on both new files. Tests cover the key behaviors well. The single 🟡 issue (dead `cursor.visible` field vs. active `modes.cursor_visible`) is a design inconsistency that will confuse whoever writes the VTE handler: they'll see `cursor.visible` in `CursorState`, expect that's the canonical field, write to it, and find the renderer uses `modes.cursor_visible`. This must be resolved before the VTE handler is written.

---

### 14:42 — Connect: Server-side ConnectManager and WS endpoints re-review (cycle 2)

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/connect_manager.rs`, `apps/server/src/routes/connect.rs`, `apps/server/migrations/20260612000000_connect.sql`, `apps/server/migrations-sqlite/20260612000000_connect.sql`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|

*(No issues found)*

**Anti-pattern tally**: none

**Notes**: All four items from the initial review were resolved cleanly. The cross-workspace session hijack is correctly addressed via `get_session_workspace()` — the method is minimal, correct, and the call site in `handle_browser_message` covers all four mutation operations (SessionInput, SessionResize, SessionKill, ScrollbackRequest) under a single guard. The `unwrap_or_else` fix for SystemTime is correct. The `spawn_session_forwarder()` extraction removes the duplication without any behavioral change. The migration comments are clear and the `idx_connect_sessions_user` index is present in both Postgres and SQLite variants. The ScrollbackRequest double-lock on `subscribed_sessions` is not a TOCTOU issue because `handle_browser_message` is synchronous and the recv task is single-threaded per connection. Task lifecycle (tokio::select! + tokio::spawn) is consistent with the existing agent WS handler and acceptable for this relay use case. One design-level note outside this PR's scope: `session_id` is browser-chosen in SpawnSession, which means a malicious browser can attempt to collide with an existing session_id (DoS only, not a workspace leak, since get_session_workspace would return the overwritten workspace); this is an architectural decision to document for a future hardening pass.

---

### 00:00 — Connect page: re-review (cycle 2) after prior findings fixed

**Review type**: re-review (cycle 2)
**Result**: 0 🔴, 1 🟡, 2 🟢
**Files**: `crates/trakkt-ui/src/pages/connect.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Design System / Component Violations | connect.rs:744,746,811,813 | `bg-success-foreground` and `bg-error-foreground` are not Tailwind background-color utility classes — no `.bg-success-foreground` rule exists in `output.css`. The status dot will render with no background color and be invisible. Fix: use `bg-[color:var(--color-success-foreground)]` / `bg-[color:var(--color-error-foreground)]` (arbitrary CSS variable form). |
| 2 | 🟢 | Design System / Component Violations | connect.rs:738,805 | Header uses hand-rolled `px-6 py-3 border-b border-border bg-background` instead of the project-standard `page-header h-14 px-5` class. DESIGN.md prohibits `border-b` between header and content. All other pages use `page-header`. |
| 3 | 🟢 | Copy-Paste / Missing Abstractions | connect.rs:735-793,803-850 | Duplicate header+viewport markup across WASM and SSR branches — carried over from initial review, not yet addressed. |

**Anti-pattern tally**: Design System / Component Violations ×2, Copy-Paste ×1

**Notes**: Three of the four prior issues were cleanly resolved: `ListSessions` is correctly sent in `onopen` (line 242); both `keydown` and `paste` listeners are removed in a dedicated `on_cleanup` block with matching function pointers; session ID now includes a random component. The one regression introduced by the fix is the color token naming error — `bg-success-foreground` resolves to a CSS variable name, not a background-color utility, and Tailwind does not auto-generate `bg-*` for `--color-*-foreground` variables. The status dot will be invisible until corrected.

---

### 14:55 — Connect page: re-review (cycle 3) — color token fix verification

**Review type**: re-review (cycle 3)
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/pages/connect.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|

*(No issues found)*

**Anti-pattern tally**: none

**Notes**: Verified that `bg-success-foreground` / `bg-error-foreground` are gone from the file. Lines 744, 746, 811, and 813 all use `bg-[color:var(--color-success-foreground)]` / `bg-[color:var(--color-error-foreground)]` — the correct arbitrary CSS variable form that Tailwind generates a background-color utility for. Both WASM and SSR branches are consistent. No new issues introduced. Review signed.

---
