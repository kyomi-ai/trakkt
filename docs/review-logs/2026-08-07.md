---

### 13:32 — TRA-9978: default-team sync (workspace_service::set_workspace_default_team)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/workspace_service.rs`, `crates/trakkt-auth/src/team_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/server_fns/teams.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: `set_workspace_default_team` was converted to the exact sibling pattern (`update_workspace_name`/`update_workspace_settings`): validation on the pool before `begin()`, `tx_execute!` inside the transaction, `Ok(false)` on zero rows, transaction-scoped snapshot read, `commit_and_deliver` with `SyncAudience::Workspace`. `default_team_id` added to `WORKSPACE_SNAPSHOT_SQL`/`into_sync_value`; verified independently that `cache/apply.rs`'s two `WORKSPACE_SETTINGS` arms only bump a version counter (never decode payload fields) and that the bootstrap reader (`websocket.rs:631-650`) forwards the snapshot opaquely, so the SCHEMA_HASH-skip claim holds. `check-tx-pool.py` passes; hand-traced `get_team`, confirmed no pool-scoped call between `begin()`/`commit()`. Ran `cargo test -p trakkt-auth` (238 passed) and mutation-tested the "post-update snapshot" test by hand (moved the snapshot read before the UPDATE) — it failed as expected, confirming the test discriminates. The `delete_team` doc/comment rewrite (step 5 now an independent transaction that commits+broadcasts before step 6) was checked against the actual code and is accurate; the two-transaction ordering is unchanged from before this diff (previously invisible, now visible), not a new correctness issue. `WorkspaceFootprint` struct refactor changes no assertion's meaning — all existing call sites compare against previously-captured `before`/`workspaces_before` values, not new literals, and `cargo check` confirms. No banned patterns, no fallback code, no suppressions.

---
