---

### 13:32 — TRA-9978: default-team sync (workspace_service::set_workspace_default_team)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/workspace_service.rs`, `crates/trakkt-auth/src/team_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/server_fns/teams.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: `set_workspace_default_team` was converted to the exact sibling pattern (`update_workspace_name`/`update_workspace_settings`): validation on the pool before `begin()`, `tx_execute!` inside the transaction, `Ok(false)` on zero rows, transaction-scoped snapshot read, `commit_and_deliver` with `SyncAudience::Workspace`. `default_team_id` added to `WORKSPACE_SNAPSHOT_SQL`/`into_sync_value`; verified independently that `cache/apply.rs`'s two `WORKSPACE_SETTINGS` arms only bump a version counter (never decode payload fields) and that the bootstrap reader (`websocket.rs:631-650`) forwards the snapshot opaquely, so the SCHEMA_HASH-skip claim holds. `check-tx-pool.py` passes; hand-traced `get_team`, confirmed no pool-scoped call between `begin()`/`commit()`. Ran `cargo test -p trakkt-auth` (238 passed) and mutation-tested the "post-update snapshot" test by hand (moved the snapshot read before the UPDATE) — it failed as expected, confirming the test discriminates. The `delete_team` doc/comment rewrite (step 5 now an independent transaction that commits+broadcasts before step 6) was checked against the actual code and is accurate; the two-transaction ordering is unchanged from before this diff (previously invisible, now visible), not a new correctness issue. `WorkspaceFootprint` struct refactor changes no assertion's meaning — all existing call sites compare against previously-captured `before`/`workspaces_before` values, not new literals, and `cargo check` confirms. No banned patterns, no fallback code, no suppressions.

---

### 15:15 — TRA-10016: SyncAudience for write_sync_entry/write_sync_entry_in_tx

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/issue_service.rs`, `crates/trakkt-auth/src/comment_service.rs`, `crates/trakkt-auth/src/team_service.rs`, `apps/server/src/routes/websocket.rs`, `apps/server/tests/postgres_dialect.rs`, `apps/server/tests/sync_ws.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: Counted the substitution mechanically: `git diff --cached` on the three service files shows exactly 14 removed `None,` lines paired with 14 added `SyncAudience::Workspace,` lines, and exactly 1 removed `Some(user_id),` paired with 1 added `SyncAudience::User(user_id),` — nothing else changed in those three files. Read both writer bodies in `sync_log_service.rs`: `audience.visibility_user_id()` is read exactly once near the top of each, the rest is byte-identical; `visibility_user_id()` (private, no `pub`) maps `Workspace ↦ None` / `User(u) ↦ Some(u)`, the exact inverse of the removed call-site literals. `SyncBatch::record` now passes `audience` (not `.visibility_user_id()`) into `write_sync_entry_in_tx`, and `commit_and_deliver` still matches on `entry.audience` unchanged — delivery untouched, confirmed no `broadcast_sync_action`/`send_sync_action_to_user` call appears anywhere in the diff. Spot-checked three per-site verdicts against actual read paths: dispatched an agent to verify ISSUE (`list_issues`, no user predicate) and COMMENT (`list_comments_for_workspace`, filters only `workspace_id`) bootstraps plus all 8 issue/comment call sites — all correctly `Workspace`; independently read `ENTRIES_SINCE_SQL` (`sync_log_service.rs:382-406`) myself and confirmed its `team_members` EXISTS predicate is gated by `entity_type <> 'team' OR action = 'delete' OR EXISTS(...)`, so `team_service.rs:914` (TEAM Delete, `Workspace`) is correctly exempted by the delete clause, while `team_service.rs:1132` (the departing member's eviction, `User(user_id)`) is the one write in the diff that is genuinely user-scoped and correctly is not `Workspace` — verified `write_membership_sync_entry` sends the surviving-members `Update` as `Workspace` (narrowed at read time by the same EXISTS predicate, non-delete) and the departing member's `Delete` as `User(user_id)`. Confirmed `SyncAudience` is `pub` (required: `postgres_dialect.rs` is an external integration-test crate importing it, and `sync_log_service` is `pub mod`) while `visibility_user_id()` stays private — public surface grew by exactly one enum. Ran both doctests (`cargo test -p trakkt-auth --doc sync_log_service`): the `compile_fail,E0308` block fails to compile and the error is genuinely `E0308` (grepped the rustc output directly, not trusting the annotation); the `no_run` twin compiles; diffed the two blocks and confirmed they differ only on the audience-argument line. Sanity-checked the mutation-sweep survivor claim by hand: temporarily changed `comment_service.rs:419` (`delete_comment`)'s `SyncAudience::Workspace` to `SyncAudience::User("mutation_test_nonexistent_user_zzz")`, ran `cargo test -p trakkt-auth --lib` (238 passed, 0 failed) and grepped for any test referencing `delete_comment` (none), confirming the mutation survives as reported; reverted and verified `git diff` on the file is empty again. Verified the reported-but-untouched TEAM live-frame gap: `add_team_member`/`remove_team_member`/`update_team_member_role` (`team_service.rs:1160,1198,1223`) take no `ws_manager` parameter and emit no live frame; `delete_team`'s TEAM `Delete` broadcast (`team_service.rs:937`) passes `None` as its payload; `crates/trakkt-ui/src/cache/apply.rs:124-126`'s TEAM arm calls `store.upsert_team(item)` unconditionally on whatever arrives, with no membership check — the claim is accurate and the finding is real, appropriately left unfixed per the ticket's no-behaviour-change constraint (filed separately). Judged the decision against adding `SyncAudience::Team` as correct: the new `# What this type does not cover: TEAM` doc section (`sync_log_service.rs:727-759`) names the gap concretely (file/line references, not hand-waved) rather than adding a variant that would be byte-identical to `Workspace` today and read as enforcement while enforcing nothing. Ran `cargo clippy --locked -p trakkt-auth -p trakkt-server --all-targets -- -D warnings` (clean) and the full `trakkt-auth`/`trakkt-server` lib+bin test suites plus `sync_ws.rs` and `postgres_dialect.rs` (SQLite halves) — all green. No `#[allow(`, no new pub surface beyond the one enum, no fallback code, no lines over 100 columns among the additions.

---

---

### 17:11 — TRA-10039: Team live-frame audience fix (SyncAudience::Team)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/team_service.rs`, `crates/trakkt-auth/src/websocket/manager.rs`, `apps/server/tests/sync_ws.rs`

No table — zero findings.

**Anti-pattern tally**: none

**Notes**: Adds `SyncAudience::Team(&team_id)` whose delivery arm resolves `team_members` at delivery time (strictly post-commit, confirmed via `check-tx-pool.py`), converts `create_team`/`commit_team_update`/`delete_team` off hand-rolled broadcast onto `SyncBatch`/`commit_and_deliver`. Verified independently rather than trusting the request's claims: ran full `sync_ws` suite (11/11 pass, including 4 new TEAM live-frame tests and the agreement test comparing live vs. delta by `(sync_id, entity_id, action, data)`), ran clippy across every affected crate with `--all-targets -D warnings` (clean), confirmed `delete_team`'s hand-rolled `reassigned` accumulator is gone, confirmed the probe mechanism in `drain_live_frames` travels the identical `deliver()` path as the frames under test (non-vacuous), confirmed `write_membership_sync_entry`'s Workspace→Team move is inert (none of its 3 callers pass a `WebSocketManager`), confirmed the client cache grep claim (`team_members` absent from `crates/trakkt-ui/src/cache/`), and confirmed `delete_team` correctly keeps `SyncAudience::Workspace` (cascade empties `team_members` before the delete row is read). Judgement A (create_team delivering both sync entries live) ruled correct — necessary for live/delta agreement, not just harmless. Judgement B (no Postgres-specific live-path test) ruled sound — the SQL has zero dialect surface and reuses a query shape already exercised on Postgres via `broadcast_raw_to_workspace`, with the dialect-sensitive half already covered by the existing dual-backend `team_delta_entries_are_scoped_to_current_members`. Exceptionally well-documented diff; doc comments make checkable claims and the claims check out.

---

### 18:58 — TRA-10025: favorites.target_id dangling-row fix (no FK, per-parent cascade)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `apps/server/migrations/20260807000000_prune_dangling_favorites.sql`, `apps/server/migrations-sqlite/20260807000000_prune_dangling_favorites.sql`, `apps/server/tests/postgres_dialect.rs`, `crates/trakkt-auth/src/favorite_service.rs`, `crates/trakkt-auth/src/issue_service.rs`, `crates/trakkt-auth/src/project_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/team_service.rs`, `crates/trakkt-auth/src/view_service.rs`, `crates/trakkt-types/src/enums.rs`, `crates/trakkt-types/src/models.rs`, `crates/trakkt-ui/src/components/layout.rs`, `crates/trakkt-ui/src/pages/projects/project_detail.rs`, `crates/trakkt-ui/src/server_fns/favorites.rs`

No table — zero findings.

**Anti-pattern tally**: none

**Notes**: Closes `favorites.target_type` from a free `String` to a macro-generated `FavoriteTarget` enum (`ALL`/`as_str`/`from_wire` from one input), rejected at the server-fn boundary via `parse_target`, and wires `favorite_service::doomed_favorites_tx`/`DoomedFavorites::delete_and_record` into all four delete paths (`delete_issue`, `delete_project`, `delete_team` — including its second-order team-scoped-view cascade — `delete_view`), plus a backfill migration for rows that went dangling before the fix existed. Verified every scrutiny point independently rather than trusting the request: diffed the Postgres/SQLite migration bodies (byte-identical), confirmed each `NOT EXISTS` predicate keys off the correct PK column, confirmed via `20260803100000_dual_backend_fk_parity.sql`'s header that the DROP-cascade hazard is specific to SQLite table-rebuild migrations and doesn't apply to this plain-DML migration, ran `scripts/check-migrations.sh` (44 versions matched). Ran `scripts/check-tx-pool.py` (passes) and hand-traced all four delete paths — `doomed_favorites_tx`/`delete_and_record` only ever touch `tx: &mut DbTx` via `tx_fetch_all!`/`tx_execute!`, never the pool. Confirmed the audience claim: `favorites` carries its own `workspace_id` with no FK tying it to the target's actual workspace, and `add_favorite`'s server fn binds the caller's session workspace against a client-supplied `target_id` with no cross-check (the TRA-10044 gap), so a favorite naming a target in a different workspace than its owner's is possible today — reading `workspace_id` off the favorite row rather than the parent is therefore correct, and `SyncAudience::User` (not `Workspace`, not the TRA-10039-added `Team`) is right because `list_favorites` filters by `user_id`. Live-mutated the code twice to verify claims rather than reading them: (1) added a 5th `FavoriteTarget` variant and confirmed `cargo check --tests` produces exactly two `E0004` non-exhaustive-match errors in the test file's `create_favoritable`/`delete_favoritable`, both of which dispatch to real production delete functions, not stand-ins; (2) reverted `delete_team`'s new per-row loop to the exact pre-fix bare `DELETE FROM favorites WHERE target_type='team'` and confirmed `every_favorite_target_is_deleted_with_its_target::sqlite` passes its row-count assertions and fails only the sync-entry assertion (`left: []`) — both mutations reverted cleanly (`git diff` empty on the touched file afterward, confirmed via `git status`/`git diff --stat` against the full staged index at the end). Confirmed the strict-write/lenient-read asymmetry (`parse_target` rejects; `Favorite.target_type` stays `String` for legacy rows) is documented at both the callee (`models.rs`) and the boundary (`server_fns/favorites.rs`), matching the comment-accuracy standard. Confirmed no stray call sites still use the old `&str` signature. Recomputed added-line lengths directly from `git diff --cached` after an initial measurement bug overcounted several lines; corrected count is exactly two added lines over 100 columns, both Leptos `view!` attribute lines matching surrounding style, consistent with the known-broken `cargo fmt` (TRA-10035) applying repo-wide, not to this diff specifically. `cargo clippy --locked --all-targets -- -D warnings` and `cargo check` both clean on all affected crates. No `#[allow(`, no fallback code, no debug residue, no stray mutation left in the tree from the reviewer's own testing.

---

### 20:33 — TRA-10032: Project board/list view-tab state audit (BoardViewState/ListViewState hoist)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/pages/projects/project_board.rs`, `crates/trakkt-ui/src/pages/projects/project_detail.rs`, `crates/trakkt-ui/src/pages/projects/project_list_view.rs`, `e2e/tests/sync/project-view-state.spec.ts`

No table — zero findings.

**Anti-pattern tally**: none

**Notes**: TRA-10030 follow-up audit. Verified the central closure-chain claim by reading the actual (unmodified) code: `ProjectDetailPage`'s content closure (`project_detail.rs:475`) reads all six resources (`project_data`, `project_issues`, `server_progress`, `server_milestones`, `server_updates`, `server_members`) and constructs `ProjectDetailContent` fresh on every settle; the nested `{move || active_view…}` child (`:1010`) reading only `active_view` is one level further in, inside that reconstruction, not outside it — so it looked safe but wasn't. Verified the `tachys` claim directly against the vendored source (`tachys-0.2.15/src/reactive_graph/mod.rs:77-83`, confirmed via `Cargo.lock`): `impl<F,V> Render for F`'s `rebuild` is exactly `let new = self.build(); … old.unmount();`, an unconditional fresh build, not a diff — this is the path a nested dynamic child takes when its ancestor component function is called again from scratch, regardless of `AnyView::rebuild`'s same-type-id fast path (`any_view.rs:386-400`) diffing the outer shell. Ran the real repro: `cargo check`/`clippy --target wasm32-unknown-unknown --features hydrate --all-targets -D warnings` both clean; `wasm-pack test --headless --firefox` on `view_tab_rebuild_tests` passes (2/2) with the fix in place. Built the discriminating variant myself (shadowed `search` with a fresh body-local `RwSignal` in both `ProjectBoardContent` and `ProjectListView`, reverting the hoist for that one field) and reran the same wasm tests: both fail every run, on the exact final assertion (`left: ""`, `right: "half-typed filter"`), with the `content_builds==2` and empty-probe guards passing first — matches the claim precisely. Reverted the probe mutation and confirmed `git diff --quiet` clean against the staged index afterward (a `git stash`/`stash pop` cycle used for the fmt-drift check also transiently unstaged the three `.rs` files; re-added them and confirmed `git diff --cached --stat` matches the original 687/60 exactly). Judgement A (hidden_categories not hoisted): verified the write-through claim directly — the only mutation site is `ProjectBoardDisplayOptions`'s checkbox `on_change`, which calls `write_hidden_to_storage` synchronously in the same handler, and the per-project-keyed init `Effect` (whose own `initialized: StoredValue` is also reset on rebuild, so it re-fires) reads it straight back; no path mutates the set without persisting it. Correct not to hoist. Judgement B (tab switch doesn't reset, project switch does): confirmed `ProjectEditState::reset()` calls both `self.board.reset()` and `self.list.reset()`; confirmed `ListViewState`'s three non-empty defaults (`SortField::Priority`, `SortDirection::Asc`, `GroupBy::None`) are named as `const`s shared by `Default` and `reset`, so they cannot drift. Judgement C (drop handler also clears `dragging`): confirmed via `cargo tree -e features` and `leptos-0.8.19`'s `Cargo.toml` that `tachys/delegation` is a non-default feature not enabled anywhere in the workspace, matching the stated rationale; the extra clear is a direct, in-scope consequence of hoisting `dragging` (a fresh-per-rebuild signal never needed this defensiveness) and not scope creep. Confirmed `BoardViewState`/`ListViewState` reuse `ProjectEditState`'s exact established pattern (`#[derive(Clone, Copy)]`, constructed in `ProjectDetailPage`, passed as a `state` prop) rather than introducing a parallel mechanism. Confirmed nested ephemeral signals left alone (`did_drag` at `project_board.rs:725`, popover `open` flags at `:496` and `project_list_view.rs:460`) are reasonable — genuinely transient UI state, and `did_drag`'s reset-by-rebuild is already tracked as its own ticket per the prompt's instructions not to re-raise it. Confirmed the diff stays on the state half only — no `Show`/DOM-branch changes, that's TRA-10031. Reviewed the new e2e spec (`project-view-state.spec.ts`): both tests' barrier (waiting for the colleague's new issue title to appear in B) is genuinely outcome-independent — the created issue's title contains the literal filter token, so the card is visible whether the filter survived or was cleared, and the list test's barrier doesn't depend on grouping at all. Spec is written and typechecks but per the prompt was not executed (server build didn't finish locally); noted as-is, not re-raised as a finding. Reran `cargo fmt --package trakkt-ui -- --check` with and without the diff (via `git stash`): identical 10/30/6 per-file hunks before and after, confirming the reported rustfmt drift is pre-existing (TRA-10035) and unrelated to this change. No `#[allow(`, no fallback code (`unwrap_or_default`/`.ok()`/silent `let _ =` swallowing), no stubs or mocks; the one `let _ = dt.set_data(...)` in `project_board.rs` is pre-existing HTML5 DnD boilerplate, unchanged by this diff.

---
