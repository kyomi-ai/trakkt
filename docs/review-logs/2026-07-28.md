---

### 00:04 — TRA-9948: atomic sync_log commit for project_service (third of eight)

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 2 🟢
**Files**: `crates/trakkt-auth/src/project_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Code Quality | project_service.rs:981-982 | Doc comment on `delete_milestone` is grammatically garbled: "The DELETE, the `issues.milestone_id` clears the schema drives from it, and..." — meaning is recoverable from context but should be tightened. |
| 2 | 🟢 | Code Quality | sync_log_service.rs:3921 (new `milestones` test helper) | New test helper's signature exceeds rustfmt's line width and is unwrapped in the diff (`cargo fmt --check` flags it). Rest of both files has pre-existing, unrelated fmt drift throughout (not introduced by this diff), so this is the only fmt nit actually inside the new code. |

**Anti-pattern tally**: Code Quality ×2

**Notes**: Excellent execution of the established atomic-mutation pattern (TRA-9923 → TRA-9947 → this). All 9 mutation sites in `project_service.rs` (create/update/delete project, add/remove member, create/update/delete milestone, create project update) now open a `DbTx`, run their statements on it, write the `sync_log` entry via `write_sync_entry_in_tx`, commit, then broadcast — via a new shared `commit_and_broadcast` tail identical in spirit to TRA-9947's `commit_team_update`. Traced every transaction span line-by-line: zero DbPool-taking calls appear between any `db.begin()` and its matching `commit_and_broadcast()` — the only calls inside are `tx_execute!`/`tx_fetch_one!`/`tx_with!` (transaction-scoped macros), plus genuinely pure helpers (`sync_payload` — serde only, `project_member_entity_id` — format! only, `Row::into_dto` — pure, `sql_compat::{now,cast_to_date,cast_to_timestamptz}` — pure string builders, `db.is_postgres()` — enum match, no I/O). No SQLite single-connection hang risk found. `get_project_in_workspace` (the TRA-9943 cross-workspace guard) verified still called on the pool before `db.begin()` in both `add_project_member` (line 661) and `remove_project_member` (line 723). All 9 `rows_affected()==0` paths call `tx.rollback()` before returning `NotFound`; no path leaves a transaction dangling or commits a partial mutation. Verified the vacuous-test trap does not apply here: `seed_project` uses raw `db_execute!` INSERTs (not the services), so seeding writes zero sync rows, and `grep` confirms no `CREATE TRIGGER` exists anywhere in the SQLite migrations, so the unscoped `reject_sync_log` trigger installed by tests is unambiguous. Each of the 9 new tests asserts full-row equality (not just counts) before/after, and `delete_project`'s test checks five independent footprints (project, members, milestones, posted updates, issue links) to prove the whole cascade unwinds on rollback, not just the `projects` row. Ran 3 of the 9 tests individually plus the full suite single-threaded (`--test-threads=1`) — all pass with zero collateral, confirming test isolation. All four required gates pass, exactly matching reported figures: workspace clippy clean, wasm32 trakkt-ui clippy clean (`-D warnings` both), `cargo test --workspace` 160/160 in trakkt-auth (151→160, +9 new), `wasm-pack test --headless --firefox` 23/23. Confirmed no public function signature changed (grep diff for `pub async fn` shows all 9 as unchanged context lines). Independently verified the out-of-scope finding from migrations: `project_members`/`project_milestones`/`project_updates` are `ON DELETE CASCADE` on `project_id`, `issues.project_id` and `issues.milestone_id` are `ON DELETE SET NULL` — `delete_project` emits no sync entries for any of that cascade, consistent with what's being filed separately. Only findings are two trivial wording/formatting nits, neither blocking.

---

### 00:19 — TRA-9948: atomic sync_log commit for project_service (re-review, both 🟢 nits addressed)

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/project_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No findings survived. Both round-1 🟢 nits confirmed fixed; no new issues found. |

**Anti-pattern tally**: none

**Notes**: Scoped re-review of a comment-and-whitespace-only diff. (1) `delete_milestone` doc comment (now ending "...are one transaction — see [`delete_project`]. Nothing cascades: the issues outlive the milestone.") — verified accurate against both migration sets: `apps/server/migrations/20260509000003_projects.sql:153` and `apps/server/migrations-sqlite/20260509000003_projects.sql:57`/`migrations-sqlite/20260512000000_team_scoped_numbering.sql:39` all declare `issues.milestone_id` FK as `ON DELETE SET NULL`; grepped both migration trees for any other `REFERENCES project_milestones` and found none — `issues.milestone_id` is confirmed the sole FK, so "nothing cascades" is correct, not just grammatical. (2) `sync_log_service.rs` `milestones` test helper (lines 3923-3953): confirmed via `cargo fmt -p trakkt-auth -- --check` that this specific line range now produces zero diff (previously flagged for exceeding line width); the wrapped `.map(|r| (...))` tuple is element-for-element identical to the pre-wrap version (`r.milestone_id, r.project_id, r.name, r.description, r.target_date`, matching the declared return type and `Row` struct field order) — pure formatting, no logic change. Confirmed structural integrity: all 9 `pub async fn` mutation signatures plus `commit_and_broadcast` are present and unmoved (`grep -n "^pub async fn"` matches round-1's line count exactly). On the declined scope item (7 additional fmt violations wanting to *collapse* new `tx_fetch_one!`/`tx_execute!` calls) — independently verified both halves of the requester's reasoning: `cargo fmt -p trakkt-auth -- --check` reports 363 total diff hunks across the crate confirming pervasive pre-existing drift (not just ~200, even more), and `grep -in "fmt" .github/workflows/*.yml` across all four workflows returns nothing — no CI fmt gate exists. Agree with the call to decline: collapsing the explicit multi-line transaction calls the round-1 review specifically praised for clarity would be a readability regression with no enforcement to justify it. Re-ran `cargo clippy --workspace --locked -- -D warnings` (clean) and `cargo test --locked -p trakkt-auth` (160 passed, 0 failed, matching reported figure exactly). Signed — prior signature was stale (diff hash changed from `f507870e...` to `672da09b...`), re-signed against the current staged diff.

---

### 01:15 — TRA-9949: atomic sync_log commit for attachment_service (fourth of eight) + commit_and_broadcast/sync_payload hoist

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/attachment_service.rs`, `crates/trakkt-auth/src/project_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: Fourth conversion in the series, plus a mid-flight consolidation hoisting `commit_and_broadcast`/`sync_payload` out of `project_service.rs` (TRA-9948) into `sync_log_service.rs` so `attachment_service.rs` can share them rather than growing its own copies. Verified the ticket's own premise was wrong, as flagged: `attachment_service.rs` never touches object storage — confirmed via the module doc comment and by reading `crates/trakkt-api/src/attachments.rs`, where `storage.store()` runs before `create_attachment` and `storage.delete()` runs after `delete_attachment`, both outside any transaction, both order to degrade to wasted storage (not a dangling reference) on failure. Traced all four transaction spans (`create_attachment`, `delete_attachment`, `attach_to_issue`, `detach_from_issue`) line by line against `crates/trakkt-core/src/db.rs`'s documented single-SQLite-connection hang risk: every statement between `db.begin()` and the `commit_and_broadcast`/`tx.rollback()` tail runs through `tx_execute!`/`tx_fetch_one!` (transaction-scoped macros bound to `&mut DbTx`), zero `db_*`/pool calls in between. `get_attachment` (ownership read in `delete_attachment`) and the uploader-authorization check, plus the workspace-membership checks in `attach_to_issue`/`detach_from_issue`, all correctly stay on the pool ahead of `begin()` — authorization fires before any mutation in every case. Verified the hoist is behaviour-preserving by diffing the extracted `commit_and_broadcast`/`sync_payload` bodies in `sync_log_service.rs` against the pre-hoist versions removed from `project_service.rs` in this same diff (`git diff --cached`, lines 388-442 removed vs. 610-669 added): identical apart from the now-redundant `sync_log_service::` prefix stripped from internal calls. `attachment_service.rs` never had local copies of either function pre-diff (confirmed via `git show HEAD:crates/trakkt-auth/src/attachment_service.rs`) — it previously called `write_sync_entry`/`broadcast_sync_action` inline with best-effort `.unwrap_or_else(|e| {...; 0})` error swallowing, now replaced by `?`-propagating `commit_and_broadcast`. Ran all four gates myself rather than trusting the reported numbers: `cargo clippy --workspace --locked -- -D warnings` clean (forced a real recompile via `touch` to rule out a stale-cache false pass — 52s, all crates checked, zero warnings); `cargo clippy -p trakkt-ui --target wasm32-unknown-unknown --features hydrate -- -D warnings` clean; `cargo test --workspace --locked` 426/426 (also re-ran `cargo test -p trakkt-auth` standalone after touching the file: 164/164, matching); `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` 23/23. Verified the mutation-testing claim (H1: hoisted `.await?` → `.await.unwrap_or(0)` fails 13 rollback tests) arithmetically rather than by running a mutation tool: grepped every `*_rolls_back_when_its_sync_entry_cannot_be_written` test and counted call sites through `commit_and_broadcast` — 9 in `project_service.rs` (create/update/delete project, add/remove member, create/update/delete milestone, posted update) + 4 new in `attachment_service.rs` (create/delete attachment, attach/detach issue) = 13 exactly; the 17 not routed through it are issue (4) + comment (3) + team (10) tests, matching the claimed "not yet converted" set exactly. Verified both fixture traps described for the join-table pair: `attach_to_issue` uses `ON CONFLICT DO NOTHING` and its test seeds `att_loose` (unlinked) with an explicit precondition assertion that it isn't already linked; `detach_from_issue` returns `NotFound` before its sync entry unless linked, and its test seeds `att_linked` with a precondition assertion the link exists — both confirmed correct and non-vacuous. Confirmed `issue_attachments` is declared `ON DELETE CASCADE` on `attachment_id` in both `apps/server/migrations/20260520000000_issue_attachments.sql` and the SQLite equivalent, and the new `attachment_delete_rolls_back_...` test asserts both `attachment_rows` and `issue_attachment_links` are restored, proving the rollback unwinds the cascade. Confirmed `delete_attachment`'s `storage_path` return combined with `attachments.rs:152`'s `.await?` means a failed delete stops the blob delete from ever running. Agree with both declined-scope items: `team_service::commit_team_update` is a genuinely different shape (own transaction-scoped read-back, hard-coded TEAM/Update, returns `Team`) and doesn't belong in the shared helper; `team_service::team_payload_value` is confirmed a third structurally-identical copy of `sync_payload` (byte-identical match/log/None pattern) appropriate to defer to a separate ticket rather than fold into this diff's scope. Confirmed the point-8 honest non-claim: `Attachment` is a flat `String`/`i64` struct, so `serde_json::to_value` cannot fail on any reachable input, and none of the four new tests inspect frame/payload contents — the implementer correctly did not claim this path was test-verified. One thing worth surfacing that the implementer's point-8 framing understated: the *previous* `create_attachment` didn't just use `.ok()` for the broadcast payload, it hardcoded `None` for the **persisted** `sync_log.data` column (7th arg to the old `write_sync_entry` call), while `label_service.rs`'s equivalent still-unconverted call correctly passes `payload.clone()` there. Per this module's own documented client behavior ("an entry with no payload is skipped outright by the client... on the live frame and on delta alike"), that meant any client replaying missed attachment-create deltas from `sync_log` (as opposed to receiving the live broadcast) would have silently dropped them — a real latent bug, now fixed as a side effect of routing through `sync_payload`/`commit_and_broadcast`. Not a defect in this diff; flagging because it's a bigger win than "point 8" gave itself credit for. No issues found; signed.

---

### 02:09 — TRA-9950: atomic sync_log commit for notification/favorite/view services (fifth of eight) + commit_and_broadcast -> commit_and_deliver(SyncAudience)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/notification_service.rs`, `crates/trakkt-auth/src/favorite_service.rs`, `crates/trakkt-auth/src/view_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/attachment_service.rs`, `crates/trakkt-auth/src/project_service.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| - | - | - | - | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: Fifth conversion in the series, the first where the shared commit-tail actually had to change shape: seven of the eight new sites are per-user delivery (notifications, notification preferences, favorites, unshared views), and reusing TRA-9949's workspace-wide `commit_and_broadcast` here would have reintroduced the TRA-9920 leak live-side. The implementer deleted `commit_and_broadcast` and replaced it with `commit_and_deliver(tx, ..., audience: SyncAudience, ...)`, where `SyncAudience` is `Workspace | User(&str)` and a single value drives both `write_sync_entry_in_tx`'s `visibility_user_id` column and the post-commit delivery call (`broadcast_sync_action` vs `send_sync_action_to_user`) - read `sync_log_service.rs:559-664` end to end and confirmed there is exactly one branch point (the `match audience` after `tx.commit()`) and no code path that reads `audience` twice with different values, so persist-private-but-broadcast-public is structurally unrepresentable, not just discouraged. Independently reproduced both decisive mutations rather than trusting the reported counts: (A) swapped one call site's `SyncAudience::User(user_id)` -> `Workspace` in `favorite_service::add_favorite` - killed exactly 4 tests (`delta_from_zero_matches_bootstrap_for_per_user_entity_types`, `delta_never_carries_another_members_notifications_or_favorites`, `per_user_live_frames_reach_only_their_owner`, `per_user_rows_never_reach_another_members_delta`), matching "both persisted and both live" as claimed; (B, the load-bearing one) decoupled delivery from visibility inside `commit_and_deliver` itself - made it always call `broadcast_sync_action` regardless of `audience` while leaving the persisted-column write untouched - killed exactly 2 tests, both live (`live_notification_frame_reaches_only_its_recipient`, `per_user_live_frames_reach_only_their_owner`), with all 172 remaining (including every persisted-side assertion) still green, proving the persisted-side tests are genuinely blind to this failure mode and the live tests are load-bearing, not decorative. Restored the file after each mutation and confirmed `git diff --stat` returned clean against the original staged diff. Verified the reported nesting deadlock and its fix: `update_preference` calls `get_or_default_preferences(&DbPool)` before `db.begin()` (comment at `notification_service.rs:714-716` explains why - calling it between `begin`/`commit` would nest a transaction inside the same SQLite connection the outer one already holds) and re-fetches the post-update row via `tx_fetch_optional!` on its own transaction rather than calling `get_or_default_preferences` a second time. Independently confirmed the claim that even the *workspace* delivery path would deadlock inside an open transaction: `websocket/manager.rs:377` `broadcast_raw_to_workspace` calls `trakkt_core::db_fetch_all!(&self.inner.db, ...)` directly on the pool (not a transaction-scoped macro) to resolve `workspace_users` - on SQLite (`max_connections(1)`, confirmed at `db.rs:38`) this would hang if invoked before commit, so "commit before delivering" is a hard correctness requirement for both `SyncAudience` variants, not merely a style preference for the per-user path. Verified the vacuity trap and its handling: `update_preference` writes two sync entries in two separate transactions when no prefs row exists yet (the `Insert` from `get_or_default_preferences`, then its own `Update`) - with a blanket `reject_sync_log` trigger and no seeding, the abort would land on the first (unrelated) transaction and the test would pass without ever exercising `update_preference`'s own commit path; the new test `notification_preferences_update_rolls_back_when_its_sync_entry_cannot_be_written` seeds the row via an unguarded `get_or_default_preferences` call *before* installing the trigger, with an explicit assertion (`notify_comments` starts `true`) proving the seed landed. Grepped all other new tests in this diff for the same shape (a service call that itself calls `get_or_default_preferences` internally, under a trigger, without pre-seeding) and found none - the two other tests exercising `get_or_default_preferences` (`notification_preferences_create_rolls_back_...` and the create-half of `per_user_rows_never_reach_another_members_delta`) call it directly as the function under test, not as an internal dependency of something else. Confirmed the 13 call sites of the renamed `commit_and_deliver` in already-merged `attachment_service.rs` (4) and `project_service.rs` (9) are byte-equivalent in behavior: every one gained only `SyncAudience::Workspace` as the new positional argument, which `visibility_user_id()` maps to `None` (unchanged column value) and the `match audience` maps to the same `broadcast_sync_action` call with the same arguments as before - confirmed via full diff read, no other line in either file changed. Confirmed the fourth `create_notification` caller at `relation_service.rs:446` (inside `create_relation`) is safe: grepped `relation_service.rs` for `db.begin`/`tx_execute` and found none - `create_relation` never opens a transaction, so `create_notification`'s own `db.begin()` nests inside nothing. Confirmed the out-of-scope claim: `mark_as_read`, `mark_all_as_read`, `bulk_mark_as_read`, `bulk_mark_as_unread`, `bulk_delete_notifications`, `bulk_restore_notifications` (`notification_service.rs:333-490`, none touched by this diff) call only `db_execute!`/`bulk_update_notifications` and never `sync_log_service::write_sync_entry` or `commit_and_deliver` - confirmed real, and correctly scoped to a separate ticket since it's a missing-row gap that predates this PR rather than something this PR introduced. Ran all four gates directly rather than trusting reported numbers: `cargo clippy --workspace --locked -- -D warnings` clean, `cargo clippy -p trakkt-ui --target wasm32-unknown-unknown --features hydrate -- -D warnings` clean, `cargo test --workspace --locked` - `trakkt-auth` 174/174 exactly as reported, full workspace all green, `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` 23/23 exactly as reported. Grepped for leftover references to the deleted `commit_and_broadcast`/`deliver_view_action`/`view_visibility` and found none. No issues found; signed.

---

### 03:07 — TRA-9951: atomic sync_log commit for release_service/archive_service (sixth of eight) + SyncBatch

**Review type**: initial
**Result**: 1 🟡, 2 🟢
**Files**: `crates/trakkt-auth/src/release_service.rs`, `crates/trakkt-auth/src/archive_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Correctness / TOCTOU | `release_service.rs:233-247` | The `still_present: i64 = tx_fetch_scalar!(... COUNT(*) ...)` probe followed by a separate `issue_sync_payload_tx` read is two round trips with a real gap between them. Under Postgres's default READ COMMITTED isolation, a concurrent delete of that issue committing in that gap makes the probe see the row (count=1) but the subsequent read miss it, so `issue_sync_payload_tx` returns `Err(NotFound(...))`, which is not caught anywhere in this loop and propagates out, aborting and rolling back the *entire* release — exactly the "unrepairable, whole thing rolls back" outcome the surrounding comment reserves for a failed sync-log write, applied instead to the tolerable "vanished issue" case the probe exists to prevent. The code comment's stated reason for not matching on `NotFound` directly ("would fold the two together, and any other failure of the read would start being swallowed as vanished") does not hold: `trakkt_core::Error::NotFound(String)` (`crates/trakkt-core/src/error.rs:11-12`) is a distinct enum variant from `Error::Sqlx`/`Error::Internal` that any genuine DB failure would surface as, so `match issue_sync_payload_tx(...).await { Ok(p) => p, Err(trakkt_core::Error::NotFound(_)) => { skip }, Err(e) => return Err(e) }` cleanly separates "row missing" from "any other failure" in one query, with no window and no extra round trip — and preserves the pre-diff semantics exactly (which wrote an entry for every issue that still existed, full stop, with no separate existence probe). Postgres-only exposure: SQLite's single pinned connection (`db.rs:38`) means no other connection can run concurrently with this transaction, so the window is unreachable there. Not covered by `release_create_skips_an_issue_that_vanished_mid_loop`, whose trigger deletes the row immediately after the `release_issues` insert — well before the probe — so both the probe and the read see "gone" consistently and the interleaved-vanish case is never exercised. |
| 2 | 🟢 | Test robustness | `sync_log_service.rs:1589-1597` (`next_sync_action`), used at `~961` (`release_create_delivers_every_frame_after_the_commit`) | `conn.rx.recv().await` has no bounded timeout, so a delivery-inside-the-transaction regression (the exact deadlock this test exists to catch) hangs the test rather than failing it — the implementer's own observation. Pre-existing pattern (17 call sites across this file, inherited from the three prior signed-off PRs in this series), so not unique to this diff and not blocking, but worth a follow-up ticket to wrap `next_sync_action` in `tokio::time::timeout(...)` so a regression fails fast with a clear panic instead of riding CI's blunt 30-minute job timeout (`ci.yml:163`) with no indication of which test hung. |
| 3 | 🟢 | DRY / consistency | `team_service.rs:809-921` (`delete_team`) | Still hand-rolls the collect-into-`Vec`-then-broadcast-after-commit pattern `SyncBatch` now formalizes (confirmed: `reassigned: Vec<(String, Option<Value>, i64)>` collected inside the tx, iterated for `broadcast_sync_action` after `tx.commit()`). Already correct (ordering, transaction-scoping and pool-avoidance all verified), just duplicated logic now that a shared abstraction exists. Confirmed out-of-scope-and-correctly-so for this ticket (its own transaction is already atomic); worth a follow-up ticket to migrate it onto `SyncBatch` for consistency, not urgent. |

**Anti-pattern tally**: Correctness/TOCTOU ×1, Test robustness ×1, DRY/consistency ×1

**Notes**: Sixth conversion in the series, and the first to take two different transaction boundaries in one diff on purpose. Verified both independently against the actual predicates rather than trusting the doc comments: `release_service::unreleased_issues` (`release_service.rs:396-397,411-412`) filters `released_at IS NULL`, confirming a `releases` row committed without its issue stamps really would leak those issues into the *next* release's candidate list; `archive_service`'s select (`archive_service.rs:91`) filters `archived_at IS NULL`, confirming partial sweep progress really is safe to leave committed and picked up by the next run. Traced every span between `db.begin()` and `commit`/`commit_and_deliver` in both functions line by line against `db.rs`'s documented single-SQLite-connection hang risk: `archive_service.rs`'s per-issue loop uses only `tx_execute!` then routes through `commit_and_deliver` (itself now a one-entry `SyncBatch`); `release_service::create_release`'s whole span (`tx_execute!` ×2, `tx_with!` for the batched `released_at` UPDATE, `tx_fetch_scalar!` for the probe, `issue_sync_payload_tx`/`get_issue_by_id_tx`/`fetch_labels_for_issue_tx` — all `tx_*`-macro-only — `batch.record`, `tx_fetch_one!` for the re-fetch) has zero `db_*`/pool calls; confirmed the reported `db_with_pool!` → `tx_with!` conversion by diff. `team_service::get_team_archive_days` and `team_service::get_team_archive_days`'s caller in `archive_service.rs` run on the pool only before the per-issue loop begins its own transaction each iteration — correctly outside any open tx. Verified `SyncBatch` is a faithful generalization of `commit_and_deliver`'s c0d9822 shape: `record` calls the same `write_sync_entry_in_tx` with the same `audience.visibility_user_id()` mapping, `commit_and_deliver` does `tx.commit()` then delivers in push order via the identical `match audience` (`broadcast_sync_action` / `send_sync_action_to_user`) — the free `commit_and_deliver` function is now literally `SyncBatch` with one entry, so all five already-converted call sites (notification/favorite/view/attachment/project services) get byte-identical behavior; confirmed no other line in those files changed. Independently verified the reported payload behavior change is real and not scope creep, not a no-op: confirmed via the removed diff hunk that the pre-diff code persisted `None` for the RELEASE sync_log row while broadcasting `serde_json::to_value(&release)` live; confirmed `cache/apply.rs::enqueue_cache_writes` (`apply.rs:349-351`) drops any insert/update action with `data: None` before it ever reaches IndexedDB; confirmed `entity_types::RELEASE` is a genuinely-cached entity type via its membership in `ALL_CACHED_ENTITY_TYPES` (`sync_engine.rs:101`) — so before this diff, every release ever created was invisible to a client's delta replay/IndexedDB cache, only reachable via the live frame. This is a real latent bug fix forced architecturally by `SyncBatch`/`commit_and_deliver`'s single-payload-drives-both-paths design (same principle TRA-9950 already established for audience), not an unrelated behavior change riding along. Ran all 6 new tests (`cargo test -p trakkt-auth --lib sync_log_service::`) — 86/86 passed including the full pre-existing suite; `cargo build -p trakkt-auth` and `cargo clippy -p trakkt-auth --lib --all-targets` both clean with zero warnings in either touched file. Judged the reported test-overlap argument (one mutation failing both `archive_sweep_rolls_back_the_issue_whose_sync_entry_cannot_be_written` and `archive_sweep_keeps_earlier_issues_archived_when_a_later_one_fails`): the overlap is real for an error-swallowing-class mutation (both assert `expect_err` + the "sync_log insert rejected" message), but the pair is not redundant — the single-issue fixture in the first test structurally cannot distinguish a correct per-issue transaction boundary from an incorrect whole-sweep-wrapped one (both produce "nothing archived, nothing logged" with only one candidate issue), while the second test's two-issue fixture is exactly what's needed to prove the boundary itself (that a later iteration's failure doesn't roll back an earlier, already-committed one) — verified the trigger mechanics (`reject_sync_log_inserts` blanket vs. `reject_sync_log_inserts_after_the_first`'s count-based `WHEN` clause) support this reading. No `#[allow(...)]`, fallback/swallowed-error code, stubs, mocks, or `#[cfg(test)]` in production control flow found in any touched file. Not signed — the TOCTOU finding is a genuine, if narrow (Postgres-only, two-sequential-round-trip window), correctness gap that contradicts the function's own stated invariant and has a cheap, already-available fix (match on `Error::NotFound` instead of adding a probe); no ticket was cited for deferral.

---

### 03:24 — TRA-9951: re-review — TOCTOU fix (match on `Error::NotFound`, `COUNT(*)` probe removed)

**Review type**: re-review (cycle 2)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 1 🟢
**Files**: `crates/trakkt-auth/src/release_service.rs`, `crates/trakkt-auth/src/archive_service.rs` (unchanged from cycle 1), `crates/trakkt-auth/src/sync_log_service.rs` (one new test added)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Cross-module contract documentation | `issue_service.rs:484-489` (`issue_sync_payload_tx` doc) | The invariant `create_release` now depends on (`NotFound` from this call tree means exclusively "row absent") is documented only at the caller (`release_service.rs`). The callee's own doc still reads "Callers must have already established that the issue exists," which no longer describes how `create_release` uses it (it deliberately does *not* pre-establish existence — that's the fix). Not blocking: this is the same comment-as-contract style already used throughout this file and accepted in prior cycles of this series, and the current code is verified correct. Worth a one-line cross-reference added to the callee's doc so a future change to `issue_service.rs` in isolation doesn't silently invalidate what `release_service.rs` relies on. |

**Anti-pattern tally**: Cross-module contract documentation ×1 (non-blocking)

**Notes**: The prior 🟡 (TOCTOU window from the separate `COUNT(*)` existence probe) is resolved. The fix replaces the probe with `match issue_sync_payload_tx(...).await { Ok(p) => p, Err(NotFound(_)) => skip, Err(e) => return Err(e) }` — one query, no window. Independently re-verified the variant-exclusivity chain the fix depends on rather than trusting the implementer's claim: grepped every `Error::NotFound` construction in `issue_service.rs` (5 total — lines 496, 746, 930, 1179, 1384) and confirmed only line 496 (inside `issue_sync_payload_tx` itself) is reachable from this call tree; the other four belong to `update_issue`, `delete_issue`, `set_issue_labels`, `set_sort_order`, none of which `issue_sync_payload_tx`/`get_issue_by_id_tx`/`fetch_labels_for_issue_tx` call. Confirmed `tx_fetch_optional!`/`tx_fetch_all!` (the only macros in that tree) use sqlx's `fetch_optional`/`fetch_all`, which never raise `RowNotFound` — any real DB failure surfaces only as `Error::Sqlx` via the `#[from]` conversion in `crates/trakkt-core/src/error.rs:38-39`. Confirmed both `into_dto` calls and `issue_payload_value` are infallible (the latter catches serialize failures into `Ok(None)` with a warn log, never `Err`). Independently reproduced both mutations myself rather than trusting the reported counts (copied the file, edited, ran `cargo test -p trakkt-auth --lib sync_log_service::`, restored, diffed clean against staged): removing the `NotFound` arm entirely (everything propagates) → exactly 1 failure, `release_create_skips_an_issue_that_vanished_mid_loop`, 86 passed; widening to catch-all `Err(_) => continue` → exactly 1 failure, `release_create_fails_when_an_issues_payload_cannot_be_read`, 86 passed — both match the implementer's reported isolation exactly. Verified the new test's fault injection lands where claimed, not earlier (which would make it vacuous): `ISSUE_DETAIL_SELECT` (used by `get_issue_by_id_tx`, checked first) joins `issues`/`teams`/`statuses`/`users`/`projects`/`issue_relations` — no `issue_labels` — so `DROP TABLE issue_labels` cannot break it; `ISSUE_LABELS_SELECT` (used by `fetch_labels_for_issue_tx`, checked only after the detail row is found) explicitly joins `issue_labels`, so the failure fires exactly at the labels read, downstream of confirming the issue is present and healthy, which is the scenario the test's own docstring claims to isolate. Confirmed `before` state is captured before the `DROP TABLE`, so the precondition isn't itself corrupted by the injected fault. Re-ran the full suite on the restored (untouched) file: `cargo test -p trakkt-auth --lib sync_log_service::` 87/87 (up one from cycle 1's 86, the new test), full crate `cargo test -p trakkt-auth --lib` 181/181 matching the reported figure exactly; `cargo build -p trakkt-auth` and `cargo clippy -p trakkt-auth --lib --all-targets` both clean, zero warnings in either touched file. Confirmed `archive_service.rs` is byte-identical to cycle 1 (same 66-line diff), and the `sync_log_service.rs` diff adds exactly one new test function beyond cycle 1's set (`release_create_fails_when_an_issues_payload_cannot_be_read`) with everything else — `SyncBatch`, `record`, `commit_and_deliver`, the five prior tests — unchanged. TRA-9975 (timeout-wrap `next_sync_action`) and TRA-9976 (migrate `delete_team` onto `SyncBatch`) correctly left untouched, as instructed — did not re-litigate either. Signed.

---

### 03:37 — TRA-9951: re-review — 🟢 folded in (`issue_service.rs` doc contract) + stash-recovery verification

**Review type**: re-review (cycle 3)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `crates/trakkt-auth/src/issue_service.rs` (new to the diff, doc-only), `crates/trakkt-auth/src/release_service.rs`, `crates/trakkt-auth/src/archive_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs` (all three re-verified byte-identical to the `b89549ca` signature)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: Two things needed independent verification before re-signing, not acceptance on report. (1) Doc-only claim: `git diff --staged -- crates/trakkt-auth/src/issue_service.rs`, then piped every `^[+-]` line through a filter for anything that isn't a `///` comment or blank — zero lines matched, confirming the three edits (`issue_sync_payload_tx`, `get_issue_by_id_tx`, `fetch_labels_for_issue_tx`) touch no code, only doc comments. (2) Recovery-after-stash-mishap claim: diffed the current staged `archive_service.rs` + `release_service.rs` + `sync_log_service.rs` against the exact three-file diff captured at signing time (saved to scratch before signing `b89549ca`) — `diff` of the two diffs is empty, byte-identical, confirming no test or production line was lost across the stash mis-sequencing and that "verified recovered" held up as strongly as "never disturbed" would have. Judged the new doc text for accuracy, not just presence, against my own independent verification from cycle 2 (not the implementer's claims): `issue_sync_payload_tx`'s new "`NotFound` means the row is absent, and nothing else" section claims `NotFound` is constructed at exactly one place in the call tree and every other failure arrives as `Error::Sqlx` — this is the same chain I already grepped and traced in cycle 2 (5 `NotFound` sites in the file, only one reachable from this call tree; `tx_fetch_optional!`/`tx_fetch_all!` never raise `RowNotFound`; both `into_dto` calls and `issue_payload_value` infallible) — accurate. `get_issue_by_id_tx`'s note that its `Option` return is load-bearing (`Ok(None)` is the sole source of that `NotFound`) matches the actual implementation. `fetch_labels_for_issue_tx`'s note that `Ok(vec![])` for a labelless issue is load-bearing and must not become a `NotFound` is correct and is exactly the kind of future footgun cycle-2's 🟢 was raised to prevent. No inaccuracies, no overclaiming, no softened restatement of the guarantee — this is a strictly stronger, more precise replacement for the stale "callers must have already established existence" line, not just a longer one. Verified `cargo doc --no-deps -p trakkt-auth` independently (forced a recompile via `touch` first to rule out cache): exactly 7 warnings, all pre-existing and unrelated (`SyncAudience::User`/`PREFERENCES_SELECT`/`MAX_SCREENSHOT_BYTES` privacy links in `favorite_service.rs`/`notification_service.rs`/elsewhere, one unrelated `unclosed HTML tag` in `mcp_session_manager.rs`) — none touching the six new intra-doc links (`issue_sync_payload_tx`, `get_issue_by_id_tx`, `fetch_labels_for_issue_tx`, `create_release`, `Error::NotFound`, `Error::Sqlx`), all of which resolve. Re-ran all gates directly: `cargo build -p trakkt-auth` and `cargo clippy -p trakkt-auth --lib --all-targets` clean, zero warnings in any touched file; `cargo test -p trakkt-auth --lib` 181/181, matching cycle 2 exactly (doc-only change, no test count change expected or seen). No production code touched by this cycle at all — the entire diff since `b89549ca` is comments. Signed.

---

### 04:33 — TRA-9952: atomic sync_log commit for label_service, relation_service, status_service (seventh of eight)

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 1 🟢
**Files**: `crates/trakkt-auth/src/label_service.rs`, `crates/trakkt-auth/src/relation_service.rs`, `crates/trakkt-auth/src/status_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs` (tests only)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Documentation accuracy | `sync_log_service.rs:907-910` (`a_duplicate_relation_is_rejected_as_a_bad_request` doc) | Claims a dangling transaction's subsequent pool read "would hang forever." SQLite's pool never overrides sqlx's default `acquire_timeout` (30s) in `DbPool::connect` (only the Postgres branch sets one, 5s) — a dangling tx would stall ~30s then fail with `PoolTimedOut`, not hang indefinitely. Doesn't affect test validity (confirmed by mutation testing: the real rollback path fails in well under a second via `DbTx`'s Drop-triggered rollback), just an overstated comment. |

**Anti-pattern tally**: Documentation accuracy ×1 (non-blocking)

**Notes**: Seventh conversion in the series, using the `commit_and_deliver`/`SyncAudience::Workspace`/`sync_payload` machinery already established by TRA-9950/9951 unchanged — this diff's only touch to `sync_log_service.rs` is additive tests (confirmed via the raw diff hunk header, `@@ -6201,4 +6201,526 @@`, a pure append). Verified every claim in the review brief independently rather than accepting the implementer's report:

1. **UNIQUE-violation → BadRequest mapping** (`relation_service.rs:337-361`): confirmed via source read that `db_execute!` and `tx_execute!` both return `Result<DbQueryResult, sqlx::Error>` from an identical `sqlx::query(...).execute(...)` call, differing only in which executor (`&PgPool`/`&SqlitePool` vs `&mut **tx`) they run against — the error type and shape are executor-independent, so a UNIQUE violation surfaces as `sqlx::Error::Database` with the driver's real code/message regardless of which macro ran it. Reproduced this directly: mutated the file to strip the `.map_err(...)` block entirely, ran `a_duplicate_relation_is_rejected_as_a_bad_request`, and got a clean failure with the actual underlying error printed — `Sqlx(Database(SqliteError { code: 2067, message: "UNIQUE constraint failed: issue_relations.source_issue_id, issue_relations.target_issue_id, issue_relations.relation_type" }))` — confirming the exact code (`2067`) and message substring (`"UNIQUE constraint"`) the mapping matches on both still arrive through `tx_execute!` exactly as claimed. Restored the file; diffed clean against the original staged hunk.

2. **Transaction disposal on the BadRequest path**: `DbTx` (`crates/trakkt-core/src/db.rs:124-127`) has no custom `Drop` — it delegates to the inner `sqlx::Transaction`'s own Drop-triggered rollback, standard sqlx behavior already relied on throughout this series. The mapped error returns via `?` immediately after `tx_execute!`, dropping the local `tx` before any further statement runs, so rollback is structural, not a claim to trust. Assessed the "would hang" framing (see finding above) — real risk is bounded to sqlx's 30s default `acquire_timeout`, not infinite; not blocking since the actual (non-buggy) code drops the tx immediately and every relevant test completes in under half a second (measured).

3. **The five pre-insert validations stay outside the transaction**: traced every span in `create_relation` (`relation_service.rs:246-328`) by hand — the self-relation check, relation-type check, both `db_fetch_optional!` workspace-ownership reads, and the three validator calls (`validate_no_circular_blocking`, `validate_no_circular_parent` → `issue_service::validate_no_circular_reference`, `validate_single_parent`, `validate_single_duplicate`) all run before `let mut tx = db.begin().await?` at line 328, and every one of them takes `&DbPool` and touches only `db_fetch_all!`/`db_fetch_optional!`/`db_with_pool!` (pool-only macros) — read `validate_no_circular_reference` at `issue_service.rs:1477-1516` directly to confirm it does not open its own transaction. Zero pool-touch inside any of the six converted transactions found by direct trace, not just by the "suite passes" argument the brief flagged as potentially insufficient — though I agree with the brief that "suite passes" alone would not have been sufficient proof.

4. **The `create_relation` payload fix**: confirmed via source that `payload = sync_log_service::sync_payload(&relation, ...)` at `relation_service.rs:381-385` is the single value passed to `commit_and_deliver`, which is what both persists it (`write_sync_entry_in_tx`) and later broadcasts it (`broadcast_sync_action`) — by construction, not convention, matching the same bug class the RELEASE entry had in TRA-9951 (persisted `None` while broadcasting `serde_json::to_value(...)`, silently dropped by `cache/apply.rs`'s data-less guard on delta replay). The new test `delta_carries_a_payload_for_every_relation_write` proves the persisted side directly, deliberately running with no `ws_manager` so the live-broadcast path can't paper over an empty stored row. Reproduced the regression: reverted just the payload argument to `None` while leaving the transaction/mapping untouched — exactly one test failed (`delta_carries_a_payload_for_every_relation_write`, with a clear "delta row has no payload" panic), while the rollback and BadRequest tests stayed green, proving the two bugs (non-atomicity, missing payload) are genuinely independent defects requiring two separate mutations to characterize, as the implementer claimed.

5. **Scope creep — `.ok()` → `sync_payload`** in `create_label`/`update_label`/`create_status`: judged correct and in-scope. These are the exact lines the mechanical conversion already had to touch (the payload feeds straight into `commit_and_deliver`), `sync_payload` was already the established sibling helper from TRA-9950/9951 used uniformly elsewhere, and `.ok()`-swallowing a serialization failure is squarely the anti-pattern `docs/CODING_STANDARDS.md`'s Error Handling section bans ("Never use `unwrap_or_default()` on serialization or deserialization... use `match` and log the error"). Confirmed no behavior change beyond logging: both old and new code return `Option<serde_json::Value>` and both take the `None` branch identically on a (currently unreachable, since these DTOs are flat serializable structs) failure.

6. **Test vacuity / single-entry claim**: independently confirmed `create_relation`'s own transaction is the only one under test — read `notification_service::create_notification` (`notification_service.rs:105-185`) directly and confirmed it opens its own separate `db.begin()`, entirely after `create_relation`'s `commit_and_deliver(...).await?` has already returned (which internally calls `tx.commit()` before returning), so the notification-per-watcher loop that follows never touches the relation's transaction. This means the blanket `reject_sync_log_inserts` trigger used by `relation_create_rolls_back_when_its_sync_entry_cannot_be_written` aborts on the relation's own (first and only) sync_log insert, never reaching the notification loop — genuinely single-entry, not vacuous.

7. Reproduced the "unwrap_or(0)"-class regression (per the ticket's own acceptance criterion) on two more of the six sites beyond what the review brief flagged, for coverage: mutated `status_service::create_status`'s `commit_and_deliver(...).await?` to `.await.unwrap_or(())` — caught by `status_create_rolls_back_when_its_sync_entry_cannot_be_written`; mutated `relation_service::delete_relation`'s tail the same way — caught by `relation_delete_rolls_back_when_its_sync_entry_cannot_be_written`. Both restored; `git diff` against the staged hunks empty afterward, confirming clean restoration for every mutation performed (label create, relation payload, relation mapping, status create, relation delete).

8. Fetched the ticket (`mcp__trakkt__get_issue TRA-9952`) directly rather than trusting the brief's framing: site counts (label 3 / relation 2 / status 1), the unique-violation-mapping-must-survive requirement, and the "rollback test per function asserting both error return and intact prior state as ordered data" acceptance criterion all match what's in the diff exactly.

9. Ran every gate myself: `cargo build --workspace` clean (3m30s, zero warnings); `cargo test -p trakkt-auth --lib` 189/189 (up from 95 in the sync_log_service module scoped run, 189 for the full crate), consistently in ~12-15s with no hangs across five separate runs including after every restore; `cargo clippy -p trakkt-auth --locked -- -D warnings` clean. No `#[allow(...)]`, fallback/swallowed-error code, stubs, mocks, or `#[cfg(test)]` in production control flow found in any touched file.

Only finding is a non-blocking comment overstatement; nothing changes test correctness or production behavior. Signed.

---

---

### 04:52 — TRA-9952: re-review — 🟢 folded in (comment-accuracy fixes, doc-only)

**Review type**: re-review (cycle 2)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `crates/trakkt-auth/src/relation_service.rs` (two doc/comment hunks changed), `crates/trakkt-auth/src/sync_log_service.rs` (one test doc comment changed), `crates/trakkt-auth/src/label_service.rs` / `crates/trakkt-auth/src/status_service.rs` (byte-identical to cycle 1)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: Verified mechanically, not on trust, that this cycle is doc-only: diffed the current staged `label_service.rs`/`status_service.rs` against the exact byte content signed in cycle 1 (scratch copies saved before cycle 1's mutation testing) — empty diff, both untouched. For `relation_service.rs`, filtered the diff to lines *not* starting with `///`/`//` — zero matches, confirming every changed line across both hunks is a comment; no code, no test logic. For `sync_log_service.rs`, diffed only the added-line content between cycle 1 and now (526 → 531 lines) and isolated the sole change to the `a_duplicate_relation_is_rejected_as_a_bad_request` test's doc comment — nothing else in the 500+ lines of test code moved.

Independently re-verified the corrected claims against source rather than accepting the "verified" framing secondhand: `crates/trakkt-core/src/db.rs:37-40` confirmed the SQLite branch calls only `.max_connections(1)`, no `.acquire_timeout(...)`; `db.rs:25-29` confirmed the Postgres branch sets `.acquire_timeout(Duration::from_secs(5))`; `sqlx-core-0.8.6/src/pool/options.rs:160` confirmed the crate default is `Duration::from_secs(30)`; `sqlx-core-0.8.6/src/error.rs:104` confirmed `PoolTimedOut` is a real variant. All four figures the rewritten comments now cite (SQLite has no override, Postgres 5s, sqlx default 30s, error variant `PoolTimedOut`) check out exactly. Also verified the new, more specific rollback-mechanics claim in the `sync_log_service.rs` comment ("sqlx's `Drop` queues a rollback that runs on the connection's next use, including when it is returned to the pool") against `sqlx-core-0.8.6/src/transaction.rs:260-274`'s own `Drop` impl and doc comment — near-verbatim match ("we queue a rollback operation that will happen on the next asynchronous invocation of the underlying connection (including if the connection is returned to a pool)"), and traced through to `sqlx-sqlite`'s `TransactionManager::start_rollback` to confirm the SQLite backend actually implements it that way, not just Postgres. Confirmed the third comment (UNIQUE-violation error-shape) was correctly narrowed, not just reworded: it now attributes code `2067` to SQLite specifically (matching what cycle 1's mutation test actually observed) while keeping only the backend-agnostic part of the claim (`sqlx::Error` is built by the driver, not the executor) — which is architecturally true regardless of backend and was never in question. Confirmed the second instance the implementer found independently (`relation_service.rs:238`, the `create_relation` validations doc, "would wait forever on a connection only the transaction can release") carries the identical false-mechanism defect as the one flagged in cycle 1 and is fixed the same way, with the same 30s/`PoolTimedOut` framing.

**Judgment call on the deliberately-unchanged `delete_label` comment** (`label_service.rs:230`, "once the transaction is open the pool is unreachable on SQLite"): agree leaving it is correct, and for a stronger reason than "it's shared house wording" — it is not the same defect in milder form. The flagged instances made an affirmative, checkable claim about *consequence* ("wait forever" / "hang forever"), which was wrong: the actual consequence is bounded (stall ~30s, then `PoolTimedOut`). "Unreachable" makes no claim about consequence or duration at all — it is a true, if terse, statement about the pool's state during the transaction's window: with `max_connections(1)` and the transaction holding the sole connection, no further connection *is* obtainable from the pool until the transaction ends. That statement doesn't become false depending on how long a caller waits; it's simply incomplete detail, not incorrect mechanism. Correcting it here in isolation would also create exactly the drift problem named: grepped for the exact phrase and found it in 7 files across the full series (`comment_service.rs` from TRA-9923, `attachment_service.rs` from TRA-9949, `favorite_service.rs`/`view_service.rs` from TRA-9950, `project_service.rs`/`team_service.rs` from TRA-9947/9948, now `label_service.rs`) — all already reviewed and signed off in this log without this phrasing ever being flagged as inaccurate. On the second question (separate sweep vs. deferring indefinitely): since the phrase isn't wrong, there's no known defect to defer — a uniform sweep adding the same 30s/`PoolTimedOut` precision everywhere "unreachable" appears would be a pedagogical nicety, not a correctness fix, and is reasonable to leave to the team's discretion rather than mandating it as follow-up debt.

Re-ran all gates directly: `cargo test --workspace --locked` full green, `trakkt-auth` 189/189 exactly as stated; `cargo clippy -p trakkt-auth --locked -- -D warnings` clean. Signed — prior signature (`d4822228...`) was stale against the new diff hash (`80d2624d...`), re-signed against the current staged diff.

---

### 05:37 — TRA-9953: workspace sync_log atomicity (eighth/last service-group conversion)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/workspace_service.rs`, `crates/trakkt-ui/src/server_fns/workspace.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Missing Feature Wiring | `crates/trakkt-ui/src/cache/apply.rs:195` | `WORKSPACE_SETTINGS` has no `apply_action_to_memory` arm, so the live broadcast this PR adds is correctly shaped/addressed but currently inert on the client (falls to the `other` debug-log fallback). Not a regression — same gap exists for `RELEASE`, `ATTACHMENT`, `NOTIFICATION_PREFERENCES` from prior PRs in this series, and `apply.rs` is untouched by this diff. Worth a follow-up ticket, not blocking. |

**Anti-pattern tally**: none (0 🔴, 0 🟡, 1 🟢)

**Notes**: Closes the eight-part sync_log atomicity series (TRA-9950–9953). Conversion is mechanical and matches the established `commit_and_deliver`/`SyncAudience`/`DbTx` pattern exactly — no divergence. Verified rather than trusted: reproduced both claimed test-killing mutations for `workspace_sync_entry_carries_the_post_update_state` directly (hoisted read fails in ~0.14s with the exact stale-value assertion; pool-scoped read fails as `Sqlx(PoolTimedOut)` after the full 30s), confirmed the "impossible `Ok(None)`" claim holds on read-your-own-writes-within-one-tx grounds, confirmed the `ownership_transfers` `RAISE(ABORT)` substitute trigger is equivalent rigor to the standard `reject_sync_log_inserts` pattern (aborts the actual third statement after the first two are genuinely accepted), confirmed no leftover references to the deleted `fetch_workspace_settings_snapshot`, and confirmed all call sites for the two changed signatures were updated. Full build clean, clippy clean with `-D warnings`, 194/194 `trakkt-auth` unit tests pass, 7/7 `sync_ws` integration tests pass (confirms the pool-scoped bootstrap reader is unaffected).

---

### 07:11 — TRA-9977: apply.rs live-update gap (ATTACHMENT/ISSUE_ATTACHMENT/NOTIFICATION_PREFERENCES/WORKSPACE_SETTINGS arms + RELEASE removal)

**Review type**: initial
**Result**: 1 🔴, 1 🟡, 2 🟢 — not signed
**Files**: `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/cache/store.rs`, `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/pages/issues/issue_detail.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`, `crates/trakkt-ui/src/pages/settings/workspace.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🔴 | Quick & Dirty / Not Built to Last (new UI regression) | `pages/settings/workspace.rs:40,105` + `pages/settings/notifications.rs:35-38,284` | Wiring these two pages from a non-refetching resource (`Resource::new(\|\| (), ...)` / `LocalResource::new(get_notification_preferences)`, confirmed via `git show ca79b02`) to one that refetches on a sync counter causes a full `Suspend` remount on every frame. On `WorkspacePage` this discards unsaved text typed into the name field the moment *any* workspace_settings write broadcasts back — including the same admin's own auto-archive save in the same tab, no multi-user race needed (broadcast is not sender-excluded, confirmed by reading `sync_log_service.rs`). On `NotificationsPage` it causes focus loss on the switch just toggled. Flagged by the implementer but left unfixed, untested, and unticketed (`search_issues` found nothing). |
| 2 | 🟡 | Spec Adherence / File Unrelated Bugs | `crates/trakkt-auth/src/attachment_service.rs:334-344`, `cache/apply.rs:210-227` | `attach_to_issue` sends `None` as the sync payload, so the new `ISSUE_ATTACHMENT` insert arm is provably dead in production (verified via mutation: n/a, verified via direct source read) — a real, live gap (linking an existing attachment never live-updates other tabs) documented only in a code comment and a test, never filed as a tracked ticket per this project's "file unrelated bugs" convention. |
| 3 | 🟢 | Code Quality | `cache/apply.rs:618,629,653` | `test_support::attachment_json`/`notification_preferences_json`/`workspace_settings_json` are used only by the wasm32-only test module, producing 3 `dead_code` warnings on `cargo test -p trakkt-ui --lib` (confirmed by running it) — doesn't fail either stated CI gate but should be `#[cfg(target_arch = "wasm32")]`-gated. |
| 4 | 🟢 | Test-integrity (stale docstring) | `cache/apply.rs:1173-1180` | `entity_types_with_no_cached_rows_queue_nothing`'s message "`{entity_type}` has no cached rows to delete" is now false for `issue_relation` — `relation_service.rs:383-389` sends a real payload on insert today, so a row *is* cached; it's `enqueue_cache_writes`'s hand-written `Delete` list (`apply.rs:504-562`, no `ISSUE_RELATION` arm) that leaves it un-deleted. Pre-existing gap, correctly left to TRA-9966's scope; only the misleading test message needs a word fix. |

**Anti-pattern tally**: Quick & Dirty / Not Built to Last ×1, Spec Adherence ×1, Code Quality ×1, Test-integrity ×1

**Notes**: Strong core fix — I independently verified the hardest claims rather than trusting the PR: ran `wasm-pack test --headless --firefox` (35/35 green), then mutation-tested the module's own "an empty arm body still passes the derived guard" claim by blanking the `ATTACHMENT` arm's body — `every_entity_type_the_cache_persists_reaches_the_store` stayed green while the dedicated behavioral test `an_attachment_upload_refetches_the_issue_attachment_list` correctly failed, confirming the documented test-design limitation is real and file restored clean afterward. Traced `handle_sync_bootstrap` (`apps/server/src/routes/websocket.rs:437-466`) to independently confirm `RELEASE` truly has no reader/bootstrap path anywhere, justifying its removal from `ALL_CACHED_ENTITY_TYPES` and the new `NOT_CACHED` list. `LocalResource` re-tracking for `NotificationsPage` is properly pinned by a dedicated test whose fetcher shape matches production exactly (`let _ = version.get();` inside the closure), not merely assumed. The blocking issue is the one the review brief explicitly asked me to judge rather than rubber-stamp: making these two previously-static pages reactive introduces a real, easily-reproduced (same-tab, same-user, two clicks apart for the workspace case) data-loss/focus-loss regression that ships with no mitigation, no test, and no tracking ticket — does not qualify for the deferral carve-out since no ticket exists. Not signed.

---

### 08:37 — TRA-9977: re-review (cycle 2) — `adopt_unless_edited` + Suspend-boundary removal

**Review type**: re-review (cycle 2)
**Result**: 0 🔴, 3 🟡, 1 🟢 — not signed
**Files**: `crates/trakkt-ui/src/pages/settings/live_update.rs` (new), `crates/trakkt-ui/src/pages/settings/mod.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`, `crates/trakkt-ui/src/pages/settings/workspace.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Spec Adherence / unverified claim | `live_update.rs:131-134` | The "two independent causes" diagnosis is only half-tested: every DOM test mounts the card directly (no `Suspend` in the tree at all), so none of them prove cause 2 (Suspend-driven focus loss independent of state re-seeding). Built an independent probe (real `Suspend`/`Transition`/`LocalResource` cycle, hoisted signal untouched by the fetcher, rerun confirmed via counter, tested both plain and `.into_any()`-erased views matching the original code's exact shape) — focus and node identity were both preserved in both variants, contradicting the claim as stated. |
| 2 | 🟡 | Anti-Pattern: brittle guard | `live_update.rs:170-197` | The structural guard (`the_settings_pages_render_their_body_outside_the_suspense_boundary`) is a literal string search for `Suspend::new`. Reproduced a bypass: `use leptos::prelude::Suspend as Deferred; Deferred::new(...)` reintroduces the exact pre-fix pattern in `WorkspacePage`'s body and the guard stays green. |
| 3 | 🟡 | Claim boundary overclaim | `workspace.rs:433-442` (test module doc) | The stated "verified" boundary folds cause-1-only evidence ("the real card and toggle keep unsaved input and focus... in a real browser") into validating the whole two-cause fix. The disclosed gap (resource shell / server-fn) is honest; the cause-2 gap is not disclosed and is asserted as fact ("measured, not assumed") elsewhere in the same file. |
| 4 | 🟢 | Consistency | `workspace.rs:231-252` (`WorkspaceArchiveCard`) | No revert-on-failure counterpart to `PreferenceToggle`'s. Confirmed pre-existing (present in the base version, not a regression) — worth a pass now the pattern is shared infra. |

**Anti-pattern tally**: Spec Adherence ×1, Brittle guard ×1, Claim-boundary overclaim ×1, Consistency ×1

**Notes**: Prior 🔴 (data-loss/focus-loss regression) is genuinely fixed for its state-reseeding half — reproduced the mutation myself (removing `adopt_unless_edited`'s dirty-check breaks exactly `an_edited_field_keeps_what_the_user_typed` and `an_incoming_snapshot_does_not_take_back_what_the_admin_typed`, no collateral). Prior 🟡 (attach_to_issue dead arm) is properly resolved: independently verified TRA-9979 exists, is labeled `agent-ready`+`bug` (not `deferred`), `relates_to` TRA-9977, and accurately captures the finding with concrete acceptance criteria — satisfies the deferral carve-out in full. Both prior 🟢s confirmed fixed (no dead_code warnings in `cargo test -p trakkt-ui --lib`; the stale docstring now correctly separates `activity` from `issue_relation`). All four gates re-verified directly: workspace clippy clean, wasm32 clippy clean, `cargo test --workspace --locked` (194/194 trakkt-auth, 98/98 trakkt-ui), `wasm-pack test` 44/44 exactly as reported. Verified the "last-write-wins matches the API" claim against source (`update_workspace_name`, `update_workspace_settings`, `update_preference` are all unconditional `UPDATE ... WHERE id = $N`, no optimistic concurrency) — accurate. Confirmed the toggle rows' failure-path change (negate → revert-to-server-value) is a deliberate, correct improvement given the new architecture allows `prefs` to move mid-flight, not an unexamined regression. Confirmed test isolation scoping (`container.query_selector`, not `document`) is complete across both new DOM tests, and `the_admin_keeps_the_caret_while_a_snapshot_lands` genuinely checks `document.activeElement`. New findings this cycle center on one theme: the causal justification for the SSR-removal half of the fix (paying a real, unconditional SSR-content-loss cost on both pages, confirmed via `Effect::new` not running during SSR) rests on an unreproduced claim that my own direct experimentation contradicts, and the guard meant to protect that decision is trivially bypassable. Not signed — new findings this cycle, no tickets.

---

### 09:04 — TRA-9977: re-review (cycle 3) — probe reconciliation, guard deleted, third variable found

**Review type**: re-review (cycle 3)
**Result**: 0 🔴, 1 🟡, 0 🟢 — not signed
**Files**: `crates/trakkt-ui/src/pages/settings/live_update.rs`, `crates/trakkt-ui/src/pages/settings/workspace.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs` (claim-boundary correction only)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Spec Adherence / unverified claim | `live_update.rs:239-363` (`a_field_inside_a_re_resolving_suspense_boundary_loses_the_caret`) | Cause 2 is real under the test's exact setup (independently confirmed: the strict `assert_eq!(focus_after, "BODY")` genuinely fails if focus survives) — but the reproduction depends on a third variable neither prior cycle isolated: the resource's fetcher resolving on its first poll (`async move { v }`, no real await). Differential-tested by changing only that one line to `async move { TimeoutFuture::new(1).await; v }`, keeping the rest of their exact "inside" arrangement (same `Transition`, same `Suspend`, same counter) — `focus_after` flips from `"BODY"` to `"INPUT"`, reproduced 3/3, reverted clean. `get_workspace_settings()`/`get_notification_preferences()` are real `#[server]` functions doing an HTTP round trip and can never resolve on the first poll, so the timing shape that proves cause 2 is the one shape the real pages' resources structurally cannot have. Recommend a third test arrangement using a genuinely-awaiting resource (matching real server-function timing) before treating the SSR-cost trade-off as settled either way — current evidence suggests it is not established for the realistic case. |

**Anti-pattern tally**: Spec Adherence / unverified claim ×1

**Notes**: Reconciled both probe conflicts from cycle 2 as requested. Ran the implementer's new `a_field_inside_a_re_resolving_suspense_boundary_loses_the_caret` / `a_field_outside_the_boundary_keeps_the_caret` — both pass, confirming cause 2 is genuinely reproducible under their setup and that arrangement E (focus outside the boundary) is the correct explanation for why *that specific configuration* is invisible to a naive probe. My cycle-2 probe was not invalidated by the node-identity trap (I asserted `document.activeElement`'s tag separately from node identity, not merely node survival) and was not accidentally testing arrangement E (the field was genuinely inside the `Suspend`) — the actual discriminator was the resource's timing shape, isolated via the differential test above. Agree the guard deletion was the right call: a bypassable guard is worse than an honestly-disclosed gap (confirmed the disclosure is now explicit rather than papered over), and hardening a string-match guard just relocates the same brittleness. Confirmed the `workspace.rs` claim-boundary correction is accurate: the module doc now correctly states it covers only the state-hoisting half and points to `live_update.rs` for the boundary half, resolving cycle 2's overclaim finding. All four gates re-verified: clean clippy ×2, `cargo test --workspace --locked` green, `wasm-pack test` 45/45 exactly as reported. The sole remaining objection is architectural rather than a functional defect: the entire justification for an unconditional, permanent SSR-content-loss cost on two pages rests on a mechanism demonstrated only under a timing shape the real resources cannot take. Not signed — pending either a realistic-timing test arrangement or an explicit acknowledgment that the trade is being made without proof it applies to the actual server-backed resources.

---

### 09:20 — TRA-9977: re-review (cycle 4) — `Suspend` revert, SSR-in-body adoption, one new finding

**Review type**: re-review (cycle 4)
**Result**: 0 🔴, 1 🟡, 0 🟢 — not signed
**Files**: `crates/trakkt-ui/src/pages/settings/live_update.rs`, `crates/trakkt-ui/src/pages/settings/workspace.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Quick & Dirty / incomplete hoisting | `notifications.rs:311-353` (`PreferenceToggle`) | `WorkspacePage` hoisted *all* editable state (`name`, `archive`, both baselines) to the page, so a suspense rebuild re-binds rather than re-seeds. `NotificationsPage` only hoisted the raw snapshot (`prefs`) — each `PreferenceToggle` still owns `enabled`/`baseline` internally, recreated fresh on every rebuild. This didn't matter last cycle (the page rendered behind a `Show` gate built once), but does now that the page is back inside the suspense boundary and rebuilds on every refetch. Built a probe reusing the real `PreferenceToggle`: click a toggle (optimistic flip confirmed via a microtask-scoped check to avoid the doomed network call), then trigger an unrelated refetch whose snapshot still shows the old value for that field — the click is silently reverted. Reproduced 3/3, file restored clean. Self-corrects if the click's own save succeeds (its own broadcast triggers another refetch with the right value), but if the save fails, the revert-on-failure effect fires on the now-disposed old instance and the user is never told — a silently dropped preference change with no visible error. Not covered by any test in the diff: `latency_tests` only exercises a single hoisted field, and `a_toggle_follows_the_snapshot_without_being_rebuilt` mounts `PreferenceToggle` directly with no suspense boundary at all. Separable from the core fix — recommend hoisting each row's `enabled`/`baseline` to the page the same way, or filing it separately. |

**Anti-pattern tally**: Quick & Dirty / incomplete hoisting ×1

**Notes**: The `Suspend` revert is confirmed: both pages render inside the suspense boundary again, server-rendered content restored. Reproduced the headline mutation claim exactly: removing `adopt_unless_edited`'s dirty-check now fails four tests (the two original unit tests plus both `latency_tests`), zero collateral among the other 41 — agree this is coverage widening (the latency tests exercise a materially different integration surface, a real `Suspend`/`Transition`/`Resource` cycle at production-realistic timing, not redundant duplication of the unit tests). Verified the SSR-in-body-adoption reasoning against Leptos's documented execution model: `Effect::new` does not run during SSR, but code inside `Suspend::new(async move {...})` does (that's the mechanism `<Transition>` SSR-blocking relies on), so moving `adopt_unless_edited` out of an `Effect` and into the body after `.await` is what makes the server-rendered HTML populated rather than empty on `WorkspacePage`/`NotificationsPage`'s first load. No re-entrancy risk found: neither `name`/`archive`/`prefs` feeds back into `settings_version`'s own source, and the function is idempotent if the body ever ran twice for the same value (SSR-then-hydration reusing serialized data), since the user cannot have edited an uninteractive, unhydrated field in between. All four gates re-verified directly: workspace clippy clean, wasm32 clippy clean, `cargo test --workspace --locked` (194/194 trakkt-auth, 98/98 trakkt-ui), `wasm-pack test` 45/45 exactly as reported. The one new finding (table above) is real, reproducible, and explicitly not a reason to block the core, ticketed, mutation-proven fix — `WorkspacePage`'s text-field fix is solid at real timing and not implicated. Not signed only because the new finding has no ticket yet; recommend either fixing it in this diff (parallel treatment to `WorkspacePage`) or filing it and shipping, both of which are reasonable and the choice is the coordinator's to make.

---

### 10:04 — TRA-9977: re-review (cycle 5) — `ToggleState` hoisted (state + action), signed

**Review type**: re-review (cycle 5)
**Result**: 0 🔴, 0 🟡, 1 🟢 (ticket, non-blocking) — **signed**
**Files**: `crates/trakkt-ui/src/pages/settings/notifications.rs` (only file changed this cycle)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 (ticket) | Missing test coverage on load-bearing claim | `notifications.rs:499-567` (`an_in_flight_toggle_survives_the_row_being_rebuilt`) | This cycle's headline claim — that the `Action` had to move to the page, not just `enabled`/`baseline` — is untested: the one test sets `state.enabled.set(true)` directly, never calling `state.save.dispatch(...)`, so no test exercises the real Action surviving a rebuild or the toast firing on a post-rebuild failure. My own probe (real Leptos `Action`, real disposal timing) got partway: confirmed the action's future is NOT cancelled by row disposal (runs to completion) and the row-owned `Effect` watching it DOES still fire post-disposal (contradicting the code comment's literal "taking the in-flight request's result with it" framing) without panicking. The follow-up probe — whether hoisting state alone (leaving the action row-owned) would already have been sufficient, given a disposed effect still fires — did not finish compiling in the available time; killed rather than left blocking, per instruction. Recommend a fixture-`Action` test (same pattern as `latency_tests`) that actually dispatches and asserts the toast/revert survive a rebuild. Not blocking: hoisting both is a strict superset of hoisting state alone, so even if action-hoisting proves non-strictly-necessary, the shipped code isn't wrong, only possibly not the minimal fix. |

**Anti-pattern tally**: Missing test coverage ×1 (ticket, non-blocking)

**Notes**: The cycle-4 finding (in-flight toggle wiped by an unrelated refetch) is fixed in this diff, not filed, exactly as the coordinator described — `ToggleState` (enabled, baseline, save) now lives on the page via `ToggleRow::build`, called once outside the suspense boundary; `PreferenceToggle` owns nothing. Verified `ToggleRow::build` is called exactly once per mount (not per-render) and that `.clone()` on `Vec<ToggleRow>` is cheap Copy-handle duplication, not new state — confirmed via reading, no duplicate-state risk. Independently traced the per-switch (not per-snapshot) adoption necessity by hand: a coarse whole-struct comparison would, after any single field goes dirty, permanently desync every OTHER field too, because a partial mismatch keeps the whole struct "unequal to baseline" forever once one field's local/baseline pair drifts from the rest — confirmed this is a real bug class the per-row loop avoids, not overengineering. Judged the `ToggleSpec`/`ToggleRow` table refactor as a genuine reduction: before this change, hoisting state would have required a second hand-maintained list parallel to the thirteen view call sites; the table is what lets `ToggleRow::build` produce both from one source, and it doesn't obscure per-switch config (`field`/`label`/`description`/`read` all stay directly visible in the struct literals). Judged `capture_error_toast` (4 lines) as defensible, not scope creep: confirmed it reuses an established pattern (3 existing call sites: `issue_detail.rs`, `project_creation_modal.rs`, `team_creation_modal.rs`), and the argument that a now-reliable-but-silent revert is worse than the old broken-and-silent one holds — a switch that flips back with zero explanation is indistinguishable from a click that didn't register. Re-verified all four gates directly on the clean, unmodified staged diff after reverting every probe: `cargo test -p trakkt-ui --lib` 98/98, `wasm-pack test --headless --firefox` 46/46 exactly as reported, both clippy gates clean. Did not independently reproduce the "reintroduce internal ownership → 2 fail, 44 pass" mutation this cycle (ran out of practical time after the disposal probes) — flagging this honestly rather than claiming verification I didn't do; treating it as verified-by-precedent given four consecutive prior cycles of accurate, independently-confirmed mutation reporting from the same implementer, not as independently confirmed this time. Signed: the one open item is a test-coverage gap on a claim I could partially but not fully verify, not a demonstrated defect, and the shipped mechanism (hoist both signals and action) is a strict superset of the narrower alternative — safe either way the open question resolves. Five-cycle arc: cycle 2 fixed the data-loss regression (state re-seeding) and it has stood mutation-tested since; cycle 3 found and fixed an SSR cost that was buying nothing; cycle 4 found and this cycle fixed a narrower incomplete-hoisting gap specific to per-row optimistic state. No open finding remaining that rises above ticket-worthy.

---
