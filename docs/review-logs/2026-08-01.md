### 13:57 — TRA-9942: authenticate and workspace-scope `GET /api/v1/teams/{team_id}/icon`

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `apps/server/src/routes/team_icon.rs`, `crates/trakkt-auth/src/team_service.rs`, `apps/server/tests/common/mod.rs` (new), `apps/server/tests/team_icon_routes.rs` (new), `crates/trakkt-ui/src/components/team_icon.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: `GET /api/v1/teams/{team_id}/icon` previously took no `AuthUser` and no workspace scope, so any caller holding a team UUID could fetch that team's uploaded icon bytes. This PR brings it in line with its already-authenticated, workspace-scoped siblings `upload_icon`/`delete_icon`.

Verified both enforcement layers are real, not cosmetic:
- Handler (`apps/server/src/routes/team_icon.rs:155-172`): `get_icon` now takes `auth: AuthUser`, resolves the team via `get_team`, 403s if `auth.workspace.workspace_id != team.workspace_id`, and passes `team.workspace_id` into `get_team_icon_data`.
- Service (`crates/trakkt-auth/src/team_service.rs:632-658`): `get_team_icon_data` gained a `workspace_id: &str` param; the SQL is `WHERE team_id = $1 AND workspace_id = $2` (not appended after the fact — replaces the old unscoped query), and a team that exists but belongs to a different workspace now returns `Err(NotFound)` rather than the row.

Reproduced both load-bearing mutation claims myself rather than trusting the report, restoring the file to the exact staged byte content after each (`git diff` empty afterward):
- Reverted the SQL back to `WHERE team_id = $1` only (dropped the `workspace_id` bind). `get_team_icon_data_hides_an_icon_from_another_workspace` failed with `cross-workspace read leaked 29 bytes of team A's icon (mime image/png)` — exact match to the implementer's report.
- Removed the `auth: AuthUser` parameter (and the now-dead workspace-comparison block) from the `get_icon` handler. `get_icon_rejects_an_unauthenticated_request` failed with `left: 200, right: 401` — confirms the extractor is what rejects unauthenticated requests, not a coincidental downstream check.

Cache-control: changed from `public, max-age=3600` to `private, max-age=3600`. The comment states the reason (`private` because the response is now user-scoped and a shared/intermediary cache serving it across tenants would relocate the exact leak this PR closes) and states the `max-age=3600` reasoning (icon only changes via explicit admin action; a stale per-browser cache entry is bounded to one user seeing their own workspace's previous icon, not a cross-tenant disclosure) — satisfies the "chosen max-age must be stated with a reason" criterion.

Test fixture (`apps/server/tests/common/mod.rs`): confirmed the `assert!(!config.is_personal(), ...)` guard exists (lines 40-44) and actually fires — `Config::test_config()` sets `mode: TrakktMode::Saas` (`crates/trakkt-core/src/config.rs:204`), so `is_personal()` is false and the assertion passes for the right reason (not vacuously). `encryption_key: Arc::new([0u8; 32])` is documented in-line as deliberately not the configured key, and no route under test (`get_icon`/`upload_icon`/`delete_icon`) touches encryption — confirmed by reading the full diff, no encryption/decryption call in the touched files.

Tests present and non-vacuous per acceptance criteria: `get_icon_rejects_an_unauthenticated_request` (no token → 401, body doesn't leak bytes), `get_icon_refuses_a_member_of_another_workspace` (valid session for workspace B naming workspace A's team → 403, body doesn't leak bytes — this is the one that matters per the ticket, and it's present both at the route level and independently at the service level via `get_team_icon_data_hides_an_icon_from_another_workspace`), `get_icon_returns_the_icon_to_a_member_of_its_workspace` (same-workspace auth → 200, correct content-type, correct cache-control, exact bytes), plus `get_icon_accepts_the_access_token_cookie` (cookie fallback, proves the `<img src>` UI path still works) and `get_icon_is_not_found_for_a_team_without_a_custom_icon` (isolates the `Ok(None)` arm). All 5 route tests + 2 service tests pass.

`if let Ok(...)` → `match` on `HeaderValue::from_str(&mime)`: confirmed behavior-preserving apart from added `tracing::warn!` logging on the error arm — the `Ok` arm does exactly what the old `if let` did. Checked the comment's claim that `upload_team_icon` is the only writer of a non-NULL `icon_mime`: grepped every `icon_mime` write in the repo (`team_service.rs:567`, `603`, `674`) — lines 567 (`update_team_icon`) and 674 (`delete_team_icon`) both set it to `NULL`, only line 603 (`upload_team_icon`) writes a real value, and `upload_team_icon`'s only non-test caller is `upload_icon`, which sources the mime from `validate_magic_bytes`. Claim holds exactly.

Call sites: `get_team_icon_data`'s signature changed from 2 to 3 params; grepped the whole repo and found exactly one non-test caller (`apps/server/src/routes/team_icon.rs:172`), matching the implementer's claim.

Sibling handlers `upload_icon`/`delete_icon`: diffed — zero logic changes, only module-doc-comment path updates (`/api/teams` → `/api/v1/teams`) shared across all three handlers' comment blocks.

Gates run directly in the worktree: `cargo check --workspace --locked` clean; `cargo clippy --workspace --locked -- -D warnings` clean; `cargo test --workspace --locked` full green (the one `dead_code` warning seen in `trakkt-api/src/lib.rs:282` during a `cargo test` run is pre-existing and in a file untouched by this diff — confirmed via `git diff --cached --name-only`); `grep` for `allow(dead_code|unused_variables|unused_imports)` across changed files returned no matches. No `#[allow(...)]`, fallback paths, stubs, or mocks found anywhere in the diff.

---

### 16:20 — TRA-9977: re-land of #282 with the disposed-`Signal` panic fixed (worktree: trakkt-wt-tra-9977-apply-cached-types)

**Review type**: initial (re-land after revert #283 / TRA-9981)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 3 🟢
**Files**: `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/cache/store.rs`, `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/pages/issues/issue_detail.rs`, `crates/trakkt-ui/src/pages/settings/live_update.rs` (new), `crates/trakkt-ui/src/pages/settings/mod.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`, `crates/trakkt-ui/src/pages/settings/workspace.rs`, `docs/review-logs/2026-07-28.md`, `e2e/tests/sync/two-window-sync.spec.ts` (new)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Comment/Doc Accuracy (stale reference) | `e2e/tests/sync/two-window-sync.spec.ts:204` | Comment cites "`on:blur` (workspace.rs:191)" — the handler is actually defined at `workspace.rs:222` (off by ~31 lines, drifted during editing). Cosmetic; the claim itself (commits on blur, no save button) is correct. |
| 2 | 🟢 | Spec Adherence (pre-existing, out of diff scope, recommend ticket) | `cache/apply.rs:505-543` (`enqueue_cache_writes`, `SyncActionType::Delete` match) | This hand-enumerated delete list has no arm for `ATTACHMENT`, `ISSUE_ATTACHMENT`, `NOTIFICATION_PREFERENCES`, or `WORKSPACE_SETTINGS` — confirmed byte-identical back to `be3bfad^` (predates the whole TRA-9977 series, not introduced by this diff). Of the four, `ATTACHMENT` is live and reachable today: uploads persist a real cached row (confirmed via `attachment_json()`/the insert-arm test), but deleting an attachment never queues the matching `IdbOp::Delete`, so the row leaks in IndexedDB until the next full cache reset. `ISSUE_ATTACHMENT` is currently inert (`attach_to_issue` sends `data: None`, so nothing is ever cached to leak — tracked by TRA-9979), and `NOTIFICATION_PREFERENCES`/`WORKSPACE_SETTINGS` are documented as update-only today. Same list-drift class the diff's own `entity_types_with_no_cached_rows_queue_nothing` comment already calls out for `issue_relation`, one entity type over. Recommend filing a ticket (same convention as TRA-9979) rather than blocking this diff, since the diff does not touch this match arm. |
| 3 | 🟢 | Minor / no code path | — | Could not execute `e2e/tests/sync/two-window-sync.spec.ts` or `e2e/tests/sync/panic-probe.spec.ts` directly — running either requires a live server, and the review brief explicitly prohibited starting one or touching port 3577 in this worktree (a concurrent server/Playwright run was already in progress). Reviewed both files' source directly instead (see notes). Recommend the coordinator confirm both were run against a live server before merge, given the brief's own point that five prior review cycles missed this exact panic because none of them ran the page. |

**Anti-pattern tally**: Comment/Doc Accuracy ×1, Spec Adherence (pre-existing, deferred) ×1, Verification-coverage note ×1

**Notes**: This is the re-land of #282 (`be3bfad`), reverted by #283 for a disposed-`Signal` panic on `/settings/notifications` plain page load (TRA-9981). Verified all six things the brief asked for scrutiny on, independently rather than by trusting the diff's own doc comments:

1. **The two claimed fix sites and the getter claim.** Read `reactive_graph-0.2.14/src/wrappers.rs` directly: `Signal::derive` (line ~631) and every `From<...> for Signal<T>` impl (`Signal::from(RwSignal)` at ~853, `.into()`, a `#[prop(into)]` conversion) all allocate a `ArenaItem`-backed wrapper disposed with the current owner — confirms the stated mechanism as fact, not merely as an asserted comment. Confirmed `SyncStore`'s twelve `*_version()` getters (`cache/store.rs:212-407`) each do `let sig = self.inner.with_value(...); Signal::derive(move || sig.get())` — a fresh wrapper on every call, exactly as claimed. Grepped every `_version()` call site in the diff (`notifications.rs:35`, `workspace.rs:129`, `issue_detail.rs:3307`) and confirmed each resolves the getter once, outside the closure that re-runs, and the closure only reads the already-built `Signal` via `.map(|v| v.get())`. Checked call sites *outside* the diff too (`project_detail.rs:111-144`): these build a `Signal::derive` wrapper at the component's own top-level setup (not inside a per-refetch-rebuilt subtree), so they're safe regardless — the wrapper and everything it can be read through share the page's own lifetime. `Switch.checked: Signal<bool>` (`components/switch.rs:50`) confirmed as a plain (non-`#[prop(into)]`) field, so the panic mechanism required a caller to construct a `Signal` explicitly — exactly what `ToggleState::checked`/`ArchiveSelect::selected`+`options` now do at the page level instead of inside the rebuilt row/card.
2. **Ownership audit completeness.** Dispatched an independent sub-agent to re-derive the audit from scratch (grep every `#[prop(into)] ... Signal<...>` prop in `components/`, then re-check every call site in `notifications.rs` and `workspace.rs`). Result: audit is functionally complete — no missed unsafe (wrapper-outlives-source-survives-source category) site in either file. The specific counts floated in the review brief (17 props, ×3 `Button variant=`, ×2 `TeamCreationModal show=`) don't match the actual source (actual: 15 props, 2 `Button` sites, 1 `TeamCreationModal` site in these two files) — but that framing doesn't appear anywhere in the diff's own comments, so it's not a diff defect; the underlying safety conclusion for both categories holds under independent re-verification (`Button variant=` resolves via `Signal::stored`, a constant nothing ever writes to; `TeamCreationModal`'s `show_create_modal` is created inside `TeamsSection`, which is itself rendered inside `WorkspacePage`'s own rebuilt `Suspend` body, so source and wrapper die together on every refetch).
3. **Guard test honesty.** `every_entity_type_the_cache_persists_reaches_the_store` (`sync_engine.rs`) reads `apply.rs` as text; its own doc comment discloses the `use ... as entity_types` alias bypass explicitly and accurately, with no hedging. Confirmed the `ISSUE_CONTENT` exemption is genuinely behavioral, not hand-listed: `side_effect_only_cache_types()` pushes every declared entity type through the real `enqueue_cache_writes` and records which cache types an insert of each one produces beyond itself — for `ISSUE` (with a non-null `description`), that's `[ISSUE, ISSUE_CONTENT]`, so `ISSUE_CONTENT` self-exempts without being named. The prior review log (`docs/review-logs/2026-07-28.md`, restored verbatim in this diff, byte-identical to `be3bfad`) documents the mutation that proves the test isn't vacuous: blanking the `ATTACHMENT` arm's body left the guard test green while the dedicated behavioral test failed — I did not re-run that specific mutation myself this cycle (time budget), but independently confirmed the mechanism it depends on (`entity_types_matched` panics rather than silently skipping any `entity_types::`-mentioning line it can't parse as an arm) by reading the parser.
4. **`RELEASE` removal.** Traced `handle_sync_bootstrap` (`apps/server/src/routes/websocket.rs:400-466`) line by line — no `RELEASE` batch anywhere in the stream. Confirmed `release_service::create_release` (`crates/trakkt-auth/src/release_service.rs:260-296`) emits per-issue `ISSUE` `Update` frames (which *do* reach the UI via the ordinary issue path) followed by one `RELEASE` `Insert` frame with a real payload (not `None` — grepped `sync_log_service.rs`/`release_service.rs`, consistent with the 2026-07-28 log's own finding that this payload bug was already fixed upstream). No route, page, server function, or hydration step anywhere in `trakkt-ui` reads `entity_types::RELEASE`. `declared_entity_types()` still includes it (20 constants in `trakkt-types/src/sync.rs`, `ALL_CACHED_ENTITY_TYPES` now correctly has 19).
5. **Per-arm behavioral tests.** `cache/apply.rs`'s `observe()` helper (~line 1529) rebuilds the exact three `Signal` sources the three affected pages hand to their `Resource`/`LocalResource`, applies the action, and diffs before/after — plus asserts the *other two* did not move. This would fail against a no-op arm body, which is the documented mutation-test result in the 2026-07-28 log. Ran `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` directly: 48/48 pass, including both disposed-`Signal` reproduction tests (`a_write_after_the_row_is_rebuilt_still_reaches_the_switch`, `a_write_after_the_archive_card_is_rebuilt_still_reaches_the_select`) and the guard test.
6. **Nothing lost from the original 5-cycle review.** Diffed the entire staged tree against `be3bfad` file by file: `live_update.rs` is byte-identical (0 diff); `sync_engine.rs` is purely additive (0 removed lines); `apply.rs`'s only removed lines are two comment-wording tweaks and the `Signal::derive`-inside-closure→hoisted-outside pattern applied to its own test helpers; `workspace.rs`/`notifications.rs` are strict supersets — every prior fix (`adopt_unless_edited`, the workspace-name hoisting, `ToggleState`/`ToggleRow` per-field adoption, the SSR-in-body placement) is present unchanged, with the two new panic fixes (`ToggleState::checked`, `ArchiveSelect`) added on top. Nothing from the five-cycle arc documented in `docs/review-logs/2026-07-28.md` (also restored, byte-identical) was undone.

Gates run directly in this worktree (no server started, port 3577 untouched, no `trunk build` run, per the brief's explicit instruction): `cargo clippy --workspace --locked -- -D warnings` clean; `cargo clippy --locked -p trakkt-ui --target wasm32-unknown-unknown --features hydrate -- -D warnings` clean; `cargo test --workspace --locked` full green (196 trakkt-auth, 98 trakkt-ui, 58 trakkt-github, 38 trakkt-server, 36 trakkt-core, 25 trakkt-api, 7 sync_ws, 5 team_icon_routes, 2 trakkt-types — 0 failed); `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` 48/48 green; `grep` for `allow(dead_code|unused_variables|unused_imports)` across every staged file returned no matches. No `#[allow(...)]`, fallback/swallowed-error code, stubs, or mocks anywhere in the diff. Did not run the two Playwright suites in `e2e/tests/sync/` (would require a live server; explicitly out of bounds for this review per the brief) — reviewed both directly as source instead; noted as finding #3 above rather than silently skipped.

---

### 16:35 — TRA-9977: scoped re-review (cycle 2) — comment-only line-reference correction

**Review type**: re-review (cycle 2), scoped to one file
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢 — **signed**
**Files**: `e2e/tests/sync/two-window-sync.spec.ts` (only file changed this cycle)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: Follow-up to the initial review above. Two items from that review are now resolved outside this diff: finding 2 (`ATTACHMENT`/`ISSUE_RELATION` delete-side cache-leak gap) was recorded as a correction on TRA-9966 by the coordinator, with `ISSUE_ATTACHMENT` correctly noted as conditional on TRA-9979 landing first — not independently re-verified this cycle (out of scope, ticket-side change, no code in this repo to check). Finding 3 (unable to run the Playwright suites myself, server start being out of bounds) is resolved: the coordinator ran both `panic-probe.spec.ts` (5/5) and `two-window-sync.spec.ts` (4/4, including the rename-reaches-window-B assertion that only passes once TRA-9977 is actually fixed) against a live server, rebuilt from the final staged code first — this is exactly the "run the page" verification the original 5-cycle review series was faulted for skipping, and it's now done.

Finding 1 (stale `workspace.rs:191` line reference in a comment) prompted this cycle. The coordinator's replacement comment separates the two claims that were previously conflated into one line number: `on_blur` is *defined* at `workspace.rs:222` and *bound* to the input (`on:blur=on_blur`) at `workspace.rs:252`. Verified both independently with `grep -n "on_blur"` against the current staged `workspace.rs` — exact match, both numbers correct.

Verified scope directly against the diff, not by trusting the coordinator's description: reconstructed the full staged diff before this edit (saved from the initial review) and diffed it against the current staged diff (`git diff --cached` after `git add`-ing only `two-window-sync.spec.ts`) — the only delta anywhere in the entire staged tree is the 5-line comment block at `two-window-sync.spec.ts:204-206` (old 2-line comment → new 3-line comment). No other file changed, confirming `apply.rs` (mid mutation-sweep in the working tree, per the coordinator's note) was not touched in the index. Confirmed the `afterEach` panic assertion (`two-window-sync.spec.ts:61-64`) and both original test assertions (`toHaveValue(newName, ...)` in the rename test, `toHaveValue(typed)` in the not-destroyed test) are byte-identical to what cycle 1 reviewed — this is a comment-only change with zero test-logic risk.

Did not re-run any gate per the coordinator's explicit instruction (nothing compiled changed; a mutation sweep against `apply.rs` was running in an isolated `CARGO_TARGET_DIR` and was not to be disturbed): no `cargo`, no `wasm-pack`, no server started. Signed on the strength of the byte-level diff isolation proof above plus the two independently-verified line numbers — both are the whole surface area of this cycle's change.

---
### 17:28 — TRA-9977: scoped re-review — CI fix for global `any_spawner` executor ordering dependency (worktree: trakkt-wt-tra-9977-apply-cached-types)

**Review type**: re-review (cycle 3), scoped to the staged CI fix on top of `3e2ac1d`
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢 — **signed**
**Files**: `crates/trakkt-ui/Cargo.toml`, `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/lib.rs`, `crates/trakkt-ui/src/pages/settings/live_update.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`, `crates/trakkt-ui/src/pages/settings/workspace.rs`, `crates/trakkt-ui/src/wasm_test_support.rs` (new)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: `WASM Browser Tests` failed in CI (44 passed, 4 in `pages::settings::notifications::wasm_tests` panicked with `tried to spawn a Future ... before a global executor was initialized`) while passing locally — a latent test-ordering dependency, not a flake: `any_spawner`'s executor is process-global and set once per test binary, and before this fix the only initializer was a private helper called from two tests in `sync_engine.rs`, so every other spawning test silently depended on one of those two running first. Verified all six claims in the fix and the headline verification claim independently, not by trusting the diff's own comments:

1. **Single definition.** `grep -rn "init_wasm_bindgen" crates/` shows exactly one call site (`wasm_test_support.rs:36`); `grep -rn "boot_effect_executor"` (old name) returns nothing; all 5 test modules (`sync_engine.rs`, `workspace.rs`, `live_update.rs`, `notifications.rs` ×2 use-sites... actually 4 call files) import `crate::wasm_test_support::boot_leptos_executor` and nothing redefines it. Satisfies "no duplicated helper functions across modules" (`docs/CODING_STANDARDS.md`).
2. **`ExecutorError` exhaustiveness.** Read `any_spawner-0.3.0/src/lib.rs` directly (`~/.cargo/registry/.../any_spawner-0.3.0/src/lib.rs:94-100`): `pub enum ExecutorError { AlreadySet }` — one variant, not `#[non_exhaustive]`. `Ok(()) | Err(AlreadySet) => {}` is genuinely exhaustive; nothing is widened or hidden behind a catch-all.
3. **The double `cfg` gate.** Confirmed in `crates/trakkt-ui/Cargo.toml`: `any_spawner = "0.3"` sits under `[target.'cfg(target_arch = "wasm32")'.dev-dependencies]`, so it is not a dependency at all on the native target — a bare `#[cfg(test)]` on `mod wasm_test_support` in `lib.rs` would fail to resolve `any_spawner` on `cargo test --workspace` (native). Ran `cargo test --workspace --locked --no-run` (builds clean, exit 0) and the full `cargo test --workspace --locked` (all crates green, exit 0) to confirm the gate is correct in practice, not just in theory.
4. **Two modules found and fixed beyond what was asked.** `workspace.rs`'s `a_write_after_the_archive_card_is_rebuilt_still_reaches_the_select` and `live_update.rs`'s two `latency_tests` (`drive_resource` helper) construct `LocalResource::new`/`Resource::new` before calling `mount_to`. Proved these were genuinely broken pre-fix and genuinely fixed post-fix by round-tripping the worktree: `git stash push` (full unstage, both index and worktree, back to bare `3e2ac1d`) → ran each test **alone** (not module-filtered — a module-level run of `workspace::wasm_tests` misleadingly passes pre-fix too, because an earlier test in the same module already calls `mount_to`, which itself initializes the executor as a side effect, masking exactly the ordering bug this fix targets) → `a_write_after_the_archive_card_is_rebuilt_still_reaches_the_select` and `a_server_function_round_trip_costs_neither_the_text_nor_the_caret` both panicked (`some tests failed`, stack trace rooted in `ArcAsyncDerived::new_unsync`/`LocalResource::new`) → `an_in_flight_toggle_survives_the_row_being_rebuilt` (notifications) also panicked alone, confirming CI's failure class generalizes beyond the four tests CI happened to report. `git stash pop` + `git add -A` restored the exact original staged diff (verified via `git diff --cached | sha256sum` matching before/after: `5ddfaf90e6364b2c81343043bfdd85c7b236b62e6c1d85c2c279b361b1bda31e`, and `git diff` empty). Re-ran all three tests alone post-fix: all pass.
5. **The construction-time-spawn mechanism.** Read `reactive_graph-0.2.14/src/effect/effect.rs:168-177`: `Effect::new` calls `Executor::spawn_local` unconditionally inside `new`, at construction, not lazily. Read `leptos-0.8.20/src/mount.rs:183`: `mount_to` calls `_ = Executor::init_wasm_bindgen();` — exact line match to the claim. `workspace.rs`'s and `live_update.rs`'s fixed tests both construct their `LocalResource`/`Resource` textually before their own `mount_to` call, confirming the "initialize explicitly, don't just mount earlier" framing is the right fix shape.
6. **Cargo.toml comment.** Diffed: comment-only change, now correctly says `Effect`, `Resource`, and `LocalResource` all spawn on construction and points at `wasm_test_support.rs`; no dependency version or target-gate logic changed.

**Full-suite isolation claim.** Ran `wasm-pack test --headless --firefox crates/trakkt-ui --features hydrate` (no filter) with `CARGO_TARGET_DIR` pointed at the warm scratch dir: 48 passed, 0 failed, breakdown exactly matching the implementer's tally (notifications 4, workspace 4, live_update 6, sync_engine 10, apply 10, idb_writer 3, broadcast_queue 2, delete_route 3, tab_leader 6 = 48).

**Also confirmed:**
- Production code untouched — every hunk in the diff is inside a `#[cfg(test)]`/`mod wasm_tests`/`mod latency_tests` block, a test-only Cargo.toml dev-dependency section, or a comment.
- Nothing in `3e2ac1d` altered or reverted — `git rev-parse HEAD` is exactly `3e2ac1da35bd3952c89c607d31784fa9c49fdfe8`, and the staged diff is purely additive on top.
- `grep` for `#[allow(`, `let _ =`, `unwrap_or`, `todo!`, `unimplemented!`, `FIXME`, `XXX` across the added lines: no matches.
- Gates: `cargo clippy --workspace --locked -- -D warnings` clean; `cargo clippy --locked -p trakkt-ui --target wasm32-unknown-unknown --features hydrate -- -D warnings` clean; `cargo test --workspace --locked` full green; `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` 48/48 green. Also ran the same wasm clippy invocation with `--tests` added (not the specified gate, but the only way to actually lint the `#[cfg(test)]` code this diff touches, since the specified gate never compiles test targets): it failed, but the 7 errors are all pre-existing `clippy::unwrap_used` violations in `crates/trakkt-ui/src/components/date_picker.rs` (confirmed via `git diff --cached -- .../date_picker.rs` — empty, file untouched by this diff) — zero errors in any file this diff modified. Noting this as a pre-existing gap in the specified gate's coverage, not a blocker for this review: worth a ticket at some point so `#[cfg(test)]` code gets linted at all, but out of scope for a CI-fix re-review.

Did not touch `e2e/`, did not start a server, port 3577 untouched, as instructed.

---

### 18:24 — TRA-9954: activity_service.rs atomic sync_log commit (last of the series)

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `crates/trakkt-auth/src/activity_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs` (tests only)

**Notes**: Converts the last two `write_sync_entry` call sites (`insert_activity`, `coalesce_or_insert_activity`) to `db.begin()` → `tx_*!` writes → `sync_log_service::commit_and_deliver`, closing the TRA-9923→TRA-9954 atomic-`sync_log` series. `commit_and_deliver`/`SyncBatch`/`SyncAudience`/`write_sync_entry_in_tx` are all pre-existing infrastructure from TRA-9947–9953 — this diff only adds two call sites and a new `insert_activity_row(&mut DbTx)` helper factoring the shared INSERT out of both. Verified rather than trusted:

1. **Transaction discipline.** `grep -c "write_sync_entry("` on the file is 0; `grep "broadcast_sync_action"` in the file is 0 (both routed through `commit_and_deliver`). Independently traced every macro/helper called between `db.begin()` and the terminal `commit_and_deliver`/`SyncBatch::commit_and_deliver` in both converted functions: `tx_fetch_optional!`/`tx_execute!` (`trakkt-core/src/db.rs:297-338`) match on the `DbTx` enum and execute directly against the held `sqlx::Transaction`, no pool/`.acquire()`; `DbTx::is_postgres()` is a bare `matches!` tag check; every `sql_compat::*` call in both files (`within_seconds`, `now`, `cast_to_json`) is a pure `bool → String` builder with no `sqlx` import at all. `write_sync_entry_in_tx` itself only calls `tx_*!` macros and pure helpers, no fallback to any `db_*!`/`&DbPool` path. `SyncBatch::commit_and_deliver` calls `tx.commit().await?` before the delivery loop; `broadcast_raw_to_workspace` (which does hit the pool via `db_fetch_all!`) only runs after that commit succeeds. No stray pool-scoped call anywhere in either span.
2. **Coalescing SELECT moved inside the transaction, insert branch folded into the same tx.** Confirmed: `coalesce_or_insert_activity` opens one `tx`, runs the lookup on it (`tx_fetch_optional!`), and both the `Some(row)` (UPDATE) and `None` (`insert_activity_row` on the same `&mut tx`) arms run on that one transaction before a single `commit_and_deliver` call — not two separate transactions. This closes the find-then-update race the doc comment describes (previously two pool round trips).
3. **Non-vacuity of the rollback tests — reproduced, not trusted.** Mutated `coalesce_or_insert_activity`'s `match existing` to `match None::<CoalesceRow>` (forcing the insert branch even when a coalescable row exists) and reran `coalesced_activity_rolls_back_when_its_sync_entry_cannot_be_written` alone: it failed exactly as predicted (`expect_err` panicked because the narrowed `reject_sync_log_inserts_for_action(&db, "update")` trigger let the fallen-through insert's entry through). Restored the file, confirmed `git diff` empty afterward. This independently confirms the narrowed-trigger design does what its comment claims.
4. **Timestamp trap.** `backdate_activity` does assert `rows_affected() == 1`. Confirmed the two behaviour tests genuinely need it: reran with `DESCRIPTION_COALESCE_WINDOW_SECS` mutated to `60 * 0`, `a_second_description_change_inside_the_window_...` failed (`left: 2, right: 1`); mutated to `60 * 1000`, `a_description_change_after_the_window_...` failed (`left: 1, right: 2`). Both restored clean afterward (`git diff` empty).
5. **`.unwrap_or(0)`-class mutation on the plain-insert rollback test.** The two behaviour tests indeed don't respond to a placeholder-`sync_id` mutation (correctly not rollback tests) — the window mutations above are a legitimate, non-overlapping substitute testing the actual thing those two tests exist for. Separately reproduced the acceptance-criterion-2 concern directly: reintroduced the pre-TRA-9954 swallow pattern in `insert_activity` (`let _ = commit_and_deliver(...).await; Ok(())`) and reran `activity_insert_rolls_back_when_its_sync_entry_cannot_be_written` alone — failed exactly as predicted (`expect_err` panicked; the underlying `tx` still rolled back on drop since it was never committed, so data was correctly restored, but the caller no longer saw `Err`). Confirms the criterion is real and the current code satisfies it structurally (no `unwrap_or`/`unwrap_or_else` anywhere near a `sync_id` in the diff). Restored clean, `git diff` empty.
6. **`commit_and_deliver` reuse / `insert_activity_row` fidelity.** The extracted helper's INSERT SQL, bind order, and `json_cast`/`now` fragments are byte-identical to the pre-diff `insert_activity` body (only `self.db`/`db_execute!` swapped for `&mut *tx`/`tx_execute!`). Both callers (`insert_activity`, `coalesce_or_insert_activity`'s `None` arm) finish through `commit_and_deliver`, matching the established pattern from TRA-9947–9953 exactly — no hand-rolled commit-then-broadcast.
7. **Doc-comment claims checked, not assumed** (per `docs/CODING_STANDARDS.md`'s Comment/Doc Accuracy section): `grep -rn '\.begin()' crates/trakkt-api/src/ crates/trakkt-github/src/` returns nothing (matches the "no caller transaction to take" claim); `grep -rln "ActivityRecorder::new"` outside the two changed files hits exactly `comments.rs`, `issues.rs`, `relations.rs`, `events.rs` (matches the doc's enumerated call sites exactly); `coalesce_or_insert_activity` has exactly one call site (`record_issue_diff`'s `description_changed` branch), and `description_changed` appears nowhere else in that function's body — matches the "runs at most once per diff" claim.
8. **Call-site audit.** All 8+ callers of `record`/`record_field_change`/`record_issue_diff` across `trakkt-api`/`trakkt-github` already use the `if let Err(e) = recorder.record(...) { warn!; }` best-effort pattern — consistent with the ticket's "must not roll back a legitimate issue update" design; no caller needed updating since the public method signatures didn't change.

**Gates**: `cargo check --workspace --locked` clean. `cargo test --locked -p trakkt-auth` 200/200 passed (matches implementer's report). `cargo clippy --workspace --locked -- -D warnings` clean (forced a rebuild via `touch` to rule out a stale cache hit). `grep` for `allow(dead_code)`/`allow(unused_variables)`/`allow(unused_imports)` across changed files: no matches. `cargo clippy -p trakkt-auth --all-targets` pre-existing `unwrap_used` warnings are at `sync_log_service.rs:921` (`test_parse_action_type_unknown`, an old test) and `:4326` (a `type_complexity` note on an unrelated struct) — both outside the new lines (new tests start at line 7094+), confirming TRA-9956 is correctly out of scope. `cargo fmt -p trakkt-auth -- --check` shows diffs, but reproduced via `git stash` that the base commit (`93eb061`) is *already* not fmt-clean project-wide (e.g. `archive_service.rs`, untouched by this diff, also fails) — pre-existing repo condition, not introduced here, not a specified gate.

**Anti-pattern tally**: none (0 🔴, 0 🟡, 0 🟢)

---

### 10:45 — TRA-9955: static CI check against pool access inside an open `DbTx`

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 1 🟢
**Files**: `.github/workflows/ci.yml`, `docs/CODING_STANDARDS.md`, `scripts/check-tx-pool.py` (new), `scripts/tests/tx-pool/README.md` (new), `scripts/tests/tx-pool/*.rs.txt` (9 new fixtures)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Undocumented edge case (not required by ticket) | `scripts/check-tx-pool.py` (whole-file design) | A second `db.begin()` call on the same `&DbPool` binding while an outer span is already open (re-entrant `.begin()`) is not flagged by either the macro or `&DbPool`-fn detection, since `DbPool::begin` takes `&self`, not an explicit `&DbPool` parameter. Manually reasoned through this is a real gap, but it is outside the ticket's stated detection surface (macros + `&DbPool`-taking helpers) and I found no evidence of this shape existing anywhere in the current 56 spans. Not required to fix; worth a one-line mention in the header's "what it cannot see" list if anyone revisits this file.

**Anti-pattern tally**: none blocking

**Notes**: This is a CI guard whose entire failure mode is silence, so I did not accept any of the implementer's claims about detection completeness without reproducing them myself against the actual working tree.

**Gates, real exit codes (not piped through `tail`)**:
- `cargo check --workspace --locked` → exit 0.
- `cargo clippy --workspace --locked -- -D warnings` → exit 0.
- `cargo test --workspace --locked` → exit 0, no `FAILED` anywhere in the log.
- `python3 scripts/check-tx-pool.py` → exit 0, clean run: "56 transaction spans and 10 `&mut DbTx` helper bodies checked against 6 pool-scoped macros ... and 364 `&DbPool`-taking functions."
- `python3 scripts/check-tx-pool.py --self-test` → exit 0, 9/9 fixtures pass.

**Reproduced, not trusted, six separate claims:**

1. **The two "misses" in `trakkt-core/db.rs` are the documented cross-crate gap, not a bug.** `db.rs` itself contains 2 of the 56 transaction spans (`DbPool::begin`'s own `pg.begin()`/`sq.begin()` bodies, each of which the tool's own `.begin()` regex correctly detects as a span). I injected a call to a real `trakkt-auth` `&DbPool`-taking function (`fetch_labels_for_issues`) into one of those two spans and reran the checker: it passed clean (violation not caught) — confirms the cross-crate blind spot is real and exactly where claimed. I then injected a call to a **same-crate** `&DbPool`-taking function (`trakkt-core::ping`) into the same span: the checker caught it immediately (`crates/trakkt-core/src/db.rs:75: ... call to \`ping()\`, which takes \`&DbPool\``). This isolates the miss to the crate boundary specifically, not a general defect in db.rs's own detection — both edits reverted, `git diff` empty afterward.
2. **Sabotage #1 (span detection emptied → 6/9 fixtures fail).** Patched a copy of the script so `find_spans` yields nothing, ran `--self-test`: exactly 6 of 9 fixtures failed (the 3 that expect `clean` still trivially pass since no findings were ever going to be reported either way). Matches the implementer's claim exactly. Restored the original file (`diff` against the pre-sabotage copy: identical).
3. **Sabotage #2 (signature parser broken → 2/9 fail).** Patched `POOL_PARAM_RE` to never match, ran `--self-test`: exactly 2 of 9 fixtures failed (`indirect_fn_in_span.rs.txt`, `two_hops_via_tx_helper.rs.txt` — the two fixtures whose expected violations depend on the `&DbPool`-fn signature scan). Matches the implementer's claim exactly. Restored and confirmed byte-identical.
4. **`set_sort_order` decline is correct, not a silently-skipped acceptance criterion.** Read the ticket directly (`mcp__trakkt__get_issue TRA-9955`): the "fold in" item asks to move the existence check ahead of `begin()`. Current code resolves `issue_id` via `tx_with!` *inside* the transaction (`issue_service.rs:1406-1421`). `git show f1a8920` (PR #261, "commit each sync_log row with the mutation it describes") shows this file used to do exactly what the ticket now asks for — `db_with_pool!` before `begin()` — and was deliberately converted to the in-transaction `tx_with!` form as part of the atomicity series this very ticket depends on. Reverting would reopen a genuine TOCTOU window (issue deleted between the pool read and `begin()`, UPDATE silently affects 0 rows, function still reports success). The implementer's ticket comment documents this reasoning explicitly rather than silently skipping the item — this is the right call, and flagging-with-reasoning rather than quiet omission is exactly the right way to decline a stale sub-instruction.
5. **`docs/CODING_STANDARDS.md` consistency.** Line 18 now states the transactional rule and cross-references `### Transactions`; the `### Transactions` section itself now names the CI job (`Transaction Pool Safety`) by its exact `name:` value in `ci.yml:78` and states the same two limitations (cross-crate, helpers that don't name `&DbPool`) as the script's own header — no drift between the two.
6. **CI wiring.** `tx-pool` job runs on `pull_request: branches: [main]` (same trigger as every other job, not a schedule), has `timeout-minutes: 5`, and runs `--self-test` as a hard prerequisite step before the real check — so a broken guard fails the job outright rather than silently passing. Only the job-count comment block and the new job were touched in `ci.yml`; diffed the removed lines and confirmed nothing else in the file changed.

**Design read**: `discover_pool_macros` and the `&DbPool`-taking function set are both genuinely derived from source text (regex over `macro_rules!` bodies in `db.rs` for the former, a per-crate `fn`-signature scan for the latter) — no hardcoded macro/function name list anywhere in the script (`grep` confirms). The span-end liveness tracking (brace-depth-aware, `return`/`break`/`continue`-gated divergence, hard `Failure` on anything unrecognized) held up under adversarial hand-tracing of nested-block and loop scenarios I constructed myself — in every case I could think of, the depth-based revival correctly re-activates liveness exactly at the point control flow rejoins a path where the transaction was never closed, and it errs toward over-flagging (false positive on unreachable dead-code paths) rather than under-flagging. `strip_noncode` handles nested block comments, raw strings (`r#"..."#`), and the char-vs-lifetime ambiguity — traced by hand against `clean_comments_and_strings.rs.txt`, which exercises exactly these cases and passes.

No `#[allow(...)]`, no stubs, no fallback error-swallowing (`grep` across the full diff for `allow(`, `unwrap_or`, `.ok()`, `TODO`, `FIXME` returns only the pre-existing reference to the *other* CI job's name in a comment). `--self-test` and its 9 fixtures are scope beyond the literal acceptance criteria, but directly serve the explicit brief ("this guard's failure mode is silence") and are wired as a required CI prerequisite step, not a standalone convenience script nobody runs — judged justified, not scope creep.

---

### 11:10 — TRA-9955: scoped re-review — document the re-entrant `db.begin()` gap

**Review type**: re-review (cycle 2), scoped to one file
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢 — **signed**
**Files**: `scripts/check-tx-pool.py` (only file with an executable/comment change this cycle)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: Direct response to the 🟢 in the initial review — the coordinator correctly treated an undocumented limitation as worth closing given "limitations documented, stated plainly rather than implied covered" is an explicit acceptance-criterion bullet on this ticket, not just a nice-to-have.

**(a) Comment-only, confirmed by diff, not by trusting the coordinator's description.** Diffed the current `scripts/check-tx-pool.py` against the exact copy I hashed and read during the initial review (preserved at `/tmp/check-tx-pool-good.py` from that cycle's sabotage testing): the only delta is 6 added lines, all `#`-prefixed, inserted into the existing "What it genuinely cannot see" list between the closure-capture bullet and the "It is a textual analysis..." paragraph. Nothing else in the file changed — no logic, no regex, no fixture reference. Also diffed `.github/workflows/ci.yml` and `docs/CODING_STANDARDS.md` against the exact text quoted in the initial review's own report: byte-identical, confirming the coordinator's "scope it to that one file" claim rather than assuming it.

**(b) Accuracy of "no such pattern exists in the tree today" — re-verified, not re-asserted.** My original finding was reasoning-based ("I found no evidence of this shape"), so I re-checked it properly this cycle rather than rubber-stamping my own prior claim: wrote a standalone script that, for every `fn` in every workspace source file, brace-matches the body and counts `.begin()` occurrences inside it. Exactly one function in the entire workspace has more than one: `DbPool::begin` itself (`crates/trakkt-core/src/db.rs`), where `pg.begin()` and `sq.begin()` sit in mutually exclusive `match self { Postgres => .., Sqlite => .. }` arms — not a nested/sequential re-entrant call. No function anywhere calls `.begin()` twice in sequence on the same binding. The new comment's claim holds exactly as worded; no wording narrowing needed.

**(c) Gates.** Re-ran both scoped commands myself rather than trusting the coordinator's report: `python3 scripts/check-tx-pool.py` → exit 0, same "56 transaction spans and 10 `&mut DbTx` helper bodies... 364 `&DbPool`-taking functions" output as the initial review (comment-only change cannot affect these counts, and it didn't). `python3 scripts/check-tx-pool.py --self-test` → exit 0, 9/9 fixtures pass, identical to cycle 1. Did not re-run `cargo check`/`clippy`/`test` — no `.rs` file outside the comment block changed, so those results from the initial review remain valid; re-running them would not exercise anything different.

Re-signing against the new diff hash.

---
### 19:05 — TRA-9956: enable `--all-targets` on both clippy gates, fix 108 banned-pattern violations

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 1 🟢
**Files**: `.github/workflows/ci.yml`, `docs/CODING_STANDARDS.md`, `apps/server/src/middleware/auth_refresh.rs`, `crates/trakkt-api/src/context.rs`, `crates/trakkt-api/src/lib.rs`, `crates/trakkt-auth/src/encryption.rs`, `crates/trakkt-auth/src/jwt.rs`, `crates/trakkt-auth/src/mcp_session_manager.rs`, `crates/trakkt-auth/src/password.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/totp.rs`, `crates/trakkt-core/src/kv_store_memory.rs`, `crates/trakkt-core/src/retry.rs`, `crates/trakkt-github/src/lib.rs`, `crates/trakkt-ui/src/components/date_picker.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Style consistency (not a policy violation) | `crates/trakkt-auth/src/sync_log_service.rs:914-916` | `parse_action_type(expected).expect("should parse")` was replaced with `.unwrap_or_else(\|e\| panic!("parsing the wire string {expected:?} back into a SyncActionType: {e}"))` rather than `.expect("...")`. This line was never one of the 108 counted violations (it already used `.expect()`, not `.unwrap()`), so the change is an out-of-scope, self-initiated message-quality improvement — a good instinct, but it deviates from the stated convention ("`unwrap()` → `expect(\"what was being attempted\")`") for no behavioral reason: `.expect(msg)` already prints the source error's `Debug` output on panic, so `.expect(&format!("parsing the wire string {expected:?} back into a SyncActionType"))` would have said the same thing through the standard mechanism. Cosmetic only — panics on the same condition, same information content on failure. Not blocking.

**Anti-pattern tally**: Style consistency ×1 (non-blocking)

**Notes**: Reviewed all 108 violations across all touched files (`expect()` message quality was the primary scrutiny target per the brief) and independently ran every gate rather than trusting the PR's report.

**`expect` message quality** — sampled all ~100 `.expect(`/`.expect_err(` additions across every file, not just the three quoted samples. All say what was being attempted (e.g. `encryption.rs`: "encrypting a multi-byte UTF-8 plaintext with the test key", "decrypting the empty-plaintext ciphertext with the key that produced it"; `jwt.rs`: "minting a token with a negative TTL so it is already expired"; `date_picker.rs`: "2024-02-29 to exist because 2024 is a leap year"; `kv_store_memory.rs`: "getdel on a key that was never stored"; `trakkt-github/lib.rs`: "constructing a client from non-PEM bytes must be rejected"). Grepped the full diff for filler patterns (`expect("unwrap")`, `"should work"`, `"failed"`, `"ok"`, `"works"`, `"fail"`, `"error"`) — zero matches. No filler messages found anywhere.

**No test behavior changed** — diffed every `assert!`/`assert_eq!`/`assert_ne!` line touched across the whole patch: every assertion's expected value is byte-identical before and after; only the `.unwrap()`/`.unwrap_err()` call wrapping changed (plus the one deviation noted above, which is a message-content change only, not an assertion change). The `dead_code` fix (`crates/trakkt-api/src/lib.rs:277-293`, `TestUpdateParams`) is correct: dropped the derived `Debug` and the `let _ = format!("{params:?}")` scaffold, destructured the instance (`let TestUpdateParams { id, title, priority } = ...`) and asserts on the tuple — this is a genuine field read, not a suppression, and it's the right call versus alternatives (keeping the derive+format read nothing meaningfully; a `#[allow(dead_code)]` is banned outright).

**No suppressions** — `git diff --cached | grep -n '^+' | grep '#\[allow'` and `grep '= "allow"'` both return nothing except the doc prose in `CODING_STANDARDS.md` describing the policy itself. Independently confirmed the `Lint Suppression Policy` job's own grep pattern (`.github/workflows/ci.yml:41-42`) is unmodified and would catch either form.

**`unwrap_err()` → `expect_err()`** — checked all 7 conversions (`encryption.rs` ×2, `jwt.rs` ×3, `trakkt-github/lib.rs` ×1, `sync_log_service.rs` ×1 via the `unwrap_or_else` variant noted above): all preserve exact semantics, same `Err` branch, same downstream `.to_string()`/message assertions.

**Type alias** — `crates/trakkt-auth/src/sync_log_service.rs:4243-4245`: `type MilestoneRow = (String, String, String, Option<String>, Option<String>)` with a doc comment naming the five columns, placed directly above its first use (`milestones()`'s return type) and reused at the `ProjectDeleteFootprint.milestones` field, removing what had been the same complex tuple type spelled out twice. Genuine fix, sensibly named and placed.

**Gate verification, real exit codes:**
- `cargo clippy --locked --workspace --all-targets -- -D warnings` → exit 0.
- `cargo clippy --locked -p trakkt-ui --target wasm32-unknown-unknown --features hydrate --all-targets -- -D warnings` → exit 0.
- `cargo test --workspace --locked` → exit 0, 0 failed across all 20 test binaries/doc-test groups; `redis::tests::test_redis_connects` shows `ignored, requires a live Redis` (not run, not failed) — confirms the ticket's Redis caveat doesn't apply here.
- `python3 scripts/check-tx-pool.py` → exit 0 (56 spans, 10 helper bodies, 364 `&DbPool` functions checked). `python3 scripts/check-tx-pool.py --self-test` → exit 0, 9/9 fixtures pass.
- `grep -rn --include='*.rs' '\.unwrap()\|\.unwrap_err()' crates apps tools` → 0 matches, confirmed independently.
- `ss -ltnp | grep -E ':6379|:6381'` → nothing listening; both clippy gates still passed, confirming clippy never needs Redis.

**Gate is load-bearing — reproduced myself, not trusted from the report.** Planted `x.unwrap()` on an `Option<u32>` inside a new `#[test]` fn in `crates/trakkt-core/src/retry.rs`'s existing `mod tests`. `cargo clippy --locked --workspace -- -D warnings` (old command, no `--all-targets`) → exit 0, blind to it. `cargo clippy --locked --workspace --all-targets -- -D warnings` (new command) → exit 101, flagged exactly by `clippy::unwrap_used` at `retry.rs:167`, with the standard "consider using `expect()`" help text. Removed the probe and confirmed `git diff` against the staged tree is empty afterward (working tree exactly matches the index). Did not reproduce the wasm-side demonstration (host reproduction was sufficient per the brief's "at least" wording, and the reasoning for why the host gate can't see `#[cfg(all(test, target_arch = "wasm32"))]` modules is separately verifiable from the `cfg(target_arch = "wasm32")`-is-false-on-host fact stated correctly in both `ci.yml` and `CODING_STANDARDS.md`).

**Runtime claim** — sanity-checked, not re-measured (10-minute cold rebuild out of scope per the brief). `Cargo.lock` has 636 packages, consistent with "roughly 700 dependency crates" dominating build time; `--all-targets` only adds compilation of already-resolved dependency rlibs into 18 more (test) binary crates, which is architecturally consistent with near-zero marginal cost once the shared dependency graph is built once. The documented methodology (first measurement invalidated because `~/.cargo/config.toml`'s `rustc-wrapper = "sccache"` isn't overridden by `RUSTC_WRAPPER=` or `--config build.rustc-wrapper=''`, so the second form silently read the first's populated cache; corrected with `cargo clean` + a private empty `SCCACHE_DIR` per form) is a real, well-known sccache footgun and the fix is the right one. The `timeout-minutes: 25` derivation in the `ci.yml` comment (measured 318s → scaled by the file's own recorded local/CI ratio → ~12min realistic, ~20min pessimistic, 25 covers both with headroom, stays under `Test Suite`'s 30) is transparent and defensible.

**Also confirmed**: both CI commands match the commands recorded in `docs/CODING_STANDARDS.md` exactly (`grep`-diffed, byte-identical). The `Clippy` job now has `timeout-minutes: 25` where it previously had none (confirmed via the diff hunk). Policy decision (fix all, no test-scoped allowance, no blanket `#![allow]`) is stated in `CODING_STANDARDS.md`'s new "The Clippy Gate" section with reasoning, matching the ticket comment's stated rationale.

---

### 13:15 — TRA-9957: cascade-delete an issue's comments and relations out of `sync_log`

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 1 🟢
**Files**: `crates/trakkt-auth/src/issue_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs` (tests only), `apps/server/tests/cascade_delete_sync.rs` (new)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Comment and Doc Accuracy | `apps/server/tests/cascade_delete_sync.rs:19-21` | The module doc says `MapSink` is "keyed by `(entity_type, entity_id)` — the same key the IndexedDB sink writes under." The real key (`crates/trakkt-ui/src/cache/db.rs:95-96`, `entity_key`) is a 3-part composite `entity_type\x00workspace_id\x00entity_id`; the test sink drops `workspace_id`. Harmless here (the fixture uses one workspace, so no collision the extra component would have prevented), but "the same key" overstates fidelity — should read "a workspace-free simplification of the same key" or similar. Non-blocking per `CODING_STANDARDS.md`'s comment-accuracy rule, since the surrounding claim ("proves the op stream and its effect on a keyed store; does not prove the browser's storage layer") already correctly scopes what the test does and doesn't prove.

**Anti-pattern tally**: Comment and Doc Accuracy ×1 (non-blocking)

**Notes**: This is the cascade-delete-sync-log fix TRA-9957 was filed for: `delete_issue` previously wrote only the ISSUE delete entry, so `ON DELETE CASCADE` silently destroyed comments and relations no client would ever learn to evict. Reviewed against all six numbered scrutiny points and both disclosed limitations in the review brief; every one independently re-derived, not taken on the requester's word.

**(1) The audit — re-derived independently.** Walked `apps/server/src/routes/websocket.rs:432-451`'s `PendingBatch` list myself: ISSUE, LABEL, STATUS, TEAM, PROJECT, VIEW, FAVORITE, NOTIFICATION, COMMENT, PROJECT_MILESTONE, plus a conditional WORKSPACE_SETTINGS — no ACTIVITY, no ISSUE_ATTACHMENT, no ISSUE_RELATION batch. Then checked both write sites for the excluded types: `activity_service.rs:558-570` and `:588-599` (both `commit_and_deliver(..., None, ...)`) and `attachment_service.rs:334-343` (`attach_to_issue`, also `None`) — confirmed both always pass a payload-less `None`, so `enqueue_cache_writes`'s `let Some(ref entity_data) = action.data else { return; }` guard (`crates/trakkt-ui/src/cache/apply.rs:449-451`) discards every ACTIVITY/ISSUE_ATTACHMENT frame before it can be cached. `create_relation` (`relation_service.rs`) does carry a real payload on Insert, confirming relations are genuinely cached via delta (not bootstrap — no ISSUE_RELATION `PendingBatch`, matching the doc's claim that it only reaches IndexedDB "via a payload-carrying frame from the bootstrap list... or a delta"). `issue_labels`/`issue_watchers`/`issue_stars`/`release_issues`/`github_links` confirmed absent from `trakkt_types::sync::entity_types` (`crates/trakkt-types/src/sync.rs:42-63`) — no entity type, no sync row possible. Independent conclusion matches the implementer's exactly: only COMMENT and ISSUE_RELATION need entries.

**(2) Relations read from both ends.** `issue_service.rs`'s `relation_ids` query is `WHERE source_issue_id = $1 OR target_issue_id = $1` as claimed. The test `issue_delete_emits_an_entry_for_every_relation_at_either_end` seeds exactly the structure the brief describes: `as_source` (doomed→second, `blocks`), `as_target` (third→doomed, `blocks`), and a `bystander` (second→third, `relates_to`, chosen specifically to dodge `validate_no_circular_blocking`, which only fires for `relation_type == "blocks"` — confirmed at `relation_service.rs:302-304`; `relates_to` isn't cycle-checked, so the workaround is legitimate, not a hack). Ran it: passes, and the bystander survives (asserted via `count_scalar`).

**(3) Transaction discipline.** Read the full `delete_issue` body end to end: `db.begin()` first, `issue_id` resolved via `tx_fetch_optional!` on `&mut tx`, both cascaded-id reads via `tx_fetch_all!`, the DELETE via `tx_execute!`, all `SyncBatch::record` calls take `&mut tx`, and `batch.commit_and_deliver(tx, ws_manager)` is the sole tail call — no `db_fetch_*!`/`db_execute!`/`&DbPool`-taking helper anywhere in between (grepped the function body directly, not just trusting the tool). Ran `python3 scripts/check-tx-pool.py` (exit 0, clean) and `--self-test` (exit 0, 9/9 fixtures) myself.

**(4) `SyncBatch` tail.** `SyncBatch`/`commit_and_deliver`/`SyncAudience` all predate this diff (`git log -- crates/trakkt-auth/src/sync_log_service.rs` shows them present at `HEAD`, from PR #280); this diff is a pure consumer. Read `commit_and_deliver` (`sync_log_service.rs:760-805`): commits first, delivers only after, in write order — ordering preserved. `SyncAudience::Workspace` matches what `comment_service::delete_comment` (`visibility_user_id = None`, workspace-wide `broadcast_sync_action`) and `relation_service::delete_relation` (explicit `SyncAudience::Workspace`) already use for these two entity types — confirmed by reading both functions directly.

**(5) M3 — reproduced, not just read.** Temporarily edited `delete_issue` (working tree only, never touching the index) to split it into two transactions: `batch.commit_and_deliver(tx, ...)` right after the ISSUE record, then a second `tx2`/`batch2` for the comment/relation loop. Ran `issue_delete_and_its_cascade_entries_roll_back_together` against the mutant: it failed exactly as the brief predicted — `assertion left == right failed: the DELETE has to roll back with the entries that describe it; left: 0, right: 1` — proving the test catches a "committed early, cascade entries land on a disconnected transaction" regression, not merely "some entries are missing." Restored the file from a pre-edit copy immediately after and confirmed `git diff` against the staged index is empty (working tree exactly matches `HEAD`'s index again) before continuing the review.

**(6) The replay test's honesty.** `apps/server/tests/cascade_delete_sync.rs` genuinely runs production code on both ends — `delete_issue`, `get_entries_since`, `apply_action_to_memory`, `enqueue_cache_writes`, `run_writer` are all imported from their real crates, not reimplemented. `MapSink` is a real `IdbSink` impl (fake storage, real trait boundary) — accurately described as "real apply logic, modelled storage." One inaccuracy noted above (🟢): the key comparison to the browser sink is a simplification, not a match, though it doesn't undermine what the test actually proves.

**Disclosed limitations — both verified accurate.** ISSUE_RELATION missing from `enqueue_cache_writes`'s Delete match (`crates/trakkt-ui/src/cache/apply.rs:510-561`) — counted the match arms myself: 12 types (ISSUE, LABEL, STATUS, TEAM, PROJECT, VIEW, FAVORITE, NOTIFICATION, COMMENT, PROJECT_MILESTONE, PROJECT_MEMBER, PROJECT_UPDATE), ISSUE_RELATION falls through to `_ => {}` — matches "12-type Delete list" exactly. `relation_service::delete_relation` has the identical gap today (writes a real ISSUE_RELATION delete entry via `commit_and_deliver`, same match table applies) — pre-existing, not newly introduced, correctly attributed to TRA-9966. Comments have both the memory-bump arm (`apply.rs:266-271`) and the delete arm (`apply.rs:536-538`), so the reported bug (comments surviving forever in a client's cache) is fully closed by this diff.

**Out-of-scope items — correctly out of scope.** TRA-9989 (`notifications.issue_id` has no `ON DELETE` clause, NO ACTION by default on both dialects): confirmed against `apps/server/migrations/20260507000000_issue_tracker.sql:243` and the SQLite equivalent — matches exactly. The replay test's precondition (`cascade_delete_sync.rs:179-193`) does assert zero notifications on the doomed issue before the DELETE runs, with a message explaining why an unasserted precondition would make the test "proving something else entirely" — present and correct, not merely implied. TRA-9990 (`issue_activities`/`github_links` CASCADE-on-Postgres, no-FK-on-SQLite): confirmed against both migration pairs — Postgres declares `ON DELETE CASCADE`, SQLite declares the column with no FK at all. Neither ticket's territory was touched by this diff.

**Gates — run myself, real exit codes:**
- `cargo check --workspace --locked` → exit 0.
- `cargo test --locked -p trakkt-auth` → exit 0, 203 passed (including all three new cascade tests), 0 failed.
- `cargo test --locked -p trakkt-server --test cascade_delete_sync` → exit 0, 1 passed.
- `cargo clippy --locked --workspace --all-targets -- -D warnings` → exit 0 (the new test files' `.expect()` calls all pass the `unwrap_used` lint that now covers test code per TRA-9956).
- `python3 scripts/check-tx-pool.py && python3 scripts/check-tx-pool.py --self-test` → both exit 0.
- Re-ran `cargo test -p trakkt-auth` and the integration test again after the M3 mutation-and-restore to confirm the working tree came back exactly to the staged state (662 insertions / 23 deletions unchanged, `git diff --cached` byte-identical before and after).

---

### 13:22 — TRA-9957: re-review (cycle 2) — module-doc correction in `cascade_delete_sync.rs`

**Review type**: re-review (cycle 2), scoped to one file
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢 — **signed**
**Files**: `apps/server/tests/cascade_delete_sync.rs` (module doc only — the coordinator acted on this review's 🟢 finding)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No findings. Clean pass. |

**Anti-pattern tally**: none

**Notes**: Direct response to the cycle-1 🟢 (module doc overstated the `MapSink` key as "the same key" the browser sink writes under, when the real key has a third `workspace_id` component). Fixed rather than left, consistent with `docs/CODING_STANDARDS.md`'s Comment and Doc Accuracy rule that a comment making a checkable claim must be checked.

**(a) Scope — confirmed by diff, not by trusting the coordinator's description.** Wrote out the exact 22-line module doc block I read during the initial review to a scratch file and diffed it against the current file with plain `diff -u`. The only delta is the 6-line replacement of the single sentence "the same key the IndexedDB sink writes under" with the corrected 8-line explanation (lines 20-28 of the new file) — no test logic, no assertion, no fixture, no import, nothing outside that one `//!` block touched. Also confirmed via `git diff --cached --stat` that `crates/trakkt-auth/src/issue_service.rs` (155 +/23-) and `crates/trakkt-auth/src/sync_log_service.rs` (252+) are byte-for-byte the same sizes as cycle 1 — no production file changed.

**(b) Accuracy of the new claim — checked against the source, not re-asserted.** `crates/trakkt-ui/src/cache/db.rs:95` is `fn entity_key(entity_type: &str, workspace_id: &str, entity_id: &str) -> String` with body `format!("{entity_type}\x00{workspace_id}\x00{entity_id}")` — the line reference and the "3-part `entity_type\0workspace_id\0entity_id`" description both match exactly. The soundness condition ("sound only because this fixture is single-workspace, so no two rows can collide on the shortened key") holds: `cascade_delete_sync.rs` uses one `const WORKSPACE: &str = "ws_cascade"` for every seeded row in the one test in the file, so dropping the (constant, identical-valued) workspace component from the test's map key cannot create a collision the real 3-part key would have prevented. The claim is now accurate and appropriately scoped ("a multi-workspace fixture would need the third component" correctly flags this as fixture-specific, not universally sound).

**(c) Gate** — `cargo test --locked -p trakkt-server --test cascade_delete_sync` re-run directly (not trusting the coordinator's report): exit 0, 1 passed. Did not re-run `cargo check`/`clippy`/`check-tx-pool.py`/`cargo test -p trakkt-auth` — no production `.rs` file or test assertion changed since cycle 1, so those results stand unchanged.

Re-signing against the new diff hash.

---
