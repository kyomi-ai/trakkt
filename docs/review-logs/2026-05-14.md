# Review Log — 2026-05-14

## Feature: TRA-59 Phase 1 — Foundation Infrastructure for Unified API Surface Layer

### Files reviewed
- `Cargo.toml` (workspace root)
- `crates/trakkt-types/Cargo.toml`
- `crates/trakkt-types/src/lib.rs`
- `crates/trakkt-types/src/api.rs`
- `apps/server/Cargo.toml`
- `apps/server/src/lib.rs`
- `apps/server/src/api/mod.rs`
- `apps/server/src/api/context.rs`

### Reference files read
- `docs/plans/2026-05-14-unified-api-surface.md`
- `docs/CODING_STANDARDS.md`
- `apps/server/src/routes/mcp.rs`
- `apps/server/src/state.rs`
- `crates/trakkt-core/src/error.rs`

---

### Result: CHANGES REQUESTED — 2 Major issues block merge

---

### Major Issue 1: `#[allow(dead_code)]` on test struct violates CODING_STANDARDS.md

**File:** `apps/server/src/api/mod.rs`, line 166

`CODING_STANDARDS.md` explicitly prohibits `#[allow(dead_code)]`. The attribute was added to silence a compiler warning on the test-only `TestUpdateParams` struct. Remove the attribute. If the warning fires, construct the struct in the test body to satisfy the compiler:
```rust
let _ = TestUpdateParams { id: "x".into(), title: None, priority: None };
```
This makes the fields read and eliminates the warning without a lint suppression.

---

### Major Issue 2: `resolve_issue_key_and_number` missing from `context.rs`

**File:** `apps/server/src/api/context.rs`

The Phase 1 plan (task 4) explicitly requires this function:
> `resolve_issue_key_and_number()` — issue identifier parsing (extracted from current MCP helpers)

The function exists in `mcp.rs` (lines 1120–1146) and is called by 5 of the 6 issue tool handlers. Without it in `context.rs`, Phase 2 handlers will either re-implement the same resolution logic (duplication) or introduce it without a review checkpoint.

The new implementation should take typed parameters (not `&serde_json::Value`) since Phase 2 handlers receive typed API param structs:
```rust
pub fn resolve_issue_key_and_number(
    issue_identifier: Option<&str>,
    team_key: Option<&str>,
    issue_number: Option<i64>,
) -> ApiResult<(String, i32)>
```
Add at least two tests: one for the `issue_identifier` path, one for the `team_key`+`issue_number` fallback path.

---

### Minor Issues (do not block merge once major issues are fixed)

- **Minor 1:** `from_bearer` signature departs from the plan (`fn from_bearer(auth: &McpAuth, ...)`) without explanation. The departure is architecturally correct (McpAuth is private to mcp.rs), but a one-line comment explaining why would prevent future confusion.
- **Minor 2:** The plan doc shows `schemars::schema::RootSchema` (v0.8 type name) but the code correctly uses `schemars::Schema` (v1). Consider updating the plan doc to match the actual implementation.

---

### What was done well

- Type-erasure approach for `ApiOperation::handler` (`Box<dyn Fn(...) -> Pin<Box<dyn Future>>>`) is correct for a heterogeneous async registry.
- `resolve_team` in `context.rs` exactly mirrors `resolve_team_id_from_args` in `mcp.rs` with no logic drift.
- `parse_issue_identifier` in `context.rs` exactly matches `mcp.rs` logic.
- `schemars_double_option_generates_valid_schema` test is thorough and validates all four schema invariants.
- Dependency wiring (`schemars` at workspace root with `chrono04` feature) is clean.
- No surface-specific logic in any of the new types.
- `ApiCtx` borrows from `AppState` rather than cloning, per the design intent.
- `From<trakkt_core::Error>` conversion covers all semantic variants correctly.

---

## Re-review: Phase 1 fixes — 2026-05-14

### Files re-reviewed
- `apps/server/src/api/mod.rs`
- `apps/server/src/api/context.rs`

### Verification
- `cargo check -p trakkt-server` passes with zero warnings
- `cargo test -p trakkt-server` passes: 31 tests, 0 failures

---

### Issue 1 (was Major): `#[allow(dead_code)]` — RESOLVED

The attribute has been removed. The fix uses `#[derive(Debug)]` on `TestUpdateParams` and calls `let _ = format!("{params:?}")` immediately after construction. Both fields (`title`, `priority`) are now touched by the `Debug` format call, and all three fields are bound in the struct literal. The warning is gone without any lint suppression. Correct fix.

---

### Issue 2 (was Major): `resolve_issue_key_and_number` missing — RESOLVED

The function is present in `context.rs` (lines 54–77) with the exact signature requested in the review. Logic comparison against `mcp.rs` lines 1120–1146:

- `issue_identifier` path: both call `parse_issue_identifier` and map `None` to a `BadRequest` with the identical message `"Invalid issue identifier format. Expected 'TRA-35'"`. Match confirmed.
- `team_key`/`issue_number` fallback: both produce `BadRequest("Either issue_identifier or team_key+issue_number required")` when either field is absent. Match confirmed.
- `issue_number` cast to `i32`: both use `as i32`. Match confirmed.
- Identifier-takes-precedence ordering: both check `issue_identifier` first. Match confirmed.

Five tests added: `resolve_via_identifier`, `resolve_via_key_and_number`, `resolve_identifier_takes_precedence`, `resolve_neither_returns_error`, `resolve_key_without_number_returns_error`. All five cases are meaningful and cover the complete branching surface of the function.

---

### Minor Issue 1 (was Minor): `from_bearer` comment — RESOLVED

`context.rs` line 35–36 now carries the comment: "Takes raw fields rather than `&McpAuth` because `McpAuth` is intentionally private to `routes/mcp.rs`." This fully addresses the intent of the minor issue.

---

### Minor Issue 2 (was Minor): Plan doc type name discrepancy — acknowledged, not fixed

The plan doc retains the `RootSchema` type name. This is acceptable; plan docs are historical artifacts and do not need to be kept in sync with implementation details. No action required.

---

### Result: APPROVED — No issues found

All four issues from the initial review are resolved. The implementation is clean, the logic matches the MCP reference exactly, and the test suite passes with zero warnings.

**Signed.**

---

## Re-review: Phase 3 REST surface fixes — 2026-05-14

### Files re-reviewed
- `apps/server/src/api/mod.rs` — `Unauthorized` variant + `Display` impl
- `apps/server/src/routes/rest.rs` — full new file
- `apps/server/src/routes/mcp.rs` — scopes JSON fix
- `apps/server/src/routes/mod.rs` — `pub mod rest` added
- `apps/server/src/lib.rs` — `/api/v1` mount

### Verification
- `cargo clippy -p trakkt-server -- -D warnings` passes with zero warnings
- `cargo test -p trakkt-server` passes: 31 tests, 0 failures

---

### Issue 1 (was Major): Missing `Unauthorized` variant in `ApiError` — RESOLVED

`ApiError::Unauthorized(String)` is present in `api/mod.rs` line 65. The `Display` impl covers it at line 76 (`"unauthorized: {msg}"`). The `From<trakkt_core::Error>` conversion correctly does not map to `Unauthorized` — this variant is exclusively produced by the auth layer, which is the right boundary. In `rest.rs`:

- `authenticate()` (line 196) returns `ApiError::Unauthorized(...)` when `resolve_rest_auth` returns `None`. HTTP 401. Correct.
- `check_scope()` (line 209) returns `ApiError::Forbidden(...)`. HTTP 403. Correct.
- `IntoResponse` for `RestError` (line 233) maps `Unauthorized` to `StatusCode::UNAUTHORIZED`. Correct.

---

### Issue 2 (was Major): `unwrap_or_default()` on serde_json deserialization — RESOLVED

Both files now use `match serde_json::from_str::<Vec<String>>(s)` with `tracing::warn!` on the error arm and an explicit `vec![]` fallback:

- `rest.rs` lines 154–160: `match`/`tracing::warn!` pattern. Correct.
- `mcp.rs` lines 236–242: identical `match`/`tracing::warn!` pattern. Correct.

The warning log includes the structured error field (`error = %e`) so malformed token scopes are observable in production without silently degrading.

---

### REST surface completeness check (new file, first full review)

**Handler line-count invariant** (plan requires under ~15 lines each):
All six handlers are 10–15 lines. None contain business logic. Verified line-by-line.

**`authenticate()` / personal mode bypass**: Matches MCP personal-mode injection pattern exactly (`workspace-local`/`user-local` constants, empty scopes). Correct.

**`resolve_rest_auth` vs `resolve_mcp_auth` logic parity**: Compared side-by-side.
- JWT path: identical — validate token, extract `user_id` (fall back to `sub`), check user active, extract `workspace_id` (fall back to `get_user_workspace_context`). Match.
- Legacy API token path: identical — SHA-256 hash lookup, expiry check, scopes parse, workspace fallback. Match.
- Only difference: return type (`RestAuth` vs `McpAuth`) — structurally identical structs. Duplication is documented in comments as a Phase 4 target. Acceptable.

**`unwrap_or_else` on line 77**: Falls back from `claims.extra["user_id"]` to `claims.sub`. This is the RFC 7519-correct behavior (`sub` is the standard user identifier). Not a suppressed error; a documented fallback for an absent optional enrichment claim. Same pattern in `mcp.rs` line 152. Not an issue.

**`expect` calls in `api/mod.rs`**: All inside `#[cfg(test)]` blocks. Panicking on invariant failures in tests is correct.

**Anti-pattern scan — rest.rs**: No `#[allow(...)]`, no unguarded `unwrap()`, no hardcoded credentials, no business logic in handlers, no copy-paste duplication beyond the documented Phase 4 `RestAuth`/`McpAuth` consolidation.

**Router registration** (`lib.rs` line 81): `.nest("/api/v1", routes::rest::rest_router())` — correct mount point. Ordering places it after the `/api/v1/auth` prefix, which is correct (more-specific prefix wins in Axum).

**`/issues/search` ordering**: Registered as a separate route before `/{identifier}` in `rest_router()`. In Axum, static segments beat path parameters at the same prefix, so `GET /issues/search` correctly never falls through to `get_issue_handler`. Correct.

---

### Result: APPROVED — No issues found

Both original major issues are properly resolved. The full Phase 3 REST surface is thin, correct, and logically equivalent to the MCP surface it parallels. No new issues were introduced by the fixes.

**Signed.**

---

## Feature: TRA-59 Phase 2 — Issues API Params + Handlers for the Unified API Surface Layer

### Files reviewed
- `crates/trakkt-types/src/api.rs` — 6 API param structs
- `apps/server/src/api/issues.rs` — 6 handler functions + `operations()` registration
- `apps/server/src/api/mod.rs` — module wiring

### Reference files read
- `apps/server/src/routes/mcp.rs` (lines 1169–1523, relevant tool functions)
- `apps/server/src/api/context.rs` — `resolve_team`, `resolve_issue_key_and_number`
- `crates/trakkt-types/src/models.rs` — `IssueFilters`, `CreateIssueParams`, `IssueUpdate`
- `apps/server/src/api/mod.rs` — `ApiCtx`, `ApiError`, `ApiResult`, `ApiOperation`, `DynHandler`

---

### Result: APPROVED — No issues found

All six handlers were compared line-by-line against their MCP source functions. Logic is complete and correct. No issues found.

---

### Line-by-line comparison results

**list_issues** (MCP lines 1170–1208 vs `api/issues.rs` lines 29–82):
- `limit` default 50, clamp 1–100: match.
- `status_category` comma splitting with `.trim()` and empty-filter: match.
- `include_closed` default false: match.
- `exclude_status_categories` logic (only when `status_id.is_none() && status_categories.is_none() && !include_closed`): match.
- `priority` as `i32`: match (typed param already `Option<i32>`).
- `assignee` mapped to `assignee_id`: match.
- `label` comma splitting: match.
- Team resolution via `resolve_team` vs `resolve_team_id_from_args`: logic-equivalent; `team_id` takes precedence over `team_key` in both.

**get_issue** (MCP lines 1211–1229 vs lines 87–108):
- Identifier resolution via `resolve_issue_key_and_number`: match.
- `get_issue` service call with workspace scope: match.
- `list_comments` call on `issue.issue_id`: match.
- Return `{"issue": ..., "comments": ...}` JSON: match.

**create_issue** (MCP lines 1232–1289 vs lines 113–168):
- Default-team fallback when `resolve_team` returns `None`: match.
- `labels` field: MCP extracts from JSON array, handler uses `params.labels.unwrap_or_default()` on `Option<Vec<String>>` — semantically equivalent (empty vec in both cases when absent).
- `parent_issue_id` extracted before `CreateIssueParams` construction: match.
- `priority` default 0: match (`params.priority.unwrap_or(0)`).
- `parent` relation set via `relation_service::set_parent` after issue creation: match.
- `ws_manager` passed to both `create_issue` and `set_parent`: match.

**update_issue** (MCP lines 1313–1406 vs lines 173–274):
- Identifier resolution: match.
- Move-team resolution uses `move_to_team_key`/`move_to_team_id` (NOT `team_key`/`team_id`): match. `resolve_team` parameter order `(team_key, team_id)` preserves `team_id`-first priority, matching `resolve_move_team_id` behavior.
- `IssueUpdate` field mapping — all double-Option fields (`description`, `assignee_id`, `due_date`, `project_id`, `milestone_id`, `sort_order`) correctly use typed `Option<Option<T>>` params: match.
- `parent_issue_id` clear/set logic: MCP uses `is_null() || as_str().is_some_and(|s| s.is_empty())` on raw JSON; handler uses typed `match` on `Option<Option<String>>`. Semantics are identical (null → `None` inner, empty string → `Some("")`, value → `Some(id)`).
- Labels replacement via `set_issue_labels` only when `labels` field is present: match.
- Re-fetch with `get_issue_by_id` after labels update: match.

**delete_issue** (MCP lines 1409–1426 vs lines 279–299):
- Identifier resolution: match.
- `delete_issue` service call with workspace + ws_manager: match.
- Return format difference: MCP returns a plain string `"Issue TRA-35 deleted"`, handler returns `{"message": "Issue TRA-35 deleted"}` JSON. This is an intentional and correct adaptation — the unified API surface returns `serde_json::Value` from all handlers, and wrapping in a JSON object is the correct approach for the REST surface. Not a defect.

**search_issues** (MCP lines 1496–1523 vs lines 304–338):
- `limit` default 20 (not 50): match.
- Clamp 1–100: match.
- `include_closed` default false: match.
- Team resolution: match.
- `exclude_status_categories` logic: only gated on `!include_closed` (no `status_id`/`status_categories` check, matching MCP): match.
- `IssueFilters` with `..Default::default()`: match.

### Operation registration verification

All 6 operations registered in `operations()`:

| Operation | Name | Scope | Method | Path |
|---|---|---|---|---|
| list_issues | `list_issues` | `issues:read` | GET | `/issues` |
| search_issues | `search_issues` | `issues:read` | GET | `/issues/search` |
| get_issue | `get_issue` | `issues:read` | GET | `/issues/{identifier}` |
| create_issue | `create_issue` | `issues:write` | POST | `/issues` |
| update_issue | `update_issue` | `issues:write` | PATCH | `/issues/{identifier}` |
| delete_issue | `delete_issue` | `issues:write` | DELETE | `/issues/{identifier}` |

Scopes match MCP scope enforcement table (lines 1028–1029) exactly.

### Anti-pattern scan results

- No lint suppressions (`#[allow(...)]`).
- `unwrap_or_default()` on line 133 is on `Option<Vec<String>>` — produces `vec![]` when absent, which is correct behavior matching MCP. Not a suppression of an error.
- No fallback code that masks integration issues.
- No hardcoded credentials or values.
- No copy-paste duplication.
- Handler wrappers correctly deserialize `serde_json::Value` to typed params before calling the handler function.
- `all_operations_includes_issue_ops` test verifies all 6 operations are registered.

### What was done well

- Typed param structs eliminate stringly-typed field access while preserving identical field names for JSON schema compatibility.
- Double-Option on `UpdateIssueApiParams` fields correctly represents the three-state update semantics from the design.
- `resolve_team` reuse for both normal team and move-team resolution avoids introducing a separate helper.
- `operations()` registration is comprehensive and the handler closures correctly deserialize then dispatch.
- The test suite in `mod.rs` already asserts all 6 operations are present and validates the double-Option schema generation.

**Signed.**

---

## Feature: TRA-59 Phase 4 — MCP Migration (Issues)

### Files reviewed
- `apps/server/src/routes/mcp.rs` (1872 → 1435 lines, -437 lines)

### Reference files read
- `apps/server/src/api/mod.rs` — `ApiCtx`, `ApiError`, `all_operations()`
- `apps/server/src/api/issues.rs` — shared handlers and `operations()`
- `crates/trakkt-core/src/error.rs` — `trakkt_core::Error` variants
- `docs/CODING_STANDARDS.md`

---

### Deleted functions verification

Grep confirmed: `tool_list_issues`, `tool_get_issue`, `tool_create_issue`, `tool_update_issue`, `tool_delete_issue`, `tool_search_issues`, and `resolve_move_team_id` — all absent. Deletion complete.

### Kept helpers verification

All four required helpers present at their expected lines:
- `arg_str` — line 955
- `parse_issue_identifier` — line 962
- `resolve_issue_key_and_number` — line 972
- `resolve_team_id_from_args` — line 1002

---

### Result: CHANGES REQUESTED — 1 Major issue blocks merge

---

### Major Issue: `unwrap_or_default()` on schema serialization violates CODING_STANDARDS.md

**File:** `apps/server/src/routes/mcp.rs`, line 424

```rust
let schema_value = serde_json::to_value(&schema).unwrap_or_default();
```

`CODING_STANDARDS.md` is unambiguous: "Never use `unwrap_or_default()` on deserialization (JSON parse, filter decode, etc.) — use `match` and log the error with `tracing::warn!`." This code uses `unwrap_or_default()` on a `serde_json::to_value` call, which is a serialization operation that can fail. Silently falling back to `Value::Null` (the default) would return a tool entry with a null schema to MCP clients, which breaks tool introspection in a way that is invisible in logs and hard to diagnose.

In practice, `schemars::Schema` is backed by JSON-compatible types and the `to_value` call is very unlikely to fail. But the standard exists precisely to enforce observable failure paths — the cost of adding a `match` is zero and the observability gain is real.

Required fix:

```rust
for op in crate::api::all_operations() {
    let schema = (op.json_schema)();
    let schema_value = match serde_json::to_value(&schema) {
        Ok(v) => v,
        Err(e) => {
            tracing::warn!(tool = %op.name, error = %e, "failed to serialize tool schema");
            continue;
        }
    };
    tools.push(json!({
        "name": op.name,
        "description": op.description,
        "inputSchema": schema_value,
    }));
}
```

Using `continue` skips the malformed entry rather than inserting a null schema, which is a more correct degradation (a missing tool is less harmful than a tool with a broken schema).

---

### Minor Issues (do not block merge once major issue is fixed)

**Minor 1 — `all_operations()` called three times per tool call**

`is_registry_tool`, `registry_tool_scope`, and `dispatch_registry_tool` each call `crate::api::all_operations()` independently. In a single `tools/call` request that routes through the registry, this allocates and discards the same `Vec<ApiOperation>` three times. Currently the registry has 6 operations so the cost is small, but the pattern scales poorly as more operations are migrated.

No change required now (the spec note flags this as "minor concern for now"), but worth documenting as a Phase 5 target: call `all_operations()` once at the top of `handle_tools_call`, pass the slice down, and eliminate the duplicate calls.

**Minor 2 — `dispatch_registry_tool` "Unknown registry tool" error is unreachable in practice**

Line 929 returns `trakkt_core::Error::BadRequest("Unknown registry tool: {name}")` if the op is not found, but `dispatch_registry_tool` is only called when `is_registry_tool(name)` returned true (line 848), which already confirmed the name exists in the registry. The error guard is correct as a defensive measure, but the `BadRequest` variant is misleading — a missing op at this point indicates a logic bug, not a bad client request. Consider `trakkt_core::Error::Internal` for accuracy. Not a blocking issue.

---

### Verified correct

- **Error code mapping**: The explicit `ApiError` → `trakkt_core::Error` match in `dispatch_registry_tool` (lines 938–945) covers all six variants including `Unauthorized` → `Forbidden` (→ -32001). Complete.
- **Final error mapper** (lines 879–888): `NotFound`/`BadRequest`/`Conflict` → -32602, `Forbidden` → -32001, all others → -32603. Matches the old per-tool behavior.
- **Scope enforcement**: `registry_tool_scope` returns the scope from `ApiOperation::scope` for registry tools; the static `match` in `handle_tools_call` handles the 17 remaining hand-written tools. Coverage is complete — all named tools have a non-empty scope, and an unrecognized name returns `""` (no scope check, then falls through to the `_ => Unknown tool` error in the dispatch match). Correct.
- **MCP response format**: `Ok(content)` path wraps in `{"content": [{"type": "text", "text": content}]}`. Matches old format.
- **Schema generation**: `schemars::schema_for!(T)` per operation correctly generates MCP-compatible JSON Schema objects. The `inputSchema` key name matches the MCP tools/list spec.
- **`cargo check -p trakkt-server`**: passes with zero errors and zero warnings (excluding unrelated kode-leptos patch warning).
- **6 operations registered**: `list_issues`, `search_issues`, `get_issue`, `create_issue`, `update_issue`, `delete_issue`. Matches the 6 deleted tool functions exactly.

---

### What was done well

- The `dispatch_registry_tool` error mapping is explicit and complete — all six `ApiError` variants are enumerated, no catch-all that could silently swallow 401s.
- The scope enforcement split (registry path via `ApiOperation::scope`, hand-written path via static `match`) is clean and extensible.
- The `is_registry_tool` check before dispatch keeps the hand-written and registry-driven paths clearly separated.
- Comment quality is high throughout the new functions — the "why" is explained, not just the "what".
- The line count reduction (437 lines removed) is substantial with no loss of functionality.

---

## Re-review: Phase 4 MCP migration fix — 2026-05-14

### Scope
Single fix: `apps/server/src/routes/mcp.rs` lines 422–436 (schema serialization loop).

### Verification
- `cargo clippy -p trakkt-server -- -D warnings` passes with zero warnings
- `cargo test -p trakkt-server` passes: 31 tests, 0 failures

---

### Major Issue (was Major): `unwrap_or_default()` on schema serialization — RESOLVED

Lines 424–430 now read:

```rust
let schema_value = match serde_json::to_value(&schema) {
    Ok(v) => v,
    Err(e) => {
        tracing::warn!(tool = %op.name, error = %e, "failed to serialize tool schema");
        continue;
    }
};
```

This is exactly the required fix. The error arm logs a structured warning with both the tool name and the error, then `continue`s to skip the malformed entry rather than inserting a null schema into the tool list. The `continue` path is strictly better than the previous `unwrap_or_default()` behavior: a missing tool is less harmful than a tool with a broken schema, and the failure is now visible in logs.

---

### Result: APPROVED — No issues found

The single blocking issue is resolved. No regressions introduced.

**Signed.**

---

## Feature: TRA-59 Phase 5 — Remaining Modules (Params + Handlers for Comments, Labels, Teams, Statuses, Relations, Projects, Milestones)

### Files reviewed
- `crates/trakkt-types/src/api.rs` — 17 new param structs (AddComment, ListLabels, CreateLabel, ListTeams, ListStatuses, AddRelation, RemoveRelation, ListRelations, ListProjects, GetProject, CreateProject, UpdateProject, DeleteProject, ListMilestones, CreateMilestone, UpdateMilestone, DeleteMilestone)
- `apps/server/src/api/comments.rs` — 1 handler + `operations()`
- `apps/server/src/api/labels.rs` — 2 handlers + `operations()`
- `apps/server/src/api/teams.rs` — 1 handler + `operations()`
- `apps/server/src/api/statuses.rs` — 1 handler + `operations()`
- `apps/server/src/api/relations.rs` — 3 handlers + `operations()`
- `apps/server/src/api/projects.rs` — 5 handlers + `verify_project_ownership` helper + `operations()`
- `apps/server/src/api/milestones.rs` — 4 handlers + `resolve_milestone_project_id` helper + `operations()`
- `apps/server/src/api/mod.rs` — 7 module declarations + `all_operations()` wiring + test

### Reference files read
- `apps/server/src/routes/mcp.rs` (all 17 source tool functions + 2 helpers)
- `docs/CODING_STANDARDS.md`

---

### Result: CHANGES REQUESTED — 1 Critical issue blocks merge

---

### Critical Issue: `handle_tools_list` in `mcp.rs` emits duplicate tool entries for all 17 migrated operations

**File:** `apps/server/src/routes/mcp.rs`, lines 438–774

The `handle_tools_list` function builds the MCP `tools/list` response from two sources:
1. A loop over `crate::api::all_operations()` (lines 421–436) — now includes all 23 operations (6 issues + 17 new).
2. A hardcoded block (lines 438–774) that still manually pushes all 17 new tool entries.

Because Phase 5 migrated all 17 tools into `all_operations()`, every one of them is now emitted **twice** by `handle_tools_list`: once from the registry loop, and once from the leftover hand-written block. An MCP client receiving a `tools/list` response with duplicate tool names either renders them twice in its UI or treats the response as a protocol violation. This is a functional breakage of the MCP surface.

The hand-written `tool_*` functions in `mcp.rs`, the 17-entry scope static match table (lines 831–842), and the 17 `tool_*` match arms in `handle_tools_call` (lines 858–874) are also now dead code — `is_registry_tool` returns `true` for all 17 names so the `else` branch is never reached. These must all be removed to eliminate dead code and compiler warnings.

Required fix:

1. Remove the hand-written block from `handle_tools_list` (lines 438–774 of current `mcp.rs`), leaving only the registry loop.
2. Remove the 17 hand-written `tool_*` function bodies (tool_add_comment, tool_list_labels, tool_create_label, tool_list_statuses, tool_list_teams, tool_add_relation, tool_remove_relation, tool_list_issue_relations, tool_list_projects, tool_get_project, tool_create_project, tool_update_project, tool_delete_project, tool_list_milestones, tool_create_milestone, tool_update_milestone, tool_delete_milestone).
3. Remove the static scope match entries for these 17 tools (lines 831–840), since `registry_tool_scope` now handles them all.
4. Remove the 17 match arms in the `else` dispatch block (lines 858–874), leaving only the `_ => Unknown tool` fallback.

After cleanup, `handle_tools_list` should look like:

```rust
fn handle_tools_list(id: Option<Value>) -> JsonRpcResponse {
    let mut tools = Vec::new();

    for op in crate::api::all_operations() {
        let schema = (op.json_schema)();
        let schema_value = match serde_json::to_value(&schema) {
            Ok(v) => v,
            Err(e) => {
                tracing::warn!(tool = %op.name, error = %e, "failed to serialize tool schema");
                continue;
            }
        };
        tools.push(json!({
            "name": op.name,
            "description": op.description,
            "inputSchema": schema_value,
        }));
    }

    JsonRpcResponse::success(id, json!({ "tools": tools }))
}
```

---

### Line-by-line handler comparison results

All 17 handlers verified against their MCP source functions:

**comments::add_comment** vs tool_add_comment (mcp.rs 1027–1051): Issue resolution, `get_issue` lookup, `comment_service::create_comment` call — match. Handler additionally passes `params.parent_id.as_deref()` where MCP hardcodes `None`. Intentional enhancement; the registry-generated schema will expose the `parent_id` field once the hand-written schema block is removed.

**labels::list_labels** vs tool_list_labels (mcp.rs 1054–1061): `label_service::list_labels` with workspace_id — match.

**labels::create_label** vs tool_create_label (mcp.rs 1072–1092): Handler resolves `team_key`/`team_id` via `resolve_team` where MCP hardcodes `None`. Intentional enhancement. `name`, `color`, `ws_manager` — match.

**teams::list_teams** vs tool_list_teams (mcp.rs 1063–1069): `team_service::list_teams` with workspace_id and `Some(&user_id)` — match.

**statuses::list_statuses** vs tool_list_statuses (mcp.rs 1094–1103): `resolve_team` logic equivalent to `resolve_team_id_from_args`. `status_service::list_statuses` — match.

**relations::add_relation** vs tool_add_relation (mcp.rs 1105–1149): Both source AND target resolved from identifiers to issue IDs. Error messages match. `relation_service::create_relation` with all args — match.

**relations::remove_relation** vs tool_remove_relation (mcp.rs 1151–1168): `relation_service::delete_relation` — match. Return adapted to JSON object: correct.

**relations::list_relations** vs tool_list_issue_relations (mcp.rs 1170–1192): Issue resolution, `relation_service::list_relations_for_issue` — match.

**projects::verify_project_ownership** vs mcp.rs helper (1196–1209): get_project → NotFound if absent, workspace_id equality check → NotFound (intentionally masked) — match.

**milestones::resolve_milestone_project_id** vs mcp.rs helper (1211–1228): `db_fetch_optional!` on `project_milestones WHERE milestone_id = $1`, returns project_id or NotFound — match.

**projects::list_projects** vs tool_list_projects (1230–1237): match.

**projects::get_project** vs tool_get_project (1239–1250): ownership check, `list_milestones`, return `{"project":…,"milestones":…}` — match.

**projects::create_project** vs tool_create_project (1252–1279): `CreateProjectParams` all 8 fields — match.

**projects::update_project** vs tool_update_project (1281–1328): ownership check, double-Option handling for `lead_id`/`start_date`/`target_date` using `.as_ref().map(|opt| opt.as_deref())` — match.

**projects::delete_project** vs tool_delete_project (1330–1347): ownership check, `delete_project`, return JSON message — match.

**milestones::list_milestones** vs tool_list_milestones (1349–1360): ownership check, `list_milestones` — match.

**milestones::create_milestone** vs tool_create_milestone (1362–1385): ownership check, `create_milestone` with all args — match.

**milestones::update_milestone** vs tool_update_milestone (1387–1418): `resolve_milestone_project_id` + ownership check, double-Option for `target_date` — match.

**milestones::delete_milestone** vs tool_delete_milestone (1420–1441): `resolve_milestone_project_id` + ownership check, `delete_milestone` — match.

---

### Operation registration verification

All 17 scopes match the MCP static scope table (mcp.rs lines 831–840). All operation names match tool names in `handle_tools_list`. Total: 6 (issues) + 17 (this phase) = 23. Confirmed by `all_operations_includes_all_ops` test which enumerates all 23 names individually and asserts `ops.len() == 23`.

---

### Anti-pattern scan results

No `#[allow(...)]` suppressions, no `unwrap_or_default()` on deserialization, no unguarded `unwrap()` in production paths, no hardcoded credentials, no copy-paste duplication. Handler wrappers propagate deserialization errors via `?` through `From<serde_json::Error>` → `ApiError::BadRequest`. No fallback code.

---

### What was done well

- All 17 handlers are correct line-by-line ports with no logic drift.
- Double-Option semantics for `UpdateProjectApiParams` and `UpdateMilestoneApiParams` are implemented identically to the MCP source.
- `add_relation` correctly resolves BOTH source and target identifiers — the most complex handler is correct.
- `verify_project_ownership` is factored into `projects.rs` and imported by `milestones.rs` — no duplication.
- `resolve_milestone_project_id` is private to the milestones module — correctly scoped.
- `all_operations_includes_all_ops` test enumerates all 23 names individually and asserts exact count.

---

## Re-review: Phase 5 cleanup (duplicate tools fix) — 2026-05-14

### Files reviewed
- `apps/server/src/routes/mcp.rs` — cleaned-up file (562 lines, was 1442)
- `apps/server/src/api/comments.rs`
- `apps/server/src/api/labels.rs`
- `apps/server/src/api/teams.rs`
- `apps/server/src/api/statuses.rs`
- `apps/server/src/api/relations.rs`
- `apps/server/src/api/projects.rs`
- `apps/server/src/api/milestones.rs`
- `crates/trakkt-types/src/api.rs`
- `apps/server/src/api/mod.rs`

### Verification
- `cargo clippy -p trakkt-server -- -D warnings` passes with zero warnings
- `cargo test -p trakkt-server` passes: 31 tests, 0 failures

---

### Critical Issue (was Critical): Duplicate tool entries in `handle_tools_list` — RESOLVED

`handle_tools_list` now contains only the registry loop (lines 413–433). No hand-written tool entry blocks remain. All 23 operations are emitted exactly once.

Confirmed via grep: zero `tool_add_comment`, `tool_list_issues`, `tool_get_issue`, and all other `tool_*` function names in `mcp.rs`. The hand-written function bodies are gone. The static scope match table is gone. The split dispatch block is gone. `is_registry_tool` is gone.

---

### Deleted code verification

**All 17 `tool_*` functions**: Absent. Confirmed.

**`arg_str`, `parse_issue_identifier`, `resolve_issue_key_and_number`, `resolve_team_id_from_args`, `verify_project_ownership`, `resolve_milestone_project_id` helpers**: All absent from `mcp.rs`. These were correctly moved into `api/context.rs` and `api/projects.rs` / `api/milestones.rs` in prior phases and are now fully cleaned up.

**`is_registry_tool`**: Absent. Confirmed. No longer needed now that all tools go through the registry.

**Unused imports**: Zero. Clippy confirms.

---

### mcp.rs contains zero business logic — verified

The 562-line file consists exclusively of:
- JSON-RPC types (`JsonRpcRequest`, `JsonRpcResponse`, `JsonRpcError`)
- Auth structs (`McpAuth`, `ApiTokenLookupRow`)
- Auth resolution functions (`resolve_mcp_auth`, `authenticate_bearer_token`)
- WWW-Authenticate middleware
- Route registration
- Method dispatch (`handle_post`, `handle_sse`, `handle_delete`, `handle_initialize`, `handle_tools_list`, `handle_resources_list`, `handle_tools_call`)
- Registry helpers (`registry_tool_scope`, `dispatch_registry_tool`)

No database queries for issue/label/project/milestone data. No service layer calls except auth lookups. Correct.

---

### Scope enforcement — complete

`registry_tool_scope` is now the sole scope lookup path for all 23 operations (lines 522–527). The static scope match table that previously existed alongside it is gone. `has_scope` semantics: empty scopes vector means full access (JWT users), non-empty means restricted to listed scopes. Correct.

---

### Minor Issues (pre-existing, not introduced by this change)

**Minor 1 — `all_operations()` called twice per `tools/call` request**

`registry_tool_scope` (line 523) and `dispatch_registry_tool` (line 539) each call `crate::api::all_operations()`, allocating and discarding a `Vec<ApiOperation>` twice for every `tools/call`. This was flagged as Minor 1 in the Phase 4 review. Still present; still not a blocker. Future improvement: call `all_operations()` once at the top of `handle_tools_call` and pass the result into both helpers.

**Minor 2 — Double `resolve_mcp_auth` call for `initialize` requests**

`handle_post` calls `resolve_mcp_auth` at line 335 (gate check) and again at line 354 (to extract `workspace_id` for session creation). For an authenticated `initialize` request, this is two full DB roundtrips. Pre-existing from Phase 4 commit `1d11576`. Not introduced by this change. Fix: capture the `Option<McpAuth>` from the gate check and thread it down to the session creation block.

**Minor 3 — No duplicate-names assertion in the registry test**

`all_operations_includes_all_ops` verifies presence by name and asserts total count (23), which catches accidental omissions. It does not assert that all names are unique. A module accidentally registering the same op name twice would pass the length check if another op was dropped. A `HashSet` dedup assertion would close this gap. Not a blocker.

---

### Result: APPROVED — No critical or major issues found

The CRITICAL issue from the Phase 5 review is resolved. `mcp.rs` is clean: no duplicate tool entries, no dead code, no hand-written schemas, no residual `tool_*` functions. All 23 operations are served exclusively from the registry. The cleanup is complete and correct.

**Signed.**

---

## Feature: TRA-59 Phase 6 — REST + MCP Migration (Remaining)

### Files reviewed
- `apps/server/src/routes/rest.rs` — 17 new handler functions + expanded router (all 23 endpoints)
- `crates/trakkt-types/src/api.rs` — 4 fields changed from `String` to `Option<String>`
- `apps/server/src/api/relations.rs` — `add_relation` handler unwraps `source_issue` Option
- `apps/server/src/api/projects.rs` — `update_project` handler unwraps `project_id` Option
- `apps/server/src/api/milestones.rs` — `create_milestone` and `update_milestone` handlers unwrap their Option fields
- `apps/server/src/routes/mcp.rs` — cleanup: removed stale comment block in `handle_tools_list` and one leftover import

### Verification
- `cargo check -p trakkt-server` passes with zero warnings
- `cargo test -p trakkt-server` passes: 31 tests, 0 failures

---

### Result: APPROVED — No issues found

---

### Endpoint completeness (23 total)

All 23 endpoints are present with correct HTTP methods and URL paths per the Phase 6 plan:

| Group | Count | Methods+Paths |
|---|---|---|
| Issues | 6 | GET /issues, GET /issues/search, GET+PATCH+DELETE /issues/{identifier}, POST /issues |
| Comments | 1 | POST /issues/{identifier}/comments |
| Relations | 3 | GET+POST /issues/{identifier}/relations, DELETE /relations/{id} |
| Labels | 2 | GET+POST /labels |
| Teams | 1 | GET /teams |
| Statuses | 1 | GET /statuses |
| Projects | 5 | GET+POST /projects, GET+PATCH+DELETE /projects/{id} |
| Milestones | 4 | GET+POST /projects/{id}/milestones, PATCH+DELETE /milestones/{id} |

Count confirmed: 23.

---

### Handler line-count invariant

All 17 new handlers are 10–12 lines (plan requires under ~15). No handler contains business logic. All follow the identical pattern: authenticate → check_scope → build ApiCtx → inject path param → call shared handler → return JSON.

---

### Path param injection correctness

The four `Option<String>` fields are correctly injected in all handlers:

- `AddRelationApiParams.source_issue` — `add_relation_handler` sets `params.source_issue = Some(identifier)` before calling the shared handler. `relations::add_relation` unwraps with `as_deref().ok_or_else(|| ApiError::BadRequest(...))`. Correct.
- `UpdateProjectApiParams.project_id` — `update_project_handler` sets `params.project_id = Some(id)`. `projects::update_project` unwraps with `ok_or_else`. Correct.
- `CreateMilestoneApiParams.project_id` — `create_milestone_handler` sets `params.project_id = Some(id)`. `milestones::create_milestone` unwraps with `ok_or_else`. Correct.
- `UpdateMilestoneApiParams.milestone_id` — `update_milestone_handler` sets `params.milestone_id = Some(id)`. `milestones::update_milestone` unwraps with `ok_or_else`. Correct.

All four unwraps produce `400 Bad Request` via the `BadRequest` variant. Path param always overwrites any body-supplied value — this is intentional and correct (REST path is authoritative).

---

### POST endpoints return 201

`add_comment_handler`, `add_relation_handler`, `create_project_handler`, `create_milestone_handler`, and `create_label_handler` all return `(StatusCode::CREATED, Json(result))`. Confirmed. The remaining POST endpoint (`create_issue_handler`) was already verified in Phase 3. All POST handlers that create resources return 201.

---

### Scope consistency

REST handler `check_scope` calls were compared against `ApiOperation::scope` fields one-to-one:

| Handler | REST check_scope | ApiOperation::scope |
|---|---|---|
| add_comment | `comments:write` | `comments:write` |
| list_labels | `labels:read` | `labels:read` |
| create_label | `labels:write` | `labels:write` |
| list_teams | `teams:read` | `teams:read` |
| list_statuses | `issues:read` | `issues:read` |
| add_relation | `issues:write` | `issues:write` |
| list_relations | `issues:read` | `issues:read` |
| remove_relation | `issues:write` | `issues:write` |
| list_projects | `projects:read` | `projects:read` |
| get_project | `projects:read` | `projects:read` |
| create_project | `projects:write` | `projects:write` |
| update_project | `projects:write` | `projects:write` |
| delete_project | `projects:write` | `projects:write` |
| list_milestones | `projects:read` | `projects:read` |
| create_milestone | `projects:write` | `projects:write` |
| update_milestone | `projects:write` | `projects:write` |
| delete_milestone | `projects:write` | `projects:write` |

All 17 new handlers: consistent. Zero scope mismatches.

---

### No Axum route conflicts

The router uses consistent param names within each path prefix (`{identifier}` for issue sub-resources, `{id}` for all others). Static segments (`/issues/search`) precede parameterized segments (`/issues/{identifier}`) in registration order, so Axum's static-beats-dynamic routing rule correctly prevents `/search` from matching `get_issue_handler`. No conflicts.

---

### mcp.rs cleanup

The diff removes a stale comment block that described the two-source approach in `handle_tools_list` (pre-Phase 5 text describing hand-written tools) and removes the now-unused import of seven service modules (`comment_service`, `issue_service`, `label_service`, `project_service`, `relation_service`, `status_service`, `team_service`). No new content was added. `mcp.rs` remains at 562 lines with zero business logic.

---

### Anti-pattern scan

- No `#[allow(...)]` suppressions.
- No unguarded `unwrap()` or `unwrap_or_default()` in production paths.
- No fallback code masking integration failures.
- No hardcoded credentials or URLs.
- No copy-paste beyond the documented Phase 4-target `RestAuth`/`McpAuth` duplication (pre-existing, not introduced here).
- No business logic in any handler body.

---

### Pre-existing minor items (not introduced by Phase 6)

The three minor items flagged in the Phase 5 re-review carry forward unchanged:

- **Minor 1**: `all_operations()` called twice per `tools/call` (in `registry_tool_scope` and `dispatch_registry_tool`).
- **Minor 2**: `handle_post` calls `resolve_mcp_auth` twice for `initialize` requests.
- **Minor 3**: No duplicate-name assertion in `all_operations_includes_all_ops` test.

Additionally, the four "Phase 4 will extract a shared auth module" comments in `rest.rs` (lines 32, 37, 53, 63) are now factually stale — Phase 4 executed as MCP issues migration, not auth extraction. These comments were accepted and signed in the Phase 3 review; they are not introduced by Phase 6. Worth updating in a future cleanup pass.

None of these block the Phase 6 change.

---

### What was done well

- All 17 handler bodies are correctly thin and structurally identical to the Phase 3 issue handlers — consistent pattern across all 23 endpoints.
- The path-param injection pattern (inject into `mut params` before calling the shared handler) is applied uniformly across all four Option fields.
- No scope was invented or changed — all 17 new scopes match what the MCP surface had been enforcing.
- The mcp.rs cleanup is minimal and correct: only stale comments and the now-unused import block were removed.
- `cargo check` and `cargo test` pass cleanly with zero warnings.

**Signed.**

---

## Feature: TRA-59 Phase 7 — Extract `apps/server/src/api/` into `crates/trakkt-api`

### Files reviewed
- `Cargo.toml` (workspace) — new member + workspace path entry
- `apps/server/Cargo.toml` — `trakkt-api` dependency added
- `apps/server/src/lib.rs` — `pub mod api` removed
- `apps/server/src/routes/mcp.rs` — 8 reference sites updated (`crate::api::` → `trakkt_api::`)
- `apps/server/src/routes/rest.rs` — 1 import line updated
- `crates/trakkt-api/Cargo.toml` — new crate manifest
- `crates/trakkt-api/src/lib.rs` — moved from `apps/server/src/api/mod.rs`; `from_leptos` constructor added
- `crates/trakkt-api/src/context.rs` + 8 handler files — moved, `use super::` → `use crate::`

### Reference files read
- Prior review log entries (all 6 phases above)
- `crates/trakkt-auth/src/relation_service.rs` — `delete_relation` signature
- `apps/server/src/routes/mcp.rs` — triple `all_operations()` call sites

---

### Result: APPROVED — No critical or major issues found

---

### Structural correctness

**Move is clean and complete.** All 10 files are accounted for. `pub mod api` is removed from `apps/server/src/lib.rs`. Every `crate::api::` reference in `mcp.rs` (8 occurrences) and the single import in `rest.rs` are updated to `trakkt_api::`. No stranded `use crate::api` references exist in the server crate. Verified by inspecting both diff sides.

**`use super::` → `use crate::` migration.** All intra-module imports in the moved files now reference `crate::` correctly for the new crate root. No `super::` residue observed.

**Workspace wiring.** `crates/trakkt-api` is added as a workspace member and to the `[workspace.dependencies]` table. `apps/server/Cargo.toml` lists it as a workspace dependency. `trakkt-ui` does not yet depend on it (the Leptos server function integration is deferred to a future phase, consistent with the task spec). Correct.

**`Cargo.toml` — minimal and correct.** The new crate lists exactly the dependencies its source files use: `trakkt-core`, `trakkt-auth`, `trakkt-types`, `axum`, `schemars`, `serde_json`, `sqlx`, `tracing`. `serde` itself is not listed directly — `serde_json` is sufficient since `trakkt-api` never derives `Serialize`/`Deserialize` on its own types; it delegates that to `trakkt-types`. Workspace lints are inherited. Correct.

**`ws_manager: Option<&'a WebSocketManager>` change.** The structural change is sound. `from_bearer` wraps in `Some(ws_manager)` so all existing MCP/REST callers are unchanged. `from_leptos` accepts `Option` directly. Every handler call site that used `ctx.ws_manager` previously (where the type was `&WebSocketManager`) now passes `ctx.ws_manager` where the service expects `Option<&WebSocketManager>` — confirmed present in `issue_service`, `relation_service`, `project_service`, `label_service`, `comment_service` all accept `Option<&WebSocketManager>`. No handler wraps it in `Some()` needlessly; the `Option` flows through transparently. Correct.

**`relation_service::delete_relation` workspace scoping.** The `remove_relation` handler passes `&ctx.workspace_id` as the second argument. The underlying SQL is `WHERE relation_id = $1 AND workspace_id = $2`. Cross-workspace deletion is prevented at the database level. Correct.

---

### Carried minor items — status check

Three minor items were open from the Phase 5 re-review (Minor 1–3) and one from Phase 6. This change does not address them (none are in scope for a pure move refactor) and does not worsen any of them:

- **Minor 1** (`all_operations()` called twice per `tools/call`): Still present in `mcp.rs` lines 523 and 539. Unchanged by this diff.
- **Minor 2** (`resolve_mcp_auth` called twice for `initialize`): Still present at lines 335 and 354. Unchanged.
- **Minor 3** (no duplicate-name assertion in registry test): Still present. Unchanged.
- **Phase 6 Minor** (stale "Phase 4 will extract auth" comments in `rest.rs`): Unchanged.

None are introduced or worsened by this change. All remain non-blocking.

---

### New observations

**`from_leptos` is currently unused.** No caller exists yet in `trakkt-ui` or anywhere else. This is expected — the constructor is forward infrastructure for Leptos server functions. No issue; dead code in a library crate intended for a future consumer is appropriate, and `cargo clippy` passes cleanly which means the compiler is not flagging it as unused (it won't for `pub` items).

**`context` module items not re-exported at crate root.** `parse_issue_identifier`, `resolve_team`, and `resolve_issue_key_and_number` are only accessible via `trakkt_api::context::*`. This is acceptable for internal use by the handler modules, which import them via `crate::context::`. External callers (future Leptos server functions) would need to use the full path. If these functions become part of the public API surface, a `pub use context::{parse_issue_identifier, resolve_team, resolve_issue_key_and_number}` at the crate root would be cleaner. Not required now.

**`ApiError` does not implement `std::error::Error`.** It implements `Display` but not the `Error` trait. This is intentional — `ApiError` is a transport adapter, not a general-purpose error type. Callers convert it to `trakkt_core::Error` via the explicit match in `dispatch_registry_tool`. No issue.

---

### Anti-pattern scan

- No `#[allow(...)]` suppressions introduced.
- No new `unwrap()`, `unwrap_or_default()`, or `.ok()` in production paths.
- No fallback code.
- No hardcoded credentials, URLs, or port numbers.
- No business logic added to any transport layer.
- No copy-paste duplication introduced (the move is a rename, not a copy).
- Module visibility unchanged — all handler functions remain `pub`, all internal helpers remain module-private.

---

### What was done well

- The extraction is surgical — purely mechanical (file move + import path updates) with one deliberate structural change (`Option<&WebSocketManager>`) that is correctly isolated to `ApiCtx` with no blast radius to handlers.
- The two constructors (`from_bearer` / `from_leptos`) give callers a clear API without exposing the `Option` wrapping detail to the MCP/REST paths.
- `cargo check`, `cargo clippy -D warnings`, and `cargo test` all pass on both affected crates. The prior review cycle's 31 server tests + 12 new api-crate tests all pass.
- The completion report (`docs/TRA-59_COMPLETION.md`) documents deferred work (TRA-75 and TRA-76) clearly, so the remaining minor items have explicit follow-up tickets.

**Signed.**

---

## Feature: TRA-59 Phase 8 — Leptos Server Function Migration to trakkt-api Handlers

### Files reviewed
- `crates/trakkt-ui/Cargo.toml` — trakkt-api dependency behind ssr feature
- `crates/trakkt-ui/src/server_fns/mod.rs` — `api_ctx()` helper
- `crates/trakkt-ui/src/server_fns/statuses.rs` — list_statuses migration
- `crates/trakkt-ui/src/server_fns/teams.rs` — list_teams migration
- `crates/trakkt-ui/src/server_fns/labels.rs` — create_label migration
- `crates/trakkt-ui/src/server_fns/comments.rs` — create_comment migration
- `crates/trakkt-ui/src/server_fns/relations.rs` — all 3 functions migrated, dead helpers removed
- `crates/trakkt-ui/src/server_fns/issues.rs` — create_issue, update_issue, delete_issue migrated
- `crates/trakkt-ui/src/server_fns/projects.rs` — 9 functions migrated (5 project + 4 milestone)

### Reference files read
- `crates/trakkt-api/src/lib.rs` — ApiCtx, ApiError, all_operations
- `crates/trakkt-api/src/context.rs` — resolve_team, resolve_issue_key_and_number, parse_issue_identifier
- `crates/trakkt-api/src/issues.rs` — create_issue, update_issue, delete_issue handlers
- `crates/trakkt-api/src/projects.rs` — project handlers, verify_project_ownership
- `crates/trakkt-api/src/milestones.rs` — milestone handlers
- `crates/trakkt-api/src/relations.rs` — relation handlers
- `crates/trakkt-api/src/comments.rs` — add_comment handler
- `crates/trakkt-api/src/labels.rs` — create_label handler
- `crates/trakkt-api/src/statuses.rs` — list_statuses handler
- `crates/trakkt-api/src/teams.rs` — list_teams handler
- `crates/trakkt-ui/src/pages/projects/project_detail.rs` — get_project caller

---

### Result: APPROVED — No critical or major issues found

---

### Behavioral equivalence verification

**api_ctx() helper** (mod.rs lines 107–114): Passes `ws_id.clone()` (workspace_id), `auth.user_id.clone()`, `db()`, and `ctx.ws_manager.as_ref()` to `ApiCtx::from_leptos`. All four map directly to the fields that `AuthenticatedContext` already held before the migration. Correct.

**list_statuses** (statuses.rs): Old code called `status_service::list_statuses(db, ws_id, team_id.as_deref())`. New code calls through `trakkt_api::statuses::list_statuses` which calls `resolve_team(db, ws_id, team_key, team_id.as_deref())`. Because `team_key: None` is set, `resolve_team` short-circuits to returning `team_id` directly when non-None — identical to passing `team_id.as_deref()` directly to the service. Correct.

**list_teams** (teams.rs): Old code called `team_service::list_teams(db, ws_id, Some(&user_id))`. New code routes through `trakkt_api::teams::list_teams` which calls the same service with `Some(&ctx.user_id)`. Match confirmed.

**create_label** (labels.rs): Old code passed `team_id.as_deref()` directly to `label_service::create_label`. New code routes through `trakkt_api::labels::create_label` which calls `resolve_team(db, ws_id, None, team_id.as_deref())` then passes the result to the same service call. `resolve_team` with `team_key=None` and `team_id=Some(id)` returns `Some(id)` directly — equivalent to `Some(id.as_str())`. Correct.

**create_comment** (comments.rs): Old code resolved `issue_id` via `resolve_issue_id(db, ws_id, team_key, issue_number)` then called `comment_service::create_comment`. New code builds identifier `"{team_key}-{issue_number}"` and passes to `trakkt_api::comments::add_comment` which calls `resolve_issue_key_and_number` → `issue_service::get_issue` → same `comment_service::create_comment` call. Correct.

**add_relation** (relations.rs): Old code parsed source/target identifiers with `rsplit_once('-')` (splits at last hyphen); new code uses `parse_issue_identifier` which uses `splitn(2, '-')` (splits at first hyphen). For valid team keys (single alphabetic tokens like "TRA", "ENG"), these produce identical results. For pathological inputs like `"FOO-BAR-7"`, old code would parse as team `"FOO-BAR"` / number `7`, while new code returns `None` (then an error). This behavioral difference affects only malformed input and is a correctness improvement, not a regression.

**remove_relation** (relations.rs): Direct pass-through of `relation_id` to `relation_service::delete_relation`. Old code returned the service result directly on a `Result<(), _>`, new code does `await.into_sfn()?; Ok(())`. Semantically equivalent.

**list_issue_relations** (relations.rs): Old code called `list_relations_for_issue(db, issue_id, ws_id)` after resolving `issue_id`. New code calls through `trakkt_api::relations::list_relations` which first calls `issue_service::get_issue` to verify existence before calling the same service. For valid issues this is equivalent. Minor overhead of an extra lookup in the happy path, not a behavioral regression.

**create_issue** (issues.rs): Label parsing: old code called `parse_label_ids(&label_ids)` and always passed a Vec. New code inlines the same logic but passes `labels: None` for an empty Vec. The API handler uses `params.labels.unwrap_or_default()` which produces `vec![]` for `None` — identical behavior. `parent_issue_id.filter(|s| !s.is_empty())` matches old code. `priority: Some(priority)` with `unwrap_or(0)` in the handler matches old code's direct field. All other fields match. Correct.

**update_issue** (issues.rs): The `sentinel_to_opt` helper correctly implements the three-state semantics: `clear=true` → `Some(None)`, `val=Some("") clear=false` → `Some(None)`, `val=Some("x") clear=false` → `Some(Some("x"))`, `val=None clear=false` → `None`. Matches the old code's per-field conditional pattern exactly. `parent_issue_id` clear/set logic: `clears.contains(&"parent")` → `Some(None)`, else `parent_issue_id.filter(|s| !s.is_empty()).map(Some)` — correct. `sort_order` clear: `Some(None)` when in clear_fields, `None` otherwise — matches old code. `labels: None` is correct since label updates have a dedicated `set_issue_labels` server fn. Correct.

**delete_issue** (issues.rs): Routes through API handler which calls the same service. Return type: old code returned `.into_sfn()?` on `Result<(), _>`, new code adds explicit `Ok(())`. Equivalent.

**list_projects / create_project** (projects.rs): `create_project` passes all 7 fields directly through the API handler to `CreateProjectParams`. Match confirmed.

**get_project** (projects.rs): Behavioral change documented in Minor Issues below.

**update_project** (projects.rs): `opt_clear` helper matches old code's inline sentinel pattern. Old code called `verify_project_ownership` before updating; new code routes through `trakkt_api::projects::update_project` which calls it internally. Ownership check preserved. Correct.

**delete_project** (projects.rs): API handler calls `verify_project_ownership` then `delete_project`. Ownership check preserved. Correct.

**list/create/update/delete milestone** (projects.rs): All four route through API handlers which call `verify_project_ownership` (and for update/delete, `resolve_milestone_project_id` too). All authorization checks preserved. Correct.

---

### Minor Issues

**Minor 1 — get_project cross-workspace error type change**

Old `get_project` server fn: project found but wrong workspace → `Ok(None)` → UI renders `ProjectNotFound` component.

New `get_project` server fn: project found but wrong workspace → `Err(ApiError::NotFound(...))` from `verify_project_ownership` → mapped to `Err(ServerFnError)` → UI renders generic "Failed to load project. Please try again." message.

This is a user-experience regression for the cross-workspace isolation path. It is an edge case (users cannot normally navigate to another workspace's project URL), and the security isolation is still correct. The `ServerFnError` type does not support structured variants that would let the server fn map `NotFound` back to `None` cleanly. Resolving this cleanly would require bypassing the API layer for this function. Acceptable for now.

**Minor 2 — get_project fetches milestones unnecessarily**

`trakkt_api::projects::get_project` fetches both project and milestones (returning `{"project": ..., "milestones": ...}`). The server fn only uses the `project` field, discarding `milestones`. The old code called `project_service::get_project` directly without fetching milestones. This is a wasted query on every `get_project` call from the Leptos UI. Pre-existing design of the API handler (built for the MCP tool that does need milestones). Not a regression introduced by this diff.

**Minor 3 — verify_project_ownership duplicated across projects.rs server fns and trakkt-api**

`crates/trakkt-ui/src/server_fns/projects.rs` retains a local `verify_project_ownership(db, ws_id, project_id)` function for the non-migrated server fns. `trakkt_api::projects::verify_project_ownership` exists with equivalent logic. This duplication is intentional — the non-migrated functions have no API handler equivalent — and is explicitly noted in the task spec. When those functions are migrated, the local helper should be removed.

---

### Positive Observations

- `api_ctx()` is a clean, single-point constructor that correctly forwards all four fields. Future migrations add zero boilerplate.
- The `ssr` feature gate on `trakkt-api` in `Cargo.toml` correctly prevents the crate from being compiled into the WASM binary.
- Dead helpers in `relations.rs` (`parse_identifier`, `resolve_issue_id_from_identifier`) and `projects.rs` (`resolve_milestone_project`) were cleanly removed. No stranded code.
- The `sentinel_to_opt` inner function in `update_issue` eliminates the repeated conditional pattern that appeared 4 times in the old code.
- All non-migrated functions are genuinely untouched (confirmed: `list_issues`, `get_issue`, `list_sub_issues`, `get_parent_issue_id`, `set_issue_sort_order`, `set_issue_labels` all still call `trakkt_auth` directly).
- Authorization checks (`verify_project_ownership`, milestone ownership checks) are all preserved in the migrated path through the API handlers.
- `cargo clippy -p trakkt-ui --features ssr -- -D warnings` clean confirms no lint suppressions were introduced.

---

### Result: APPROVED — No critical or major issues found

**Signed.**

---
