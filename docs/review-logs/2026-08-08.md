---

### 09:15 — TRA-9985: Every SPA deep link returns HTTP 404 while rendering correctly

**Review type**: initial
**Result**: Clean (approved)
**Files**: `apps/server/src/leptos_frontend.rs`, `apps/server/src/lib.rs`, `apps/server/tests/spa_routing.rs`, `crates/trakkt-core/src/config.rs`, `e2e/tests/sync/panic-probe.spec.ts`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Spec Adherence / Mechanical Enforcement | apps/server/src/leptos_frontend.rs:56-61 | Dot-heuristic invariant ("every client route is dot-free") is documentary only; a future route with a dot would silently 404. Disclosed honestly by implementer, low risk, optional follow-up to make it a mechanical test. |

**Anti-pattern tally**: none blocking; 1 minor (Mechanical Enforcement) noted.

**Notes**: Implementer rejected the ticket's own suggested one-line fix (`.fallback` alone) after demonstrating it regresses missing-asset 404s to 200 — verified this myself by reproducing all 5 claimed mutations (M1-M5) by hand-editing the file and running `cargo test`, each killing exactly the one test claimed, no overlap. Verified `tower-http-0.6.10` vendored source line-by-line against every claim in the PR description (set_status.rs:134, serve_dir/mod.rs:198-199,240-242, future.rs:108,196-215) — all accurate. Verified `Config.dist_dir` refactor is behavior-preserving (same env var, same default, no other reader of old path, already documented). Verified `pub mod common;` is a legitimate visibility fix (not a lint evasion) by empirically toggling it and confirming `-D warnings` genuinely fires dead_code on the private form. Verified e2e assertion is non-vacuous by reading the ordering dependency (TRA-10020, known/deferred) and confirming `beforeAll` throws rather than silently passing if setup fails. This is one of the more thoroughly self-verified PRs reviewed — every checkable claim held up exactly as stated.

---
