# Review Log — 2026-08-08

---

### 00:47 — TRA-9982: MCP DELETE /mcp missing auth check

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `apps/server/src/routes/mcp.rs`, `apps/server/tests/mcp_session_routes.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: Security fix closing an unauthenticated `DELETE /mcp` session-termination endpoint. Verified independently rather than trusting the writeup: (1) diffed the new gate block against `handle_post`'s binding shape — byte-identical, confirming it is not a third auth-gate shape; (2) diffed lines 1-220 against `main` — byte-identical, confirming `handle_post`/`handle_sse`/routing/middleware untouched; (3) read `mcp_session_manager.rs` to confirm `create_session`/`validate_session` already round-trip `workspace_id` with no new persistence added; (4) reproduced red-before-fix by reverting the handler — got the exact claimed `3 passed; 2 failed` with matching panic values; (5) reproduced mutations M1, M2, and M4 myself via `cargo test --workspace --no-fail-fast` — each failed exactly the claimed test(s) with zero collateral; (6) verified the e2e-non-regression claim by reading `apiRequest`'s `page.request.fetch` cookie-sharing behavior and confirming both DELETE tests in the pre-existing stale spec share one `page` context across `initialize` and `DELETE`, so the new cross-workspace check can never fire between them.

Judgment calls: the 204-vs-403 response on a refused cross-workspace delete (rather than 403/404) is deliberate to avoid handing an authenticated caller an oracle for session-id existence across workspaces — sound, since a legitimate client never presents a session id it wasn't handed by its own `initialize`. The `tracing::warn!` preserves operator visibility. Comments accurately calibrate the security impact as availability (single session eviction), not disclosure — no overstatement found.

One process note: an early `git stash`/`git stash pop` (used to diff untouched regions against `main`) had the side effect of un-staging the fix from the index. Caught via `git status`, confirmed the working tree still held the correct fixed content against a pre-mutation snapshot, and restored the index with `git add` before finishing. Final state verified: `git diff` empty, `git diff --cached` byte-identical to the diff captured at review start, all 5 tests passing.

---

### 02:20 — TRA-9983: team icon errors discarded on save

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 1 🟢
**Files**: `crates/trakkt-ui/src/pages/teams/team_settings.rs`, `crates/trakkt-ui/src/components/team_icon_picker.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`, `crates/trakkt-ui/src/wasm_test_support.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Comment and Doc Accuracy | (review request, not code) | The mutation-results table's M1 row ("delete the two `set` calls in the `Err` arm") is reported as failing T1, T2, T3 only. Reproduced M1 directly: it actually fails T1, T2, T3, **and T5** (`a_rejection_falls_back_to_the_last_accepted_change`) — 4 tests, not 3. Not a code or test defect (T5 correctly detects the same regression M1 introduces; the suite is more rigorous than described), but the report's own checkable claim was wrong and should be corrected before merge for anyone using it as documentation of coverage. |

**Anti-pattern tally**: Comment and Doc Accuracy ×1

**Notes**: Fixes the exact defect described — `on_icon_change` discarded both `clear_team_icon`/`update_team_icon` results via `let _ =`. Verified independently rather than trusting the writeup, using a live `wasm-pack test --headless --firefox` harness (available in this environment):
(1) hoisting necessity — read `team_icon_picker.rs` on `main`: `selected_name`/`selected_color` were `signal()` calls, `preview_team` a `Memo::new`, and both `is_selected` values `Signal::derive`, all created *inside* `TeamIconPicker`'s component body, which is confirmed (via `components/popover.rs:457-469`) to render behind `<Show when=move || open.get()>` wrapping a `<Portal>` — genuinely unreachable from a failure handler living above the popover. Hoisting into `TeamGeneralCard`-owned `IconSelection` was necessary, not cosmetic.
(2) wrapper removal — grepped the current `team_icon_picker.rs` for `Memo::new`, `Signal::derive`, `StoredValue::new`: zero matches outside the doc comment stating the rule. All three wrappers are gone; the view closures now read `selected_name`/`selected_color` inline with the identical expressions the wrappers used to compute, so behavior is unchanged by construction.
(3) disposal evidence (M3) — reproduced live: temporarily moved the `Effect::new` construction out of `IconSelection::with_saver` into a `.watch()` method called from inside `TeamIconPicker`'s own component body (behind the popover's `<Show>`), threading `IconSelection` down as an extra prop. Ran the full wasm test suite: exactly one failure, `an_icon_change_rejected_after_the_picker_closes_still_puts_it_back` (T2), all 69 others green — matches the claimed table exactly. Reverted; working tree confirmed byte-identical to the original via `diff`.
(4) deviation A (toast vs. `set_validation_error`) — read `TeamGeneralCard`'s view: icon trigger is the first field, then Team name, then Team key, then the `validation_error` `<p>`, then Save. The popover is `Placement::BOTTOM_START` off the 40px icon (first field) at `w-[300px]` with the picker's own `max-h-[400px]`, so it geometrically overlaps exactly the fields the report claims (name, key, validation line). `capture_error_toast` is untouched by this diff (confirmed via `git diff --cached` on `notifications.rs` — zero lines touch that call), and is the same mechanism `ToggleState` already uses for its own identical revert-and-report situation. Deviation justified, not a new mechanism.
(5) deviation B (`Ok` payload as new baseline) + test 5 — reproduced M6 (ignore the `Ok` payload, `baseline` never updated): exactly one failure, `a_rejection_falls_back_to_the_last_accepted_change` (T5), all others green. Confirms test 5 genuinely discriminates "fell back to last accepted change" from "fell back to opening snapshot" (the two colors it compares are deliberately different), and that using the server's returned `Team` as the new baseline is correct — a naive revert-to-opening-snapshot would silently undo an already-accepted change.
(6) seam's stated gap — confirmed `IconSelection::new`'s `if icon_type.is_none() && icon_name.is_none() && icon_color.is_none()` branch is never executed by any browser test (the test-only caller replaces the whole closure via `with_saver`). It is a single boolean condition selecting between two direct passthrough `#[server]` calls with no transformation or error handling of its own, and test 3 pins the value the condition is evaluated against. Acceptable limit, not a real hole — integration coverage of the server functions themselves belongs elsewhere.
(7) M1/M2 isolation — M2 (delete `show_error(...)` only) reproduced exactly as claimed: T1, T2, T3 fail, T4 and T5 both green. M1 reproduced with one discrepancy noted above (finding #1).
(8) seam callers — grepped `with_saver`: exactly two call sites, `IconSelection::new` (shipped) and the test-only `ProbeIconCard` in `#[cfg(all(test, target_arch = "wasm32"))]`. No test/production branch inside `with_saver` itself.
(9) `notifications.rs` — diffed in isolation: pure extraction of `Gate`/`SaveLog`/`LocalFuture` into `wasm_test_support.rs` (direct field sets replaced by new `record_start`/`record_finish` methods; `Gate::wait` made `pub` since now cross-module), no logic changes. Ran the full suite: all 6 `notifications::wasm_tests` plus the file's 2 `row_owned_save_probe` tests pass, alongside all 5 new `team_settings` tests — 70/70.

No `#[allow(...)]`, no `unwrap_or_default()`, no `.ok()` discards, no bare `.unwrap()`, no fallback/stub/mock code in the diff. `cargo fmt` not run (known broken, TRA-10035, per instructions). The eleven other discarded server-fn results and the picker's upload-path panic risk are unmodified by this diff and correctly left out of scope. Final state verified: working tree `diff --stat` against the pre-review original empty, `git diff --cached` unchanged from the diff reviewed, full wasm test suite green (70 passed, 0 failed).

---
