---

### 00:44 — TRA-9994: page-wiring test for `refetch_on_live_activity`

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/pages/activity.rs`, `crates/trakkt-ui/src/wasm_test_support.rs`, `crates/trakkt-ui/src/pages/projects/project_detail.rs`, `crates/trakkt-ui/src/pages/settings/live_update.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`, `crates/trakkt-ui/src/pages/teams/team_settings.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found after independent mutation testing |

**Anti-pattern tally**: none

**Notes**: Independently re-ran both required mutations rather than trusting the author's report. Mutation A (call site deleted): new test fails, three pre-existing helper tests stay green — confirmed via actual `wasm-pack test` run. Mutation B (call site deleted + a genuine second caller of `refetch_on_live_activity`/`FetchMode::MergePageOne` wired into `Layout`, a component mounted on every authenticated route): ran `cargo clippy --locked -p trakkt-ui --target wasm32-unknown-unknown --features hydrate --all-targets -- -D warnings`, got exit 0 with zero dead_code/never-used/never-constructed findings, then ran the wasm suite and confirmed `the_mounted_page_refetches_when_a_live_activity_frame_arrives` still fails with `left: 1, right: 2` while the other three tests pass. This is exactly the required property. Also independently verified: (1) the `panic = "abort"` claim, empirically — wrote a throwaway test wrapping a panic in `std::panic::catch_unwind`; the panic propagated straight through without being caught and without the Drop-tracking flag being set, proving no unwind/no destructor runs on this build even though the Cargo.toml override is textually scoped to `[profile.release]` only (wasm32-unknown-unknown's own default panic strategy is abort, exactly as the doc comment states); (2) the xxh64-of-`CARGO_MANIFEST_DIR`-and-`module_path!()` claim, by reading `server_fn_macro-0.8.10/src/lib.rs` directly — matches; (3) the `server_fn-0.8.12` 400-599-as-error protocol claim — matches; (4) the `cache/apply.rs:1730-1736` citation about previously-deleted source-parsing tests — the quoted line ("credited an arm it could read without ever running it") is genuinely there, so the rejection of ticket Option 3 is well-founded; (5) the `make_container`→`mount_container` consolidation — ran each of the five affected modules in isolation (`wasm-pack test ... -- <module>`), all green, and confirmed the misattached `LocalResource::new` re-tracking doc comment now sits directly above the test that demonstrates it; (6) confirmed `refetch_on_live_activity` resolves `activities_version()` once outside the effect closure (TRA-9991/#283 shape, untouched by this diff) and that `Replace`/`MergePageOne` genuinely build byte-identical requests, so the test's stated scope limit (not pinning which mode fires) is honest; (7) grepped for `#[allow(` — none added — and ran both CI clippy gates verbatim against the real staged diff, both exit 0. No banned patterns, no fallback code, no suppressions, no scope creep. This is a well-executed, thoroughly self-documented test that earns the property it claims.

---

---

### 02:20 — TRA-9995: sidebar unread badge counts soft-deleted notifications

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 1 🟢
**Files**: `crates/trakkt-types/src/models.rs`, `crates/trakkt-ui/src/components/layout.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Spec Adherence / Comment Accuracy | `crates/trakkt-ui/src/components/layout.rs:1391` (`SidebarInboxNavItem`) | The deliberate decision not to follow the inbox's team/type/search filters or pagination in the badge count is explained in the review report but not stated anywhere in code — a future reader has no way to tell it was a choice rather than an oversight. |

**Anti-pattern tally**: Comment Accuracy ×1 (minor, non-blocking)

**Notes**: Independently re-ran both required mutations via real `wasm-pack test --headless --firefox` runs rather than trusting the author's table. Mutation A (`is_unread_in_inbox() = !self.read`): delete test fails (`left: Some("2") right: Some("1")`), read test passes. Mutation B (`is_unread_in_inbox() = self.deleted_at.is_none()`): read test fails with the identical failure shape, delete test passes. Both conjuncts are load-bearing, matching the author's claim exactly. Verified the SQL citations (`notification_service.rs:233`, `:295`, `:642`) against source — all three say what the doc comment claims. Verified `store.notifications()` (`store.rs:229`) builds a fresh `Signal::derive` per call, identical to the `*_version()` getters, and is one of the "eight collection getters" the store's own contract doc (`store.rs:98-140`) claims to cover — confirming this was a real latent #283/TRA-9991-class hazard, not just a style nit. Verified `pages/inbox.rs` does all filtering server-side via `list_notifications`/`count_notifications` params, so there is no client-side second copy of "unread" to drift from. Confirmed the test drives the store through the real `apply_action_to_memory` → `upsert_notification` path and reads the count from the DOM of a genuinely mounted `SidebarInboxNavItem`, not a restatement of the predicate. Agreed with the author's own flagged gap: `no_page_resolves_a_version_counter_inline`'s regex only matches `_version().get(`-shaped calls and does not cover the eight collection getters despite the contract doc's "eight collection getters... covered" claim — recommended as a separate ticket, not fixed here.

Process note: during verification I mutated `crates/trakkt-types/src/models.rs` directly in the worktree to test it and left the mutation in place uncommitted for longer than appropriate while waiting on a background build — caught and flagged by the coordinator. Restored via `git checkout --` and confirmed `git diff -- crates/` was empty (worktree exactly matches the staged diff) both immediately after restoring and again immediately before signing.

---

### 04:17 — TRA-9996: `SyncStore` version counters return `ArcSignal<u32>` instead of `Signal<u32>`

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 2 🟢
**Files**: `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/cache/store.rs`, `crates/trakkt-ui/src/components/layout.rs`, `crates/trakkt-ui/src/pages/activity.rs`, `crates/trakkt-ui/src/pages/issues/issue_detail.rs`, `crates/trakkt-ui/src/pages/projects/project_detail.rs`, `crates/trakkt-ui/src/pages/settings/notifications.rs`, `crates/trakkt-ui/src/pages/settings/workspace.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Documentation Accuracy | `crates/trakkt-ui/src/cache/store.rs` doc comment | The author's own breakdown of the 11 production call sites ("six became `.as_ref().map(\|v\| v.get())`, two became `if let Some(v) = &opt { v.track() }`, one compiled unchanged") sums to 9, not 11. Actual breakdown is 7 `.as_ref().map(get)` sites, 3 `if let ... = &opt` sites (2 `track()`, 1 `get()`), 1 unchanged — every individual site is correctly implemented, only the summary undercounts. |
| 2 | 🟢 | Documentation Accuracy | ticket framing, "11 production resolution points across 6 files" | Verified only 5 files contain the 11 production sites (`activity.rs`×1, `issue_detail.rs`×5, `project_detail.rs`×3, `notifications.rs`×1, `workspace.rs`×1). `apply.rs` is a plausible 6th file but its 4 sites are inside `#[cfg(test)] mod wasm_tests`, i.e. test mirrors, not production — so "production" and "6 files" don't both hold at once literally, though nothing here is wrong about the code. |

**Anti-pattern tally**: Documentation Accuracy ×2 (both minor, non-blocking, self-report bookkeeping only — no code defect)

**Notes**: Walked all 11 production `*_version()` resolution points individually (`grep`-enumerated and read in context) and confirmed each `.get()`/`.track()` sits in the identical position relative to its closure boundary as before the refactor — no read was hoisted, no site needed a clone it didn't get. Independently executed mutations B, C, D, and E via real `wasm-pack test --headless --firefox` runs (not just read the table):
- **Mutation D** (read hoisted out of the closure, snapshotted at setup) — the single most important one, since it's the silent-death shape this whole ticket exists to prevent. Applied to `the_as_ref_source_shape_tracks_its_own_counter_and_only_that_one`: failed with exactly the claimed assertion, `"an attachment frame must move the attachment source"`.
- **Mutation B** (`.get()` → `.get_untracked()`): same test failed with a different assertion at the same site, confirming the source-shape test genuinely discriminates tracked vs. untracked reads.
- **Mutation C** (`.track()` deleted): `the_borrowed_track_shape_registers_a_dependency` failed as claimed.
- **Mutation E** (one arm of the equivalence test made untracked, `per_run.get()` → `per_run.get_untracked()`): `cloning_a_counter_outside_or_inside_a_closure_tracks_identically` failed, confirming the three clone-placement forms are genuinely distinguished rather than collapsing into one code path.
- Baseline (unmutated) reruns of each affected test passed after each restore, and the full `cache::store::wasm_tests` (12 tests) and `cache::apply::wasm_tests` (16 tests) modules passed clean on the fully-restored tree.
- Mutation A (getters reverted to `Signal<u32>`) was verified by reasoning rather than live execution — a true revert requires also changing the `CounterGetter` type alias and `COUNTERS` array to stay compiling, which is a larger temporary edit than the other four; the underlying claim (owner-registered `ArenaItem` panics after `Owner::cleanup`, `ArcSignal` doesn't) is directly evidenced by `a_counter_resolved_under_a_subtree_owner_survives_that_subtrees_disposal` passing on the real tree and its sibling `a_collection_resolved_under_a_subtree_owner_is_disposed_with_that_subtree` (`#[should_panic]`, targeting `store.issues()`, still `Signal`) still firing.
- Confirmed the deleted `source_guard::no_page_resolves_a_version_counter_inline` test's substring claim against its own source in the diff: it matched literal `_version().get(`/`.read(`/`.with(`/`.track(` after whitespace-stripping, which can only ever match the nine counter getters — never `projects()`, `issues()`, `notifications()`, etc. — so its removal costs nothing the collections ever had.
- Confirmed 9 counters (`COUNTERS: [...; 9]`, 9 `rewind_to_zero` calls in `reset()`) and 11 production resolution points across 5 files (not the 6 claimed, per finding #2 above) via `grep`.
- Confirmed `ArcSignal::derive(move || sig.get())` used uniformly across all 9 getters — no site took `sig.read_only().into()`.
- `cargo check -p trakkt-ui --features hydrate` (host) and the wasm build (`trunk build`, already completed successfully before this review started) both clean, zero warnings.
- Every mutation was applied via `Edit`, tested, then reverted via `Edit` back to the exact original text; `git diff -- crates/` was confirmed empty (zero output) both mid-review after each restore and again as the final step before signing — no contamination left behind.

---

### 20:27 — TRA-10001: broaden the dual-backend suite (sort_order FLOAT4 class, ago_days production fix, per-helper ledger)

**Review type**: initial
**Result**: 0 🔴, 1 🟡, 0 🟢
**Files**: `crates/trakkt-core/src/sql_compat.rs`, `crates/trakkt-core/src/test_helpers/dual_backend.rs`, `apps/server/tests/postgres_dialect.rs`, `docs/CODING_STANDARDS.md`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Spec Adherence (documentation accuracy / overstated production-impact claim) | `apps/server/tests/postgres_dialect.rs:896-897` | New doc comment claims `prune_old_entries` and `run_archive_sweep` "have never once succeeded against the database production runs on". `run_archive_sweep` is genuinely wired hourly in `apps/server/src/main.rs:136-152` and does hit the buggy `ago_days` path — true. `prune_old_entries` has no call site anywhere outside its own definition and this test — confirmed by grep and by the pre-existing, still-open TRA-10027 ("called from nowhere"). It is dead code, not a silently-failing production path; the blanket claim conflates the two. |

**Anti-pattern tally**: Spec Adherence / documentation accuracy ×1

**Notes**: This was a "verify the author's claims" review, not a from-scratch read. Independently confirmed essentially every quantitative claim in the ticket against a live Postgres 16 (`localhost:5446`) and the real source:
- **The `ago_days` defect itself**: reproduced directly in `psql` — `make_interval(days => 30::bigint)` errors `function make_interval(days => bigint) does not exist`; `::int` fixes it. Confirmed via source that `bigint` is exactly what sqlx sends for the `i64` both callers bind.
- **Blast radius**: confirmed `run_archive_sweep` is wired (hourly `tokio::spawn` in `main.rs`) and calls `sql_compat::ago_days` with `i64::from(archive_days)` — genuine production impact for any team with `auto_archive_days` set. Confirmed `prune_old_entries` is called from nowhere in production (only its own def + the new test) — this is the one finding above.
- **Mutation 1b** (revert `sort_order` migration to `REAL` + add `CAST(p.sort_order AS DOUBLE PRECISION)` workaround to `PROJECT_SELECT`): reproduced live — `sqlite ... ok`, `postgres ... FAILED` with the exact claimed asymmetric ordering (`left: [id_B, id_A] right: [id_A, id_B]`). Reverted via `git checkout --`, confirmed `git diff -- crates/ apps/` empty before continuing.
- **Mutation 4** (strip `::int` from `ago_days`): reproduced live — `sqlite ... ok`, `postgres ... FAILED` with `function make_interval(days => bigint) does not exist`. Reverted, confirmed clean tree.
- **Priority 3**: confirmed exactly 60 `*_rolls_back_when_*` bodies in `sync_log_service.rs` (52 blanket-trigger / 8 narrowed — verified programmatically against all 5 `reject_sync_log_inserts*` helper variants, including one script pass that initially undercounted by missing `reject_sync_log_inserts_for_action`, corrected and re-verified to exactly 52/8/0-unaccounted). Confirmed exactly 178 `#[tokio::test]`s across `trakkt-auth/src` (138 in `sync_log_service.rs` + 40 spread across `team_service.rs`, `mcp_session_manager.rs`, `project_service.rs`, `email_service.rs`, `websocket/manager.rs`). Read `a_rejected_entry_of_any_type_rolls_the_whole_project_cascade_back` (pre-existing, from TRA-9958) end-to-end and confirmed it does genuinely probe rejection *after* `ON DELETE CASCADE` has fired — the shape the deleted `trakkt-auth`/`delete_team` candidate would have duplicated. Deletion decision looks correct.
- **Priority 4**: confirmed the 8-called/12-uncalled `sql_compat` helper split exactly matches the 20 `pub fn`s in `sql_compat.rs`. Confirmed `full_table_name_expr`/`full_table_name_expr_prefixed` reference `dataset_id`/`table_id`, and confirmed neither column exists in any `.sql` file under the single `apps/server/migrations` directory (there is no second migrations directory) — a genuine leftover from a BigQuery-shaped port.
- **Priority 4 (CODING_STANDARDS numbers)**: confirmed **119** `is_postgres()` and **164** `sql_compat::` call sites outside `apps/server/tests` exactly (excluding the module's own self-referential doc-comment mentions of those same numbers, which would otherwise inflate the naive grep by one each). Confirmed **25** `dual_backend_test!` bodies (21 pre-existing + 4 new: `project_sort_order_decodes_at_the_width_its_row_type_declares`, `every_sort_order_column_decodes_at_the_width_its_row_type_declares`, `project_dates_and_archive_stamp_survive_the_round_trip`, `sync_log_pruning_selects_by_age_on_both_dialects`) and **54** total tests via a real `cargo test -- --include-ignored` run against the live container.
- **Priority 5**: ran the full suite live — `54 passed; 0 failed; 0 ignored`. Ran with `TEST_DATABASE_URL` pointed at a dead port — target test reported `ignored, requires a live Postgres — see crates/trakkt-core/src/test_helpers/dual_backend.rs`, `1 ignored`. Same dead port with `--include-ignored` — panicked loudly with the pointer message and container instructions, `test result: FAILED`. No silent skip anywhere. `cargo clippy --all-targets` on the touched crates (with `test-helpers` feature) was clean, zero warnings, zero `#[allow(...)]` anywhere in the diff.
- **Scope call (production fix inside a test-coverage ticket)**: agreed with keeping it. The new `sync_log_pruning_selects_by_age_on_both_dialects` body cannot pass on Postgres without the `::int` cast, so shipping the coverage without the fix would mean either a red suite or a body written to assert failure — worse than bundling. Matches standard TDD (red test proves the defect, then fix, then green).
- All mutations were reverted; `git diff -- crates/ apps/` confirmed empty immediately before signing was withheld.

**Not signed** — one 🟡 finding, not deferred to a ticket (trivially fixable in-diff by correcting two sentences), so the gate stays closed pending correction of the `prune_old_entries` characterization in `apps/server/tests/postgres_dialect.rs:896-897` (and the mirrored, milder phrasing in `sql_compat.rs:48`, "every Postgres caller of this helper fails at runtime", is defensible read as an abstract statement about the helper and does not itself need to change).

---

### 20:52 — TRA-10001 re-review: `prune_old_entries` correction, verified

**Review type**: re-review (cycle 2)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `apps/server/tests/postgres_dialect.rs` (comment-only change from cycle 1)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: The prior 🟡 (overstated `prune_old_entries` production-impact claim) is fixed at `apps/server/tests/postgres_dialect.rs:900-929`. The rewritten doc comment now states the two callers separately: `run_archive_sweep` "ships and runs" (true, and correctly scoped — the claim it lands on is "on Postgres this sweep has never archived an issue," not "fails every sweep"), and `prune_old_entries` "has never run in production at all... nothing here fixes it or should be read as having fixed it," cross-referencing TRA-10027.

Confirmed the diff is exactly this one doc comment and nothing else, three independent ways rather than trusting the author's self-report: (1) `sql_compat.rs`, `dual_backend.rs`, `docs/CODING_STANDARDS.md` all have identical insertion counts to cycle 1 (29/16/54) — only `postgres_dialect.rs` grew, by exactly +21 lines; (2) read the full current doc comment (lines 887-930) — every line is `///`-prefixed; (3) diffed the file against HEAD and confirmed every non-comment line matches test code already read verbatim in cycle 1 (the sort_order, cast_to_date, and ago_days test bodies are byte-identical to what was reviewed before) — the +21 lines are fully accounted for by the doc-comment's growth from ~21 to ~42 lines. Did not have a saved pre-fix snapshot to run the author's own `git diff -U0 | grep -vE '^[+-]\s*///'` check verbatim, so triangulated the same conclusion independently instead of accepting the report at face value.

Verified the corrected claim itself is accurate, not just narrower — the coordinator flagged this class of claim as having been half-wrong twice already in this ticket, so re-derived both branches from source rather than trusting the correction:
- Read `archive_service::run_archive_sweep` in full (`crates/trakkt-auth/src/archive_service.rs:64-143`). `get_team_archive_days` returning `None` is a `continue` at line 83, strictly before `ago_expr`/`select_sql` are built at lines 86-94 — confirmed no team without a configured window ever reaches the broken helper. The `db_fetch_all!` call at line 96-102 that would fail has no per-team `try`/catch around it — the `?` inside the macro propagates straight out of `run_archive_sweep`, aborting the whole function before any team (including ones before the failing one in iteration order) has archived anything for a run that reaches the bug at all. So the claim "on Postgres this sweep has never archived an issue" holds unconditionally: either no team has a window (nothing eligible, nothing archived) or some team does (the first one reached errors immediately and aborts before anything is archived, on this run or any prior run, since the bug is deterministic and dialect-wide, not data-dependent). Logically airtight, not merely plausible.
- Read `main.rs:142-150`'s match arms directly. `Err(e) => tracing::warn!(... "Archive sweep failed")` — confirmed for the has-a-window-and-errors branch. `Ok(count) if count > 0 => info!(...)` combined with a bare `_ => {}` fallthrough — confirmed `Ok(0)` (the no-team-has-a-window branch) hits the `_` arm and logs nothing. The two branches are genuinely asymmetric in observability, as the corrected comment now states, not "both silent" as the discarded draft had it.

Independently re-ran the reported verification rather than accepting the summary:
- `cargo clippy -p trakkt-server --test postgres_dialect -p trakkt-core --features test-helpers --all-targets -- -D warnings` — clean, 0 warnings.
- Live Postgres 16 (`localhost:5446`): `cargo test -p trakkt-server --test postgres_dialect -- --include-ignored` → `54 passed; 0 failed; 0 ignored`.
- Dead port, default invocation (no `--include-ignored`): `26 passed; 0 failed; 28 ignored` — matches exactly.
- Dead port + `--include-ignored`: fails loudly (28 failures with the pointer message), as required — this is a different, correctly-loud outcome from the plain dead-port run above, not a contradiction of it.
- `git diff -- crates/ apps/` empty; `HEAD` still `11ba928`, nothing committed.

Signing.

---

### 07:05 — TRA-10002: fix the port ladder (`Config::test_config`, `redis.rs` doc, `seed-e2e-user.py`)

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `crates/trakkt-core/src/config.rs`, `crates/trakkt-core/src/redis.rs`, `scripts/seed-e2e-user.py`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: Verified every claim in the review request against source rather than trusting the trace. (1) "Nothing connects" conclusion confirmed independently: `test_config()` has exactly 2 callers (`apps/server/tests/common/mod.rs:32`, `redis.rs:96`); `common/mod.rs`'s `AppState.db` comes from `test_helpers::test_pool()` (`DbPool::connect("sqlite::memory:")`), never from `config.database_url`; `redis::tests` reads only `cfg.redis_url`; and `grep -n "\.database_url"` across the whole workspace turns up exactly one consumer, `apps/server/src/main.rs:28`, on a `from_env` config — so this was a latent hazard, not an active one, and the severity framing in the new comment is accurate. (2) Confirmed `redis.rs` carried the wrong claim: `dual_backend.rs:79-86` documents the real ladder (5433/5434 another project's dev/test, 5435 Trakkt dev, 5436 Trakkt test), matching `docs/CODING_STANDARDS.md:190-191` and a live `podman ps` on this box (`kyomi-postgres-dev`:5433, `kyomi-postgres-test`:5434, `trakkt-postgres-dev`:5435 with `POSTGRES_USER/PASSWORD/DB=trakkt` confirmed via `podman inspect`, and a container on 5436). Confirmed the citation to `docs/review-logs/2026-07-27.md:286` — that entry does independently verify `podman ps` showing `kyomi-postgres-dev` on 5433 and then blesses the false single-project "5432/5433/5434 = prod/dev/test" framing wholesale ("All citations check out"), without ever checking what actually occupied 5434. Worth recording: a past review propagated this rather than caught it. (3) The `sqlite::memory:` choice over the ticket's suggested 5436 is well-reasoned and satisfies the ticket's actual constraint (the field can't be dropped — `Config` is a plain struct consumed by `from_env`/`main.rs`) better than hardcoding 5436 would: it avoids a second copy of the `DEFAULT_PG_TEST_URL` literal (the exact drift that caused this ticket), avoids pointing a never-read field at a live shared maintenance server, and — verified directly in `.github/workflows/realtime-e2e.yml:109` — the `realtime-e2e` job sets `DATABASE_URL=postgres://trakkt:trakkt@localhost:5437/trakkt` as a job-wide env var, so keeping the old `env::var("DATABASE_URL")` read would have made `test_config()` silently resolve to CI's live Postgres in that job. Confirmed no other test or Config method reads/parses `database_url` in a way `sqlite::memory:` would break; `cargo check`/`cargo clippy -D warnings` on `trakkt-core`+`trakkt-server` (tests included) both clean. (4) `seed-e2e-user.py`'s new default verified byte-for-byte against the live container: `podman inspect trakkt-postgres-dev` shows `POSTGRES_USER=trakkt`, `POSTGRES_PASSWORD=trakkt`, `POSTGRES_DB=trakkt`, port 5435 — exact match for `postgresql://trakkt:trakkt@localhost:5435/trakkt`. The old default (`trakkt:password@localhost:5432`) had both a wrong password and a port nothing exposes to the host — confirmed genuinely broken, not hypothetically. `grep -rln seed-e2e-user` finds no CI usage, so nothing depends on the old (broken) default. (5) `DEFAULT_PG_TEST_URL`'s doc comment is the only site with the literal ladder; `config.rs` and `seed-e2e-user.py`'s new comments both point at it by name rather than restating the ports as fact, and `redis.rs`'s rewritten comment explicitly says Postgres does not follow the same per-role scheme as Redis. (6) Spot-checked 3 of the audit's left-unfixed ⚠️ items: `.env.example` genuinely does not exist (`ls` confirms) while `CONTRIBUTING.md:32` tells contributors to `cp .env.example .env`; `profile.rs:335`'s `http://{hostname}:8002/mcp` fallback is the only occurrence of `8002` anywhere in the repo (no server binds it); and the `e2e/playwright.config.ts` (8099 default) vs. `playwright.sync.config.ts`/`realtime-e2e.yml` (3100) split is real. All three are genuine and safe to file as tickets. (7) No `#[allow(...)]`, no fallback paths, no stubs — `git diff --cached | grep '#\[allow'` empty. No mutations were applied during this review (verification only), so `git diff -- crates/ apps/ scripts/` was empty throughout and immediately before signing.

---
