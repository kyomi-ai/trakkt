# Review Log — 2026-08-09

---

### 20:10 — TRA-9997: browser-verify the six hoisted SyncStore version counters

**Review type**: initial
**Result**: 2 🟡, 1 🟢
**Files**: `e2e/tests/sync/hoisted-counters.spec.ts`, `e2e/tests/sync/realtime-harness.ts`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Spec Adherence / Comment Accuracy | e2e/tests/sync/hoisted-counters.spec.ts:19-27 | Header narrative claims PR #292/TRA-9991 "moved six … calls out of the reactive closures that read them" causing a live-update regression ("simply stops updating"). The repo's own `docs/review-logs/2026-08-03.md` (12:40 entry) independently verified and closed TRA-9991's premise as **false**: dependency tracking (`.get()`/`.track()`) was preserved exactly at all six sites: the hoist moved only where the `Signal` *handle* is constructed, not the reactive read. Confirmed against current source: every one of the six call sites still calls `.get()`/`.track()` on the hoisted handle from inside the closure that needs to track it. #292's own commit message (b59d44c) states this explicitly: "So this is a robustness fix, not a bug fix" and "running the full browser suite passes 59/59." The real risk documented at `crates/trakkt-ui/src/cache/store.rs:98-140` is disposal panics from a wrapper resolved *inside* a re-running effect/memo/subtree, not silent staleness. |
| 2 | 🟡 | Copy-Paste / Missing Abstractions | e2e/tests/sync/hoisted-counters.spec.ts:108-121 | `createIssue()` duplicates the "create an issue through `/workspace`" flow already inline in `two-window-sync.spec.ts:134-147` — same selectors (`#issue-title`, `New Issue`, `Create Issue`, `a[href*="/issues/"]`), same comments about `/workspace` vs `/issues`, differing only in timeout (20s vs 30s) and parameterization. Belongs in `realtime-harness.ts` as a shared helper both spec files call. |
| 3 | 🟢 | Comment Accuracy (minor) | e2e/tests/sync/hoisted-counters.spec.ts:91-93 | Cites `components/dropdown.rs:512-533` for the `aria-haspopup="listbox"` button; the `SelectVariant::Form` trigger is actually at ~508-527 in the checked-out source. Off by a few lines, not load-bearing. |

**Anti-pattern tally**: Comment Accuracy ×2, Copy-Paste/Missing Abstractions ×1

**Notes**: This is an unusually well-built test file — every selector, entity-type string, sync-payload field, and cited line number was cross-checked against the actual source (`project_detail.rs`, `issue_detail.rs`, `dropdown.rs`, `project_service.rs`, `relations.rs`, `relation_service.rs`, `models.rs`, `apply.rs`, `sync.rs`) and all but the one 🟢 held up exactly. The three new tests (relations, project updates, project members) each genuinely discriminate the regression they claim to guard: all three counters feed on-demand server-function reads with no other subscriber (`apply.rs` comments confirm `NOT_CACHED`), so removing the counter from the resource key would leave window B frozen with no other path to update it — verified this is not a vacuous check. `waitForEntityId` is sound: fails loudly on timeout, searches from `t: 0` deliberately (bootstrap streams the same frame shape, documented and correct), and its one caller's discriminating id (a server-minted UUID) has no cheaper source. `expectDeliveredByCounter` reuse is correct and its single-caller-per-entity-type discriminating strings are genuinely unique (random hex, project id scoped to this run). The nav-cycle disposal test's `window.__trakktNavCycle` marker is a deliberate, justified choice over reusing `probe.navigations` (which fires on the SPA's own client-side route changes and would produce false positives here, unlike the other three tests whose windows never navigate mid-test). Flake discipline (timeouts, `markWhenQuiet`, no-navigation guards) matches the existing suite's standard throughout. The two 🟡s are both cheap to fix — a comment rewrite and a ~15-line extraction — and neither reflects a functional defect in the tests themselves; both were called for explicitly by the review brief's own checklist (accuracy of "why this exists" narrative, and copy-pasted harness setup). Attempted a live build of the WASM bundle + `dev-server` profile + a throwaway Postgres 16 container to actually run the suite; build did not finish inside the review window (WASM+server cold build is ~15-25 min per this repo's own CI timing notes), so live pass/fail was not obtained — static verification against source was exhaustive enough to reach a verdict without it, but a live run is recommended before merge and would be cheap to add to the next review cycle once these two items are fixed.

---

### 21:20 — TRA-9997: re-review after both 🟡 and the 🟢 addressed

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `e2e/tests/sync/hoisted-counters.spec.ts`, `e2e/tests/sync/realtime-harness.ts`, `e2e/tests/sync/two-window-sync.spec.ts`, `e2e/tests/sync/milestone-realtime.spec.ts`, `e2e/tests/sync/project-view-state.spec.ts`

**Prior findings, verified resolved, not taken on report**:

1. **Header narrative (was 🟡 #1)** — rewritten (`hoisted-counters.spec.ts:16-75`) to state that #292 moved only *where* each getter is called (setup vs. per-evaluation), not what is tracked, and that TRA-9991's "latent panic"/live-update-regression framing was investigated and closed false in `docs/review-logs/2026-08-03.md`. Independently re-verified every line citation against current source, all exact: `v.track()` at `issue_detail.rs:872`; `.get()` at `:1994` and `:2392`; `project_detail.rs:330`, `:345`, `:355` (the `.get()` reads); `:325`, `:340`, `:350` (the `let X_version = sync_store.map(...)` bindings the fourth test's comment cites); `:863`, `:1987`, `:2383` in `issue_detail.rs`. The fourth (disposal) test's comment was rewritten in the same spirit and now states plainly: "Nothing here claims the current code has that defect — the review that cleared #292 established it does not." Matches `store.rs:98-140`'s actual contract (disposal-on-reuse, not dropped tracking). No overclaim found anywhere in the new text.
2. **`createIssue` duplication (was 🟡 #2)** — extracted to `realtime-harness.ts` as `createIssueViaUi(page, title)`. The requester correctly caught a third pre-existing copy I had missed (`milestone-realtime.spec.ts:272-285`) in addition to the one I flagged (`two-window-sync.spec.ts`) — all three now call the shared helper, ~47 lines of duplication removed. Diff-reviewed all three call-site changes (`two-window-sync.spec.ts`, `milestone-realtime.spec.ts`, `project-view-state.spec.ts`): each is a mechanical swap, same selectors, same assertions, no logic changed beyond the call. The one deliberate behavior change — `two-window-sync.spec.ts`'s created-row wait going from 20s to 30s (the helper standardizes on the majority's 30s) — is correctly reasoned: a longer `expect(...).toBeVisible({timeout})` can only convert a timeout-failure into a pass if the row was going to appear anyway, never the reverse, so it cannot turn a legitimately-passing assertion into a failure. Confirmed live (below) that this test still passes, well inside the old 20s bound (5.9s).
3. **`dropdown.rs` citation (was 🟢)** — corrected to `components/dropdown.rs:520` for the `aria-haspopup="listbox"` attribute, `SelectVariant::Form` arm spanning `:512-533`. Recounted the source myself with `awk 'NR>=510 && NR<=535'`: `SelectVariant::Form => {` is exactly line 512, `aria-haspopup="listbox"` is exactly line 520, the arm's closing `}` is exactly line 533. My original ~508-527 guess was wrong; the correction is exactly right.

**New verification performed this cycle**:

- **Typecheck, independently reproduced**, not taken on report: installed `typescript@5.9.3` and `@types/node@22` (`--no-save`) into the real `e2e/node_modules` `npm ci` had already built, then ran `tsc --noEmit --strict --target es2020 --module esnext --moduleResolution bundler --skipLibCheck tests/sync/*.ts` → 0 errors, and again with `--noUnusedLocals --noUnusedParameters` → 0 errors. `git status` on `e2e/package.json`/`package-lock.json` confirmed clean afterward (`--no-save` held).
- **Live run, both against a real server**: built `crates/trakkt-ui` (`trunk build`) and `trakkt-server` (`cargo build --locked --profile dev-server`) to completion in this worktree, started the server against a throwaway `postgres:16` container (`trakkt-review-pg`, port 5439), and ran the suite twice with `BASE_URL=http://localhost:3100 npx playwright test --config playwright.sync.config.ts`:
  - First run: `tests/sync/hoisted-counters.spec.ts` alone — 4/4 passed, each with a `[delivered-by-counter]` log line showing the real sync frame → refetch causality the tests claim (e.g. `issue_relation insert frame for "<uuid>" ... list_issue_relations re-read at +1534ms`).
  - Second run: the full suite, all 5 files — **21/21 passed** in 4.0m, including the three files whose call sites were touched by the `createIssueViaUi` extraction (`two-window-sync.spec.ts`, `milestone-realtime.spec.ts`, `project-view-state.spec.ts`), confirming no behavioral regression from the extraction.

**Anti-pattern tally**: none outstanding.

**Verdict: APPROVE.** Zero 🔴, zero 🟡. Both prior 🟡s and the 🟢 are fixed and independently re-verified against source, not taken on report — including catching that the fix was more thorough than my own original finding (a third duplicate site I had missed). Typecheck and a full live two-browser run against a real built server both pass. Signed via `scripts/sign-review.sh`.

---
