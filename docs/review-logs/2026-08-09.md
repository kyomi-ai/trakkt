# Review Log — 2026-08-09

---

### 20:10 — TRA-9997: browser-verify the six hoisted SyncStore version counters

**Review type**: initial
**Result**: 2 🟡, 1 🟢
**Files**: `e2e/tests/sync/hoisted-counters.spec.ts`, `e2e/tests/sync/realtime-harness.ts`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Spec Adherence / Comment Accuracy | e2e/tests/sync/hoisted-counters.spec.ts:19-27 | Header narrative claims PR #292/TRA-9991 "moved six … calls out of the reactive closures that read them" causing a live-update regression ("simply stops updating"). The repo's own `docs/review-logs/2026-08-03.md` (12:40 entry) independently verified and closed TRA-9991's premise as **false**: dependency tracking (`.get()`/`.track()`) was preserved exactly at all six sites: the hoist moved only where the `Signal` *handle* is constructed, not the reactive read. Confirmed against current source: every one of the six call sites still calls `.get()`/`.track()` on the hoisted handle from inside the closure that needs to track it. #292's own commit message (b59d44c) states this explicitly: "So this is a robustness fix, not a bug fix" and "running the full browser suite passes 59/59." The real risk documented at `crates/trakkt-ui/src/cache/store.rs:98-140` is disposal panics from a wrapper resolved *inside* a re-running effect/memo/subtree, not silent staleness. |
| 2 | 🟡 | Copy-Paste / Missing Abstractions | e2e/tests/sync/hoisted-counters.spec.ts:108-121 | `createIssue()` duplicates the "create an issue through `/workspace`" flow already inline in `two-window-sync.spec.ts:134-147` — same selectors (`#issue-title`, `New Issue`, `Create Issue`, `a[href*="/issues/"]`), same comments about `/workspace` vs `/issues`, differing only in timeout (20s vs 30s) and parameterization. Belongs in `realtime-harness.ts` as a shared helper both spec files call. |
| 3 | 🟢 | Comment Accuracy (minor) | e2e/tests/sync/hoisted-counters.spec.ts:91-93 | Cites `components/dropdown.rs:512-533` for the `aria-haspopup="listbox"` button; the `SelectVariant::Form` trigger is actually at ~508-527 in the checked-out source. Off by a few lines, not load-bearing. |

**Anti-pattern tally**: Comment Accuracy ×2, Copy-Paste/Missing Abstractions ×1

**Notes**: This is an unusually well-built test file — every selector, entity-type string, sync-payload field, and cited line number was cross-checked against the actual source (`project_detail.rs`, `issue_detail.rs`, `dropdown.rs`, `project_service.rs`, `relations.rs`, `relation_service.rs`, `models.rs`, `apply.rs`, `sync.rs`) and all but the one 🟢 held up exactly. The three new tests (relations, project updates, project members) each genuinely discriminate the regression they claim to guard: all three counters feed on-demand server-function reads with no other subscriber (`apply.rs` comments confirm `NOT_CACHED`), so removing the counter from the resource key would leave window B frozen with no other path to update it — verified this is not a vacuous check. `waitForEntityId` is sound: fails loudly on timeout, searches from `t: 0` deliberately (bootstrap streams the same frame shape, documented and correct), and its one caller's discriminating id (a server-minted UUID) has no cheaper source. `expectDeliveredByCounter` reuse is correct and its single-caller-per-entity-type discriminating strings are genuinely unique (random hex, project id scoped to this run). The nav-cycle disposal test's `window.__trakktNavCycle` marker is a deliberate, justified choice over reusing `probe.navigations` (which fires on the SPA's own client-side route changes and would produce false positives here, unlike the other three tests whose windows never navigate mid-test). Flake discipline (timeouts, `markWhenQuiet`, no-navigation guards) matches the existing suite's standard throughout. The two 🟡s are both cheap to fix — a comment rewrite and a ~15-line extraction — and neither reflects a functional defect in the tests themselves; both were called for explicitly by the review brief's own checklist (accuracy of "why this exists" narrative, and copy-pasted harness setup). Attempted a live build of the WASM bundle + `dev-server` profile + a throwaway Postgres 16 container to actually run the suite; build did not finish inside the review window (WASM+server cold build is ~15-25 min per this repo's own CI timing notes), so live pass/fail was not obtained — static verification against source was exhaustive enough to reach a verdict without it, but a live run is recommended before merge and would be cheap to add to the next review cycle once these two items are fixed.

---

### 21:20 — TRA-9997: re-review after both 🟡 and the 🟢 addressed

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `e2e/tests/sync/hoisted-counters.spec.ts`, `e2e/tests/sync/realtime-harness.ts`, `e2e/tests/sync/two-window-sync.spec.ts`, `e2e/tests/sync/milestone-realtime.spec.ts`, `e2e/tests/sync/project-view-state.spec.ts`

**Prior findings, verified resolved, not taken on report**:

1. **Header narrative (was 🟡 #1)** — rewritten (`hoisted-counters.spec.ts:16-75`) to state that #292 moved only *where* each getter is called (setup vs. per-evaluation), not what is tracked, and that TRA-9991's "latent panic"/live-update-regression framing was investigated and closed false in `docs/review-logs/2026-08-03.md`. Independently re-verified every line citation against current source, all exact: `v.track()` at `issue_detail.rs:872`; `.get()` at `:1994` and `:2392`; `project_detail.rs:330`, `:345`, `:355` (the `.get()` reads); `:325`, `:340`, `:350` (the `let X_version = sync_store.map(...)` bindings the fourth test's comment cites); `:863`, `:1987`, `:2383` in `issue_detail.rs`. The fourth (disposal) test's comment was rewritten in the same spirit and now states plainly: "Nothing here claims the current code has that defect — the review that cleared #292 established it does not." Matches `store.rs:98-140`'s actual contract (disposal-on-reuse, not dropped tracking). No overclaim found anywhere in the new text.
2. **`createIssue` duplication (was 🟡 #2)** — extracted to `realtime-harness.ts` as `createIssueViaUi(page, title)`. The requester correctly caught a third pre-existing copy I had missed (`milestone-realtime.spec.ts:272-285`) in addition to the one I flagged (`two-window-sync.spec.ts`) — all three now call the shared helper, ~47 lines of duplication removed. Diff-reviewed all three call-site changes (`two-window-sync.spec.ts`, `milestone-realtime.spec.ts`, `project-view-state.spec.ts`): each is a mechanical swap, same selectors, same assertions, no logic changed beyond the call. The one deliberate behavior change — `two-window-sync.spec.ts`'s created-row wait going from 20s to 30s (the helper standardizes on the majority's 30s) — is correctly reasoned: a longer `expect(...).toBeVisible({timeout})` can only convert a timeout-failure into a pass if the row was going to appear anyway, never the reverse, so it cannot turn a legitimately-passing assertion into a failure. Confirmed live (below) that this test still passes, well inside the old 20s bound (5.9s).
3. **`dropdown.rs` citation (was 🟢)** — corrected to `components/dropdown.rs:520` for the `aria-haspopup="listbox"` attribute, `SelectVariant::Form` arm spanning `:512-533`. Recounted the source myself with `awk 'NR>=510 && NR<=535'`: `SelectVariant::Form => {` is exactly line 512, `aria-haspopup="listbox"` is exactly line 520, the arm's closing `}` is exactly line 533. My original ~508-527 guess was wrong; the correction is exactly right.

**New verification performed this cycle**:

- **Typecheck, independently reproduced**, not taken on report: installed `typescript@5.9.3` and `@types/node@22` (`--no-save`) into the real `e2e/node_modules` `npm ci` had already built, then ran `tsc --noEmit --strict --target es2020 --module esnext --moduleResolution bundler --skipLibCheck tests/sync/*.ts` → 0 errors, and again with `--noUnusedLocals --noUnusedParameters` → 0 errors. `git status` on `e2e/package.json`/`package-lock.json` confirmed clean afterward (`--no-save` held).
- **Live run, both against a real server**: built `crates/trakkt-ui` (`trunk build`) and `trakkt-server` (`cargo build --locked --profile dev-server`) to completion in this worktree, started the server against a throwaway `postgres:16` container (`trakkt-review-pg`, port 5439), and ran the suite twice with `BASE_URL=http://localhost:3100 npx playwright test --config playwright.sync.config.ts`:
  - First run: `tests/sync/hoisted-counters.spec.ts` alone — 4/4 passed, each with a `[delivered-by-counter]` log line showing the real sync frame → refetch causality the tests claim (e.g. `issue_relation insert frame for "<uuid>" ... list_issue_relations re-read at +1534ms`).
  - Second run: the full suite, all 5 files — **21/21 passed** in 4.0m, including the three files whose call sites were touched by the `createIssueViaUi` extraction (`two-window-sync.spec.ts`, `milestone-realtime.spec.ts`, `project-view-state.spec.ts`), confirming no behavioral regression from the extraction.

**Anti-pattern tally**: none outstanding.

**Verdict: APPROVE.** Zero 🔴, zero 🟡. Both prior 🟡s and the 🟢 are fixed and independently re-verified against source, not taken on report — including catching that the fix was more thorough than my own original finding (a third duplicate site I had missed). Typecheck and a full live two-browser run against a real built server both pass. Signed via `scripts/sign-review.sh`.

---

### 20:39 — TRA-9986: isolated wasm test sweep (scripts/wasm-test-isolated.sh)

**Review type**: initial
**Result**: 1 🟡, 0 🟢
**Files**: `scripts/wasm-test-isolated.sh`, `.github/workflows/ci.yml`, `CONTRIBUTING.md`, `docs/CODING_STANDARDS.md`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Spec Adherence | `.github/workflows/ci.yml:344-376` (and no ticket comment) | TRA-9986's body gives three explicit CI-cost mitigation options — per-PR sweep, nightly/merge-only, or randomized ordering with a printed seed — and says unconditionally "State which you chose and why," with an additional instruction to escalate if the trade-off is close. The implementing agent's own pickup comment on the ticket promised "I'll measure the actual added wall-clock first and state the choice with the numbers behind it." The diff does measure the wall-clock cost exhaustively (`--list` 5s, whole-binary 25s, 13 modules 131s, ~136s marginal, job growing from ~200-250s to a projected ~6min) but never states *why* every-PR beats nightly-only or randomized ordering, and never mentions those alternatives anywhere — not in `ci.yml`'s comments, not in `CONTRIBUTING.md`, not in `docs/CODING_STANDARDS.md`, not as a follow-up ticket comment, and no `needs-jason` escalation ticket exists (checked via `mcp__trakkt__search_issues`). A recurring ~55-65% increase to every PR's CI job duration is exactly the kind of judgment call the ticket asked to see reasoned about explicitly rather than defaulted into. This is trivially fixable in-diff — a few sentences either in the `ci.yml` comment block (which already carries the rest of this reasoning) or as a ticket comment — so it should be added now rather than tracked separately. |

**Anti-pattern tally**: Spec Adherence ×1

**Notes**: The technical implementation is excellent and I could not break it. Verified independently, not just by reading: (1) the reverted demonstration file (`crates/trakkt-ui/src/pages/settings/notifications.rs`) is byte-identical to `main` — `git diff main..HEAD -- <file>` is empty, `git status --porcelain` shows only the four intended files staged, no residue; (2) the original PR #285 regression genuinely crossed module boundaries (`cache/sync_engine.rs`'s helper, called from two tests there, was silently relied on by tests in `pages/settings/notifications.rs`, `pages/settings/live_update.rs`, and `pages/settings/workspace.rs` — four distinct files/modules under this script's per-file derivation), so the per-module sweep would have caught the real historical bug, not just the constructed single-file demo; (3) manually ran the module-derivation logic (steps 1-2 of the script) against the actual source tree — it produces exactly 13 modules covering exactly 74 `#[wasm_bindgen_test]` occurrences (both independently `grep`-counted), matching every number the script's own comments and the `ci.yml` timeout justification cite; (4) checked for the substring-collision failure mode the review brief called out by name (a short module name being a prefix of another's) — none exists in the current 13-module set, and by construction any hypothetical case is provably self-catching: an over-broad filter's extra matches always belong to some other file's own (correctly derived) module, so they get claimed twice and fail the step-3 double-claim check rather than passing silently; (5) traced every claimed guarantee from the review brief's item 2 (cross-check against `--list` before any browser launch, set-wise not count-wise coverage, `selected == expected` and `selected + filtered == total` per run, `Σselected` reconciled against the whole-binary total) against the actual script logic — all four hold, including on the wasm-pack-crash path (a harness that never prints "test result:" is treated as a hard failure, never as zero) and the empty-derivation path (`set -u` on an empty `modules[@]` is caught explicitly rather than left to an "unbound variable" message); (6) confirmed the script is staged at mode 100755, cd's to the repo root via `${BASH_SOURCE[0]}` so it behaves identically regardless of invoking cwd, uses `IFS= read -r` throughout so filenames with spaces are safe, and guards every grep/comm-style zero-match case that would otherwise trip `set -e` under pipefail; (7) the CI timeout math (20→25) is internally consistent with the stated local measurements and the file's own prior 20-minute justification, and the stale "0.7s test time, 12 tests" claim was correctly corrected to "8.6s, 74 tests." Docs (`CONTRIBUTING.md`, `docs/CODING_STANDARDS.md`) accurately describe what the script does, including the honestly-disclosed limitation that an intra-module ordering dependency is still invisible. The one gap is process, not code: the ticket's explicit "state which you chose and why" for the three-way CI-cost trade-off was never delivered. Not signing until that's added — it is cheap, and per this project's own carve-out rules a MAJOR with no tracking ticket blocks sign-off regardless of how strong the rest of the diff is.

---

### 20:58 — TRA-9986: re-review, comment-only rationale addendum

**Review type**: re-review (cycle 2)
**Result**: 1 🟡, 0 🟢
**Files**: `.github/workflows/ci.yml` (5 new paragraphs above the `wasm-test` job; no other file changed)

**Prior finding, verified resolved**:

1. **Missing "why this option and not the others" (was 🟡 #1)** — five new paragraphs added above the `wasm-test` job state per-PR was chosen and why it's a step in this job rather than a new one, restate the measured costs, reject nightly/merge-only on the numbers (~2min marginal buys almost no PR wall-clock, trades it for late detection), and reject randomized ordering with the runner's evidence rather than a guess. Resolved.

**New verification performed this cycle** (independent, not taken on report):

- **Functional-change scope**: confirmed via `git diff HEAD -- crates/` (empty), `git ls-files -s scripts/wasm-test-isolated.sh` (blob `7a1b98e...`, identical to cycle 1), and byte-for-byte diff of `CONTRIBUTING.md`/`docs/CODING_STANDARDS.md` against cycle 1 (unchanged). The only diff is the new comment block in `ci.yml`; the three functional changes (`timeout-minutes: 20→25`, step name, `run:` line) are untouched from cycle 1.
- **Projection vs. measurement labeling**: the new text explicitly says "That is a projection, not a measurement," states its anchor (0.99 local:CI ratio, 8.58s CI vs 8.69s local for the identical 74-test in-browser portion), and separates measured-local numbers (`--list` 5.3s, whole-binary 25.2s, 13 module runs 130.9s) from the projected CI total (~6min vs ~3.7min today). Clear and accurate.
- **The 60% figure, independently recomputed**: baseline job ≈ 222s (≈3.7min, matches the six-run range from the existing timeout comment), new marginal ≈ 136s (5.3s `--list` + 130.9s of module runs — correctly excluding the 25.2s whole-binary run, which already existed as this job's sole step pre-change, not new cost). 136/222 = 61.3%, correctly stated as "roughly 60%." Both the before (~3.7min) and after (~6min) figures are given inline, so a reader can check the percentage rather than take it on faith — satisfies `docs/CODING_STANDARDS.md:44-45` ("a comment that makes a checkable claim must be checked"). The rejected "~25%" framing (the coordinator's own initial figure) was correctly identified as measuring a different thing (the timeout ceiling bump, 20→25) and dropped rather than conflated with the cost figure.
- **The "no ordering control" claim, verified against the actual binary, not the doc's word**: `Cargo.lock` pins `wasm-bindgen-test 0.3.71`, matching the version cited. Ran `wasm-bindgen-test-runner --help` (the actual installed runner) directly — its full flag set is exactly `--bench --include-ignored --ignored --exact --skip --list --nocapture --format` plus a positional `FILTER`, with no shuffle/seed flag anywhere. The comment's claim is not just plausible, it is exactly what the binary reports.
- **Cross-doc agreement on the intra-module limitation — NOT satisfied, see finding below.**

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Comment/Doc Accuracy | `.github/workflows/ci.yml:337-339` | New text: "a module is still run as a whole, so one contained entirely within a single module stays invisible, as `docs/CODING_STANDARDS.md` and `CONTRIBUTING.md` **both record**." `docs/CODING_STANDARDS.md` does say this ("What it cannot see is an ordering dependency *within* one module, since a module is still run as a whole" — unchanged since cycle 1). `CONTRIBUTING.md` does not: its WASM Browser Tests section (added in cycle 1, unchanged here) only says a *single whole-binary run* "cannot see the dependency" — describing the problem the sweep fixes, never the sweep's own remaining blind spot for a dependency confined to one file. Re-read the live file directly (not just the diff) to be sure no context line covers it — it doesn't. This is exactly the failure mode `docs/CODING_STANDARDS.md:44-45` names: a checkable claim ("CONTRIBUTING.md records this") that doesn't hold up when checked. Fix by either adding the intra-module caveat to `CONTRIBUTING.md` so the claim becomes true, or narrowing the `ci.yml` sentence to cite only `docs/CODING_STANDARDS.md`. |

**Anti-pattern tally**: Comment/Doc Accuracy ×1

**Notes**: Everything the coordinator asked me to specifically adjudicate checked out correctly — the projection is properly labelled and anchored, the 60% figure is arithmetically right and presented with both underlying numbers so it's independently checkable, and the randomized-ordering rejection was verified against the real installed `wasm-bindgen-test-runner --help` output rather than taken on faith. The one item that didn't survive independent checking is the specific thing the coordinator flagged as needing agreement, not just non-collision, across all three docs: the claim that `CONTRIBUTING.md` records the intra-module blind spot is false as written. `docs/CODING_STANDARDS.md` is fine on its own; the `ci.yml` sentence just over-claims what `CONTRIBUTING.md` says. Small, cheap fix, but it is precisely the class of defect this repo's own standard was written to catch, so not signing until it's corrected. Not signed.

---

### 21:12 — TRA-9986: re-review, CONTRIBUTING.md-only fix for the cross-reference

**Review type**: re-review (cycle 3)
**Result**: Clean (approved)
**Files**: `CONTRIBUTING.md` only; `.github/workflows/ci.yml` and `docs/CODING_STANDARDS.md` untouched this cycle

**Prior finding, verified resolved**:

1. **False cross-reference claim (was 🟡 #1)** — `CONTRIBUTING.md`'s WASM Browser Tests section, read live: "A whole-binary run produces one order and only one, so on its own it cannot see that dependency at all — which is how the suite stayed green while the dependency was in it," immediately followed by "A dependency contained entirely within one module will not be caught: the granularity is per source file, so a module's own tests still run together and can still lean on each other." That second sentence is the fact `ci.yml:337-339` claims both docs record, and now it genuinely does — it says the same thing as `docs/CODING_STANDARDS.md`'s unchanged "What it cannot see is an ordering dependency *within* one module, since a module is still run as a whole," just in different words. Re-read `ci.yml:337-339` live against both files side by side; the claim checks out now.

**Overclaim check, both directions, as specifically requested**:

- **(a) the old-problem paragraph does not read as "the sweep has no blind spot."** It is scoped strictly to "a whole-binary run" (singular, the pre-fix mechanism), and the very next paragraph immediately states the sweep's own remaining gap. No implication the new mechanism is complete.
- **(b) the new blind-spot paragraph does not read as "the sweep is unreliable in general."** It explicitly says a cross-module dependency "fails the check" (i.e., the sweep does catch it), and scopes the miss precisely to "contained entirely within one module." The closing line ("Do not treat a green sweep as proof that a new test stands alone") is tied to a specific, narrow scenario — a new test that hasn't been isolated against a fresh module — with a concrete mitigation (`boot_leptos_executor()`), not a blanket disclaimer.

**Housekeeping, independently verified**:

- Script blob hash: `git ls-files -s scripts/wasm-test-isolated.sh` → `7a1b98ee154e5850ccba4fb7674453425463c518`, unchanged from cycles 1 and 2. (The `9458f4cb` intermediate the author flagged was never the hash I reviewed at any point — nothing to reconcile.)
- Scope: `git diff HEAD -- crates/` empty; `git diff --cached -- docs/CODING_STANDARDS.md` empty (byte-identical to cycle 2); `git diff --cached -- .github/workflows/ci.yml` byte-identical to what I reviewed and approved-pending-fix last cycle (confirmed via direct re-read of lines 330-378, not just trusting the stat). Only `CONTRIBUTING.md` changed, as the coordinator described.
- `docs/review-logs/2026-08-09.md` remains unstaged in the worktree per the coordinator's note; this entry appended to it.

**Anti-pattern tally**: none outstanding.

**Verdict: APPROVE.** Zero 🔴, zero 🟡. All three docs now genuinely agree on the intra-module limitation rather than merely not colliding, neither new paragraph overclaims in either direction, and every number and cross-reference touched across all three review cycles has been independently re-verified against live source, the actual `Cargo.lock` pin, and the real installed `wasm-bindgen-test-runner` binary — not taken on report at any point. Signed via `scripts/sign-review.sh`.

---
