---

### 17:22 — TRA-9916: Per-connection sync responses (fix cross-tab bootstrap/delta leakage)

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 2 🟢
**Files**: `apps/server/src/routes/websocket.rs`, `crates/trakkt-auth/src/websocket/manager.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Code Quality (doc accuracy) | apps/server/src/routes/websocket.rs:271-274 | `to_sync_values` doc comment says it replaces "11 copy-pasted" blocks; actual count of removed `filter_map(\|x\| serde_json::to_value(x).ok())` blocks is 10 (workspace_settings was never one of them — it's a single `Option<Value>`, not a list). Cosmetic only. |
| 2 | 🟢 | Design note (not a defect) | apps/server/src/routes/websocket.rs:339-356 | `send_sync_response` returning `false` on a serialization failure aborts the *entire remaining bootstrap* (all subsequent entity-type batches via the `stream_batch!` macro's `return`), not just the current batch/item. This is intentional per the implementer's stated rationale (never let `SyncComplete` outrun what was sent) and is consistent across `handle_sync_bootstrap`/`handle_sync_delta`/`stream_entities`. In practice this path is close to unreachable since the data going into `SyncAction.data` is already a validated `serde_json::Value` from `to_sync_values` — worth knowing but does not need a change. |

**Anti-pattern tally**: none (0 🔴/🟡)

**Notes**: Clean, well-reasoned fix for a real cross-tab cache-corruption bug. Verified the core invariant by tracing every call site: `conn_tx` (the per-connection `mpsc::Sender`) is threaded from `WebSocketManager::connect()` through `recv_task` into `handle_client_message` → `handle_sync_bootstrap`/`handle_sync_delta` → `stream_entities`/`send_sync_response`, with no clone or escape into any longer-lived scope — confirmed via grep (no other reference to `conn_tx` outside `websocket.rs`, no `.clone()` on it). Task-lifetime concern (item 2 in review brief) is safe: `tokio::select!` detaching the loser doesn't abort it, but the only sender that matters for `send_task`'s `manager_rx.recv()` termination is the manager's own clone (dropped at `disconnect()`); `recv_task`'s extra clone of `conn_tx` does not create a new leak class since tokio's `mpsc::Sender::send().await` returns `Err` immediately once the receiver is dropped (no blocking). Early-abort in `handle_sync_bootstrap` is real — the `stream_batch!` macro's `return` prevents any subsequent `stream_batch!` line (and its `to_sync_values` serialization) from ever running, and skips `SyncComplete`. Backpressure is safe: bounded-channel `.send().await` only blocks while the receiver is alive-but-slow (intended), and resolves immediately on receiver-drop — no deadlock. Deletion of `send_to_user_raw_backpressure`/`deliver_backpressure`/`deliver_to_local_user_backpressure` is clean — zero remaining references anywhere in the workspace, and `broadcast_sync_action`/`broadcast_raw_to_workspace`/`deliver_to_local_user` (the live-broadcast path, explicitly out of scope) are untouched and verified via a dedicated test (`live_broadcast_still_reaches_every_connection_of_the_user`) that still fans out to all of a user's connections. The two-connection isolation test (`connection_sender_addresses_only_its_own_connection`) and the early-stop test (`stream_entities_stops_early_when_the_connection_dies`, using a capacity-1 channel to force a tight bound on in-flight frames and asserting `delivered <= 2` out of 50 items) both genuinely exercise the invariant rather than passing trivially. `cargo build --workspace --tests`, `cargo check -p trakkt-auth -p trakkt-server --tests`, and `cargo test -p trakkt-auth -p trakkt-server websocket::` all pass cleanly (16/16 new+existing tests green, zero warnings in the changed crates). No lint suppressions, no `let _ =`, no silent `.ok()`/`unwrap_or_default()` on serialization anywhere in the diff — the `to_sync_values` refactor actually *improves* standards compliance versus the old code by adding a `tracing::warn!` on serialization failure where the old `filter_map(...).ok())` pattern silently dropped it. Both judgment calls flagged by the implementer are the right call and are applied consistently. Approved as-is.

---
