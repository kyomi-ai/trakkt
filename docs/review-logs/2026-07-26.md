---

### 22:19 — TRA-9918: Single FIFO IndexedDB writer (fix cursor-ahead-of-entities durability hole)

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 3 🟢
**Files**: `crates/trakkt-ui/src/cache/idb_writer.rs` (new), `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/cache/store.rs`, `crates/trakkt-ui/src/cache/db.rs`, `crates/trakkt-ui/src/cache/mod.rs`, `crates/trakkt-ui/Cargo.toml`, `Cargo.toml`, `Cargo.lock`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Design judgment call (verified sound) | `idb_writer.rs:205-219` | A **failed** `DeleteAllOfType` leaves `failed_writes` untouched (neither incremented nor cleared), so `SetCursor("0")` following it is not poisoned. Traced the consequence: rewinding to "0" despite the wipe failing is actually the *correct* choice given the existing cursor-based retry mechanism — poisoning here would strand the cursor at its old stale value, causing the next reconnect to retry `sync_delta` from the very cursor the server already rejected (re-triggering the loop `SyncReset` exists to break), rather than retrying the wipe via the no-cursor bootstrap path. Residual risk (zombie entities of the un-wiped type lingering if the server deleted them during the staleness window and no further reconnect/reset occurs in-session) is real but narrow, and the realistic trigger for a delete-range failure is a concurrent version-change close from another tab — already out of scope per TRA-9919. Worth a follow-up ticket to log louder (`tracing::error!`) or retry the specific failed wipe; not blocking. |
| 2 | 🟢 | Copy-Paste / residual fire-and-forget (pre-existing, unchanged) | `store.rs:385-407`, call sites at `layout.rs:1143`, `project_detail.rs:1999`, `team_settings.rs:177` | The three UI-initiated `remove_team`/`remove_project` call sites still delegate to `delete_from_idb`, which opens its own independent `CacheDb` connection and writes outside the FIFO writer queue entirely — the exact "fire-and-forget with its own DB connection" pattern this PR eliminates everywhere else. Confirmed this is intentional and behavior-identical to pre-PR (all three sites are unchanged one-line calls; `remove_*` delegates to `remove_*_in_memory` + the same `delete_from_idb` as before). Not a regression and cannot violate the cursor invariant under review (it never touches the cursor), but it is a residual inconsistency with the new "one writer owns all cache writes" architecture — worth routing through `IdbWriter` in a follow-up so the invariant is structurally total rather than total-except-three-call-sites. |
| 3 | 🟢 | Code Quality | `idb_writer.rs:172` | `failed_writes: usize` could saturate only after ~2^64 failed writes in one stream — non-issue in practice, noting only because it's the one piece of mutable state carried across the whole loop lifetime. |

**Anti-pattern tally**: Design judgment (verified sound) ×1, Copy-Paste/residual fire-and-forget ×1, Code Quality ×1

**Notes**: This is a well-executed fix for a real, previously-shipped durability bug. Verified rather than trusted on every review priority: (1) Traced all three `SetCursor` paths (`SyncComplete`, `SyncReset`'s rewind, and confirmed the connect-effect's no-cursor branch never itself writes a cursor — it only wipes+flushes+requests bootstrap, with the actual cursor write deferred to that bootstrap's own later `SyncComplete`) — FIFO ordering holds in all three because `mpsc::UnboundedReceiver` drains strictly in send order and WASM's single-threaded cooperative scheduling means `enqueue()` (synchronous, non-yielding) calls from different call sites can never interleave mid-call. (2) `Unavailable` sink: confirmed `Flush` never touches the sink at all (resolves unconditionally, so no startup hang regardless of cache-open failure) and confirmed `set_cursor`/`upsert`/`delete` all report `Err` via `parts()`, so the cursor genuinely cannot advance when the cache never opened. (3) Assessed the `DeleteAllOfType`-success-clears-the-counter deviation from spec — reasoning sound and correctly implemented (see 🟢 #1 above for the one edge not obviously covered by the stated rationale, which I independently verified is still safe). (4) Grepped every `initialized()` read site across `crates/trakkt-ui/src` (10+ call sites, all page-level skeleton/loading gates) — none depend on "initialized implies cursor durable," confirming synchronous `set_initialized(true)` on `sync_complete` is safe. (5) Verified the `remove_*`/`remove_*_in_memory` split: exactly 8 `_in_memory` methods, exactly 3 unchanged UI call sites (see 🟢 #2), and the sync engine's delete path enqueues via the FIFO writer including the `issue_content` companion delete for issues (`sync_engine.rs:476-478`). (6) Confirmed the dev-dependency target-gating causes no silent test loss: the only `mod tests` blocks in the crate are `idb_writer`, `date_picker`, `popover`, `auto_link` — none reference `trakkt_core`/`trakkt_auth`/`tokio` as dev-deps (those only appear as `#[cfg(feature = "ssr")]`-gated production `[dependencies]`, untouched by this diff). (7) Test quality is genuine, not a parallel implementation: `run_writer`/`IdbSink` in the native tests is the literal production function (also driving the real `CacheDbSink` on wasm32), and I independently ran both test layers myself rather than trusting the implementer's report — `cargo test -p trakkt-ui --lib` (37/37 passed, including all 10 new native tests) and `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` (3/3 passed against real IndexedDB in a real browser). Also ran `cargo check -p trakkt-ui --tests --target wasm32-unknown-unknown --features hydrate` (clean) and `cargo clippy -p trakkt-ui --lib --tests --no-deps` (only pre-existing warnings in unrelated `date_picker.rs` test code, zero new warnings). No lint suppressions, no `unwrap()`/`expect()` outside tests, no silently-discarded errors, no TODOs anywhere in the diff. Approved as-is; the two 🟢 architectural notes are recommended follow-up tickets, not blockers.

---

### 17:22 — TRA-9916: Per-connection sync responses (fix cross-tab bootstrap/delta leakage)

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 2 🟢
**Files**: `apps/server/src/routes/websocket.rs`, `crates/trakkt-auth/src/websocket/manager.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Code Quality (doc accuracy) | apps/server/src/routes/websocket.rs:271-274 | `to_sync_values` doc comment says it replaces "11 copy-pasted" blocks; actual count of removed `filter_map(\|x\| serde_json::to_value(x).ok())` blocks is 10 (workspace_settings was never one of them — it's a single `Option<Value>`, not a list). Cosmetic only. |
| 2 | 🟢 | Design note (not a defect) | apps/server/src/routes/websocket.rs:339-356 | `send_sync_response` returning `false` on a serialization failure aborts the *entire remaining bootstrap* (all subsequent entity-type batches via the `stream_batch!` macro's `return`), not just the current batch/item. This is intentional per the implementer's stated rationale (never let `SyncComplete` outrun what was sent) and is consistent across `handle_sync_bootstrap`/`handle_sync_delta`/`stream_entities`. In practice this path is close to unreachable since the data going into `SyncAction.data` is already a validated `serde_json::Value` from `to_sync_values` — worth knowing but does not need a change. |

**Anti-pattern tally**: none (0 🔴/🟡)

**Notes**: Clean, well-reasoned fix for a real cross-tab cache-corruption bug. Verified the core invariant by tracing every call site: `conn_tx` (the per-connection `mpsc::Sender`) is threaded from `WebSocketManager::connect()` through `recv_task` into `handle_client_message` → `handle_sync_bootstrap`/`handle_sync_delta` → `stream_entities`/`send_sync_response`, with no clone or escape into any longer-lived scope — confirmed via grep (no other reference to `conn_tx` outside `websocket.rs`, no `.clone()` on it). Task-lifetime concern (item 2 in review brief) is safe: `tokio::select!` detaching the loser doesn't abort it, but the only sender that matters for `send_task`'s `manager_rx.recv()` termination is the manager's own clone (dropped at `disconnect()`); `recv_task`'s extra clone of `conn_tx` does not create a new leak class since tokio's `mpsc::Sender::send().await` returns `Err` immediately once the receiver is dropped (no blocking). Early-abort in `handle_sync_bootstrap` is real — the `stream_batch!` macro's `return` prevents any subsequent `stream_batch!` line (and its `to_sync_values` serialization) from ever running, and skips `SyncComplete`. Backpressure is safe: bounded-channel `.send().await` only blocks while the receiver is alive-but-slow (intended), and resolves immediately on receiver-drop — no deadlock. Deletion of `send_to_user_raw_backpressure`/`deliver_backpressure`/`deliver_to_local_user_backpressure` is clean — zero remaining references anywhere in the workspace, and `broadcast_sync_action`/`broadcast_raw_to_workspace`/`deliver_to_local_user` (the live-broadcast path, explicitly out of scope) are untouched and verified via a dedicated test (`live_broadcast_still_reaches_every_connection_of_the_user`) that still fans out to all of a user's connections. The two-connection isolation test (`connection_sender_addresses_only_its_own_connection`) and the early-stop test (`stream_entities_stops_early_when_the_connection_dies`, using a capacity-1 channel to force a tight bound on in-flight frames and asserting `delivered <= 2` out of 50 items) both genuinely exercise the invariant rather than passing trivially. `cargo build --workspace --tests`, `cargo check -p trakkt-auth -p trakkt-server --tests`, and `cargo test -p trakkt-auth -p trakkt-server websocket::` all pass cleanly (16/16 new+existing tests green, zero warnings in the changed crates). No lint suppressions, no `let _ =`, no silent `.ok()`/`unwrap_or_default()` on serialization anywhere in the diff — the `to_sync_values` refactor actually *improves* standards compliance versus the old code by adding a `tracing::warn!` on serialization failure where the old `filter_map(...).ok())` pattern silently dropped it. Both judgment calls flagged by the implementer are the right call and are applied consistently. Approved as-is.

---

### 19:01 — TRA-9917: Real sync_id on live broadcasts + disconnect-on-full for saturated connections

**Review type**: initial
**Result**: 0 🔴, 1 🟡, 1 🟢
**Files**: `apps/server/src/routes/websocket.rs`, `crates/trakkt-auth/src/websocket/manager.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/{activity,archive,attachment,comment,favorite,issue,label,notification,project,relation,release,team,view}_service.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Architecture / State Management (shared resource, no isolation) | `crates/trakkt-auth/src/websocket/manager.rs:438-470` (`deliver_to_local_user`), interacting with `apps/server/src/routes/websocket.rs:354-504` (`handle_sync_bootstrap`) | `sync_bootstrap`/`sync_delta` stream through the *same* per-connection bounded `mpsc` channel (capacity 1024, `WS_CHANNEL_CAPACITY`) that live broadcasts use. Bootstrap uses real backpressure (`.send().await`, never fails) so it can hold the channel near-full for the whole time a large workspace streams out. Any live edit landing via `deliver_to_local_user`'s `try_send` during that window sees `Full` and now kills the connection outright (previously it just dropped one message). This workspace itself already has >9,900 issues and `list_issues`/bootstrap has no pagination cap, so a bootstrapping client in an active workspace is a realistic, not hypothetical, trigger — every concurrent edit by any teammate during the bootstrap window can abort and restart that client's bootstrap. Net effect: the fix for "silent staleness" can make busy, large workspaces harder to ever finish syncing. Recommend decoupling bulk catch-up traffic from the live-broadcast kill trigger (separate channel/path for bootstrap+delta, or exempt a connection from disconnect-on-full until its first `SyncComplete`), or file a tracked follow-up ticket before merge if this is accepted as-is. |
| 2 | 🟢 | Behavior change (verified inconsequential today) | `apps/server/src/routes/websocket.rs:170-186` | `send_task.abort()`/`recv_task.abort()` now run unconditionally after the `tokio::select!`, so on a *normal* client-initiated close the server no longer runs `ws_sender.close().await` (the old code let the loser task self-terminate via channel-drop and send a courtesy close frame). Checked `crates/trakkt-ui/src/cache/websocket.rs:374-391` — the client's `onclose` handler branches on its own `intentional_close` flag, not `event.code()`, so this is currently harmless to reconnect behavior. Worth a one-line comment noting the trade-off was made deliberately, since it's a minor RFC 6455 handshake-completeness regression that could matter to a future proxy/observability consumer. |

**Anti-pattern tally**: Architecture / State Management ×1

**Notes**: The core mechanism (kill-signal-based forced disconnect) is correct and well-tested: `Arc<Notify>` shared between `TrackedConnection` (in the DashMap) and `ConnectionHandle` (held by the connection's task), `notify_one()`'s stored-permit semantics correctly survive firing before `.notified()` is first polled (verified against Tokio's documented behavior and empirically via `saturated_connection_is_killed_and_unregistered`, which calls `deliver_to_local_user` — and thus `notify_one()` — before ever awaiting `.notified()`). `.abort()` on both `send_task`/`recv_task` genuinely closes the socket since they own the split sink/stream halves and nothing else references them. Traced all 45 `broadcast_sync_action` call sites, including every loop (`archive_service::run_archive_sweep`, `release_service::create_release`, `team_service::delete_team`, `issue_service::update_issue`'s blocked-issue re-broadcast): each `sync_id` binding is freshly scoped per iteration from its own immediately-preceding `write_sync_entry`, no cross-contamination. Compile-time enforcement (`sync_id: i64` is non-optional) means a missed call site would have failed `cargo check` — ran it clean across the workspace. `issue_service.rs:876-912`'s restructure is behavior-preserving and strictly better: it now logs the previously-silent blocked-issue serialization failure via `tracing::warn!`, and correctly binds `sync_id = 0` when that serialization fails (no sync_log write attempted). `team_service.rs:195-213`'s choice to broadcast the Insert entry's `sync_id` (not the immediately-following member-add Update's) is deliberate and correct for any gap-recovery strategy that re-fetches from `frame.sync_id - 1` — broadcasting the later id would let a client that never saw this team at all skip straight past the Insert row. The `if let Err(e) = write_sync_entry(...) { warn }` → `let sync_id = write_sync_entry(...).unwrap_or_else(|e| { warn; 0 })` refactor is a straight logging-preserving transform at all 45 touched sites — confirmed identical warn! text pre/post at every occurrence, no site changed from `?`-propagation to swallowed error (none propagated before either). The 9 remaining un-refactored `write_sync_entry` sites (`project_service.rs` ×3, `status_service.rs` ×1 — the four `broadcast_sync_notify` callers explicitly out of scope per this ticket's correction comment and deferred to TRA-9929; `team_service.rs` ×3 member add/remove/role-update and `workspace_service.rs` ×2 — none of these call `broadcast_sync_action` at all, so there was nothing to thread through) are all correctly untouched. No lint suppressions, no `let _ =`, no mocks, no TODOs. `cargo check --workspace --all-targets` and `cargo test -p trakkt-auth --lib` (90/90) both green; `cargo clippy -p trakkt-auth -p trakkt-server --all-targets` shows only pre-existing warnings in files this diff doesn't touch. Blocking on finding #1 only — no ticket was cited deferring it, so the carve-out does not apply; everything else is either resolved-by-verification or non-blocking.

---

### 20:15 — TRA-9917 (re-review, cycle 2): Catch-up exemption for disconnect-on-full

**Review type**: re-review (cycle 2)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 1 🟢
**Files**: `apps/server/src/routes/websocket.rs`, `crates/trakkt-auth/src/websocket/manager.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Code Quality (doc precision) | `crates/trakkt-auth/src/websocket/manager.rs:358-359` | `CatchUpGuard`'s doc comment says "`Drop` covers the early returns, the `?`s and the panics." True for `panic = "unwind"` (the default `[profile.dev]` used by `cargo test`), but `[profile.release]` (and `dev-server`, which inherits it) sets `panic = "abort"` (`Cargo.toml:134`), under which a panic aborts the whole process before any `Drop` runs. Not a functional bug — process-abort wipes the entire in-memory `DashMap` (including the flag) on restart, so this can't produce the "silently and permanently stuck" failure mode the guard exists to prevent, and it's the same blast radius as any other panic anywhere in this server. Just imprecise wording for the deployed profile. |

**Anti-pattern tally**: none (0 🔴/🟡)

**Notes**: This closes cycle-1's sole 🟡 MAJOR finding (bootstrap/delta sharing the live-broadcast kill trigger). Verified all four things the coordinator asked me to check independently rather than accept on the implementer's word:

---

### 21:04 — TRA-9915: Lean `list_issues` rows (strip description/comments/activities)

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 2 🟢
**Files**: `crates/trakkt-api/src/issues.rs`, `crates/trakkt-types/src/api.rs`, `crates/trakkt-api/Cargo.toml`, `Cargo.lock`, docs (`docs/ARCHITECTURE.md`, `docs/book/src/**`)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Code Quality (magic number) | `crates/trakkt-api/src/issues.rs:928` | `full_page_response_stays_small`'s 25,000-byte ceiling is a hardcoded literal explained only in a comment; not derived from the lean-row byte budget, so it could silently lose its regression-catching margin if the row shape grows. Not blocking. |
| 2 | 🟢 | Docs generator quirk (pre-existing, out of scope) | `docs/book/src/api-reference/other.md` | Generator bucket name "Other" for misc endpoints (releases, etc.) is awkward but pre-existing generator behavior, unrelated to this ticket. |

**Anti-pattern tally**: none (0 🔴/🟡)

**Notes**: Verified rather than trusted the diff on all 7 review priorities. (1) Layering/sync safety: `git diff --cached --stat -- crates/trakkt-auth/ apps/server/` is empty — the service-layer `issue_service::list_issues` (called directly by `handle_sync_bootstrap` in `apps/server/src/routes/websocket.rs:381`) and `IssueWithDetails` in `crates/trakkt-types/src/models.rs` are untouched, so sync payloads keep full `description`. Traced both consumers of the modified API-layer `list_issues`: REST (`apps/server/src/routes/rest.rs:115`) and MCP's registry dispatcher (`apps/server/src/routes/mcp.rs:439`, via `op.handler` populated from `issues::operations()`) — same single handler, neither is the sync path. (2) Filtering correctness: `apply_clause`/`.retain`/`total_matched`/`.truncate` all run on the full `IssueWithDetails` vec; `IssueListRow::from` projection happens last via a 1:1 non-filtering `.map()`, on both the envelope and flat-array return paths, so `matched_count`/`returned_count`/`truncated` are unaffected by projection order. (3) Completeness: `grep -rln "ListIssuesResponse|IssueListRow"` hits only the two touched files; `IssueListRow`/`IssueListLabel` carry no heavy fields at the type level. (4) Test quality: ran `cargo test -p trakkt-api --lib` (23 passed, 1 pre-existing known failure `all_36_operations_present`/TRA-9924) and `cargo test -p trakkt-types --lib` (2 passed) — real in-memory-SQLite handler-level tests, not hand-built structs; the 25,000-byte ceiling against a measured 13,126 has real teeth (a lean row is ~273 bytes, so even a few 4KB descriptions reappearing on any subset of 48 rows blows the ceiling). (5) Docs regen: spot-checked all 8 changed doc files against current handler code — pre-existing drift (star/unstar, search offset, activities/labels/projects params) reads correctly; keeping full generator output rather than hand-reverting is correct since a hand-edited subset wouldn't be reproducible by the generator. (6) `tokio` added to `crates/trakkt-api/Cargo.toml` under `[dev-dependencies]` only; `Cargo.lock`'s one-line change is consistent. (7) No `#[allow(...)]`, no TODOs, all new `.expect()`/`.unwrap()` confined to `#[cfg(test)]`; the one pre-existing `unwrap_or_default()` at issues.rs:327 is untouched by this diff. Approved as-is.

(1) **Hole closure** — `CatchUpGuard` is created as the first statement of `handle_sync_bootstrap`/`handle_sync_delta`, before any `.await`, and dropped via RAII on every exit path (confirmed by reading the full functions, not just the diff context: all `stream_batch!` macro `return`s in bootstrap, both `SyncReset` early exits and the mid-stream abort `return` in delta all occur after guard creation). This closes the dominant vulnerable window (the actual multi-second/thousand-frame streaming phase). The documented "known residual" (flag clears when `SyncComplete` is *enqueued*, not *drained*, so up to ~1024 already-queued frames can still be sitting in the channel when the guard drops) is accurately described — traced through `send_sync_response`'s `.send().await` semantics to confirm "enqueued" is exactly when the guard drops, not "delivered to the socket."

(2) **Stuck-flag risk** — confirmed there is exactly one `notify_one()` call site in the whole codebase (`manager.rs:523`), and it's gated on `!catching_up`, so the kill signal structurally cannot fire on a connection that's mid-catch-up — a kill can only race the guard by acting on some *other* task ending first (send_task's socket write failing), and `recv_task.abort()` in that case still drops the guard correctly, because Tokio task-abort drops the task's Future in place, which runs Drop for every value live across the suspended `.await` — this is standard Rust drop semantics, not a Tokio-specific guarantee, and holds independent of *why* the task was aborted. Independently verified the "never catching up twice on one connection" claim by tracing execution rather than trusting the comment: `recv_task`'s `while let Some(Ok(msg)) = ws_receiver.next().await` loop `.await`s `handle_client_message` (which directly `.await`s into `handle_sync_bootstrap`/`delta`, no `tokio::spawn`) to completion before it can loop back and read a second client message, so a second guard on the same connection's flag cannot be created until the first one has already dropped — this is true by construction, not a race that happens to usually resolve favorably. One precision nit filed above (🟢 #1) about the panic-abort profile.

(3) **Recoverability of the dropped-during-catch-up frame** — rigorously true for `handle_sync_delta` (its `SyncComplete.last_sync_id` is derived from the max entry in the same query result it streamed, not a separately-timed read, so any edit landing after that query necessarily gets a higher `sync_id` and survives a future delta). For `handle_sync_bootstrap` the argument is sound for the dominant case (edits landing during the actual streaming phase, after the watermark read) but has a narrow theoretical gap for edits landing between guard-acquisition and the watermark read (before entity streaming even starts) — traced this back to the same pre-existing, already-filed, already-linked TRA-9923 (bootstrap watermark not snapshot-consistent with its data reads), not a new gap this diff introduces. In practice this sub-window is very unlikely to matter: the channel can't be `Full` before this bootstrap's own streaming has pushed enough frames to fill it, and no frames are pushed until after the watermark is read, so the "Full" trigger required for a drop essentially can't fire during that earlier sub-window. Coordinator's framing ("state whether TRA-9923's ordering weakens the argument") — answer: yes in theory, negligibly in practice, and it's already tracked separately rather than newly introduced.

(4) **Test determinism** — `sync_bootstrap_flags_the_connection_for_the_whole_stream` is not a real race: the guard is set unconditionally as the literal first statement of the function, before any `.await`, so by the time the spawned task is polled even once the flag is already true; `wait_until_flagged`'s polling loop with a 5s timeout only bounds *how long* the assertion waits to observe an already-deterministic fact, it doesn't depend on winning a race. Stress-tested empirically rather than reasoning about it in the abstract: 56 clean runs (30 plain + 25 with a `yes > /dev/null` background process pinning a core to simulate noisy CI, plus the initial run), 0 failures. `cargo check --workspace --all-targets`, `cargo test -p trakkt-auth --lib` (92/92), `cargo test -p trakkt-server --lib` (28/28), and `cargo clippy -p trakkt-auth -p trakkt-server --all-targets` (no new warnings, only pre-existing ones in untouched files) all green.

(5) Nothing else in the new code would have drawn a cycle-1 flag: `Ordering::Release`/`Ordering::Acquire` are used correctly and consistently for the flag's store/load pairing; the `catching_up: Arc<AtomicBool>` sharing between `TrackedConnection` and `ConnectionHandle` mirrors the already-verified `kill` signal's Arc-sharing pattern exactly; the drop-during-catch-up path still logs via `tracing::warn!` before dropping, so it stays observable rather than silent.

Approved. The one 🟢 is a doc-wording nit, not a blocker.

---

### 23:38 — TRA-9920: sync_log visibility scoping (per-user delta sync leak)

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 2 🟢
**Files**: `apps/server/migrations/20260610600000_sync_log_visibility_user_id.sql`, `apps/server/migrations-sqlite/20260610600000_sync_log_visibility_user_id.sql`, `apps/server/src/routes/websocket.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/{activity,archive,attachment,comment,favorite,issue,label,notification,project,relation,release,status,team,view,workspace}_service.rs`, `crates/trakkt-ui/src/cache/db.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Code Quality (index shape) | `apps/server/migrations/20260610600000_...sql` (idx_sync_log_visibility) | The `(workspace_id, visibility_user_id, sync_id)` index has an `OR` predicate (`visibility_user_id IS NULL OR visibility_user_id = $3`) in its main consumer query, which typically needs two index scans or a bitmap-OR rather than one clean range scan. Not a correctness issue, just a possible future perf tuning target if `sync_log` grows very large per workspace. |
| 2 | 🟢 | Minor TOCTOU (negligible impact) | `crates/trakkt-auth/src/view_service.rs:489-494` (`delete_view`) | `get_view` reads `is_shared` before the `DELETE`; a concurrent `update_view` flipping `is_shared` in that window could scope the delete's broadcast to the pre-update value instead of the value at the moment of deletion. Impact is minimal because `SyncActionType::Delete` always carries `data: None` — worst case is a benign "entity X removed" frame reaching a scope one write off, never leaking payload content. |

**Anti-pattern tally**: none (0 🔴/🟡)

**Notes**: This is a security fix for a live cross-user leak (unscoped `sync_log` delta was replaying every workspace member's notifications, favorites, and personal views to every other member). Verified rather than trusted, against all 9 stated priorities:

(1) **Classification completeness** — independently grepped all 56 `write_sync_entry` call sites (55 in `crates/trakkt-auth/src/*.rs` + 1 test call in `websocket.rs`), cross-referencing each against `trakkt_types::sync::entity_types` (18 constants total: 17 used across the 56 sites + `ISSUE_CONTENT`, which is client-local-only with zero server call sites). Dispatched an independent subagent to re-verify each of the 13 `None`-classified types (ACTIVITY, ATTACHMENT, ISSUE_ATTACHMENT, COMMENT, ISSUE, LABEL, PROJECT, PROJECT_MILESTONE, ISSUE_RELATION, RELEASE, STATUS, TEAM, WORKSPACE_SETTINGS) against their actual list/bootstrap queries — all confirmed workspace-scoped, no per-user ACL found. Also checked for other per-user-shaped tables that might have been missed entirely: `star_service.rs` (issue_stars, genuinely per-user) and `watcher_service.rs` never call `write_sync_entry` at all — pre-existing gap outside the sync protocol, not a regression from this diff.

(2) **Classification correctness (vanishing risk)** — `view_service.rs`'s new `view_visibility()` helper correctly derives from `is_shared` matching `list_views`' WHERE clause exactly (`workspace_id = $1 AND (created_by = $2 OR is_shared = TRUE)`), confirmed by reading both. `deliver_view_action` correctly branches `send_sync_action_to_user` vs `broadcast_sync_action`.

(3) **Migration backfill** — confirmed by reading `favorite_service.rs`/`view_service.rs`: both pass `data: None` to every `write_sync_entry` call, so `data->>'user_id'` would be a no-op for those two — validating the implementer's join-based approach. Confirmed `notification`/`notification_preferences` DO carry `user_id` in their serialized payload (checked `NotificationPreferences` struct and `serde_json::to_value` call sites), so the migration's `COALESCE(data->>'user_id', <join>)` for those two is correct and the plain join for favorite/view is correct. Verified every joined column (`favorites.user_id`, `views.created_by`/`is_shared`, `notifications.user_id`, `notification_preferences.user_id`) exists in the actual `CREATE TABLE` statements.

(4) **Migration mechanics** — confirmed identical filenames in both migration dirs, no version collision (`ls | sed -E 's/_.*//' | sort | uniq -d` empty in both dirs), sorts after `20260610500000_project_archived_at.sql`, no pg/sqlite filename mismatch (`diff` of sorted listings empty). SQL validated dialect-appropriate: pg `data JSONB` uses `->>` natively (no CAST needed — the CODING_STANDARDS cast rule is about whole-column JSONB→String sqlx decode, not `->>` key extraction), sqlite `data TEXT` uses `json_extract`.

(5) **Unfiltered cursor helpers** — confirmed `get_latest_sync_id`/`is_sync_id_available` return only an id/boolean, never row contents; reasoning about spurious `SyncReset` holds.

(6) **notification_preferences scope extension** — verified independently: not in `handle_sync_bootstrap`'s `stream_batch!` list, not in `trakkt-ui/src/cache/sync_engine.rs`'s entity_type dispatch (grepped, absent from both the initial-load `db::read_all` block and the live-apply match), and the UI only reads it via `get_notification_preferences` server fn (`trakkt-ui/src/pages/settings/notifications.rs:24`). Scoping it cannot cause vanishing since nothing consumes it from the sync cache.

(7) **`create_view`/`delete_view` restructuring** — confirmed `NotFound` semantics unchanged for `delete_view` (previously DELETE + `rows_affected()==0` check; now `get_view` first with the same error message, same fallback if a race deletes between the read and the DELETE). Filed the TOCTOU nuance above (🟢 #2) — negligible since deletes carry no payload.

(8) **Test quality — verified via actual mutation, not the implementer's claim**: reverted the `get_entries_since` WHERE clause visibility filter myself and ran `cargo test -p trakkt-auth --lib sync_log_service::tests` — exactly 3 tests failed (`delta_never_carries_another_members_notifications_or_favorites`, `delta_from_zero_matches_bootstrap_for_per_user_entity_types`, `shared_views_reach_every_member_and_personal_views_only_their_owner`), matching the implementer's claim precisely. Separately reverted `create_notification`'s delivery from `send_sync_action_to_user` back to `broadcast_sync_action` — exactly 1 test failed (`live_notification_frame_reaches_only_its_recipient`), also matching exactly. Restored both files afterward and confirmed `git diff` against the staged version was empty. The migration-backfill test runs the real SQLite migration file via `include_str!`, confirmed the relative path resolves correctly and the comment-stripping/statement-count assertion (`4` UPDATE statements) matches the file.

(9) **Standards** — no `#[allow(...)]`, no new silent error discards, no TODOs added (grepped the full staged diff for all three). `cargo build -p trakkt-auth -p trakkt-server` clean with zero warnings. `cargo test -p trakkt-auth --lib` (99/99) and `cargo test -p trakkt-server --lib routes::websocket` (17/17) both pass. The `trakkt-ui/src/cache/db.rs` `SCHEMA_HASH` bump (v3→v4) is a correct, necessary companion fix — server-side filtering alone cannot retract already-leaked rows sitting in a client's existing IndexedDB cache, so forcing a full re-bootstrap is the only way to actually evict them.

No findings needed the deferred-with-ticket carve-out; the one open follow-up (stale shared-view copy after un-sharing, until next bootstrap) was explicitly called out by the requester as tracked separately and not a blocker for this review.

---
