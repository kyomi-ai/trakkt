---

### 01:06 — TRA-9919: Cross-tab sync leadership (Web Locks election + BroadcastChannel followers)

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 2 🟢
**Files**: `crates/trakkt-ui/src/cache/tab_leader.rs` (new), `crates/trakkt-ui/src/cache/apply.rs` (new), `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/cache/idb_writer.rs`, `crates/trakkt-ui/src/cache/websocket.rs`, `crates/trakkt-ui/src/cache/mod.rs`, `crates/trakkt-ui/src/components/layout.rs`, `crates/trakkt-ui/Cargo.toml`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Code Quality | apply.rs:375 | `UnboundedReceiver::try_next()` is deprecated in the pinned `futures` version; use `try_recv()`. Test-only, no functional effect. |
| 2 | 🟢 | Missing Test / Doc precision | layout.rs:220, websocket.rs:213-218 | `provide_context(ws_client.clone())` on promotion only reaches consumers created *after* the promotion (Leptos context is a setup-time snapshot, not reactive); a page already mounted when promotion happens keeps the stale `Disconnected` handle if it ever reads `WebSocketClient` via context. No current consumer exists (verified: only `sync_engine.rs`/`websocket.rs` reference the type, and the engine is handed the handle directly, not through context), so this has zero live impact today. Flagging so the caveat is explicit before any future page starts consuming `WebSocketClient` from context. |

**Anti-pattern tally**: Code Quality ×1, Missing Test/Doc precision ×1

**Notes**: This is the leadership-election piece of the tab-leader sync series. Independently re-derived the two headline evidence claims rather than accepting the summary: (1) diffed the Insert/Update memory-store match arm byte-for-byte against `sync_engine.rs` at `0a195bc` — identical; (2) extracted the call-sequence of `store.*`/`enqueue_delete`/`bump_*` calls from the original interleaved Delete arm and confirmed the split's memory-half (11 calls) and cache-half (10 calls) preserve the exact original order and set. Ran `cargo check --target wasm32-unknown-unknown --features hydrate` (clean — this exercises the `layout.rs` block the implementer said was compile-checked only, not executed) and `cargo clippy` on both native and wasm32 targets (zero warnings). Ran `cargo test -p trakkt-ui --lib` (59/59 pass, all new tests included) and confirmed the one workspace test failure (`trakkt-api::openapi::tests::all_36_operations_present`) reproduces identically on `origin/main` — pre-existing and unrelated, matches the ticket's known-red note. Re-verified the previously-vacuous election test is now real: `only_one_tab_is_granted_leadership_at_a_time` stands two genuine `acquire_leadership` calls (production path, not the test's own `hold_lock` helper) against the same exclusive lock name and asserts exactly one is granted — this is a real browser-enforced mutual exclusion test, not a tautology. Traced every double-leadership path named in the brief (Unsupported fallback, promoted-tab-races-new-tab, grant-callback re-entrancy, the two `Rc<Cell<bool>>` guards) and found each correctly closed: the `Unsupported` fallback is a documented, symmetric regression to pre-fix behavior across all tabs of an unsupporting browser, not an asymmetric hole; the exclusive Web Lock itself (not app logic) is what prevents the race-a-new-tab scenario; the grant callback guards re-entrancy with `Option::take()`; and the `sync_started`/`leader_started` cells are read-then-set within a single synchronous effect execution with no yield point between, so they cannot double-fire. Broadcast ordering is enforced structurally (every broadcast rides `IdbOp::Notify` on the same FIFO writer queue as the entity writes, with native tests proving both the single-notify-ordering and multi-notify-ordering cases) and the one exception called out as read-only-verified (`apply_sync_action`'s enqueue-then-broadcast-then-memory-update ordering) checks out. The added `Reset` broadcast on the cursor-less bootstrap path is correct and necessary — without it a follower would keep rendering entities the leader just wiped via `DeleteAllOfType`. Follower purity holds: no path from the broadcast channel reaches `IdbWriter` or `db::*`. Overall this is an unusually well-evidenced diff — the implementer's own doubt about the election test and the context hand-off were the right things to be suspicious of, and both check out clean or benign on close inspection.

---

---

### 02:21 — TRA-9929: Parseable broadcasts for status/project sync (live frame + delta payload fix)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/project_service.rs`, `crates/trakkt-auth/src/status_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`

**Anti-pattern tally**: none found

**Notes**: Two-part fix — (1) four+one call sites converted from `broadcast_sync_notify` (an envelope the client's `SyncAction` deserializer can't parse — logs and drops) to `broadcast_sync_action` with a real payload and real `sync_id`; `broadcast_sync_notify` deleted along with its now-unused `MessageType`/`WebSocketMessage` imports (verified zero remaining references repo-wide; `broadcast_to_workspace` confirmed still used at `websocket/helpers.rs:191,252` via its own independent import). (2) The same call sites' `sync_log` rows, previously written with `data: None`, now carry a serialized entity — the client skips data-less Insert/Update on both the live frame and delta (`cache/apply.rs:47-53`), so this half was equally broken. Verified the re-fetch reordering in `create_status`/`create_project` (refetch hoisted above the sync write) does not change the function's return value (same `row.into_dto()` computation, just earlier), does not lose DB-assigned `created_at`/`updated_at` (asserted non-empty in new tests), and narrows rather than widens the failure window: if the post-insert refetch now fails, the function propagates `Err` and writes nothing to `sync_log` — previously a refetch failure after a successful `data: None` sync write. Traced every converted site's payload source: `project_sync_payload` (new helper, 3 call sites) always reads the project row fresh via `get_project`, and for `add_project_member`/`remove_project_member`/`create_project_update` the project row is provably unaffected by the satellite-table write, so there's no staleness. `update_project`'s payload is the post-update re-fetched row (test asserts the new `name` value, not the pre-update one). The 5th site (`create_project_update`, ticket spec's write-site list was incomplete) was found and fixed by the implementer and is now complete — confirmed no other `write_sync_entry` call in these two files still passes `None` for a non-Delete action outside the explicitly deferred `PROJECT_MILESTONE` sites (TRA-9938). `add_project_member`'s pre-insert existence check changes a silent-success-with-orphan-row (no FK on `project_members`) into a clean `NotFound` — checked all three callers (all route through `verify_project_ownership` in `server_fns/projects.rs`, which already guarantees the project exists) and confirmed nothing depends on the old permissive path. Ran the 6 new tests plus the full `trakkt-auth` lib suite (105/105 pass) and independently confirmed the two claims that mattered most: the delta test (`delta_carries_a_payload_for_every_status_and_project_write`) runs with `ws_manager: None` throughout (grep-verified — no live broadcast possible), and its `assert_eq!(statuses, 1)` / `assert_eq!(projects, 4)` are exact counts, not `>= 1` style checks that would pass vacuously. The live-frame tests parse through `serde_json::from_str::<SyncResponse>`, matching the client's real `from_value::<SyncResponse>` call in `cache/websocket.rs:359`. Sanity-checked the `project_members`-is-not-a-sync-entity claim directly against `trakkt_types::sync::entity_types` (no `PROJECT_MEMBER` constant) and `apply.rs` (no arm) — correct, and the member-add/remove tests honestly assert the received payload equals the *parent project*, not a member record. No lint suppressions, no new silent error discards (the `sync_id` `unwrap_or_else(|e| { warn!; 0 })` pattern is the pre-existing documented "0 = no sequence info" convention, already used before this diff). `cargo clippy -p trakkt-auth -- -D warnings` clean. Pre-existing `cargo fmt --check` drift on both touched and untouched files in the crate confirmed present identically on `origin/main` via `git stash` — not introduced by this diff, not flagged.

---

---

### 02:52 — TRA-9936: CI migration-consistency guard (scripts/check-migrations.sh)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `scripts/check-migrations.sh` (new, mode 100755), `.github/workflows/ci.yml`, `CONTRIBUTING.md`

**Anti-pattern tally**: none found

**Notes**: Guard adds a `migration-consistency` job (parallel, no `needs:`, no `fetch-depth: 0` since it only inspects tree state — correctly diverges from `lint-suppression`'s base-diff pattern rather than copying it blindly) that runs `scripts/check-migrations.sh`, which checks (a) unique version prefixes within each of `apps/server/migrations` and `apps/server/migrations-sqlite`, (b) well-formed `<version>_<description>.sql` filenames, and (c) identical filename sets across both directories via `comm` on sorted listings, with a dedicated `comm -12` pass to distinguish "same version, renamed in one dir" from "version missing entirely" so the error message is specific. Independently built and ran every fixture named in the review brief rather than reading the script and assuming: duplicate version within `migrations/` (exit 1, correct message), duplicate within `migrations-sqlite/` (exit 1), version present in one directory only (exit 1, correct message), and the case the ticket's own suggested snippet missed — same version, different filenames in each directory (`20990101000000_pg_name.sql` vs `20990101000000_sqlite_name.sql`) — caught correctly with a message naming both filenames, because the script compares full sorted filename lists via `comm`, not bare version numbers via `cut -d_ -f1` as the ticket snippet did. Untouched tree passes cleanly (`41 versions, unique and matched`). False-positive check: dropped a `README.md` into both migration directories — script ignores it (filters to `*.sql` via `find -name`), confirming the implementer's claim that the ticket's own snippet (which would have globbed all files) would have false-positived here and this one doesn't. Also verified: correct root resolution from `/tmp` via `$(dirname "${BASH_SOURCE[0]}")` (cwd-independent), a missing `migrations-sqlite` directory fails with a pointed message referencing `db.rs`, an emptied directory fails with "cannot guard a directory it can't see" rather than silently passing, a malformed filename (`bad_name.sql`) is caught before version-based duplicate/cross checks run on it (avoids a confusing cascade). `apps/server/migrations` / `apps/server/migrations-sqlite` paths match the two `sqlx::migrate!()` call sites in `crates/trakkt-core/src/db.rs:30,49` exactly. Ran every fixture then `git status --porcelain apps/server/migrations apps/server/migrations-sqlite` — empty both mid-session and at the end; no test artifact was left behind. `shellcheck` wasn't on the box; pulled `docker.io/koalaman/shellcheck:stable` via podman and ran it directly against the script — zero warnings (sanity-checked the container itself catches real issues, e.g. SC2086 on an unquoted `$foo`, so the clean result isn't a tooling no-op). Script uses `set -euo pipefail`, no `jq`/Python, only coreutils (`find`, `sort`, `comm`, `cut`, `uniq`, `grep`, `sed`, `basename`), `LC_ALL=C sort` for deterministic byte-order comparison, executable bit is set (`create mode 100755`), and every `while read <<<"$var"` loop is guarded with `[ -n "$var" ]` before entering so an empty candidate list never produces a spurious empty-string iteration. On the design question raised for a second opinion — implementer rejected an allowlist for dialect-only migrations, arguing sqlx's per-database applied-version tracking makes a version-gap in one dialect's sequence permanent and unreconcilable (once later versions are applied, an earlier missing version can't be inserted retroactively without an out-of-order-migration problem), and that the correct escape hatch is a symmetric no-op `.sql` file rather than a documented exception list — this reasoning is sound and matches how sqlx's migrator actually behaves; the no-op-file escape hatch also has the virtue of being enforced by the same check rather than needing a separate allowlist-maintenance step. CI job placement mirrors the `lint-suppression` precedent (independent parallel job, `actions/checkout@v6`, no toolchain install); CONTRIBUTING.md addition is well-placed under "Development Setup" alongside the existing migration-running instructions. The two items the requester flagged as theirs to decide (job not yet in branch-protection required-checks, `pull_request`-only trigger allows a direct push to bypass) were left unflagged per instruction.

---

---

### 04:00 — TRA-9939: Fill in sync payloads for the remaining 20 data-less sync_log writes

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/favorite_service.rs`, `crates/trakkt-auth/src/issue_service.rs`, `crates/trakkt-auth/src/label_service.rs`, `crates/trakkt-auth/src/release_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs` (tests only), `crates/trakkt-auth/src/team_service.rs`, `crates/trakkt-auth/src/view_service.rs`

**Anti-pattern tally**: none found

**Notes**: Same class of fix as TRA-9929 (#255), extended to the 20 remaining `data: None` sync_log writes across favorite/issue/label/release/team/view services. Verified the six re-fetch reorderings named in the brief (`create_team`, `update_team`, `update_team_icon`, `upload_team_icon`, `delete_team_icon`, `create_label`, plus a fetch hoisted into `release_service`'s per-issue loop and one into `team_service::delete_team`'s reassignment loop): each still returns the same computed value, `rows_affected() == 0` NotFound checks still fire before the reorder in every case, and DB-assigned `created_at` is present (asserted non-empty in the new tests). The reorder narrows rather than widens the failure window, matching the established 1a07d1e precedent exactly (confirmed by diffing `project_service::create_project`/`status_service::create_status` from that commit) — a post-write fetch failure now aborts before any sync_log row is written, instead of leaving a phantom `data: None` row as it would have before. Confirmed `TEAM_SELECT` is byte-identical to the 4 inline SELECTs it replaced (diffed each). Confirmed every payload's serialized type matches what `cache/apply.rs`'s corresponding match arm deserializes (`IssueWithDetails` for ISSUE via the new `issue_sync_payload` helper, `Label`, `Team`, `View`, `Favorite`) by reading `apply.rs:47-140` directly rather than trusting the implementer's description. Ran two independent mutations to sanity-check the test suite isn't vacuous: (1) reverted `write_membership_sync_entry`'s payload to `None` — `delta_carries_a_payload_for_every_team_write` failed with a payload-missing panic naming the exact sync row; (2) replaced `issue_sync_payload`'s output with a truncated JSON object missing `team_key`/`workspace_id` — `delta_carries_a_payload_for_every_issue_write` failed with a deserialize-into-`IssueWithDetails` panic, confirming the test catches a wrong-shaped payload, not just a missing one. Both files restored and diffed byte-identical to the staged version afterward. Grep-confirmed all 8 new delta tests pass `ws_manager: None` at every call site (no live-broadcast path could satisfy them). Assessed the release_service judgement call explicitly flagged for review: `create_release`'s per-issue loop treats a vanished issue (`issue_ids` are caller-resolved, may be stale) as a skip-with-warning via `let Some(x) = get_issue_by_id(...).await? else { continue }`, while a genuine DB error still propagates via the leading `?` — this is the right split, since the release and release_issues rows are committed with no wrapping transaction by this point (confirmed by reading the full function — no `tx.begin()` around this section), so promoting the ordinary "issue got deleted mid-race" case to a hard error would abort on a non-bug while leaving no worse a state than today. `team_service::delete_team`'s reassignment loop takes the harder line (errors via `?` on the analogous fetch) because its issue ids are resolved by the function itself, moments before use, not handed in by a caller — the risk profile the two sites are graduated on is real, not arbitrary. No lint suppressions, no `#[allow(...)]`, no new silent error discards beyond the pre-existing accepted `serde_json::to_value(&x).ok()` convention (13 occurrences, all matching that one pattern). `cargo build -p trakkt-auth -p trakkt-ui` and `cargo clippy -p trakkt-auth --lib` both clean. `cargo test -p trakkt-auth --lib`: 112/112 pass. Full `cargo test --workspace`: only the known pre-existing `trakkt-api::openapi::tests::all_36_operations_present` failure (unrelated crate, not touched by this diff) plus an environmental `trakkt-core::redis::tests::test_redis_connects` hang with no local Redis — neither caused by this change.

---

---

### 06:42 — TRA-9921: Hydrate-before-dial ordering via HydrationGate latch

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/cache/hydration_gate.rs` (new), `crates/trakkt-ui/src/cache/mod.rs`, `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/cache/websocket.rs`, `crates/trakkt-ui/src/components/layout.rs`

**Anti-pattern tally**: none found

**Notes**: Fixes the race where the leader tab's startup effect spawned IDB hydration fire-and-forget then dialled the WebSocket synchronously in the same execution, letting a bootstrap/delta action get applied and then wiped by hydration's bulk `set_*`, with the cursor already past it and nothing to re-deliver it. Fix is a new one-shot `HydrationGate` latch (`hydration_gate.rs`): hydration opens it, `dial_when_hydrated` awaits it before calling the new `WebSocketClient::dial`. Traced both startup orderings by hand against `layout.rs`: `hydration_gate` is constructed once at component setup (line 127) and only `.clone()`d into the two effect halves, so the identical `Rc<RefCell<GateState>>` reaches both the every-tab hydrate spawn and the leader-only dial spawn regardless of whether the effect fires once (leader-in-same-pass, `Leadership::Unsupported`/synchronous grant) or twice (promoted-long-after, `is_leader.set(true)` from an async grant callback) — `sync_started`/`leader_started` guards prevent either half from re-running, but the gate itself isn't guarded by them and doesn't need to be, since it's idempotent. Confirmed `hydrate_then_open_gate` (sync_engine.rs:301) calls `gate.open()` unconditionally after the `match` on every path — the `Ok` branch awaits `hydrate_store_from_db` (which itself has no early return, only `if let Ok(...)` per entity type with `tracing::warn!` on individual dedup/parse failures, never a panic path) and the `Err` branch sets `initialized(true)` before falling through to the same `gate.open()` call — so there is no path out of hydration that leaves the gate closed. Independently verified the reactive_graph 0.2.14 claim against the actual crate source (not the changelog or docs): `Effect::new`'s executor loop wraps every run (not just the first) in `owner.with_cleanup(|| ...)` (`effect.rs:193`), and `Owner::with_cleanup` unconditionally calls `self.cleanup()` — which runs every `on_cleanup` registered under that owner and drops arena-backed values registered under it (`owner.rs:294-305`) — before running the closure again. Since the effect's `owner` is created once in `effect_base()` and reused across every re-run, an `on_cleanup`/`provide_context` called *inside* the effect body on run N would be torn down by run N+1's cleanup step. On `main`, `websocket::connect()` was called inside the effect body and returned a `WebSocketClient` whose `StoredValue` (an arena item, confirmed via `ArenaItem::new_with_storage` registering under `OWNER.with(...)` at construction) would register under that same effect owner if constructed there — so a hypothetical third execution of the effect (the docs correctly flag this as latent-not-active today, since nothing currently re-triggers `user_ctx` while Layout stays mounted) would silently dispose the live socket's backing arena slot while `leader_started` permanently blocks re-dialling. Moving `provide_context`/`on_cleanup` to component setup (outside any `Effect::new`) ties both to the component's own owner instead, which is only disposed on unmount — correct Leptos usage, and also fixes the separately-acknowledged pre-existing issue that context provided from inside an effect is a setup-time snapshot invisible to already-mounted consumers. Confirmed `websocket::connect()` has no remaining callers anywhere in the repo (grep for `websocket::connect(` and `connect(` across all crates — the only hits are the unrelated server-side `WebSocketManager::connect` in `trakkt-auth`). Read `apps/server/src/routes/websocket.rs` to check the token-fallback reasoning in `dial_when_hydrated`/layout.rs rather than trust the comment: `state.config.is_personal()` bypasses JWT entirely (line 72-73) so an empty-token dial still succeeds in personal mode, while `authenticate_ws` closes with `CLOSE_AUTH_REQUIRED` on an empty/missing token in multi-user mode (line 221-227), and `schedule_reconnect` in `websocket.rs` already re-fetches a fresh token on every backoff attempt — so dialling with an empty string on token-fetch failure is a deliberate, logged, reasoned choice that reuses the existing recovery path rather than a silent fallback masking a broken flow. Ran the tests rather than just reading them: `cargo test -p trakkt-ui --lib hydration_gate` (8/8 native gate tests pass), and `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` in a real headless Firefox (12/12 pass, including the 3 new wasm tests: `the_gate_opens_only_once_the_store_is_hydrated`, `a_tab_promoted_after_hydration_is_released_immediately`, `the_socket_is_not_dialed_until_the_gate_opens`). Confirmed by reading `any_spawner-0.3.0/src/lib.rs` that `leptos::task::spawn_local` is a direct passthrough to `wasm_bindgen_futures::spawn_local` only once `Executor::init_wasm_bindgen()` has set the global executor (which `leptos-0.8.19/src/mount.rs` calls on every mount/hydrate entry point in production), and that without it `Executor::spawn_local` hits an uninitialized-executor fallback that drops or panics — so the wasm test's use of `wasm_bindgen_futures::spawn_local` directly (since `wasm-bindgen-test` never boots the Leptos runtime) is not a shortcut but the only way to reproduce production's actual scheduling in that harness, exactly as the code comment claims. `docs/CODING_STANDARDS.md:59`'s `Resource::new()`-in-wasm32 ban is respected (no `Resource::new()` anywhere in the diff; the gate is a hand-rolled `Future`, not a Leptos resource). No lint suppressions, no `#[allow(...)]`, no `unwrap()`/`expect()`/`unwrap_or_default()` added, no TODOs. `cargo check -p trakkt-ui --features ssr` and `--target wasm32-unknown-unknown --features hydrate` (both with `--tests`) build clean; `cargo clippy -p trakkt-ui -- -D warnings` clean on both targets. Did not flag the three issues the ticket explicitly carved out as filed separately (follower `BroadcastChannel` has the same race and needs buffering, not gating; `start_sync_engine`'s inner connection-state `Effect` created inside the outer effect run; `get_ws_token`'s doc-vs-behavior mismatch in personal mode) — verified each is real but out of scope for this diff, not swept under the rug.

---

---

### 05:20 — TRA-9941: Scope team-membership mutations to the caller's workspace

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/team_service.rs`, `crates/trakkt-api/src/context.rs`, `crates/trakkt-api/src/issues.rs`, `crates/trakkt-api/src/teams.rs`

**Anti-pattern tally**: none found

**Notes**: Fixes a tenant-isolation hole where `add_team_member`/`remove_team_member`/`update_team_member_role` accepted a caller-supplied `team_id` but never checked it against the session's `workspace_id` before mutating `team_members` — only using `workspace_id` for the downstream sync-log write. New `get_team_in_workspace(db, team_id, workspace_id)` resolves-and-verifies before all three mutations, returning `NotFound` (not `Forbidden`/`BadRequest`) on a foreign team, consistent with the rest of the module's existence-hiding pattern. Verified the hole is actually closed by mutating the fix myself: reverted `get_team_in_workspace`'s SQL to drop `AND workspace_id = $2`, reran `cargo test -p trakkt-auth team_service::tests`, and reproduced the exact original bug — `add_team_member`/`remove_team_member`/`update_team_member_role` all returned `Ok(())` for a cross-workspace `team_id` instead of erroring, confirming the new tests are not vacuous and the workspace check is what closes the hole (not something else). Restored the file and reran — diff against staged was byte-identical (no accidental leftover mutation), 6/6 new tests pass. Verified all three internal callers the review brief named as at-risk of over-restriction — `apps/server/src/lib.rs:63` (personal-mode bootstrap: creates `team-local` in `workspace-local` two statements earlier, same call), `user_service.rs:278` (`create_workspace_for_user`: creates the team_id it passes, in the same `workspace_id`, one statement earlier), `workspace_service.rs:571` (`accept_invitation_for_user`: resolves via `get_default_team(pool, &invitation.workspace_id)`, already scoped) — each passes a team_id proven to belong to the workspace_id it also passes, so none can regress. Traced all 7 call sites of `context.rs`'s `resolve_team` (labels.rs×2, teams.rs, issues.rs×3, statuses.rs) — all pass `ctx.workspace_id`, the session-derived value, so the added validation only rejects ids that were never legitimately reachable; the 5 read-only sites (list/search) change behavior only for a bogus/foreign team_id, from silent-empty-result to an explicit `NotFound`, which is a correctness improvement, not an over-restriction of any real workflow. Confirmed `issues.rs:320` (`create_issue`) and `teams.rs:59` (`update_team_settings`) now use the scoped resolver; for `update_team_settings` specifically, traced that `team_id` reaching the re-fetch was already validated by `resolve_team` one call up, so the fix here (closing an actual foreign-row read, per the ticket's own note) is not redundant nor risky. `write_membership_sync_entry` now takes `&Team` instead of re-reading — `Team` carries `workspace_id` itself, so the derivation is a field read, not a fallback; confirmed via the `sync_log_service.rs` `delta_carries_a_payload_for_every_team_write` test (still passing, still asserts 9 payloads with correct `team_id`) that the write path and payload shape are unchanged. Test quality claims verified directly: `member_count`/`member_role` read `team_members` via raw `COUNT(*)`/`SELECT role`, not return values; `sync_rows_for_workspace` asserts zero rows in `WS_B`'s sync_log after each blocked cross-workspace attempt (the original bug would have written one, since the old code passed `workspace_id` straight to the sync write regardless of which workspace the team belonged to); `membership_mutations_still_work_within_the_workspace` and `add_team_member_is_idempotent_within_the_workspace` exercise the legitimate same-workspace path end to end, and the mutation test above confirms they would fail if the fix over-restricted (they didn't fail under the reverted-scoping mutation — only the cross-workspace tests failed, showing the two test groups are actually discriminating). No lint suppressions, no `#[allow(...)]`, no fallback error-swallowing (`.expect()` calls are confined to `#[cfg(test)]` fixture setup). `cargo build --workspace` clean; `cargo test -p trakkt-auth` 118/118 pass; `cargo test -p trakkt-api` has one pre-existing unrelated failure (`openapi::tests::all_36_operations_present`, stale operation-count assertion), confirmed failing identically on unmodified `origin/main` via `git stash`/`git stash pop` — not caused by this diff. Ticket also asked to confirm the implementer correctly declined to copy the (non-existent) "TRA-9929 pattern" and instead built its own resolve-then-mutate approach — confirmed sound on its own merits, matching how the sibling `resolve_team` fix in `context.rs` was built.

---

---

### 07:35 — TRA-9922: Server-side pagination drain loop for sync_delta

**Review type**: initial
**Result**: 0 🔴, 1 🟡, 0 🟢
**Files**: `apps/server/src/routes/websocket.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Missing Test Coverage (critical branch) | websocket.rs:52,602-677 | `MAX_DELTA_BATCHES = 50` is a hard-coded literal baked into `handle_sync_delta`'s loop bound, with no way for a test to exercise the cap-exhaustion branch (the one that knowingly sends a partial `SyncComplete`) without seeding 500,001 rows. Implementer verified it by temporarily editing the const to 1, confirming 3 of 5 new tests fail, then reverting — a real but non-repeatable, non-CI-enforced check. I independently reproduced this exact experiment (see Notes) and confirmed the finding is real. Recommend extracting the loop into an inner function parameterized by `batch_size: i64, max_batches: usize`, with the outer `handle_sync_delta` passing the production consts; a test can then pass `batch_size: 3, max_batches: 2` against ~10 seeded rows and honestly assert the cap-hit path (partial watermark, `!backlog_drained`, correct cursor) without touching the production constant or needing a huge fixture. This is the one branch that deliberately reports a partial watermark — a silent regression here (e.g. an accidental `<=` become `<`, or the `!backlog_drained` branch quietly removed) reproduces the exact bug TRA-9922 exists to fix, and today nothing in CI would catch it. |

**Anti-pattern tally**: Missing Test Coverage ×1

**Notes**: Read the diff and traced all 3 exit paths (DB error → no watermark, failed send → no watermark, cap exhausted → watermark with honest cursor) — implemented exactly as intended, `CatchUpGuard` is RAII-scoped over the whole function so every new early return is covered, and the pruned-cursor `SyncReset` check at the top is untouched. Confirmed via `cargo check`/`cargo test -p trakkt-server --lib routes::websocket` that all 22 tests (5 new) pass on the staged diff. Ran mutation testing myself rather than trusting the diff read: (1) changed the cursor advance to `last.sync_id - 1` (duplication bug) — 3 of the 5 new tests failed, including the boundary-ordering test's "strictly ascending" assertion; (2) changed it to `last.sync_id + 1` (skip bug) — same 3 tests failed, via both the length-mismatch and gap-detection assertions, confirming `sync_delta_streams_every_entry_exactly_once_across_page_boundaries` genuinely catches both directions of an off-by-one at the page boundary, not just one. (3) Reproduced the implementer's own verification method for the untested cap branch: set `MAX_DELTA_BATCHES = 1` and reran — `sync_delta_drains_a_backlog_larger_than_one_page`, `sync_delta_streams_every_entry_exactly_once_across_page_boundaries`, and `sync_delta_abandons_a_multi_page_stream_when_the_client_disconnects` all failed, confirming the manual-verification claim was accurate. Restored the file to the exact staged content after each mutation (`git diff` against the index was empty afterward each time). Verified `get_entries_since`'s SQL (`WHERE sync_id > $2 ... ORDER BY sync_id ASC LIMIT $4`) cannot return more rows than requested (hard SQL `LIMIT`) and that advancing `cursor` to the last *filtered* row's `sync_id` and re-querying with strict `>` cannot skip or duplicate rows regardless of how many rows were filtered out by the per-user visibility clause, since `LIMIT` is applied after the `WHERE` filter across the whole remaining table, not per raw scan window. The disconnect test (`sync_delta_abandons_a_multi_page_stream_when_the_client_disconnects`) genuinely crosses into the second page: it reads `DELTA_BATCH_SIZE + 1` frames before closing the channel, which can only succeed if the second page's fetch and initial send already occurred, and it asserts no watermark appears even among frames buffered before the close took effect. The concurrent-drain pattern (`tokio::spawn` for the handler, a separate task/loop draining the bounded `mpsc::channel`) is correct and is what a bounded channel requires to avoid deadlock on a 12,000-row backlog against a 64-slot channel; verified no test can pass vacuously by checking exact counts (`entry_count`, contiguous id span) rather than loose bounds. No fallback code reintroduced — confirmed the removed `.unwrap_or_default()` has no equivalent anywhere in the new code; the DB-error path returns early with no watermark, matching the exact defect (silently fabricated "caught up" state) this ticket fixes. No `#[allow(...)]`, no silently-discarded errors, no stringly-typed state. The single finding above is a testing-coverage gap, not a correctness bug — the loop behaves correctly today, verified by both reading and mutation testing — but it is the one branch where a future silent regression would be indistinguishable from the original bug and undetectable by CI, which is why it doesn't clear the bar for a clean sign-off here.

---

---

### 07:38 — TRA-9922: Server-side pagination drain loop for sync_delta (round 2)

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `apps/server/src/routes/websocket.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found. Round-one's single 🟡 finding is resolved. |

**Anti-pattern tally**: None

**Notes**: Round one flagged that the `MAX_DELTA_BATCHES` cap-exhaustion branch (the only path sending a deliberately partial watermark) had no CI-enforced test. Implementer extracted the loop into `async fn drain_delta(conn_tx, db, user_id, workspace_id, last_sync_id, batch_size: i64, max_batches: usize) -> DrainOutcome` (websocket.rs:688-761), with `handle_sync_delta` passing the production consts and a new `enum DrainOutcome { Drained { cursor }, CapExhausted { cursor }, FetchFailed, ClientGone }` plus `fn watermark(&self) -> Option<i64>` (websocket.rs:643-664) replacing the old boolean-flag design. Two new tests added: `drain_delta_stops_at_the_batch_cap_with_a_partial_watermark` (batch_size:3, max_batches:2, 10 seeded rows) and its below-cap control. I did not trust the implementer's mutation report — reproduced it myself: changed the `CapExhausted` fallthrough at websocket.rs:760 to `Drained`, reran, and got a specific, non-vacuous failure (`left: Drained { cursor: 6 } right: CapExhausted { cursor: 6 }`, "hitting the cap must report a backlog remaining, not a drained log"); restored and confirmed `git diff` against the index was empty. Also re-ran round one's own off-by-one mutation (`cursor = last.sync_id - 1` at websocket.rs:751) against the *refactored* code to confirm the extraction didn't lose that guarantee — same 5 tests failed as round one reported, confirming byte-for-byte behavioral equivalence at the boundary. Traced `handle_sync_delta` end to end: the pruned-cursor `SyncReset` check is untouched (diff hunks via `git diff -U8` start immediately after that block, with full unchanged context shown); `CatchUpGuard` (websocket.rs:553) is declared once at function top and its scope spans the single `.await` on `drain_delta`, so every one of `drain_delta`'s four exit variants is still covered from the caller's side; exactly one `SyncComplete` is sent, on the two `Some`-watermark variants (`Drained`, `CapExhausted`), and none on the two `None`-watermark variants (`FetchFailed`, `ClientGone`), via the early `return` at websocket.rs:620-622. Confirmed the empty-first-page case (0 rows, cursor stays at `last_sync_id`, returns `Drained`) and the exactly-one-full-page boundary (a full first page is followed by a required empty second-page fetch before `Drained` is returned — an extra round trip but not a correctness issue, and unchanged from round one's design) by direct code tracing. Verified test-helper generalization (`collect_stream_frames`/`collect_delta_frames`/`collect_drain`/`seeded_sync_ids`) cannot deadlock or pass vacuously: `conn_tx` is moved by value into the spawned closure and dropped when the streamed function returns, which is what closes the channel and ends the concurrent drain loop; `seeded_sync_ids` reads real assigned ids back from the DB rather than assuming a contiguous range. Full `cargo test -p trakkt-server --lib` 35/35 pass (17 pre-existing + 5 round-one + 2 round-two = 24 in this module, consistent with `git show origin/main` baseline of 17), `cargo clippy -p trakkt-server --lib -- -D warnings` clean, `cargo build --workspace` clean. No `#[allow(...)]`, no `let _ =`, no fallback code (the actual root-cause fallbacks — `.unwrap_or_default()` on the fetch and `.unwrap_or(last_sync_id)` on the cursor — are removed, not reintroduced). Signed clean.

---

---

### 23:09 — TRA-9923: Atomic sync_log writes + bootstrap watermark ordering

**Review type**: initial
**Result**: 0 🔴, 1 🟡, 4 🟢
**Files**: `apps/server/src/routes/websocket.rs`, `crates/trakkt-auth/src/comment_service.rs`, `crates/trakkt-auth/src/issue_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-core/src/db.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Test Coverage | `issue_service.rs` (delete_issue:1202, set_issue_labels:1278, set_sort_order:1410, blocked-loop:977), `comment_service.rs` (update_comment:1877, delete_comment:1946) | Only 3 of 9 converted `write_sync_entry_in_tx` call sites (create_issue, create_comment, update_issue's own write) have a "sync-log insert fails ⇒ mutation rolls back" regression test. Proved the gap is real, not theoretical: reverted `delete_issue`'s `.await?` to the old `.await.unwrap_or(0)` anti-pattern and the entire `trakkt-auth` suite (124/124) still passed. A regression on any of the other 6 sites ships silently. |
| 2 | 🟢 | Process | ticket TRA-9923 | Ticket's own acceptance criteria requires follow-up tickets for the 12 deferred service groups (label, notification, favorite, view, project, status, team, relation, release, attachment, archive, workspace), linked back to TRA-9923. None exist yet (verified via search + relations list). Not a code defect — flagging so it isn't missed before merge. |
| 3 | 🟢 | Robustness | `db.rs` | `db: &DbPool` stays in scope for the whole function after `db.begin()`, so nothing stops a future edit from calling a `db_*` macro against the live pool while `tx` is open, silently reintroducing the SQLite deadlock this PR removes. Worth a lint/grep guard in a fast-follow. |
| 4 | 🟢 | Efficiency | `issue_service.rs:1380` (set_sort_order) | Opens a transaction before validating the issue exists; not-found path begins+rolls back a no-op tx. Harmless, minor overhead. |
| 5 | 🟢 | Behavior note | `issue_service.rs:758-763` (update_issue) | `find_blocked_issue_ids` gate on `ws_manager.is_some()` was correctly removed (sync_log durability must not depend on live-socket presence) and its error now propagates instead of warn-and-continue — both are deliberate, correct improvements, but they do mean every status change now pays this query's cost (previously only when a socket was attached), and a transient failure in this ancillary lookup now fails the whole update. Noting the trade-off, not disputing it. |

**Anti-pattern tally**: Test Coverage ×1, Process ×1, Robustness ×1, Efficiency ×1, Behavior note ×1

**Notes**: Exhaustively traced every `begin()`→`commit()` span across all 9 converted call sites (issue_service.rs ×6, comment_service.rs ×3) for pool access — none found; all in-transaction helpers (`get_issue_by_id_tx`, `fetch_labels_for_issue_tx`, `get_workspace_for_issue_tx`, `issue_sync_payload_tx`) are fully tx-scoped. Went beyond "read carefully" for the untested Postgres arms: stood up a real Postgres (podman container, scratch DB, migrations applied) and wrote two throwaway smoke tests (never committed, removed after running) proving on real Postgres — not just SQLite — that (a) `write_sync_entry_in_tx`'s `RETURNING sync_id` returns distinct, correct ids for two writes in one open transaction, invisible before commit and visible after, and (b) `create_issue` genuinely rolls back the `issues` INSERT when a real trigger-raised exception rejects the `sync_log` insert. This directly satisfies the ticket's own stated verification bar ("run the rollback test against a real Postgres, not just SQLite") ahead of the test-verification-architect stage. Rollback semantics verified correct throughout: every rejection either precedes any mutation (safe no-op Drop-rollback) or calls `tx.rollback()` explicitly (update_issue affected==0, update_comment ownership failure). No TRA-9920 regression: all 9 sites pass `visibility_user_id = None`, matching prior behavior for these workspace-visible entity types. `set_issue_labels`'s `TODO: wrap in a transaction` removal is now actually true. Part A (websocket.rs watermark reordering) is clean and has a genuinely forcing (not sleep-based) regression test using the SQLite single-connection pool as a synchronization baton. Full `cargo test -p trakkt-auth --lib` 124/124 and `cargo test -p trakkt-server --lib websocket::` 25/25 pass; `cargo clippy -p trakkt-auth -p trakkt-core --lib --tests` has zero warnings attributable to the diff. Not signing: the test-coverage gap (#1) is exactly the kind of regression this PR exists to prevent, and it is unresolved (no ticket) for 6 of 9 sites.

---

---

### 09:46 — TRA-9923: Atomic sync_log writes + bootstrap watermark ordering (round 2)

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/sync_log_service.rs` (tests only — `apps/server/src/routes/websocket.rs`, `crates/trakkt-auth/src/comment_service.rs`, `crates/trakkt-auth/src/issue_service.rs`, `crates/trakkt-core/src/db.rs` verified byte-identical to round 1, not re-reviewed)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | Round one's single 🟡 (only 3 of 9 rollback call sites tested) is resolved — all 9 now covered. |

**Anti-pattern tally**: None

**Notes**: Round one proved a real hole by reverting `delete_issue` to `.await.unwrap_or(0)` and watching 124/124 still pass. Six new tests were added to `sync_log_service.rs`'s `mod tests` (lines 2104-2568): `issue_delete_rolls_back_when_its_sync_entry_cannot_be_written`, `issue_labels_roll_back_when_their_sync_entry_cannot_be_written`, `issue_sort_order_rolls_back_when_its_sync_entry_cannot_be_written`, `comment_update_rolls_back_when_its_sync_entry_cannot_be_written`, `comment_delete_rolls_back_when_its_sync_entry_cannot_be_written`, and `issue_status_change_rolls_back_when_a_blocked_issues_sync_entry_cannot_be_written`, covering all 6 previously-untested call sites (delete_issue, set_issue_labels, set_sort_order, update_comment, delete_comment, and update_issue's blocked-issue loop write). Plus two new test helpers: `reject_sync_log_inserts_for_entity` (the same real `RAISE(ABORT)` trigger as round one's `reject_sync_log_inserts`, narrowed with a `WHEN NEW.entity_id = …` clause) and `issue_label_ids`.

Verified the "production files unchanged" claim rather than trusting it: no saved snapshot of round one's exact diff exists (checked reflog/stash/tags — none), so I diffed all 4 files (`websocket.rs`, `comment_service.rs`, `issue_service.rs`, `db.rs`) against `origin/main` in full and cross-checked every line against round one's detailed notes (the `DbTx` enum/macros in `db.rs`, the `ISSUE_ROW_BY_ID_SELECT`/`fetch_labels_for_issue_tx`/blocked-loop restructuring in `issue_service.rs`, the removed `TODO: wrap in a transaction`, the `ws_manager.is_some()` gate removal) — every detail matches exactly, and `grep -c '#\[test\]|mod tests'` on all 4 files returns 0, confirming no test code was added there. Diff stat for all 5 files matches the pre-review baseline (`149/414/496/825/176`) with no drift during review.

Ran the full mutation program myself rather than trusting the report. For all 6 new call sites, reverted `.await?` to `.await.unwrap_or(0)` one at a time and reran `cargo test -p trakkt-auth --lib sync_log_service::tests` — each mutation failed exactly and only its target test (`issue_delete_...`, `issue_labels_...`, `issue_sort_order_...`, `comment_update_...`, `comment_delete_...`), and for `comment_update`/`issue_status_change` the panic output showed the mutation's title/status change had actually committed (e.g. `status_id: "sts_done"`), proving the swallowed error let the transaction commit — the exact bug class this PR exists to prevent. Restored the file after each mutation and confirmed `git diff` against the index was empty every time.

Gave the scoped-trigger test (`issue_status_change_rolls_back_when_a_blocked_issues_sync_entry_cannot_be_written`) the most scrutiny, since it's the one designed to prove a harder case — a write that already succeeded within the transaction gets unwound by a later failure in the same transaction. Checked for vacuous pass by scoping the trigger to a nonexistent entity id instead of `iss_blocked`: the test failed cleanly (`update_issue` returned `Ok`, `expect_err` panicked), confirming the test's pass genuinely depends on the trigger firing on the loop's write, not on some unrelated condition. Then added temporary `eprintln!` probes at both write sites in `update_issue` (removed after, `git diff` empty afterward) and ran the unmutated test with `--nocapture`: output showed `PROBE: primary write for iss_vis succeeded, sync_id=2` followed by `PROBE: loop write for iss_blocked result = false` — direct proof that the primary write for the updated issue really does succeed and get a real sync_id before the loop's write fails, and the test's final assertion (`SELECT COUNT(*) FROM sync_log WHERE entity_id = 'iss_vis'` == 0) is therefore genuine proof that the whole transaction unwound a statement that had already succeeded, not a trivially-true check. Confirmed SQLite's `RAISE(ABORT)` semantics (statement backed out, transaction stays open) matches `db.rs`'s own doc comment on `DbTx`.

Checked all 6 new tests assert both halves (error returned + prior DB state intact), not just the error: `issue_delete`/`comment_delete` assert row-count unchanged, `issue_labels` asserts the label list reverted to the pre-mutation set (proving both the DELETE and the INSERT half of the relabel rolled back), `issue_sort_order`/`comment_update` assert the field value reverted, and the blocked-loop test asserts three independent facts (issue's own status reverted, blocked issue's derived `is_blocked` still true, zero sync_log rows for the updater). None of the six assert only the error, which was exactly round one's finding reappearing — it didn't.

Re-ran round one's own `delete_issue` experiment against the current diff rather than trusting that it would still reproduce: reverted `.await?` to `.await.unwrap_or(0)` and ran the **full** `cargo test -p trakkt-auth --lib` suite (not just the targeted test) — now fails at 129 passed / 1 failed, versus round one's 124/124 all-pass. This is the direct, load-bearing confirmation that the gap round one found is closed. Restored and confirmed clean. Full `cargo test -p trakkt-auth --lib` (130/130, exactly +6 over round one's 124) and `cargo test -p trakkt-server --lib websocket::` (25/25) pass on the actual staged diff; `cargo clippy -p trakkt-auth -p trakkt-core --lib --tests` (no `-D warnings`, matching round one's invocation) shows zero warnings attributable to the new test code (checked by grepping clippy output for `sync_log_service.rs:20xx`/`21xx` — none).

Round one's two 🟢 process/robustness notes (follow-up tickets for the 12 deferred service groups; no compiler guard against calling a `db_*` macro while a `tx` is open) are unchanged and still open — neither is a blocker, and this round's brief scoped review to the test-coverage gap only, so I did not re-litigate them.

---

### 01:45 — TRA-9925: Fix rustdoc doctest fence on Button component

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/components/button.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: One-character-range fix (` ```rust ` → ` ```ignore `) at button.rs:118, resolving 3 doctest failures (Button/ButtonProps/__component_button all share the doc comment via `#[component]`). Verified independently:
- Convention check: crate has 12 `///`-fenced examples + 4 `//!`-fenced (confirm_dialog.rs:9, empty_state.rs:8, modal.rs:12, toast.rs:7 — module-level, missed by a `///`-only grep) = 16 total fenced blocks. 13 pre-existing `ignore` (Leptos component usage syntax, e.g. avatar.rs:48, dropdown.rs:109/184/295) + 2 `text` (ASCII layout diagrams, board.rs:841, issue_row.rs:31) + button.rs now the 14th `ignore`. Matches implementer's claimed convention exactly.
- Full-crate survey via script tracking fence open/close state across every `.rs` file (both `///` and `//!`) for any opening fence that is bare or `rust`-annotated: zero hits after the fix, confirming button.rs was the only compiled fence in the crate.
- utils/auto_link.rs:19's triple-backticks are mid-sentence prose (not a line-initial fence, not a doctest at all), and line 171's backticks are inside a `#[test]` string literal, not a doc comment — confirmed both are inert, matching implementer's note.
- `ignore` vs `text`: content is Leptos component-invocation pseudocode (`<Button on:click=handler>"Save"</Button>`), structurally identical to the 13 existing `ignore` precedents, and distinct in kind from the 2 `text` ASCII-diagram cases. Rendered output confirms `ignore` still applies Rust syntax highlighting (comment/string spans) while `text` would not — consistency and rendering both favor `ignore` here; would not have flagged had `text` been chosen either, but `ignore` is the better call.
- `cargo test -p trakkt-ui --doc --locked`: 0 passed; 0 failed; 30 ignored (up from 27, exactly the 3 Button variants). `cargo clippy --workspace --locked -- -D warnings`: clean. `cargo doc -p trakkt-ui --no-deps`: succeeds, zero warnings attributable to button.rs; generated `fn.Button.html` shows the Usage example still rendered in full (Save/Cancel/aria_label lines intact) under the ⓘ "not tested" marker.
- Diff confirmed exactly 1 file, 1 line (`git diff --cached --stat`), base matches `origin/main` @ f1a8920.

---

---

### 01:16 — TRA-9926: Integration-test harness for WebSocket sync (trakkt_core::test_helpers + tests/sync_ws.rs)

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 1 🟢
**Files**: `crates/trakkt-core/src/lib.rs`, `crates/trakkt-core/src/test_helpers.rs` (new), `apps/server/src/routes/websocket.rs`, `apps/server/tests/sync_ws.rs` (new)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Copy-Paste / Extensibility | `crates/trakkt-core/src/test_helpers.rs` vs `apps/server/src/lib.rs:23-70` (`auto_provision_personal_mode`) | The tenancy-row INSERTs (`users`, `workspaces`, `workspace_users`, `teams`) are near-identical to the production personal-mode provisioning path, hand-duplicated rather than shared. Also, both `apps/server` and `trakkt-ui` dev-depend on `trakkt-auth` directly (confirmed in both Cargo.tomls) as well as on `trakkt-core`'s `test-helpers` feature, so a `trakkt_auth::test_helpers` module (its own feature flag, no cycle since trakkt-auth already depends on trakkt-core) could have hosted pool+tenancy+entity-seeding as one reusable builder instead of splitting "rows with no service" (trakkt-core) from "rows with a service" (called ad hoc by each consuming test file). Not a defect — only one test file exists today, so no duplication has manifested yet — but worth a fast-follow if a second integration-test file needs the same `seeded_workspace()` shape. |

**Anti-pattern tally**: Copy-Paste/Extensibility ×1 (non-blocking)

**Notes**: This review required independently verifying five specific claims rather than trusting the implementer's report, all confirmed:

1. **Dependency-cycle claim (seam design)**: Confirmed `trakkt-auth` depends on `trakkt-core` (`crates/trakkt-auth/Cargo.toml:7`) and not vice versa. Empirically reproduced the claimed impossibility by temporarily adding `trakkt-auth` as an optional/feature-gated dependency of `trakkt-core` and running `cargo check` — Cargo rejected it with `error: cyclic package dependency: package trakkt-auth ... depends on itself`, confirming even a feature-gated edge in that direction is a hard Cargo error, not a style choice. Reverted cleanly (working tree verified clean after). Given the constraint is real, the resolution (trakkt-core owns the pool + tenancy rows with no service behind them; the test file itself dev-deps trakkt-auth and calls the real `add_team_member`/`seed_default_statuses`/`create_label`/`create_issue`) is correct and matches production's own `auto_provision_personal_mode` ordering byte-for-byte (verified by reading `apps/server/src/lib.rs:23-70`). See the one 🟢 finding above for a forward-looking alternative (hosting it all in `trakkt_auth::test_helpers` instead) that would avoid the future duplication risk — not required now.

2. **Scenario (c) reframing (unfalsifiability of "withholds trailing SyncComplete")**: Reproduced mutation 3 myself — changed `stream_batch!` in `handle_sync_bootstrap` to ignore a failed send and fall through to attempt the trailing `SyncComplete` unconditionally (previously an early `return` on failed send skipped it). Ran the full pre-existing `cargo test -p trakkt-server --lib` (36/36 pass) and the new `cargo test -p trakkt-server --test sync_ws` (3/3 pass) against this mutation — confirmed it is undetectable by any test in the suite, including the new scenario (c) test itself. This is because `send_sync_response` already discards its boolean result at the trailing-`SyncComplete` call site even in the unmutated code (`websocket.rs:632-638` doesn't check the return value), so attempting-and-failing to send is byte-for-byte indistinguishable from not attempting at all once the channel is closed. The reframed property (CatchUpGuard release on abort) is therefore the correct choice — a genuinely falsifiable substitute, not scope-dodging.

3. **Vacuous-pass guards (scenario a)**: Traced `register_connections`' heartbeat-count assertion against `WebSocketManager::connect`/`deliver_to_local_user` (`crates/trakkt-auth/src/websocket/manager.rs:189-258,499-529`) — `connect()` inserts the new connection into the map *before* firing the heartbeat, so heartbeat fan-out reaches the new connection too; the `count - i` formula is the correct closed-form consequence, confirmed by reading the delivery order in code. `assert_all_live`'s probe-then-empty-check genuinely fails if a connection were dead or never registered (a closed sender hits `TrySendError::Closed`, dropping the message and pruning the connection, so the subsequent `try_recv` would return `Err`, not `Some(probe)`). The post-bootstrap check on the idle connection reading the probe as the *first* thing in its queue is the same mechanism and rules out both dead and buffered-frames-then-probe scenarios.

4. **Full mutation matrix, reproduced independently**: M4 (`std::mem::forget` the `CatchUpGuard` on the bootstrap abort path) — reproduced exactly: compiles clean, passes all 36 pre-existing `trakkt-server --lib` tests, fails only the new scenario (c) test (`aborting mid-bootstrap must still release the catch-up exemption` panics). This is the load-bearing justification for scenario (c)'s existence and it holds. M5 (`send_sync_response` unwraps instead of returning `false`) — also reproduced: fails only scenario (c), with the handler panicking on the closed channel and the test's `handler.await.expect(...)` surfacing the panic via `JoinError::Panic`, exactly as claimed. M1/M2/M3 were not independently re-mutated beyond M3 (covered above) since the review brief's priority scrutiny centered on M3 and M4; M1/M2's claimed effect (bootstrap/delta responses fanned out via `send_to_user_raw` instead of addressed to `conn_tx` directly) is structurally consistent with the code (`send_sync_response` writes straight to `conn_tx`, never touches the manager) and with the fan-out guard analysis in item 3.

5. **`pub` widening**: `git diff --cached -- apps/server/src/routes/websocket.rs` shows exactly two hunks, each adding a doc comment plus changing `async fn` to `pub async fn` on `handle_sync_bootstrap` and `handle_sync_delta` — nothing else in the file changed (confirmed by reading the full diff, 13 lines total). Both doc comments state a genuine reason (the fan-out property can only be observed with a real multi-connection `WebSocketManager`, which lives outside the module's own `#[cfg(test)]` unit tests). `routes::websocket` and `routes` were already `pub mod` before this change, so no additional surface was opened beyond the two functions.

6. **Additive-only guarantee**: `git diff --cached --stat` touches exactly 4 files (`trakkt-core/src/lib.rs`, `trakkt-core/src/test_helpers.rs`, `apps/server/src/routes/websocket.rs`, `apps/server/tests/sync_ws.rs`) — no rollback tests, no bootstrap-interleave test, nothing in `trakkt-auth` is touched at all, so the "don't rewrite existing tests" instruction is satisfied trivially and verifiably.

7. **No external services / timing**: `test_pool()` calls `DbPool::connect("sqlite::memory:")` and the test file constructs `WebSocketManager::new(None, db.clone())` — no Redis, no Postgres. Ran `cargo test -p trakkt-server --test sync_ws` twice: 0.30-0.42s, consistent with the reported 0.34s.

All temporary mutations (dependency-cycle probe, M3, M4, M5) were reverted from disk and `git diff`/`git diff --cached --stat` confirmed to exactly match the original staged state after each. `trakkt-core::redis::tests::test_redis_connects` (TRA-9930, needs live Redis on :6381) was not touched by this diff and is out of scope per the review brief. Signed clean — the one 🟢 finding is a forward-looking design note, not a defect in the shipped code.

---

---

### 12:37 — TRA-9927: Abort sync_bootstrap instead of certifying a partial dataset with SyncComplete

**Review type**: initial
**Result**: Clean (approved)
**Files**: `apps/server/src/routes/websocket.rs`, `apps/server/tests/sync_ws.rs`, `crates/trakkt-auth/src/workspace_service.rs`

**Anti-pattern tally**: none found

**Notes**: Fixes three ways `handle_sync_bootstrap` could send `SyncComplete` after a short read: (1) `to_sync_values` `filter_map`-dropped unserializable entities and kept going, (2) all ten entity fetches used `.unwrap_or_else(|e| { warn!; vec![] })`, turning a DB error into an empty list plus a full watermark, (3) `get_workspace_settings_for_sync` collapsed a DB error and a settings-JSON parse error into the same `None` as "no row." Restructured into `fetch_bootstrap_data() -> Result<BootstrapData>` (all-or-nothing read phase, via a `bootstrap_read` wrapper that names the failing query and returns `Err` for the caller's `?`) followed by `stream_bootstrap()` (owns the loop and the sole `SyncComplete` construction site for the bootstrap path).

Traced the invariant directly rather than accepting the doc comment's claim: grepped every `SyncComplete` construction in the file — exactly one in the bootstrap path (inside `stream_bootstrap`, websocket.rs:730) and one in the unrelated `handle_sync_delta` path. Confirmed the read phase (`fetch_bootstrap_data`) is entirely separate from the send phase (`stream_bootstrap`) — the function returns `Result<BootstrapData>` as a whole, so no partial read result can ever reach the streaming loop; a read failure returns before any frame is sent, a serialize failure or dead connection aborts mid-stream with no watermark, and only a fully successful read + fully successful stream reaches the trailing `send_sync_response`. This is airtight.

Assessed the retained `get_latest_sync_id(...).unwrap_or(0)` (unchanged context in the diff, not new): traced the client's handling of `SyncComplete { last_sync_id: 0 }` through `crates/trakkt-ui/src/cache/sync_engine.rs:134-148` (stores cursor "0", no special-casing) and the reconnect effect at `sync_engine.rs:187-256`, which treats a stored cursor of `0` identically to "no cursor at all" — full local wipe (`DeleteAllOfType` for every entity type) plus a fresh `sync_bootstrap`, never a `sync_delta`. So a watermark-read failure alongside an otherwise-successful bootstrap only costs a redundant full re-bootstrap on the client's next reconnect (until the watermark read succeeds again) — wasteful but not data-losing, and not the "silently caught up forever" failure mode the ticket is about. The implementer's argument holds.

Verified the `PendingBatch`/`BatchSerializer<'a>` lazy-boxed-closure design compiles and is sound (`Box<dyn FnOnce() -> Result<Vec<Value>, serde_json::Error> + Send + 'a>`, capturing `&'a [T]` with `T: Sync` bound to satisfy `Send`), and that laziness doesn't create a worse partial-stream state: a later batch's serialize failure can only happen after earlier batches already streamed via `stream_entities`, which is the same "streamed-but-no-watermark" state a mid-stream connection death produces — correct per the invariant, not something worse.

Verified the retained swallow in `workspace_service.rs`'s `fetch_workspace_settings_snapshot` (used only by the already-committed-write callers `update_workspace_name`/`update_workspace_settings`, not by the bootstrap path) by reading the client's `apply_action_to_memory`/`enqueue_cache_writes` in `crates/trakkt-ui/src/cache/apply.rs:46-54,248-250` directly — both paths log-and-return on `action.data == None` rather than clobbering cached state, confirming a dataless upsert is inert and the next bootstrap is what actually repairs it. Claim holds.

Confirmed the `warn!` → `error!` change in `to_sync_values` is consistent with the file's existing severity grading (grepped all `tracing::error!`/`tracing::warn!` call sites: `error!` is used for DB/serialization failures that abort a stream or a request, `warn!` for connection/input-level rejections that don't compromise data integrity).

Confirmed `stream_entities`'s pre-existing `.unwrap_or_default()` on a missing `entity_id` (websocket.rs:1023) is untouched by this diff — out of scope, filed separately per the brief.

Confirmed no behavioral drift versus base: the batch order (issue → label → status → team → project → view → favorite → notification → comment → milestone → workspace_settings) is identical to the original macro-driven sequence; the watermark-read-before-entity-reads ordering established by TRA-9923 is untouched (unmodified context line); and the fetch-all-then-stream memory shape matches base, which also fetched every entity list eagerly before any `stream_batch!` call.

Test integrity — did not trust the implementer's mutation report, ran the mutations myself in a scratch worktree (`/home/jason/repos/trakkt-mutate-tmp`, sibling of the real repo so the `../kode` path dependency resolves, removed after):
- Reverted the `favorites` read to `.unwrap_or_else(|_| vec![])` (old swallow behavior) → exactly `a_failed_entity_read_ends_the_bootstrap_without_a_watermark` failed (6/7 passed), with the failure output showing a full entity stream plus `SyncComplete { last_sync_id: 6 }` — the exact bug this test exists to catch. Independently confirmed via `grep` that `favorites` is read by exactly one bootstrap query (`favorite_service.rs`); `team_service.rs`'s only reference to `favorites` is in `delete_team`, unrelated to the `list_teams` bootstrap read — so the test's stated reasoning for choosing `favorites` over `issues` is correct.
- Reverted `to_sync_values` to filter-and-continue (old behavior, wrapped in `Ok` to preserve the signature) → exactly `a_batch_that_cannot_be_encoded_ends_the_bootstrap_without_a_watermark` failed (26/27), with the panic showing a `SyncComplete { last_sync_id: 99 }` sent after a batch silently lost its unencodable entity.
- Reverted `get_workspace_settings_for_sync`'s parse failure to `.ok()`-swallowed `None` (old behavior) → exactly `unparseable_workspace_settings_end_the_bootstrap_without_a_watermark` failed (6/7), failing at its own premise assertion ("settings that cannot be parsed are unknown, not absent") — an even earlier, more precise catch than the end-to-end assertion.
- Made `stream_bootstrap` return before the trailing `send_sync_response` unconditionally (simulating a "never completes" regression) → 5 of 7 integration tests failed, including all three "positive path" tests (`a_workspace_with_no_settings_row_still_completes`, `an_intact_bootstrap_ends_with_exactly_one_watermark`) plus the pre-existing `bootstrap_reaches_only_the_connection_that_requested_it`, and separately 2 of 27 unit tests failed (`a_fully_encodable_stream_ends_with_the_watermark` plus a pre-existing test) — confirming none of the "happy path" tests are vacuous.

All four mutations were reverted (`git checkout --`) between runs; scratch worktree deleted after. `cargo test --test sync_ws` (7/7) and `cargo test -p trakkt-server --lib routes::websocket` (27/27) both pass on the actual staged diff; the initial `cargo test --test sync_ws --no-run` build was clean with zero warnings. No `#[allow(...)]`, no `let _ =`, no new `unwrap_or_default()`/`unwrap_or_else()` on serialization paths — the only remaining `unwrap_or(0)` is the pre-existing, deliberately-retained watermark read, assessed above and confirmed harmless. No existing test assertion was weakened; the only change to pre-existing test code was `spawn_bootstrap` delegating to a new `spawn_bootstrap_in(.., WORKSPACE)` (pure refactor, default argument, no behavior change).

---

### 13:07 — TRA-9930: Guard test_redis_connects against no-Redis 473s stall

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-core/src/redis.rs` (test module only)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No issues found |

**Anti-pattern tally**: none

**Notes**: Small, tightly-scoped diff (+50/-1, one file, test module only) fixing a 473.78s stall in `test_redis_connects` caused by `ConnectionManager::new`'s internal retry-with-backoff when no Redis is reachable. Fix is `#[ignore]` plus a 2s `tokio::time::timeout` wrapped around `create_pool` at the test call site; `create_pool` itself (redis.rs:14-23) is confirmed untouched, preserving its production retry semantics for real callers.

Assessed the `#[ignore]`-over-catch-and-return decision on its merits: a catch-and-return test prints `... ok` indistinguishable from a run where Redis was genuinely present and working, which is a false green — the day `create_pool`/`ping` regresses, every Redis-less machine (most of them) would report success. `#[ignore]` instead prints `1 ignored`, an honest "nothing was verified." This reasoning holds up, and the doc comment on `CONNECT_TIMEOUT` explains it clearly enough to stop a future "helpful" downgrade to silent-skip. Cross-checked the cited sibling: `/home/jason/repos/kyomi/apps/server/tests/contract_scheduler.rs:26-38` does carry the same `#[ignore]` fix for the identical `ConnectionManager` stall (referencing its own KYO-236/KYO-252), and its `Ok(r) => r, Err(_) => return` at line 36 is exactly the false-green pattern this diff avoids — confirmed by reading the file directly, not taking the implementer's characterization at face value.

Verified the port-6381 citations rather than trusting the summary: `/home/jason/repos/kyomi/.env.example:12` reads `# Redis URL (dev uses port 6380, prod uses 6379)`, confirmed verbatim. `contract_scheduler.rs:32-33` does default to `redis://localhost:6381` for its test Redis, completing the 6379/6380/6381 (prod/dev/test) ladder claim. `crates/trakkt-core/src/config.rs:199` does carry `postgres://tane_test:test@localhost:5434/tane_test` in `test_config()`, matching the parallel Postgres 5432/5433/5434 ladder cited. `podman ps` on this box independently confirms the ladder is live infrastructure, not just doc claims: `kyomi-postgres-dev` on 5433, `kyomi-redis-dev` on 6380. All citations check out — 6381 is deliberate and correctly left alone.

Reproduced all three measurements myself instead of accepting the reported numbers: `cargo test -p trakkt-core --locked` with no Redis on 6381 (confirmed via `ss -ltn`, only 6380 listening) completed near-instantly and reported `36 passed; 0 failed; 1 ignored`. `cargo test -p trakkt-core --locked -- --ignored` with no Redis failed in exactly `2.00s` with a panic naming the URL and Redis explicitly (`no Redis answered at redis://localhost:6381 within 2s`) — not a multi-minute stall. Then started a real `docker.io/library/redis:7-alpine` on `-p 6381:6379` via podman and reran `-- --ignored`: `1 passed; 0 failed` — the Redis path is genuinely exercised, not just structurally present. Stopped and removed the container afterward (`podman stop`, confirmed empty via `podman ps -a --filter name=...`).

Confirmed `cargo clippy --workspace --locked -- -D warnings` (the required check, no `--all-targets`) is clean. `--all-targets` does fail, but only on pre-existing `unwrap_used` in unrelated files (`kv_store_memory.rs`, `retry.rs`) — matches the stated TRA-9956 carve-out, not introduced by this diff. Confirmed via `git diff --cached --stat` the diff touches only `crates/trakkt-core/src/redis.rs`, and confirmed by reading the diff directly that no `#[allow(...)]` was added anywhere (would also be caught by CI's `lint-suppression` job). No CI job currently runs `cargo test` at all (checked `.github/workflows/ci.yml`), so this fix is purely a local-dev-workflow improvement, not a CI-blocking one — consistent with how the ticket describes the pain.

---

### 13:57 — TRA-9924: Fix hardcoded operation-count assertion; add missing `cargo test` CI job

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 1 🟢 (approved)
**Files**: `crates/trakkt-api/src/openapi.rs`, `.github/workflows/ci.yml`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Code Quality / Unverified Claim | `.github/workflows/ci.yml:189` (`Ensure trakkt-ui/dist/ exists for ServeDir`) | Claimed necessary because `leptos_frontend.rs:18` resolves it for `ServeDir`. Empirically false for the `test` job: `build_router()` (the only caller of `static_files_service`/`ServeDir::new(dist_dir())`) is invoked only from `apps/server/src/main.rs:155`, never from any test — confirmed by removing `crates/trakkt-ui/dist` entirely and running both `cargo test -p trakkt-server --locked` and the full `cargo test --workspace --locked`: zero failures either time. Same applies to the `Install tailwindcss`/`Build Tailwind CSS` steps immediately after — nothing in the workspace `include_str!`/`include_bytes!`s `style/output.css`. Harmless (adds a few seconds, doesn't break anything), and matches a checklist the implementer wrote in their own prior ticket comment when investigating the clippy job, but the "genuinely needed" framing for this specific job is not accurate as stated. Not blocking — pure documentation/tech-debt nit for a follow-up. |

**Anti-pattern tally**: Unverified Claim ×1

**Notes**: Two-part diff: (1) replaced a rotting `all_36_operations_present` count assertion with a hand-maintained `EXPECTED_OPERATIONS` set comparison, verified via direct mutation testing to bite in both directions — added a fake op → correctly reported as "listed but absent" citing the collision-vs-removal ambiguity; removed a real op from the list → correctly reported as "present but missing from list", with a hint to add it in sorted position. Entries print pre-indented (8 spaces) and comma-terminated, confirmed pastable directly back into the source, and confirmed to print in sorted order even when multiple mutations are inserted out of order (BTreeSet::difference iterates sorted). The "operation set collected from the generated spec, not the registry" design claim was independently reproduced: forced a real path+method collision (temporarily made `create_label` share `GET /labels` with `list_labels`) and confirmed `path_entry[method] = operation` in `openapi.rs:69` silently overwrites, surfacing as `list_labels` reported missing by the test with the exact collision-aware message. Cross-checked all 43 `EXPECTED_OPERATIONS` entries 1:1 against the real `name: "..."` fields across every `crates/trakkt-api/src/*.rs` operations() registrant — exact set match, no typos either side. Confirmed the list is a plain literal `&[&str]`, no macro/build.rs deriving it from `all_operations()`. (2) Added the missing `Test Suite` CI job. Structural diff against `clippy`'s steps confirmed byte-identical except the toolchain `with:` block (clippy adds `components: clippy, targets: wasm32-unknown-unknown`) and the cargo command (plus clippy's extra wasm32-target step, absent here). Confirmed the wasm32 omission is correct: every `#[wasm_bindgen_test]` in `trakkt-ui` (idb_writer.rs, sync_engine.rs, tab_leader.rs) is gated behind `#[cfg(target_arch = "wasm32")]`, and the `wasm-bindgen-test` dev-dependency itself is target-gated in Cargo.toml, so host-target `cargo test --workspace` never touches it (tracked separately per the comment, and independently verified TRA-9932 exists and `relates_to` this ticket). Confirmed no Postgres/Redis needed: the one Redis test is `#[ignore]` (TRA-9930), Postgres-mentioning tests are either `DbPool::Postgres` match arms or pure SQL-string-generation unit tests, integration tests explicitly document "no Postgres, no Redis, no sockets", and all DB-backed tests use `DbPool::connect("sqlite::memory:")`. `timeout-minutes: 30` arithmetic checked out (295s × ~3 CI/local ratio + 2-3min setup ≈ 17-18min worst case, 30min ≈ 1.7x headroom) and lines up with the real TRA-9930 incident referenced in commit history (`f46f462`, #265). Ran the full suite myself twice (with and without `crates/trakkt-ui/dist` present): `cargo test --workspace --locked` — 0 failures, 1 ignored (Redis), ~75s warm-cache wall time; `cargo clippy --workspace --locked -- -D warnings` — clean, ~43s. Fetched TRA-9924 directly to confirm all five acceptance criteria are addressed and the cited follow-up tickets (TRA-9932, TRA-9961) are real and correctly linked. Only finding is a minor unverified/inaccurate claim about one setup step; does not affect correctness. Signed.

---

### 14:04 — TRA-9924: Re-review — comment-only correction to prior 🟢 finding

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `.github/workflows/ci.yml` (comment text only)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No new findings. Prior 🟢 (unverified/inaccurate justification for the asset-prep steps) is resolved. |

**Anti-pattern tally**: none

**Notes**: Narrow scope: confirm the requested comment fix landed without collateral changes, since I no longer trusted the implementer's own diff-of-the-diff at face value. Located the exact pre-fix blob (`880c612`) in the object database via a distinctive-string search (`grep` across all loose+ref blobs for "Setup mirrors the clippy job") since the change was never committed between review rounds, then diffed it directly against the currently staged blob (`15baba8`) myself rather than trusting the implementer's reported diff-of-the-diff. Confirmed exactly two hunks, both 100% `#`-prefixed comment lines (job-header blanket clause reworded to separate "genuinely required" build/sccache/patch-stripping from "kept for parity, not required" asset-prep; a new comment block added above the `Ensure trakkt-ui/dist/ exists for ServeDir` step) — zero step names, `run:`/`uses:`/`with:`/`env:` values, or ordering touched. Parsed both versions with `yaml.safe_load` and diffed the resulting dicts: all 5 jobs present in both, `test` job's 12 steps identical in content and order, `timeout-minutes: 30` unchanged, full parsed-jobs dict equality confirms no semantic drift anywhere in the file, not just the touched region. Verified the new comment's factual claims myself rather than accepting the implementer's re-derivation: `ServeDir::new(dist_dir())` lives in `apps/server/src/leptos_frontend.rs:32`, wired into `build_router()` at `apps/server/src/lib.rs:73/119`, whose only caller anywhere in the repo is `apps/server/src/main.rs:155` (confirmed via workspace-wide grep for `build_router(`) — no test calls it. Confirmed via workspace-wide `include_str!`/`include_bytes!` grep that neither macro touches `style/output.css` anywhere (the only two hits are an email-logo PNG and a migration SQL string, both unrelated). Also confirms the corrected comment no longer overstates (doesn't claim the steps are useless — explicitly says "kept on purpose... mean nothing here has to change the day a test does touch dist/ or the built CSS") nor understates (doesn't hide that no current test needs them). Separately confirmed `crates/trakkt-api/src/openapi.rs` untouched since the first review: only one blob with `EXPECTED_OPERATIONS` content exists anywhere in the object database (the currently staged one — no alternate version to diff against), staged index matches working tree exactly (empty `git diff`), and its mtime predates the ci.yml edit. Everything I verified in the first pass on that file stands unexamined. Signed.


---

### 14:35 — TRA-9932: WASM Browser Tests CI job (wasm-pack test in headless Firefox)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `.github/workflows/ci.yml`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| — | — | — | — | No blocking issues found |
| 1 | 🟢 | Copy-Paste / Missing Abstraction | ci.yml:270-283 (wasm-test) vs clippy:75-88, test:167-180 | The checkout/build-deps/toolchain/sccache-verify/strip-patches/cargo-update boilerplate is now duplicated a third time verbatim. Pre-existing pattern (already duplicated between clippy and test before this diff), not new debt introduced by this PR, and out of scope given the ticket is "one file, CI config only." Worth a composite action if a fourth job ever needs this setup. |

**Anti-pattern tally**: Copy-Paste ×1 (non-blocking, pre-existing pattern)

**Notes**: Adds a sixth, fully independent (`grep -c needs: → 0`) `wasm-test` job running the 12 `wasm-bindgen-test`s in headless Firefox — the only behavioral coverage of the IndexedDB cache, Web Locks leader-election shim, and hydration gate, previously only compile-checked by clippy's wasm32 target. Verified all 8 points raised in the review brief directly rather than trusting the implementer's report:

1. **sed + cargo update ordering**: Present at ci.yml:307/312, in the same order as clippy/test. Ran the exact `sed -i '/# BEGIN_LOCAL_DEV_PATCHES/,/# END_LOCAL_DEV_PATCHES/{...}'` expression against a scratch copy of the real `Cargo.toml` — it removes exactly the `[patch.crates-io]` / `kode-leptos = { path = ... }` lines and leaves both marker comments intact, matching `git diff` output.

2. **wasm-pack URL**: `curl -sSIL` against `https://github.com/rustwasm/wasm-pack/releases/download/v0.14.0/wasm-pack-v0.14.0-x86_64-unknown-linux-musl.tar.gz` resolves 200 (redirects to a signed `release-assets.githubusercontent.com` URL). The same path under `drager/wasm-pack` 404s, confirming the org-migration claim.

3. **YAML validity**: `python3 -c "yaml.safe_load(...)"` parses clean, 6 jobs present. Separately downloaded `actionlint` v1.7.7 myself (not reusing the implementer's report) and ran it against the file — exit 0, zero findings.

4. **Omitted dist/Tailwind steps**: `cargo tree -p trakkt-ui --target wasm32-unknown-unknown --features hydrate --invert cc` confirms `cc` is a real transitive build-dependency (via `kode-leptos → arborium-* → tree-sitter` grammars), justifying the apt install block. Grepped the whole workspace for `include_str!`/`ServeDir`/`dist_dir` — `dist_dir()`/`ServeDir` exist only in `apps/server/src/leptos_frontend.rs`, which is absent from `trakkt-ui`'s wasm32+hydrate dependency graph entirely, and nothing anywhere does `include_str!("output.css")`. Went further than the brief asked: actually ran `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate --locked` locally (Firefox present, wasm-pack 0.14.0 matching the pinned version) with `crates/trakkt-ui/dist/` absent and `style/output.css` absent — **12/12 passed**, empirically confirming the omission is correct, not just plausible. This also validates the exact command/flag syntax in the new "wasm-pack test" step (`--locked`/`--lib`/`--features hydrate` are forwarded to the underlying `cargo test` as documented by `wasm-pack test --help`'s "extra options" behavior).

5. **Path-filter trap**: Confirmed the underlying premise — no `needs:` anywhere in the file, so all 6 jobs are wall-clock-parallel, and no `on: pull_request: paths:` filter exists at the workflow level, consistent with the decision not to add one. The described trap (a `paths:`-filtered required check never posts a check run on a non-matching PR, permanently blocking merge) is a real, well-documented GitHub Actions footgun, not run unconditionally is the correct call given the stated intent to make this a required check.

6. **timeout-minutes: 20**: Re-derived both estimates in the comment. Method 1: `0.64 × 438s (7m18s) ≈ 280s` + ~60s Firefox install ≈ 5.7min ≈ "~6min typical" — checks out. Method 2: `3× × 189s ≈ 567s` + 3-4min setup ≈ 12.45-13.45min ≈ "~13min worst legitimate" — checks out. `20min / 13min ≈ 1.54×` headroom, close to Test Suite's `30min / 18min ≈ 1.67×` as claimed.

7. **Firefox choice**: `firefox-version: latest` is a documented valid value for `browser-actions/setup-firefox@v1` (confirmed against the action's own README matrix example, not just the action.yml input description). Agree unpinned is the right call — pinning would freeze the one variable this suite exists to exercise. Confirmed via `grep uses:` that `trivy-action` is the only SHA-pinned action in the file (by full commit hash) and everything else, including the new `setup-firefox@v1`, is tag-pinned — the implementer's "matches dominant convention, flagging as a policy question rather than deciding unilaterally" framing is accurate; agree this is out of scope for a single-file CI-job-addition PR.

8. **Cargo.toml untouched**: `git diff --cached -- crates/trakkt-ui/Cargo.toml` is empty; `git diff --cached --stat` shows only `.github/workflows/ci.yml` changed. The target-split dev-dependencies at Cargo.toml:124-132 (native-only `sqlx`/`tokio`/`trakkt-core`/`trakkt-auth` vs wasm32-only `wasm-bindgen-test`) are exactly what the new job's `--lib --features hydrate` invocation expects and match the doc comment there ("Run: wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate").

No lint suppressions, no hardcoded secrets, no fallback/error-swallowing patterns introduced. Job ordering in the file matches the updated header comment (1-6). Signed.

---

### 15:46 — TRA-9933: Route UI-initiated cache deletes through the single IdbWriter

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/cache/delete_route.rs` (new), `crates/trakkt-ui/src/cache/store.rs`, `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/cache/tab_leader.rs`, `crates/trakkt-ui/src/cache/mod.rs`, `crates/trakkt-ui/src/components/layout.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No issues found. |

**Anti-pattern tally**: none

**Notes**: `SyncStore::delete_from_idb` used to open its own `init_cache_db` handle and write fire-and-forget from whichever tab took the click, bypassing the FIFO `IdbWriter` TRA-9918/9919 built. This PR replaces it with a `DeleteRoute`: the leader enqueues directly on its own writer, a follower posts `SyncBroadcastMessage::CacheDelete` and the leader enqueues on its behalf. Verified point-by-point: (1) `components/layout.rs:235-237` does guard `start_sync_engine` behind `if !leader_now || leader_started.get() { return; }` — a follower genuinely has no `IdbWriter`, so the broadcast design isn't over-engineering, it's the only option. (2) the unrouted-tab drop case (`delete_route.rs:106-115`) only applies to a tab with Web Locks but no `BroadcastChannel`, a combination essentially absent in real browsers (feature support parity); logged via `tracing::warn!` with the dropped entities, not silent. (3) leave-team emits `TEAM`/`Update` not `Delete` (confirmed at `trakkt-auth/src/team_service.rs:1007` via `write_membership_sync_entry`), and both `remove_team` call sites (`layout.rs:1276`, `team_settings.rs:177`) are unchanged and durability is covered by dedicated leader and follower wasm tests. (4) mutation-tested all three `delete_route.rs` wasm tests by restoring the old bypass in `remove_team` — all three failed at their intended assertion (lines 279, 325, 402) for the intended reason (sink call log never shows the delete because it never reached the queue); reverted cleanly, diff matches HEAD's staged content again, re-ran clean (15/15). (5) `set_workspace_id`/`workspace_id()` removal verified dead — grepped whole tree, zero remaining callers. (6) all seven deleted `remove_*` methods (issue, label, status, view, favorite, notification, comment) confirmed unreachable across the tree; `remove_*_in_memory` variants retained and still used by `cache::apply`. (7) promoted-follower path: `cache_writer` `StoredValue` is re-read per broadcast message via `.with_value`, so a tab promoted after the handler was installed starts servicing others' deletes without re-registering — verified in the follower wasm test. (8) grepped `git diff --cached --name-only` — only `crates/trakkt-ui/` files touched, no server code. All four required CI gates run clean: workspace clippy, wasm32 clippy (hydrate), `cargo test --workspace` (all green), `wasm-pack test --headless --firefox` (15/15, up from 12 as claimed). `IdbWriter::clone()` calls confirmed cheap (wraps `mpsc::UnboundedSender`, all clones feed one queue). No lint suppressions, no fallback/error-swallowing, no dead code left behind. Signed.

---

### 16:50 — TRA-9938: Fix milestone sync so create/rename/re-date reach other clients

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/project_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/cache/store.rs`, `crates/trakkt-ui/src/pages/issues/issue_detail.rs`, `crates/trakkt-ui/src/pages/projects/project_detail.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Comment accuracy | `issue_detail.rs:864-867` | The comment's specific claim ("reading it inside the branch would leave the effect subscribed to `project_id` alone, so moving into a project later would never pick up the dependency") overstates the risk. Leptos re-derives an effect's subscriptions from what it actually reads on each run; since `project_id.get()` is already read unconditionally, the very run where `project_id` transitions `None → Some` re-enters the `if let` branch and would establish the `milestones_version` subscription then, not "never." The unconditional read itself is still correct and the more defensive style — this is a documentation nit, not a functional bug. |
| 2 | 🟢 | Testing gap (verified pre-existing, not unique to this PR) | `project_detail.rs:104-118`, `issue_detail.rs:849-880` | No automated test exercises the two page-level `Resource`/`Effect` wiring changes — reverting either leaves the whole suite green. Confirmed via subagent research this is a genuine, systemic gap: the three prior version counters (`comments_version`, `activities_version`, `relations_version`) have identical store-level-only coverage, and the repo's Playwright suite (`e2e/`) has never been used for realtime multi-client wiring of any kind. Not a corner uniquely cut here. Recommend manual/browser verification before merge and, separately, a follow-up ticket to add Playwright coverage for this whole class of behavior — but holding this PR to a stricter bar than every prior instance of the same pattern would be inconsistent. |

**Anti-pattern tally**: none blocking; 2 informational/minor items above.

**Notes**: Verified every item in the review brief empirically rather than by reading claims:

1. **Correction to the brief**: confirmed against `origin/main` that `project_service.rs`'s live broadcasts already passed `serde_json::to_value(&milestone).ok()` and the real `sync_id` for both create and update — only the three `write_sync_entry(..., None)` calls (create, update, delete) were defective. The implementer's correction to the reviewer's brief was accurate. The new shared `payload` variable (`payload.clone()` to the sync-log write, `payload` moved into the broadcast) genuinely couples the two — proved this by independently mutating each call site: forcing the sync-log write to `None` broke only `delta_carries_a_payload_for_every_milestone_write`; forcing the broadcast to `None` broke only `milestone_create_frame_carries_the_new_milestone`. Both mutations reverted cleanly (`git diff` against the index was empty afterward).
2. **Payload/bootstrap shape match**: `MILESTONE_SELECT` (used by the create/update re-fetch) and `list_milestones_for_workspace`'s hand-rolled SQL (used at bootstrap) select the identical column set with the same `CAST(... AS TEXT)` on `target_date`/`created_at`, both mapped through the same `MilestoneRow::into_dto`. Delta and bootstrap serialize the same shape. Confirmed the re-fetch now runs before `write_sync_entry` in both `create_milestone` and `update_milestone` (diff moves the block up, doesn't duplicate it). `db_fetch_one!` still propagates via `?` — reordering only changes *when* a re-fetch failure surfaces (now before an orphaned no-payload sync_log row could be written, previously after), a strict improvement, not a regression. No new race window: the DB-level `INSERT/UPDATE → SELECT` sequence is unchanged; only the position of the unrelated `sync_log` write moved.
3. **Delete arm addition**: confirmed `enqueue_cache_writes`'s Insert/Update match has no entity-type gate ahead of the generic `IdbOp::Upsert` (aside from issue description-splitting) — `PROJECT_MILESTONE` payloads were already being persisted to IDB by the generic path once fix #1/#2 gave them a payload. The added `PROJECT_MILESTONE` arm in the Delete match is a real fix (a deleted milestone's IDB row would otherwise outlive it until a full reset), not dead code. Scoped, tested, low-risk — acceptable to include in this PR rather than split out.
4. **Effect dependency claim**: code change verified safe (see finding #1 above for the nuance on the stated justification).
5. **Test integrity**: mutation-tested three claims directly (see brief items above) plus hoisted the `bump_milestones_version()` call in `apply.rs` above the data-less guard — confirmed it breaks only `a_milestone_action_without_a_payload_reaches_nothing` (not the sibling bump-count tests), so the "counter is 0" assertion is not vacuous. All mutations reverted; `git diff` against the staged index confirmed empty before re-running CI gates.
6. **Coverage gap**: see finding #2 above.

All four required CI gates run clean and match the reported numbers exactly: `cargo clippy --workspace --locked -- -D warnings` (clean), `cargo clippy --locked -p trakkt-ui --target wasm32-unknown-unknown --features hydrate -- -D warnings` (clean), `cargo test --workspace --locked` (376 passed, 0 failed), `wasm-pack test --headless --firefox crates/trakkt-ui --lib --features hydrate` (15 passed, 0 failed). No lint suppressions, no `unwrap_or_default`/silent error-swallowing (the new `milestone_sync_payload` helper matches the coding standard's serialization-error pattern exactly: `match` + `tracing::warn!`), no hardcoded values, no new duplication. Signed.

---

### 16:52 — TRA-9938: Milestone sync — round 2, comment correction only

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/pages/issues/issue_detail.rs` (comment only)

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| — | — | — | — | No issues found. The 🟢 from round 1 (`issue_detail.rs:864-867` comment overstated Leptos dependency tracking with a "never" claim) is resolved. |

**Anti-pattern tally**: none.

**Notes**: Scoped re-review of a single requested correction — the round-1 comment at `issue_detail.rs:864-867` claimed reading `milestones_version` inside the `if let Some(pid)` branch would mean a later `None → Some` transition "would never pick up the dependency," which is wrong: Leptos re-derives an effect's subscription set every run, so that transition run would establish it. Verified three things independently rather than trusting the implementer's report:

1. **Genuinely comment-only, code unchanged**: `git diff --cached` (against `origin/main`) is cumulative for this uncommitted branch, so it can't isolate round 2's edit by itself. Cross-referenced instead against the round-1 log entry above, which quotes the exact old sentence verbatim and describes the exact code shape it was reviewing ("since `project_id.get()` is already read unconditionally"). Read the current file directly (`issue_detail.rs:852-885`): `let _ = milestones_version.get();` → `let pid = project_id.get();` → `if let Some(pid) = pid { ... } else { ... }` is byte-identical in structure and position to what round 1 described and approved — only the 5-line comment block above it (lines 862-866) changed. Attempted git-object archaeology (dangling blobs via `git fsck --unreachable`) to find a literal round-1 snapshot for a byte diff; the closest candidate (`ce6703c4`, 3 hunks) turned out to carry unrelated star-button styling deltas too, meaning it's a stale draft from earlier in the file's life, not a clean round-1↔round-2 pair — abandoned that path as unreliable rather than presenting it as proof. The verbatim-quote cross-reference plus direct code reading is the solid evidence here.
2. **New comment is accurate**: no longer claims "never" — now says reading inside the branch "would pick the dependency up later and less predictably," which matches Leptos's per-run re-subscription model exactly (the `None → Some` transition run re-enters the branch and reads `milestones_version` then, establishing the subscription from that point, not before). Does not swing to the opposite overstatement of calling the conditional form "fine" — still correctly argues the unconditional read is the better pattern because it keeps the dependency set stable rather than data-dependent. No new inaccuracy introduced.
3. **`project_detail.rs:104-110` comment** (flagged as the one other place the same mistake could recur): confirmed it describes the `Resource::new` keying mechanism (`move || (project_id.get(), milestones_version.get())`), a genuinely different and simpler mechanism than an `Effect`'s dynamic dependency tracking — a `Resource` always fully re-derives its key tuple and refetches on any change, no "picked up later" subtlety applies. The comment makes no claim about subscription timing at all, so there was nothing to get wrong. Accurate as written.
4. **Other five files untouched**: diffed `project_service.rs`, `sync_log_service.rs`, `apply.rs`, `store.rs` against `origin/main` and matched each against the specific claims recorded in the round-1 log (shared `payload` variable at both call sites, re-fetch moved above the sync-log write, `milestone_sync_payload` helper, `PROJECT_MILESTONE` delete arm in `enqueue_cache_writes`, test names `a_milestone_action_bumps_only_the_milestones_version` / `milestone_create_frame_carries_the_new_milestone` / etc.) — all present, unchanged, consistent. Nothing round 1 verified needed re-examination.

Re-ran the required gate for this file: `cargo clippy --locked -p trakkt-ui --target wasm32-unknown-unknown --features hydrate -- -D warnings` — clean, zero warnings. Did not re-run the full test suite or repeat mutation testing, per the narrowed scope of this round — nothing in the verified-unchanged files warranted it. Signed.

---
