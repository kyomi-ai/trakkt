---

### 01:06 — TRA-9919: Cross-tab sync leadership (Web Locks election + BroadcastChannel followers)

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 2 🟢
**Files**: `crates/trakkt-ui/src/cache/tab_leader.rs` (new), `crates/trakkt-ui/src/cache/apply.rs` (new), `crates/trakkt-ui/src/cache/sync_engine.rs`, `crates/trakkt-ui/src/cache/idb_writer.rs`, `crates/trakkt-ui/src/cache/websocket.rs`, `crates/trakkt-ui/src/cache/mod.rs`, `crates/trakkt-ui/src/components/layout.rs`, `crates/trakkt-ui/Cargo.toml`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|--------------|
| 1 | 🟢 | Code Quality | apply.rs:375 | `UnboundedReceiver::try_next()` is deprecated in the pinned `futures` version; use `try_recv()`. Test-only, no functional effect. |
| 2 | 🟢 | Missing Test / Doc precision | layout.rs:220, websocket.rs:213-218 | `provide_context(ws_client.clone())` on promotion only reaches consumers created *after* the promotion (Leptos context is a setup-time snapshot, not reactive); a page already mounted when promotion happens keeps the stale `Disconnected` handle if it ever reads `WebSocketClient` via context. No current consumer exists (verified: only `sync_engine.rs`/`websocket.rs` reference the type, and the engine is handed the handle directly, not through context), so this has zero live impact today. Flagging so the caveat is explicit before any future page starts consuming `WebSocketClient` from context. |

**Anti-pattern tally**: Code Quality ×1, Missing Test/Doc precision ×1

**Notes**: This is the leadership-election piece of the tab-leader sync series. Independently re-derived the two headline evidence claims rather than accepting the summary: (1) diffed the Insert/Update memory-store match arm byte-for-byte against `sync_engine.rs` at `0a195bc` — identical; (2) extracted the call-sequence of `store.*`/`enqueue_delete`/`bump_*` calls from the original interleaved Delete arm and confirmed the split's memory-half (11 calls) and cache-half (10 calls) preserve the exact original order and set. Ran `cargo check --target wasm32-unknown-unknown --features hydrate` (clean — this exercises the `layout.rs` block the implementer said was compile-checked only, not executed) and `cargo clippy` on both native and wasm32 targets (zero warnings). Ran `cargo test -p trakkt-ui --lib` (59/59 pass, all new tests included) and confirmed the one workspace test failure (`trakkt-api::openapi::tests::all_36_operations_present`) reproduces identically on `origin/main` — pre-existing and unrelated, matches the ticket's known-red note. Re-verified the previously-vacuous election test is now real: `only_one_tab_is_granted_leadership_at_a_time` stands two genuine `acquire_leadership` calls (production path, not the test's own `hold_lock` helper) against the same exclusive lock name and asserts exactly one is granted — this is a real browser-enforced mutual exclusion test, not a tautology. Traced every double-leadership path named in the brief (Unsupported fallback, promoted-tab-races-new-tab, grant-callback re-entrancy, the two `Rc<Cell<bool>>` guards) and found each correctly closed: the `Unsupported` fallback is a documented, symmetric regression to pre-fix behavior across all tabs of an unsupporting browser, not an asymmetric hole; the exclusive Web Lock itself (not app logic) is what prevents the race-a-new-tab scenario; the grant callback guards re-entrancy with `Option::take()`; and the `sync_started`/`leader_started` cells are read-then-set within a single synchronous effect execution with no yield point between, so they cannot double-fire. Broadcast ordering is enforced structurally (every broadcast rides `IdbOp::Notify` on the same FIFO writer queue as the entity writes, with native tests proving both the single-notify-ordering and multi-notify-ordering cases) and the one exception called out as read-only-verified (`apply_sync_action`'s enqueue-then-broadcast-then-memory-update ordering) checks out. The added `Reset` broadcast on the cursor-less bootstrap path is correct and necessary — without it a follower would keep rendering entities the leader just wiped via `DeleteAllOfType`. Follower purity holds: no path from the broadcast channel reaches `IdbWriter` or `db::*`. Overall this is an unusually well-evidenced diff — the implementer's own doubt about the election test and the context hand-off were the right things to be suspicious of, and both check out clean or benign on close inspection.

---

---

### 02:21 — TRA-9929: Parseable broadcasts for status/project sync (live frame + delta payload fix)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/trakkt-auth/src/project_service.rs`, `crates/trakkt-auth/src/status_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`

**Anti-pattern tally**: none found

**Notes**: Two-part fix — (1) four+one call sites converted from `broadcast_sync_notify` (an envelope the client's `SyncAction` deserializer can't parse — logs and drops) to `broadcast_sync_action` with a real payload and real `sync_id`; `broadcast_sync_notify` deleted along with its now-unused `MessageType`/`WebSocketMessage` imports (verified zero remaining references repo-wide; `broadcast_to_workspace` confirmed still used at `websocket/helpers.rs:191,252` via its own independent import). (2) The same call sites' `sync_log` rows, previously written with `data: None`, now carry a serialized entity — the client skips data-less Insert/Update on both the live frame and delta (`cache/apply.rs:47-53`), so this half was equally broken. Verified the re-fetch reordering in `create_status`/`create_project` (refetch hoisted above the sync write) does not change the function's return value (same `row.into_dto()` computation, just earlier), does not lose DB-assigned `created_at`/`updated_at` (asserted non-empty in new tests), and narrows rather than widens the failure window: if the post-insert refetch now fails, the function propagates `Err` and writes nothing to `sync_log` — previously a refetch failure after a successful `data: None` sync write. Traced every converted site's payload source: `project_sync_payload` (new helper, 3 call sites) always reads the project row fresh via `get_project`, and for `add_project_member`/`remove_project_member`/`create_project_update` the project row is provably unaffected by the satellite-table write, so there's no staleness. `update_project`'s payload is the post-update re-fetched row (test asserts the new `name` value, not the pre-update one). The 5th site (`create_project_update`, ticket spec's write-site list was incomplete) was found and fixed by the implementer and is now complete — confirmed no other `write_sync_entry` call in these two files still passes `None` for a non-Delete action outside the explicitly deferred `PROJECT_MILESTONE` sites (TRA-9938). `add_project_member`'s pre-insert existence check changes a silent-success-with-orphan-row (no FK on `project_members`) into a clean `NotFound` — checked all three callers (all route through `verify_project_ownership` in `server_fns/projects.rs`, which already guarantees the project exists) and confirmed nothing depends on the old permissive path. Ran the 6 new tests plus the full `trakkt-auth` lib suite (105/105 pass) and independently confirmed the two claims that mattered most: the delta test (`delta_carries_a_payload_for_every_status_and_project_write`) runs with `ws_manager: None` throughout (grep-verified — no live broadcast possible), and its `assert_eq!(statuses, 1)` / `assert_eq!(projects, 4)` are exact counts, not `>= 1` style checks that would pass vacuously. The live-frame tests parse through `serde_json::from_str::<SyncResponse>`, matching the client's real `from_value::<SyncResponse>` call in `cache/websocket.rs:359`. Sanity-checked the `project_members`-is-not-a-sync-entity claim directly against `trakkt_types::sync::entity_types` (no `PROJECT_MEMBER` constant) and `apply.rs` (no arm) — correct, and the member-add/remove tests honestly assert the received payload equals the *parent project*, not a member record. No lint suppressions, no new silent error discards (the `sync_id` `unwrap_or_else(|e| { warn!; 0 })` pattern is the pre-existing documented "0 = no sequence info" convention, already used before this diff). `cargo clippy -p trakkt-auth -- -D warnings` clean. Pre-existing `cargo fmt --check` drift on both touched and untouched files in the crate confirmed present identically on `origin/main` via `git stash` — not introduced by this diff, not flagged.

---
