# Review Log — 2026-06-06

---

### 14:00 — TRA-206: Add project sidebar section and create-project modal

**Review type**: initial
**Result**: 0 🔴, 2 🟡, 3 🟢
**Files**: `crates/trakkt-ui/src/components/layout.rs`, `crates/trakkt-ui/src/components/project_creation_modal.rs`, `crates/trakkt-ui/src/components/mod.rs`, `crates/trakkt-ui/src/pages/projects/project_list.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Fallback Code / Suppressing Instead of Fixing | project_creation_modal.rs:79 | `unwrap_or_default()` on `list_workspace_members()` — silently swallows server error; banned by CODING_STANDARDS.md. Should be `match` + `tracing::warn!`. |
| 2 | 🟡 | Logic and Correctness | project_creation_modal.rs:52,140 | `set_submitting` declared and reset on open but never set to `true` before `spawn_local` and never set back to `false` on completion. Button never disables during submission; "Creating..." label never shows; all inputs stay enabled. Dead `error` signal also declared but never written. |
| 3 | 🟢 | Quick & Dirty / Not Built to Last | layout.rs:518 | `is_expanded` snapshotted as plain `bool` for `is_expanded.then(...)` — works coincidentally because `expanded.get()` is also read for the caret icon, but fragile. Should use `move \|\| expanded.get().then(\|\| ...)`. |
| 4 | 🟢 | Code Quality | layout.rs:542 | `+` button uses toggle (`!*v`) instead of `set(true)` — double-click closes the modal before it renders. Pre-existing in Teams section too. Minor. |
| 5 | 🟢 | Code Quality | layout.rs:634 | Doc comment still mentions "Pinned projects come from favorites" — the code that did this was just removed by this PR. Stale comment. |

**Anti-pattern tally**: Fallback Code ×1, Logic Correctness ×1, Quick & Dirty ×1, Code Quality ×2

**Notes**: Overall a clean, faithful port of the `SidebarTeamsSection` / `TeamCreationModal` patterns. SPDX header present, localStorage persistence correctly gated, SyncStore upsert called on all code paths. The two blockers are: a banned `unwrap_or_default` on a server call, and a functional bug where `submitting` signals are wired up but never actually set during the async lifecycle, meaning the duplicate-submission guard and loading state never fire.

---

### 15:30 — TRA-206: Add project sidebar section and create-project modal

**Review type**: re-review (cycle 2)
**Result**: Clean (approved)
**Files**: `crates/trakkt-ui/src/components/layout.rs`, `crates/trakkt-ui/src/components/project_creation_modal.rs`, `crates/trakkt-ui/src/pages/projects/project_list.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Quick & Dirty / Not Built to Last | layout.rs:518 | `is_expanded` snapshot still present — works in practice but fragile; inner closure should read `expanded.get()` reactively. |
| 2 | 🟢 | Code Quality | layout.rs:542 | Sidebar `+` button still uses toggle (`!*v`) instead of `set(true)` — double-click closes modal before render. |
| 3 | 🟢 | Code Quality | layout.rs:633–637 | Stale doc comment on `SidebarWorkspaceSection` still references "pinned projects come from favorites" — removed by this PR. |

**Anti-pattern tally**: Quick & Dirty ×1, Code Quality ×2

**Notes**: Both 🟡 blockers from pass 1 are correctly resolved. `unwrap_or_default` replaced with `match` + `tracing::warn!`. `set_submitting(true)` placed before `spawn_local`, `set_submitting(false)` on error path, all input fields and button gated correctly. Three minor issues carried over unfixed from pass 1 — none block approval. Signed.

---

### 16:15 — TRA-201: Add project-scoped board and list views

**Review type**: initial
**Result**: 0 🔴, 2 🟡, 2 🟢
**Files**: `crates/trakkt-ui/src/pages/projects/project_board.rs`, `crates/trakkt-ui/src/pages/projects/project_list_view.rs`, `crates/trakkt-ui/src/pages/projects/project_detail.rs`, `crates/trakkt-ui/src/pages/projects/mod.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟡 | Design System / Component Violations | project_detail.rs:1056–1080 | `ProjectViewTab` uses raw `<button>` with inline Tailwind instead of `<Button>` component. `button.rs` explicitly mandates "All buttons in the app MUST use this component." `ButtonVariant::Pill`/`PillActive` + `ButtonSize::Xs` already exist for this exact use case. |
| 2 | 🟡 | Logic and Correctness | project_list_view.rs:93–97 | `GroupBy::Milestone` groups all milestoned issues into a single "Has milestone" bucket — `IssueWithDetails` has no `milestone_name` and `SyncStore` has no milestones. The option should be removed or milestone data added as a prop before shipping. |
| 3 | 🟢 | Copy-Paste / Missing Abstractions | project_board.rs:629–636, project_list_view.rs:429–436, project_detail.rs:1573–1580 | `team_color` lookup is an identical 5-line block in three separate components. Should be extracted to a shared helper. |
| 4 | 🟢 | Copy-Paste / Missing Abstractions | project_board.rs:51–57 | `sort_column_issues` is a trimmed copy of the same function in `board.rs`. Comment should explain the intentional omission of `sort_order`. |

**Anti-pattern tally**: Design System ×1, Logic Correctness ×1, Copy-Paste ×2

**Notes**: The core kanban logic is correct — drag-drop correctly resolves team-specific status by category, toast error fires when no status exists in the target category, optimistic update reverts on server error. URL tab routing and project filtering are both correct. SPDX headers present. The two blockers are: a mandated component violation (raw button) and a misleading/broken grouping option (Milestone groups all milestoned issues together instead of per-milestone). Two minor DRY issues noted.

---
