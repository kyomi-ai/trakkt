---

### 16:10 — TRA-9974: sync_log entries for the six notification state changes

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 6 🟢 (approved)
**Files**: `crates/trakkt-auth/src/notification_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/issue_service.rs`, `crates/trakkt-ui/src/cache/apply.rs`, `crates/trakkt-ui/src/server_fns/notifications.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Comment and Doc Accuracy | sync_log_service.rs:9277 | "The control: A's own delta does carry them, so the assertion above is about audience and not about the entries being missing outright" — the control is satisfied by the seed's own `Insert` entries and stays green when the function under test writes nothing at all (reproduced: Mutation A). Fix: filter the delta to `SyncActionType::Update`, as line 9595 already does. |
| 2 | 🟢 | Comment and Doc Accuracy | notification_service.rs:348 | "Every predicate below is one the change itself falsifies" is false for `mark_as_read`, which passes `deleted_at IS NULL` while changing `read`. Same overclaim at :336 ("one entry per row they actually changed"). Verified on both backends: marking an already-read notification writes an entry. |
| 3 | 🟢 | Comment and Doc Accuracy | notification_service.rs:521 | `mark_as_read` doc says an already-read mark "succeeds and writes an entry, which is the behaviour before this became transactional" — the succeeding is prior behaviour, the entry is new. |
| 4 | 🟢 | Missing Error Context / invariant at callee | notification_service.rs:366, :419 | Both helpers emit `IN ()` (a syntax error on both dialects) for an empty id set; the guard is documented only at the `change_notifications` call site. Standards ask the invariant be recorded at the callee. |
| 5 | 🟢 | Quick and Dirty | apply.rs:2440 | Stray blank line before the closing brace of `mod wasm_tests`; rustfmt would drop it. No CI fmt gate, so cosmetic. |
| 6 | 🟢 | Testing coverage | apps/server/tests/postgres_dialect.rs | No `dual_backend_test!` for the six converted paths. Not a live defect — reviewer executed all six against the live Postgres on 5436 via a temporary probe and every assertion passed on both backends — but a regression-guard gap in the suite that exists for this class. |

**Anti-pattern tally**: Comment and Doc Accuracy ×3, invariant-at-callee ×1, Quick and Dirty ×1, Testing coverage ×1

**Notes**: Unusually strong change. Reviewer independently reproduced, rather than inspected, six load-bearing claims: per-function entry suppression for `mark_all_as_read` and `bulk_restore_notifications` (2 tests each fail, and the right ones); audience downgrade to `Workspace` (both isolation halves fail independently, confirming the persisted and delivery sides are each load-bearing); payload read-back moved before the UPDATE (the TRA-9971 m5 class — caught by exactly one test); `mark_as_read` entry suppressed (the two live-frame tests fail via the bounded `next_sync_action_soon` panic in 49s total rather than hanging, confirming the 10s bound is real and the reported hang was genuine); and the client arm disabled in `cache/apply.rs` (both new wasm tests fail, so they exercise the arm rather than their own setup). The blanket `reject_sync_log_inserts` is correct here because `seed_notifications` runs through `create_notification`, which commits its own entries before the trigger is installed — the TRA-9950 trap is genuinely avoided. All six gates green with real exit codes, plus the Postgres dialect suite at 36 passed / 0 ignored. All mutated files restored byte-exact (sha256 verified, `5 files changed, 1068 insertions(+), 118 deletions(-)` unchanged). Only theme in the findings is comment overclaim — three comments assert slightly more than holds, none with behavioural consequence.

---
---

### 17:30 — TRA-9974: sync_log entries for the six notification state changes

**Review type**: re-review (cycle 2)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files** (delta only): `crates/trakkt-auth/src/notification_service.rs`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-ui/src/cache/apply.rs`, `apps/server/tests/postgres_dialect.rs` (+348, new). `issue_service.rs` and `server_fns/notifications.rs` byte-identical to cycle 1 (sha256 verified).

All six cycle-1 🟢 findings resolved. No production code changed — verified by reconstructing the cycle-1 tree (`git archive e078f8a` + the saved cycle-1 patch, sha256-matched to the cycle-1 baseline) and diffing: every `notification_service.rs` hunk is doc-comment text, `apply.rs` is one blank line, `sync_log_service.rs` is one test assertion, `postgres_dialect.rs` is new test code.

| Cycle-1 # | Resolution | Verified how |
|---|---|---|
| 1 (dead control assertion) | Fixed — control now filters to `NOTIFICATION` + `SyncActionType::Update` | **Reproduced the middle row**: re-ran cycle-1's Mutation A (`mark_all_as_read` writes no entry). Cycle 1: 2 failures, control green. Cycle 2: **3 failures**, control FAILS with "A's own delta is missing the read-state update for 521f6837-…: []". The assertion is now live. |
| 2 (false "every predicate is falsified") | Fixed — new text splits 5 state-selecting vs `mark_as_read` | Checked all six WHERE/SET pairs against source: 5 select on the state they change, `mark_as_read`'s only predicate is `deleted_at IS NULL`. Claim accurate. |
| 3 (`mark_as_read` doc conflated old/new behaviour) | Fixed — now separates "only the success is prior behaviour; the entry is new" | Accurate. |
| 4 (empty-`IN ()` invariant at caller only) | Fixed — documented at both callees | Judged sufficient; see notes. |
| 5 (stray blank line) | Fixed | diff confirms. |
| 6 (no `dual_backend_test!`) | Fixed — `notification_state_changes_log_one_recipient_scoped_update_each` | 4 mutations, both arms each. |

**Mutations run this cycle** (all restored byte-exact, `c2.sha256` verified after each):

| Mutation | Result |
|---|---|
| A (cycle-1 repeat): `mark_all_as_read` writes no entry | 3 FAILED incl. the newly-live control — the fix works |
| P1: `SyncAudience::User` → `Workspace` | both arms FAIL on `visibility_user_id`, `left: None / right: Some("usr_dialect")` |
| P2: payload read before the UPDATE | both arms FAIL at phase 1, `read left: false / right: true` |
| P3: payload correct except `deleted_at` always cleared | phases 1–2 **pass**, phase 3 FAILS both arms — proves the phase structure does work no single phase does |
| P4: give `mark_as_read` a `read` predicate | both arms FAIL — the added two-call test does hold the doc comment's claim |

**Gates** (real exit codes): `cargo check` 0 · clippy workspace 0 · clippy wasm32 0 · `check-tx-pool.py` 0 · `cargo test --workspace` 0 · `postgres_dialect --include-ignored` 0 (**38 passed, 0 failed, 0 ignored**) · `wasm-pack --headless --firefox` 0 (59 passed).

**Anti-pattern tally**: none.

**Notes**: Finding 4 — the implementer declined the structural fix (early `Ok(vec![])` or a non-empty newtype). Documenting at the callee is the right outcome here, though their stated reason ("a production behaviour change") is imprecise: no caller can reach `Some(&[])`, so an early return would be *unreachable defensive code*, which the project bans outright. Correct call, better reason available. Sub-🟢 observations recorded in the review text and explicitly not worth another cycle: the phrase "the same not-found-versus-not-owned distinction as the other five" reads as attributing that distinction to all six when only `mark_as_read` has it; the phase dispatch in the new test matches on string literals with a `_` catch-all; and the new test never cross-checks `payload["notification_id"]` against `entity_id` (unreachable given both derive from one `row.into_dto()`). `.review-approval` was stale — it held cycle 1's hash `a808b35a…` against a staged diff now hashing `e27b01e7…`, so it was correctly blocking; re-signed.

---

### 20:07 — TRA-10030: Hoist project-detail interaction state out of the page's reactive rendering closure

**Review type**: initial
**Result**: Clean (approved) — 0 🔴, 0 🟡, 3 🟢
**Files**: `crates/trakkt-ui/src/pages/projects/project_detail.rs`, `e2e/tests/sync/milestone-realtime.spec.ts`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Comment and Doc Accuracy | project_detail.rs:2225 | `is_archived` is a seventh optimistic server-state mirror left un-hoisted, and correctly so (the archive toggle commits in the same handler that sets it), but it is absent from the implementer's enumerated "deliberately not hoisted" list. The judgment is right; only the list is incomplete. |
| 2 | 🟢 | State Management | project_detail.rs:1241,1592 | `deleting_id` / `removing_id` now survive the re-render that used to clear them, so a confirm dialog can outlive the entity it names if another client deletes it first. Confirming then logs a `warn!` and refetches — degrades gracefully, no panic, no data loss. |
| 3 | 🟢 | State Management | project_detail.rs:1328,1850 | The add form and an inline rename can be open simultaneously and both now persist, so `section.locator('input[type="text"]')` could match two inputs. Equally possible before; the difference is only that accidental cleanup-by-re-render is gone. The new tests pin `toHaveCount(1)` in their own scenarios. |

**Anti-pattern tally**: Comment and Doc Accuracy ×1, State Management ×2. No 🔴/🟡.

**Verification performed** (not inspection — reproduced):
- **Pre-fix reproduction**: reverted only the production half in the working tree (index untouched), rebuilt WASM + server, ran the two new tests against `main`'s `project_detail.rs`. Both fail deterministically with `toHaveCount` `Expected: 1, Received: 0` (34 polls over 15s resolving 0 elements). Restored byte-exact, sha256 verified.
- **Partial-hoist claim**: built a variant hoisting only the three mode signals named in the ticket, leaving `edit_name`/`edit_date`/`new_name`/`new_date` local with the original reset `Effect`. It fails one assertion *later* — passes `toHaveCount(1)`, fails `toHaveValue` with the server value `"Rename base e2903d16"` in place of the typed `"Renamed by B b9e40896"`. The implementer's correction to the ticket's diagnosis is real, and the delivered tests discriminate between a partial and a complete fix.
- **Removed `Effect`**: `state.editing_id` is set to `Some` at exactly one site (`begin_edit`, called only from `on_edit`), so seeding on the way in is equivalent to the deleted reset effect.
- **`close()` leaving the draft alone**: verified all four open paths for name/description re-seed the draft before setting `editing`.
- **Project-navigation reset**: independent probe via the sidebar link (`layout.rs:742`). Confirmed the transition is same-document, so the router really does reuse the component and the `Effect` is load-bearing rather than defensive; no draft leaked between projects in either direction.
- **TRA-10031 citations**: `tachys-0.2.18/src/reactive_graph/mod.rs:78-83` and `leptos-0.8.20/src/show.rs:26-28` checked against the vendored crates — quoted verbatim, including the contrasting attribute-position impl at :447 that does thread `prev_value`.

**Gates**: `cargo check --workspace --locked` 0 · host clippy `-D warnings` 0 · wasm clippy `-D warnings` 0 · `cargo test --workspace --locked` 0 · `wasm-pack test --headless --firefox` 0 (59 passed) · `milestone-realtime.spec.ts --repeat-each=10` 50 passed / 0 failed (8.6m) · full sync suite 15 passed.

**Notes**: The strongest thing about this change is that the tests fail for the right reason and at the right assertion — the partial-hoist experiment showed the suite distinguishes "form stayed open" from "form kept the user's text", which is exactly the distinction a fix-shaped-to-a-test would blur. No `retries` added (`e2e/playwright.sync.config.ts` untouched at `retries: 0`), no sleeps in the new tests (`markWhenQuiet` is a real condition-poll that throws on failure to settle), and the test diff is a pure append — 164 insertions, 0 deletions — so the three original `expectDeliveredByCounter` assertions are byte-identical. The two removed `toBeFocused()` assertions are a scoping decision on *new* tests, not a weakening of existing ones, and TRA-10031 (agent-ready, `relates_to` TRA-10030) captures them with the mechanism and restoration in its ACs.

---

---

### 14:42 — TRA-9975: Bound every sync-delivery receive in trakkt-auth tests

**Review type**: initial
**Result**: 0 🔴, 0 🟡, 3 🟢 (approved)
**Files**: `crates/trakkt-core/src/test_helpers/channel.rs`, `crates/trakkt-core/src/test_helpers.rs`, `crates/trakkt-auth/Cargo.toml`, `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-auth/src/websocket/manager.rs`, `crates/trakkt-auth/src/mcp_session_manager.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🟢 | Comment and Doc Accuracy | (PR report, not code) | Reported raw-receive count is 26 (17/5/4); actual is 27 (17/5/5). A single-line `.recv().await` grep misses the multi-line chain at `mcp_session_manager.rs:530` (base numbering). Zero receives remain unconverted, so the code is complete — only the figure stated against the ticket AC is wrong. |
| 2 | 🟢 | Missing Error Context | `sync_log_service.rs:6816`, `:7285` | Loop `what` strings ("one of A's six own-mutation frames", "one of the release's three frames") do not name which iteration stalled. The same diff already solves this at `websocket/manager.rs:571` with `format!("connect heartbeat {} of {expected}", drained + 1)`. |
| 3 | 🟢 | Quick & Dirty (scope) | `apps/server/src/routes/websocket.rs:1302,1611,1617,1825,1828,1852,2102`, `apps/server/tests/sync_ws.rs:482` | Same hazard shape survives in `apps/` test code. Out of TRA-9975's stated `trakkt-auth` scope and honestly reported as such; `recv_soon` now lives in `trakkt-core` so a follow-up ticket can reach these. |

**Anti-pattern tally**: Comment and Doc Accuracy ×1, Missing Error Context ×1, Quick & Dirty (scope) ×1

**Gates** (all run in the worktree, real exit codes): `cargo check --workspace --locked` 0 · `cargo clippy --locked --workspace --all-targets -- -D warnings` 0 · `cargo clippy --locked -p trakkt-ui --target wasm32-unknown-unknown --features hydrate --all-targets -- -D warnings` 0 · `cargo test --workspace --locked` 0 (555 passed, 0 failed, 61.61s) · `scripts/check-tx-pool.py` 0 · `cargo test -p trakkt-server --test postgres_dialect -- --include-ignored` 0 (38 passed, **0 ignored**).

**Notes**: The load-bearing claim — that the change is confined to test code — verified two ways. Structurally: `#[cfg(test)] mod tests` is the *last* top-level item in all three `.rs` files (912/551/290), and the lowest original-side hunk start per file is 918/553/292. Empirically: `trakkt-core` compiles with `features = []` in a non-test `-p trakkt-server` build and `['test-helpers']` only under `--all-targets`, so resolver-3 keeps the new dev-dependency out of production. `Cargo.lock` genuinely unchanged.

Acceptance test reproduced byte-for-byte. With `tx.commit()` moved after the delivery loop in `SyncBatch::commit_and_deliver`, `status_create_frame_carries_the_new_status` failed with the new message at `channel.rs:72` in `40.22s` (30s sqlx acquire timeout + 10s bound), exit 101. Control run — identical mutation, chokepoint reverted to a bare `conn.rx.recv().await.expect(…)` as the only variable — hung, printed only "has been running for over 60 seconds", and was SIGKILLed at 300s. The bound is the isolating variable.

Supporting claims all reproduced: 22 direct `next_sync_action` sites + 3 former `next_sync_action_soon` sites; all channels are `tokio::sync::mpsc` (no `broadcast::` anywhere in `trakkt-auth`, contra the ticket's loose wording); the audience nuance holds (`broadcast_raw_to_workspace` does `db_fetch_all!` on the pool, `deliver_to_local_user` uses `try_send` and never awaits) so a workspace-audience test was the right choice for the deadlock; the 10s bound costs zero on a passing run because `timeout` polls an already-ready `recv()`; TRA-9974's empirical doc record survived the `next_sync_action_soon` deletion into the new module docs; all six `try_recv()` sites are non-blocking by API contract; the three new helper tests genuinely execute under `cargo test --workspace`. TRA-10007 correctly reported not covered — it is a parked *send* into a prefilled capacity-1 channel, which a receive bound cannot help, and zero `apps/` files were touched.

---

---

### 15:38 — TRA-9975: Bound every sync-delivery receive in trakkt-auth tests

**Review type**: re-review (cycle 2)
**Result**: Clean (approved) — 0 🔴, 0 🟡, 0 🟢
**Files**: `crates/trakkt-auth/src/sync_log_service.rs`, `crates/trakkt-core/src/test_helpers.rs` (delta only; other four byte-identical to cycle 1)

**Prior issues resolved**: 🟢 #1 (false "three test modules across the workspace" doc claim) fixed. 🟢 #2 (unindexed loop `what` strings) fixed. 🟢 #3 (`apps/` receives) filed as TRA-10034 rather than fixed — correctly out of TRA-9975's `trakkt-auth` scope.

**`cargo fmt` recovery — the cycle's main risk, verified complete.** The implementer ran `cargo fmt` mid-task, reformatting 54 unrelated files, then recovered the pre-edit blobs from the object store. I verified the recovery by comparing every current staged blob against the blob hashes recorded in the diff I signed in cycle 1, rather than by eyeballing the diff:

| file | cycle-1 blob | current | |
|---|---|---|---|
| `trakkt-auth/Cargo.toml` | e92e03a | e92e03a | identical |
| `mcp_session_manager.rs` | 9d3fc09 | 9d3fc09 | identical |
| `websocket/manager.rs` | 58221cb | 58221cb | identical |
| `test_helpers/channel.rs` | cf686b4 | cf686b4 | identical |
| `sync_log_service.rs` | d95e330 | dbd7f40 | changed — nit 2 only |
| `test_helpers.rs` | 2c33a89 | f819921 | changed — nit 1 only |

Four of six byte-identical to what was signed. `diff -u` of the two changed files against their cycle-1 blobs shows *only* the two intended edits and no stray reformatting. Cross-check: my own cycle-1 file backup (sha256 `487eddbb…`) is byte-identical to recovered blob `d95e330`, independently corroborating that the recovered blob really is the signed content. `git status` worktree-wide shows no unstaged modifications at all, so the other 54 reformatted files were restored too. The repo was **not** left formatted.

**Nit 1 claims — all verified true, not merely less wrong.** Three callers, all `trakkt-auth` test modules ✓. `apps/server/Cargo.toml:49` already dev-depends on `trakkt-core` with `test-helpers` ✓. Load-bearing claim: `crates/trakkt-auth/Cargo.toml` has sections `[package] [dependencies] [dev-dependencies] [lints]` and **no `[features]`** ✓ — so a helper there could only live in a `#[cfg(test)]` module, invisible to dependents. The reword replaces a false claim with the narrower true one plus the real architectural reason, which is what the Comment and Doc Accuracy rule asks for.

**Nit 2 — correct.** `for received in 0..6` / `"A's own-mutation frame {} of 6"` and `for received in 0..3` / `"the release's frame {} of 3"`. Bounds match the asserted frame vectors (6 entries and 3 entries respectively), and `received + 1` yields 1..6 / 1..3 — the frame *being awaited*, no double-count. Completeness claim independently reproduced with a brace-depth scan of all `recv_soon`/`next_sync_action`/`drain_heartbeats`/`watching_member` call sites against every loop block in the three files: exactly three sites are in loops — the two fixed plus the already-correct `manager.rs:571`. No wrapping helper is called from a loop.

**Gates**: `cargo check --workspace --locked` 0 · `cargo clippy --locked --workspace --all-targets -- -D warnings` 0 · `cargo test --workspace --locked` 0 (555 passed, 0 failed, 67.03s; the three `test_helpers::channel` tests execute). Wasm clippy and Postgres dialect skipped per coordinator — both green in cycle 1 and unaffected by this delta.

**`cargo fmt --all --check`** (reported, not fixed): exit **1**, **1394 hunks across 199 files** of 266 tracked `.rs` files — an independent match for the coordinator's 199, now tracked as TRA-10035. Importantly the diff's own new lines are canonically formatted: `channel.rs` (133 new lines) draws zero hunks, and rustfmt's nearest complaints bracket but never touch the edited regions (6779/6827 vs the 6814-6820 edit; 7314/7324 vs 7286-7292; test_helpers 59/65/109 vs the 21-28 edit). Confirms the implementer's scratchpad-rustfmt check against the repo itself.

**Notes**: No mutations this cycle, so nothing to restore; staged stat still reads 6 files / 263 insertions / 90 deletions. Nothing from cycle 1 was re-run — the deadlock reproduction and control were already reproduced to the decimal and are unaffected by a doc reword and two format strings. Signed over diff hash `f9825347…`.

---
