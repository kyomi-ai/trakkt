apiVersion: v1
kind: ConfigMap
metadata:
  name: trakkt-config
  namespace: trakkt
data:
  # Deployment mode: personal, self_hosted, or saas
  trakkt-mode: "self_hosted"

  # Database configuration
  db-host: "postgres.trakkt.svc.cluster.local"
  db-port: "5432"
  db-name: "trakkt"

  # Redis configuration
  redis-host: "redis.trakkt.svc.cluster.local"
  redis-port: "6379"

  # Server configuration
  port: "8003"

  # URLs
  base-url: "https://app.trakkt.app"
  frontend-url: "https://app.trakkt.app"

  # WebAuthn RP configuration
  webauthn-rp-id: "app.trakkt.app"
  webauthn-rp-name: "Trakkt"

  # Support email
  support-email: "support@trakkt.local"

  # Logging
  rust-log: "info"
